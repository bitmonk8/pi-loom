"use strict";

// Loadable ESLint plugin skeleton for loom's bespoke local rules.
//
// H1a ships only this loadable package skeleton so the
// `eslint-plugin-loom-local` `file:` devDependency resolves and loads on a
// fresh `npm install`. The `no-broad-catch` rule body and the
// `no-restricted-syntax` wiring are populated by H2a; this module exports an
// empty rule set until then.
module.exports = {
  rules: {},
};
