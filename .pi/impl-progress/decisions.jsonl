{"date":"2026-06-30","leaf":"H2a","task":"Cross-cutting lint and architectural gates","summary":"Built the code-quality guardrails as small custom checks because the off-the-shelf checker couldn't read the same-line 'this is allowed here' notes the rules rely on.","detail":"The plan describes the sequential-by-default and blocking-call gates using ESLint's stock 'no-restricted-syntax' rule. That stock rule cannot inspect a flagged line's own trailing allow-comment, but the leaf's binding tests require exactly that (a same-line // allow:, // allow-sync:, or // allow-broad-catch: comment makes the construct pass). Implemented three custom comment-keyed rules in eslint-plugin-loom-local instead; Adds. prose is descriptive/illustrative while the comment-keyed pass/fail behaviour is the binding obligation, so the custom rules are the faithful implementation."}
