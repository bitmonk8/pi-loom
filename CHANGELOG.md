# Changelog

2026-06-30 H1a — Project scaffold and toolchain: package.json deps/scripts/engines/type, tsconfig.json, Vitest runner, lint toolchain (eslint, @typescript-eslint/parser, loadable eslint-plugin-loom-local skeleton), and architectural manifest tests; npm install/build/typecheck/test all green on an empty src tree.
2026-06-30 H2a — Cross-cutting lint + architectural gates: comment-keyed no-broad-catch / no-unguarded-promise-combinator / no-blocking-sync ESLint rules in eslint-plugin-loom-local wired by eslint.config.js over src/**, plus a module-level-mutable-binding architectural scan; lint + architectural assertions run under npm test against fixtures and the real src/** tree.
2026-06-30 H3a — Dependency-injection seam skeleton: the seven host-seam interfaces (Checkpoint/SchemaValidator/Clock/FileSystem/FileWatcher/TokenEstimator/IdSource, PIC-10/11/12/13/14/16/20) under src/seams/, a constructor-injection RuntimeRoot + createRuntimeRoot factory threading them, and an identifier-keyed ambient-primitive scan (process.env/process.cwd/crypto.randomUUID + the PIC-12 timing set, // allow-ambient: comment-keyed) wired into npm test over src/**; two-runtime-root isolation and the direct-reference ambient ban asserted.
2026-06-30 H4a — Extension factory shell + end-to-end harness: never-throw synchronous-arm src/extension/factory.ts (per-call try/catch Pi-SDK-boundary registrations of the --loom flag, the loom-system-note renderer, and the resources_discover/session_start/session_shutdown subscriptions; registerCommand fires from session_start), the thin extensions/index.ts entry shim re-exporting the factory, an in-memory fixture-supply seam (LoomExtensionDeps.fixtures) M/M-T bind against, and a reusable in-process session double + harness with a minimal response-emission capability; session-double fidelity-contract self-check (streaming-before-idle, single-turn append, pi.on cancel-forward subscription, CNCL-4 reason propagation) wired into npm test. tsconfig now includes extensions/.
2026-06-30 H4b — Response-programming surface: a single deterministic input-side scripting API (tests/harness/response-program.ts, reached through H4a's harness via double.responses / driveResponses()) extending H4a's minimal response-emission into categories (a) scripted assistant turns + per-turn streamed fragments, (b) tool_use results incl. isError:true and mixed-success parallel batches (V14b), (c) binder/provider response & failure honouring the V11f per-invocation retry budget (≤3 calls: 1 + 1 transport + 1 malformed, per determinism-cancellation-failure.md), (d) tool_loop.max_rounds round-exhaustion (ERR-19/FRNT-1, V13c), and (e) abort injection at pre-call/in-flight/during-retry (V11f/V17a); determinism gate + per-category functional-effect self-checks wired into npm test.
2026-06-30 H4c — Modeled-behaviour response-programming surface: extends H4b's scripting contract (tests/harness/response-program.ts, reached via double.responses / driveResponses()) with the two modelling categories V4c/V4f/V9i/V9o consume — (f) scriptInvokeChild driving a nested invoke(...) child to completion, surfacing its produced final value as a completed-invoke-child outcome (ERR-13 no-rollback), and (g) scriptSubagentCallee dispatching a private subagent AgentSession per the V9i contract, resolving the subagent-loom outcome from the terminal agent_end event and ignoring willRetry:true events (PIC-43) rather than emitting a plain scripted turn; determinism gate + per-category functional-effect self-checks wired into npm test.
