# S3 — TOOL area coverage map

Source requirements: `docs/e2e-campaign/analysis/spec-requirements.md:485-507` (REQ-TOOL-1..23).
Method: mapped by reading `describe`/`it` text under `tests/`. Coverage is claimed only where a concrete test location is cited. Read-only pass over `src/`, `tests/`.

## Coverage table

| Campaign REQ | Testability | Covering test file :: describe/it (or path:line) | COVERED/PARTIAL/UNCOVERED | note |
|---|---|---|---|---|
| REQ-TOOL-1 | conformance | `tests/subagent-model-loom-tool.test.ts:494` :: "presents the `.loom` in the loom-owned complete() loop's tool schemas (SHARED callable set)"; `tests/callable-set-runtime-enforcement.test.ts:206` :: "a DECLARED tool … dispatches through the held reference and lowers to Ok(text)"; `tests/callable-set.test.ts:311/321/337` :: presented-name/hyphen→underscore/`as`-rename resolution | COVERED | bare-identifier resolution + post-`as`/hyphen rewrite + set-shared-with-model all pinned |
| REQ-TOOL-2 | conformance | — | UNCOVERED | no test asserts a code-side call adds no conversation turn / consumes no tokens / is absent from the transcript |
| REQ-TOOL-3 | offline-unit | `tests/tool-calls.test.ts:52` :: "loom/parse/tool-arg-arity: a multi-argument Pi-tool call fires"; `tests/tool-calls.test.ts:65` :: "loom/parse/tool-arg-not-literal: a non-literal single Pi-tool argument fires" | COVERED | both parse codes for a Pi-tool arg pinned |
| REQ-TOOL-4 | conformance | `tests/tool-calls.test.ts:85` :: "loom/parse/tool-arg-type-mismatch: a statically-resolvable `.loom`-callable argument mismatch fires"; `tests/tool-calls.test.ts:180` :: input-validation → `InvokeInfraError{cause:'validation'}` | COVERED | static mismatch + runtime-net (non-static) both pinned |
| REQ-TOOL-5 | live | `tests/tool-calls-depth-ceiling.test.ts:40` :: depth-6 code-driven arg → `Err(CodeToolError{cause:'validation'})` via AJV; `tests/tool-calls.test.ts:52/65` :: parse-time `tool-arg-arity`/`tool-arg-not-literal` still fire for Pi tools | PARTIAL | runtime AJV→CodeToolError{validation} shown only via the maxDepth path; a plain input-schema-shape mismatch → CodeToolError{validation} not directly asserted |
| REQ-TOOL-6 | offline-unit | `tests/invoke-diagnostics.test.ts:156` :: `invoke-arity-too-few`; `:185` :: `invoke-arity-too-many`; `:200` :: in-range fires neither; `:212` :: arity checked before type | COVERED | invoke arity rules applied to `.loom` callable |
| REQ-TOOL-7 | offline-unit | `tests/invocation-core.test.ts:118` :: "INV-1: a load-time escape emits the loom/load/invoke-path-escape diagnostic"; `tests/hardening/invoke-parse-load.test.ts:137` :: "INV-5: an invoke path escaping every discovery root un-registers with an invoke-path-escape diagnostic" | COVERED | escaping path rejected + callable not created (un-register) |
| REQ-TOOL-8 | conformance | `tests/tool-calls.test.ts:158` :: "a conforming Pi-tool return lowers to Ok(string)"; `tests/tool-calls.test.ts:166` :: "a conforming subagent-mode `.loom` return lowers to Ok(T)" | COVERED | both callee-kind return shapes pinned |
| REQ-TOOL-9 | live | `tests/tool-calls.test.ts:180` :: `.loom` → `InvokeInfraError`; `:192` :: `.loom` callee failure → `InvokeCalleeError`; `tests/tool-calls-execute-lowering.test.ts:344` :: Pi-tool `execute()` throw → `Err(CodeToolError{cause:'execution'})` | COVERED | Pi-tool vs `.loom` split pinned; `unknown_tool` safety-net edge via REQ-TOOL-10 row |
| REQ-TOOL-10 | conformance | `tests/tool-calls.test.ts:139` :: "CodeToolError.cause is closed at validation / execution / cancelled / unknown_tool"; `tests/callable-set-runtime-enforcement.test.ts:152/185` :: an out-of-set name surfaces Err with the registry not re-queried (strong refs held) | COVERED | closed cause-set + "unknown_tool cannot occur within one invocation" both pinned |
| REQ-TOOL-11 | live | `tests/tool-calls-off-surface-routing.test.ts:259` :: "a never-settling execute() Promise blocks until loomAbort fires, then surfaces cause:'cancelled'"; `tests/tool-calls-off-surface-live-wiring.test.ts:347` :: seam blocks at await then surfaces cancelled (no hang) | COVERED | no-internal-timeout, cancel-driven `cause:'cancelled'` pinned |
| REQ-TOOL-12 | live | `tests/tool-calls-off-surface-routing.test.ts:101` :: setup throw → `{isError:true}` with bare callable-set name; `:123` :: exactly one `loom/runtime/internal-error` + one `loom-system-note`; `:142` :: `<no stack available>` hint | COVERED | translation + one-note + no-unhandled-rejection pinned |
| REQ-TOOL-13 | live | `tests/tool-calls-off-surface-routing.test.ts:160/176/187/202/217` :: non-conforming return → `internal-error{details.kind='tool-return-shape'}`, NOT CodeToolError; `tests/tool-calls-off-surface-live-wiring.test.ts:99-161` :: real seam routing; `tests/tool-return-shape-one-note-production-wired.test.ts:207` :: exactly one note through the production seam | COVERED | routed off CodeToolError, shape_check arms + production wiring pinned |
| REQ-TOOL-14 | live | `tests/tool-calls-off-surface-routing.test.ts:310/320/330/340` :: CNCL-1/2/3 late resolve/reject discarded, OOM-style reject still discarded; `tests/tool-calls-swallowing-handler.test.ts:106/146/168/194` :: construction-site swallowing handler, three-channel suppression | COVERED | total discard + pre-microtask handler pinned |
| REQ-TOOL-15 | conformance | `tests/tool-calls.test.ts:148` :: "CodeToolError is distinct from ModelToolError (different `kind` wire tags)"; `tests/tool-calls-execute-lowering.test.ts:247` :: same `execute()` throw → `CodeToolError{cause:'execution'}` code-side | PARTIAL | distinctness + code-side lowering pinned; the `tool_call_id`/`raw_response` presence-vs-absence contrast not asserted (no grep hit) |
| REQ-TOOL-16 | live | `tests/tool-calls.test.ts:180` :: `InvokeInfraError{cause:'validation'}`; `:192` :: `InvokeCalleeError` cascade | PARTIAL | callee-failure cascade + infra-validation pinned; the `cause:'validation'` vs return-side `cause:'return_validation'` distinctness not asserted (no grep hit) |
| REQ-TOOL-17 | live | `tests/statement-executor.test.ts:334` :: "cka-50: strictly sequential — each effect commits (start→end) before the next is entered" | PARTIAL | sequential/await-each-before-next pinned at statement level; "yields to Pi's event loop (non-blocking)" and "streaming `onUpdate` partials not surfaced" not asserted (no `onUpdate` hit anywhere in tests) |
| REQ-TOOL-18 | live | `tests/tool-calls-parallel-batch.test.ts:106` :: cka-13 settle-all before next user turn; `:136` :: independent per-sibling lowering keyed by toolUseId; `:165/185` :: failing sibling → `isError:true` alongside successes | PARTIAL | settle-all + independent lowering + mixed success/failure pinned; "batch consumes one `tool_loop.max_rounds` slot" not directly pinned to this batch (slot-accounting lives in `query-tool-loop.test.ts` but not tied to a parallel batch) |
| REQ-TOOL-19 | live | `tests/subagent-model-loom-tool.test.ts:221` :: "is re-entrant: two concurrent calls dispatch through independent collaborators without cross-talk" | COVERED | re-entrant defineTool adapter; independent-invocation isolation pinned |
| REQ-TOOL-20 | conformance | — | UNCOVERED | no test exercises the schema cache read-mostly/immutable-post-compile/concurrent-share/watcher-invalidation posture |
| REQ-TOOL-21 | manual/inspection | — (inspection-only; see §manual-only) | UNCOVERED | absence-of-a-primitive; not machine-testable |
| REQ-TOOL-22 | manual/inspection | — (inspection-only; see §manual-only) | UNCOVERED | Pi-side delivery posture; routes to editorial review, no build-time SDK signal |
| REQ-TOOL-23 | conformance | `tests/invoke-cross-mode.test.ts:69/96/124` :: subagent-mode callee gets a FRESH isolated conversation, no prior messages, child's own model/tools/system; `tests/subagent-model-loom-tool.test.ts:510` :: model tool_use for the `.loom` spawns a fresh child AgentSession; `:494` :: callee sees the shared/own callable set | PARTIAL | fresh-isolation + own-callable-set + fresh child session pinned; the explicit "`invoke(...)` and a `.loom`-callable call are operationally equivalent (same QueryError variants / arity / return-compat / path rules across both surfaces)" is inferred from the two surfaces' shared seams, not asserted in one equivalence test |

## UNCOVERED offline/conformance REQs worth new tests

- **REQ-TOOL-2 (conformance)** — no test asserts that a code-side tool call adds no conversation turn, consumes no model tokens, and is absent from the transcript (the code → side-effect / child-conversation contract vs code → model). Worth a conformance test observing turn count / transcript length unchanged across a code-side call.
- **REQ-TOOL-20 (conformance)** — schema cache read-mostly / immutable-post-compile / safe-to-share-across-concurrent-invocations / writes-only-on-compile-or-watcher-invalidation is entirely unpinned. Worth a conformance test that shares one compiled-validator cache across concurrent invocations and asserts no mutation except on initial compile / file-watcher invalidation.
- **REQ-TOOL-5 (live) — partial-to-complete** — add a plain input-schema-shape mismatch (not depth) → `Err(CodeToolError{cause:'validation'})` to complement `tests/tool-calls-depth-ceiling.test.ts:40`.
- **REQ-TOOL-15 (conformance) — partial-to-complete** — add an assertion that a code-side `CodeToolError` carries no `tool_call_id`/`raw_response` while a model-loop adapter failure carries both.
- **REQ-TOOL-16 (live) — partial-to-complete** — add an assertion distinguishing input-side `InvokeInfraError{cause:'validation'}` from return-side `cause:'return_validation'`.
- **REQ-TOOL-18 (live) — partial-to-complete** — pin "one `tool_loop.max_rounds` slot per batch regardless of success/failure mix" directly against a parallel batch.
- **REQ-TOOL-23 (conformance) — partial-to-complete** — add a single equivalence test asserting `invoke("./x.loom",...)` and a `.loom`-callable call share the same `QueryError` variants / arity / return-compat / path rules.

## Manual / inspection-only REQs

- **REQ-TOOL-21 (manual/inspection)** — "No loom-level concurrency primitive (e.g. `parallel { ... }`) is exposed in loom 1.0." Asserting the *absence* of a surface is not machine-testable; verify by grammar/spec inspection (no such production). `spec-requirements.md:505`.
- **REQ-TOOL-22 (manual/inspection)** — "The parallel-tool-batch delivery shape … is a loom-side consumption posture entirely Pi-side; detection routes to editorial review, not a build-time SDK signal." No build-time signal exists to test; verify by editorial review. `spec-requirements.md:506`.
