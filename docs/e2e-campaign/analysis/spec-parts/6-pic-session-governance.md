# Cluster 6 — Pi integration contract, session model, governance

Source cluster: `docs/spec_topics/pi-integration.md`, `docs/spec_topics/pi-integration-contract.md` + `docs/spec_topics/pi-integration-contract/*`, `docs/spec_topics/session-model-and-appendix.md`, `docs/spec_topics/governance.md` + `docs/spec_topics/governance/*`, `docs/spec_topics/future-considerations.md` + `docs/spec_topics/future-considerations/*`, `docs/spec_topics/language-and-architecture.md`, `docs/spec_topics/overview.md`, `docs/spec_topics/overview-and-orientation.md`, `docs/spec_topics/implementation-notes.md`.

Testability classes: **offline-unit** (pure logic, no model), **conformance** (production host-integration composition path via fakes/harness, no model), **live** (needs a real model/provider turn), **manual/inspection** (host-integrator wiring or spec-corpus form verified by code/doc inspection).

## AREA: PIC (pi-integration-contract)

| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-PIC-1 | — | The extension factory runs a synchronous Step-0 capability probe before any `pi.registerFlag`/`registerCommand`/`registerTool`/`registerMessageRenderer`/`pi.on` call, in fixed short-circuit order `(a)→(b)→(c)+(d)→(e)`, stopping at the first failure. | `pi-integration-contract/capability-probe.md` §entry-capability-probe | conformance |
| REQ-PIC-2 | — | Step 0(a) compares `process.versions.node` against floor `>=22.19.0` under SemVer with prerelease tags eligible; a non-SemVer value routes to `kind:"probe-failed"`, not `"node-floor"`. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (a) | offline-unit |
| REQ-PIC-3 | — | Step 0(b) checks the nine WHATWG `AbortSignal`/`AbortController` members per the kind table (`typeof===function` for ctor/method members, `"x" in AbortSignal.prototype` for `aborted`/`reason`); it MUST NOT instantiate `new AbortController()`. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (b) | conformance |
| REQ-PIC-4 | — | Step 0(c) checks the nine factory-probable function members by `typeof<path>==="function"` in listed order, stops at first false, and on failure emits `loom/load/host-incompatible` with `details.kind="sdk-capability-missing"`, `details.member=<first failing path>`. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (c) | conformance |
| REQ-PIC-5 | — | Step 0(d) iterates the four `@earendil-works/*` peer packages in fixed order, reads each own `package.json#version`, compares under tilde-range SemVer, and routes to `peer-dep-malformed-version` (4 conditions) or `peer-dep-out-of-range` with `details.package`. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (d) | conformance |
| REQ-PIC-6 | — | Step 0(e) checks `typeof Type.Unsafe==="function"` against the imported `typebox` `Type` namespace; failure routes to `kind:"typebox-shape"`; the probe MUST NOT construct a schema. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (e) | conformance |
| REQ-PIC-7 | PIC-3 | The probe MUST NOT use any member it is itself checking. | `pi-integration-contract/capability-probe.md` §pic-3 | conformance |
| REQ-PIC-8 | PIC-4 | The probe is `typeof`- and `in`-only: no arity sniffing, no return-shape sniffing, no construction. | `pi-integration-contract/capability-probe.md` §pic-4 | offline-unit |
| REQ-PIC-9 | PIC-5 | No probes may be added beyond the five enumerated checks. | `pi-integration-contract/capability-probe.md` §pic-5 | manual/inspection |
| REQ-PIC-10 | PIC-6 | The factory MUST NOT throw; each check is wrapped in try/catch. | `pi-integration-contract/capability-probe.md` §pic-6 | conformance |
| REQ-PIC-11 | — | On any probe failure the factory skips every subsequent host-binding call (four `register*` + all `pi.on`), emits exactly one `loom/load/host-incompatible` with `details.kind` in the closed 7-member set, and returns normally. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (On failure) | conformance |
| REQ-PIC-12 | — | A throw inside any check terminates the probe and emits `host-incompatible` with `details.kind="probe-failed"`, `details.step` naming the throwing check, and `details.cause` = coerced underlying-error string. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (Self-failure) | conformance |
| REQ-PIC-13 | — | `/reload` re-runs the probe with no carried state; subagent mode runs only after step 0 has gated the factory. | `pi-integration-contract/capability-probe.md` §entry-capability-probe (Idempotency) | conformance |
| REQ-PIC-14 | — | Post-probe interaction with a previously-probed member that throws/rejects/returns wrong shape routes to `loom/runtime/internal-error` except where one of four dedicated codes applies (`active-set-restore-failed`, `system-note-delivery-failed`, `subagent-dispose-failure`, `registry-swap-failed`); silent shape drift is undefined behaviour. | `pi-integration-contract/capability-probe.md` §post-probe-sdk-shape-drift | conformance |
| REQ-PIC-15 | PIC-52 | The runtime interposes no privilege layer between loom code and the Pi host and MUST NOT introduce sandbox/capability-filter/mediated-proxy channels in any 1.x release; host-side access denials surface through the issuing tool as `Err(QueryError{kind:"code_tool",cause:"execution"})`; silent success on denial is forbidden. | `pi-integration-contract/trust-boundary.md` §pic-52 | conformance |
| REQ-PIC-16 | — | The supported Pi minor is pinned at `@earendil-works/pi-coding-agent ~0.75.5`; the `#pi-sdk-pin` anchor is the single source of truth and every other corpus reference cites it rather than restating the literal. | `pi-integration-contract/host-prerequisites.md` §pi-sdk-pin | manual/inspection |
| REQ-PIC-17 | PIC-33 | The four `@earendil-works/*` `peerDependencies` entries MUST move together at a single Pi minor line; no skew across the four is carried. | `pi-integration-contract/host-prerequisites.md` §pic-33 | manual/inspection |
| REQ-PIC-18 | PIC-34 | Before the loom `peerDependencies` range is widened on a Pi minor change the contributor MUST follow the Pi version bump procedure; the build-time literal-read + surface-inventory assertions gate the build until it is completed. | `pi-integration-contract/host-prerequisites.md` §pic-34 | manual/inspection |
| REQ-PIC-19 | PIC-35 | The `typebox:"*"` declaration MUST NOT be collapsed into the four-entry tilde-pinned group; it is asserted by its own build-time literal-read assertion. | `pi-integration-contract/host-prerequisites.md` §pic-35 | conformance |
| REQ-PIC-20 | — | The four `@earendil-works/*` `package.json#peerDependencies` entries MUST literally equal the canonical `~0.75.5` range (manifest lock-step); a partial landing where spec literal and manifest literal disagree fails the build-time literal-read assertion. | `pi-integration-contract/host-prerequisites.md` §pi-sdk-pin-manifest-lock-step | conformance |
| REQ-PIC-21 | — | Non-bypass looms require a structured-output-capable LLM via `ctx.modelRegistry`; on unresolved registry the loom fails to load with `loom/load/binder-model-unresolved`; under the pin the strict-capability probe degrades to `loom/load/binder-model-strict-capability-unknown` (W). | `pi-integration-contract/host-prerequisites.md` §host-prerequisites (item 2) | conformance |
| REQ-PIC-22 | — | Loom does not store/fetch/cache credentials; it relies on Pi's own credential resolution for the resolved model reached via `ctx.modelRegistry`/`Model<Api>`. | `pi-integration-contract/host-prerequisites.md` §host-prerequisites (item 3) | manual/inspection |
| REQ-PIC-23 | — | Subagent mode does not pass a `signal` option to `createAgentSession` (no such field at the pin) and instead consumes `AgentSession.abort()` from a one-shot `loomAbort.signal` listener registered at spawn. | `pi-integration-contract/host-prerequisites.md` §host-prerequisites (item 4) | conformance |
| REQ-PIC-24 | — | loom pins a TypeScript floor `>=5.4.0` because the build-time `SessionShutdownEvent['reason']` type-equality assertion relies on `tsc` surfacing the literal brand string `loom/typecheck/session-shutdown-reason-snapshot` grep-ably. | `pi-integration-contract/host-prerequisites.md` §tsc-toolchain-floor | conformance |
| REQ-PIC-25 | PIC-7 | A Pi extension instance is bound to exactly one active user session at any given time. | `pi-integration-contract/host-prerequisites.md` §pic-7 | manual/inspection |
| REQ-PIC-26 | — | The extension is ES-modules-only: `package.json#type` MUST be `"module"`, verified with the absence of CJS-reach shapes in `src/` by a build-time gate. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §extension-esm-type-module | conformance |
| REQ-PIC-27 | — | A single Pi extension module `extensions/index.ts` exports the standard `default function (pi: ExtensionAPI)` factory, auto-discovered from `package.json#pi.extensions=["./extensions"]`. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §extension-entry-point | conformance |
| REQ-PIC-28 | — | The factory registers `--loom` via `pi.registerFlag('loom',{type:'string',description})` before subscribing `resources_discover`; `pi.getFlag('loom')` narrows to `string\|undefined`; absent flag contributes no paths; a present value is split on `path.delimiter`. | `pi-integration-contract/registration-steps.md` (step 1) | conformance |
| REQ-PIC-29 | — | The factory does NOT call `pi.registerCommand` synchronously; calling any `notInitialized`-tagged session-state action method (`getCommands`/`getActiveTools`/`setActiveTools`/…) pre-bind aborts factory load; registration sinks and `refreshTools` are safe pre-bind. | `pi-integration-contract/registration-steps.md` (step 2) | conformance |
| REQ-PIC-30 | PIC-39 | The `SlashCommandInfo[]` array from `pi.getCommands()` MUST be treated as read-only by convention; the cross-format collision check is a single forward pass that never mutates it. | `pi-integration-contract/registration-steps.md` §pic-39 | conformance |
| REQ-PIC-31 | — | The `session_start` handler consults `pi.getCommands()`, drops any pending loom whose slash name collides with an existing entry whose `source ∈ {"prompt","extension","skill"}`, registers survivors via `pi.registerCommand(name,{description,handler})` (only `description`+`handler` keys), and emits one `loom/load/cross-format-collision` per drop. | `pi-integration-contract/registration-steps.md` §slash-handler-registration | conformance |
| REQ-PIC-32 | — | Every registered slash `handler` MUST invoke `LoomRegistry.readDrainState()` as its first action and route through the closed two-arm enumeration, including the read-failure fail-safe. | `pi-integration-contract/registration-steps.md` §slash-handler-registration (Drain-state-gated dispatch) | conformance |
| REQ-PIC-33 | — | On a dispatched slash name whose entry-table entry was dropped by a supersession pass, the handler returns the fixed note `"loom /<name>: superseded; /reload to refresh"` rather than dispatching. | `pi-integration-contract/registration-steps.md` §superseded-entry-dispatch | conformance |
| REQ-PIC-34 | — | A loom whose effective slash name resolves to `reload` MUST be dropped at the session-start cross-format collision check (emitting `loom/load/cross-format-collision`), and loom's drain-state short-circuit MUST NOT intercept Pi-owned `/reload`. | `pi-integration-contract/host-prerequisites.md` §degraded-state-host-prerequisites (c-i/c-ii) | conformance |
| REQ-PIC-35 | — | The `session_shutdown` handler owns disposing every resource the extension instance holds (subagent connections, chokidar handles, debounce timers, listeners) across quit/reload/new/resume/fork. | `pi-integration-contract/registration-steps.md` (step 4) | conformance |
| REQ-PIC-36 | — | The chokidar watcher debounces (~250 ms, measured against the injected `Clock`; the literal is an implementer choice), re-parses the changed file + transitive `.warp` importers, and swaps `LoomRegistry` entries via build-aside-then-publish; the runtime MUST NOT call `ctx.reload()` for content edits. | `pi-integration-contract/registration-steps.md` §watcher-hot-reload-registration | conformance |
| REQ-PIC-37 | PIC-55 | On a watcher error/throw that continues delivering events, hot-reload survives via a `ctx.ui.notify` toast; on a stopped-delivering terminal condition the runtime emits a single persistent `loom-system-note` carrying `loom/runtime/watcher-terminated` prompting `/reload`, keeps `LoomRegistry` live and dispatchable, and MUST NOT write the drain-state tag from this path. | `pi-integration-contract/registration-steps.md` §pic-55 | conformance |
| REQ-PIC-38 | PIC-36 | The watcher installs the staged `LoomRegistry`, validator cache, and prompt-mode registration cache in a single synchronous publish only after every staged step succeeds; any throw discards the staging set, keeps prior state live, and surfaces one `loom/runtime/registry-swap-failed`. | `pi-integration-contract/registration-steps.md` §pic-36 | conformance |
| REQ-PIC-39 | — | The slash-handler closure reads the entry once at handler entry; a mid-execution swap does not affect a running invocation; the next invocation sees the post-swap version. | `pi-integration-contract/registration-steps.md` (In-flight invocation rule) | conformance |
| REQ-PIC-40 | PIC-49 | At most one rebuild runs against the live registry/validator-cache/registration-cache at a time; a debounce timer firing mid-rebuild defers until the in-flight publish or `registry-swap-failed` discard completes, losing no edit. | `pi-integration-contract/registration-steps.md` §pic-49 | conformance |
| REQ-PIC-41 | — | On add/remove of a `.loom`/`.warp` file (or a `loomPaths` array change) the watcher emits one `loom-system-note` with the verbatim template `loom watcher: <N> file(s) added or removed; run /reload to refresh the slash command list`, `display:true`, only `<N>` substituted (base-10), `N = added.length + removed.length`. | `pi-integration-contract/registration-steps.md` §structural-changes-no-unregister | conformance |
| REQ-PIC-42 | PIC-37 | The structural-change note MUST NOT be emitted when `added.length + removed.length === 0`; the window's other obligations still run. | `pi-integration-contract/registration-steps.md` §pic-37 | conformance |
| REQ-PIC-43 | PIC-38 | A same-window rename observed as removed(P) then added(P) has `N===2` and MUST emit the note (the arrays are disjoint by role, not deduplicated). | `pi-integration-contract/registration-steps.md` §pic-38 | conformance |
| REQ-PIC-44 | — | The `session_shutdown` handler validates `event.reason` against the closed set `{"quit","reload","new","resume","fork"}` (read wrapped in try/catch); an unknown or throwing reason emits one `loom/host/session-shutdown-reason-unknown` (W) via `console.error` with `details.observed=String(event.reason)` before sub-step 1, then runs the full five-sub-step teardown. | `pi-integration-contract/unknown-reason-rule.md` §unknown-reason-rule | conformance |
| REQ-PIC-45 | — | The no-active-invocations fast-path MUST NOT be taken on the unknown-reason path; drain, bounded await, watcher closure, and listener detach all run unconditionally. | `pi-integration-contract/unknown-reason-rule.md` §unknown-reason-rule | conformance |
| REQ-PIC-46 | PIC-45 | The runtime performs a closed-set membership check of `event.reason` against the snapshot's `literals` field, with the unknown-reason fallback (`session-shutdown-reason-unknown` + full teardown). | `pi-integration-contract/unknown-reason-rule.md` §pic-45 | conformance |
| REQ-PIC-47 | PIC-46 | The closed set is sourced single-site from the `SessionShutdownEvent['reason']` snapshot entry in `SDK_SURFACE_INVENTORY`; the handler imports it directly and consumes its `literals` field with no separate copy. | `pi-integration-contract/unknown-reason-rule.md` §pic-46 | conformance |
| REQ-PIC-48 | PIC-47 | The handler wraps the snapshot-lookup read and the `event.reason` read in fixed order (snapshot first) under try/catch; a snapshot-side failure emits `loom/host/session-shutdown-pinned-constant-unreadable` (W) with `details.failure ∈ {"missing-entry","literals-shape-invalid","throw:<String(error)>"}`; the two codes are mutually exclusive per shutdown event. | `pi-integration-contract/unknown-reason-rule.md` §pic-47 | conformance |
| REQ-PIC-49 | — | Lookup of the snapshot entry MUST use the composite predicate `(kind==="type-union-snapshot") && (path==="SessionShutdownEvent.reason")`; `SDK_SURFACE_INVENTORY===undefined` MUST be guarded before the iteration primitive so it routes to `"missing-entry"` not the throw arm. | `pi-integration-contract/unknown-reason-rule.md` §unknown-reason-rule (Lookup-failure-to-discriminator routing) | conformance |
| REQ-PIC-50 | — | `"literals-shape-invalid"` covers four structural sub-cases (non-array; non-string element; empty array; empty-string element); a conformance suite MUST cover each with its own fixture. | `pi-integration-contract/unknown-reason-rule.md` §unknown-reason-rule | conformance |
| REQ-PIC-51 | PIC-48 | The four substrings — codes `session-shutdown-reason-unknown`, `session-shutdown-pinned-constant-unreadable`, brace-form set `{"quit","reload","new","resume","fork"}`, and the three `details.failure` literals — are anchor-stable contract surface for inbound references. | `pi-integration-contract/unknown-reason-rule.md` §pic-48 | manual/inspection |
| REQ-PIC-52 | — | The `"throw:<String(error)>"` payload is truncated to ≤256 UTF-16 code units, control chars escaped (`\n`/`\r`/`\t` two-char, others `\uXXXX` lowercase 4-digit), interior colons/`throw:` unescaped, and a coercion throw yields byte-exact `"throw:<unreadable>"`. | `pi-integration-contract/unknown-reason-rule.md` §Acceptance criteria | offline-unit |
| REQ-PIC-53 | — | During the patch-skew window, a Pi-added reason (widening) routes through the unknown-reason path with `details.observed` carrying the coerced literal while full teardown still runs; a removed reason (narrowing) is a harmless stale snapshot entry; the build-time bidirectional type-equality assertion fires on the next typecheck. | `pi-integration-contract/patch-skew-degradation.md` §patch-skew-degradation-contract | conformance |
| REQ-PIC-54 | — | Fixture obligations (P-a) unknown-reason runtime, (P-b-1/2) snapshot-only widening pair, (P-c-1/2) narrowing no-regression pair MUST be covered; each teardown-witnessing fixture asserts all eight `details.call` sites plus sub-step-2/3 ran. | `pi-integration-contract/patch-skew-degradation.md` §patch-skew-degradation-contract | conformance |
| REQ-PIC-55 | — | The `session_shutdown` handler is idempotent: any invocation observing `drained===true \|\| tag!==undefined` at entry returns immediately without re-executing sub-steps 1–5 or re-emitting any teardown diagnostic. | `pi-integration-contract/drain-state-contract.md` §idempotent-handler-clause | conformance |
| REQ-PIC-56 | — | The handler's fixed control-flow at entry is (I) `readDrainState` + short-circuit predicate, (II) return-if-fired, (III) snapshot read, (IV) `event.reason` read, (V) pre-sub-step-1 emissions, (VI) sub-step 1 onward. | `pi-integration-contract/drain-state-contract.md` §idempotent-handler-clause (Handler control-flow ordering) | conformance |
| REQ-PIC-57 | PIC-30 | The runtime MUST NOT introduce a third drain-state field, an arm-specific post-failed-handler gate, or any suppression that would conditionally short-circuit slash dispatch on the post-handler tuple. | `pi-integration-contract/drain-state-contract.md` §pic-30 | conformance |
| REQ-PIC-58 | PIC-32 | `LoomRegistry` carries a boolean `drained` (init false, flipped true once by sub-step-1 `drain()`, never reset) and a `drainStateTag:"shutting-down"\|undefined` (init undefined, set by `initDrainStateTag()`); the two fields are independent and consumed as a tuple. | `pi-integration-contract/drain-state-contract.md` §pic-32 / §loom-registry-drain-state-contract | conformance |
| REQ-PIC-59 | PIC-29 | `readDrainState()` returns `{drained,tag}`; arm (a) dispatch iff `(false,undefined)`, arm (b) shutting-down note otherwise; the two arms exhaust the four-tuple state space and no third arm/accessor is permitted. | `pi-integration-contract/drain-state-contract.md` §pic-29 | conformance |
| REQ-PIC-60 | PIC-31 | `readDrainState()` is wrapped in try/catch at both call sites; on throw, the slash-command site short-circuits on arm (b), while the `session_shutdown` handler-entry site treats the read as steady-state and runs the full teardown; no dedicated diagnostic is emitted from either catch arm. | `pi-integration-contract/drain-state-contract.md` §read-failure-fallback | conformance |
| REQ-PIC-61 | — | Fixtures MUST witness `(drained:true,tag:undefined)` reachability (drain succeeds, `initDrainStateTag` throws → one `session-shutdown-teardown-step-failed` under `details.call="loomRegistry.initDrainStateTag"`) and that this tuple routes through arm (b). | `pi-integration-contract/drain-state-contract.md` §Acceptance criteria | conformance |
| REQ-PIC-62 | — | The `ActiveInvocationRegistry` is an extension-scoped `Set` of five-field entries `{loomAbort,disposeBarrier,shutdownReason,loom,invocationId}`; `loom` is the canonical-key name set at insertion and never mutated; `shutdownReason` is undefined at insertion and populated only by sub-step 2. | `pi-integration-contract/active-invocation-registry.md` §active-invocation-registry | conformance |
| REQ-PIC-63 | — | `invocationId` is a canonical lowercase 8-4-4-4-12 UUID minted through the injected `IdSource` at insertion; wire consumers read `entry.invocationId`/`entry.loom` rather than re-deriving at emission. | `pi-integration-contract/active-invocation-registry.md` §active-invocation-registry | conformance |
| REQ-PIC-64 | — | Each of the three insertion sites wraps the pre-evaluation setup sequence (`new AbortController`, `Promise.withResolvers`, `Set.add`, listener attach, and in subagent mode the awaited `createAgentSession` + one-shot listener) in try/catch routing to the runtime-defect surface (`internal-error`); a cleanup `abort()` throw MUST NOT mask the original setup throw. | `pi-integration-contract/active-invocation-registry.md` §active-invocation-registry (Dispatch-site setup wrap) | conformance |
| REQ-PIC-65 | — | Sub-step-2/3 iteration over registry entries is insertion order (V8 `Set` invariant); a `loomAbort.abort()` throw in the sub-step-2 loop is swallowed per-entry with no diagnostic, and the handler continues. | `pi-integration-contract/active-invocation-registry.md` §active-invocation-registry (Iteration order) | conformance |
| REQ-PIC-66 | PIC-24 | Each teardown-time `console.error` site (five teardown/guard codes, per-invocation `cancelled-by-session-shutdown`, terminal `sendSystemNote` `console.error`) MUST be wrapped in try/catch. | `pi-integration-contract/diagnostic-emission-isolation.md` §pic-24 | conformance |
| REQ-PIC-67 | PIC-25 | The serialisation-and-emission sequence is wrapped in a single try/catch; on a serialiser throw the catch arm emits `console.error(code)` (bare code) and MUST NOT re-throw. | `pi-integration-contract/diagnostic-emission-isolation.md` §pic-25 | conformance |
| REQ-PIC-68 | — | For the two nested-shape codes (`session-swap-instance-survived`, `cancelled-by-session-shutdown`) the serialiser-throw fallback emits the two-token form `` `${code} ${detailsEventReason}` `` from a pre-hoisted local. | `pi-integration-contract/diagnostic-emission-isolation.md` §pic-25 (Two-token fallback / Hoist obligation) | conformance |
| REQ-PIC-69 | PIC-26 | The `details.event` construction + `detailsEventReason` hoist (nested-shape codes only) are wrapped in their own try/catch, whose catch arm emits `` `${code} <unreadable>` `` (survived) or `` `${code} ${entry.loom} <unreadable>` `` (per-invocation note) and self-wraps its own `console.error`. | `pi-integration-contract/diagnostic-emission-isolation.md` §pic-26 | conformance |
| REQ-PIC-70 | PIC-27 | A throw out of `console.error` MUST be swallowed and the handler MUST continue to the next sub-step (or into sub-step 1 for the pre-sub-step-1 emission). | `pi-integration-contract/diagnostic-emission-isolation.md` §pic-27 | conformance |
| REQ-PIC-71 | PIC-28 | Every "exactly once"/"at most once" emission bound is measured at the handler invocation site (times the wrapped serialise-and-emit sequence is entered per event), not at operator-sink arrival; retry-on-inner-throw is forbidden; bare-code fallback is part of the same wrapped sequence. | `pi-integration-contract/diagnostic-emission-isolation.md` §pic-28 | conformance |
| REQ-PIC-72 | — | On the session-only reasons (`{"new","resume","fork"}`) the handler runs the same full five-sub-step teardown as `{"quit","reload"}`: no degraded tag, no `session-shutdown-runtime-degraded`, no degraded slash note, no `/reload`-recovers path. | `pi-integration-contract/session-only-degraded-state.md` §session-swap-fail-fast-tripwire | conformance |
| REQ-PIC-73 | — | After teardown on a session-only reason the handler idempotently sets private `sessionSwapTornDown=true`; every slash `handler` (at entry) and `session_start` reads it and, if set, emits exactly one `loom/host/session-swap-instance-survived` (E) via `console.error` then fail-fast-terminates the process. | `pi-integration-contract/session-only-degraded-state.md` §session-swap-fail-fast-tripwire | conformance |
| REQ-PIC-74 | — | The tripwire is dormant under the proven rebind lifecycle (fresh unarmed registry per swap) and Pi-owned `/reload` is not guarded. | `pi-integration-contract/session-only-degraded-state.md` §session-swap-fail-fast-tripwire | conformance |
| REQ-PIC-75 | — | On a sub-step-2 per-entry stamp throw the affected entry's `abort()` is skipped and its `shutdownReason` stays unset; if that invocation later reaches the clean-cancel emission, `details.event.reason` is the `"<unreadable>"` sentinel while `details.event.loom` is unaffected. | `pi-integration-contract/session-only-degraded-state.md` §substep-2-stamp-throw-residual-gap | conformance |
| REQ-PIC-76 | — | The teardown sequence runs the fixed five sub-steps regardless of `event.reason`: (1) drain + init tag, (2) stamp + `loomAbort.abort`, (3) bounded `Promise.allSettled` await, (4) close watchers, (5) detach listeners. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics | conformance |
| REQ-PIC-77 | — | Sub-step 1 runs `drain()` then `initDrainStateTag()` in fixed order; the shutting-down slash note is `"loom /<name>: extension shutting down"`; an unset tag caused by an `initDrainStateTag` throw is treated as the shutting-down arm. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (sub-step 1) | conformance |
| REQ-PIC-78 | — | Sub-step 2 stamps the captured reason onto each entry's `shutdownReason` before calling `loomAbort.abort(reason)`; both stamp and abort are wrapped per-entry; a stamp throw skips that entry's abort with no diagnostic. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (sub-step 2) | conformance |
| REQ-PIC-79 | — | Sub-step 3 awaits `Promise.allSettled(disposeBarriers)` bounded by `SHUTDOWN_AWAIT_CAP_MS=2000` (absolute deadline against `Clock`); on timeout emits one `loom/runtime/reload-teardown-timeout` naming still-in-flight invocations and proceeds; the pending timer is cleared on the success path. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (sub-step 3) | conformance |
| REQ-PIC-80 | — | Per-step isolation: sub-steps 1/3/4/5 each run in their own try/catch and emit exactly one `loom/host/session-shutdown-teardown-step-failed` (W) per failing call with `details.call` drawn from the closed normative label set; a `Clock`-arm throw skips the bounded await rather than running unbounded. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (Per-step isolation) | conformance |
| REQ-PIC-81 | — | Each entry whose `disposeBarrier` settles cleanly emits exactly one `loom/runtime/cancelled-by-session-shutdown` (E) with `display:false`, `details.event.reason` from `entry.shutdownReason`, `details.event.loom`/`invocation_id` from the entry; an invocation emits this or contributes to `reload-teardown-timeout` but not both (except the stamp-throw residual gap). | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (Per-invocation operator visibility) | conformance |
| REQ-PIC-82 | — | Conversation turns already appended before sub-step-2 abort remain in whatever conversation Pi assigns them; the runtime makes no rollback, migration, or compensation. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (Partial-append fate) | conformance |
| REQ-PIC-83 | — | Parent and child invocations are aborted in the same sub-step-2 iteration; the parent's `?`/`match` arms over the child's cancelled `Err` do not run inside the bounded-await window; each `disposeBarrier` settles independently. | `pi-integration-contract/session-shutdown-semantics.md` §session-shutdown-semantics (invoke-parent observation rule) | conformance |
| REQ-PIC-84 | — | The `session_shutdown` handler MUST be subscribed only after the `LoomRegistry` and watcher handles it reads are constructed; a `session_shutdown` MUST NOT be reachable against a partially-constructed extension state. | `pi-integration-contract/session-shutdown-semantics.md` §factory-ordering-pin | conformance |
| REQ-PIC-85 | — | A failure of a factory-time registration call is fatal at the granularity of the failing surface: `registerMessageRenderer` throw drops the renderer only (system notes degrade to `ctx.ui.notify`); `registerCommand` failure drops only that loom; `registerFlag`/`pi.on` failure is fatal to the whole extension; each emits `loom/load/extension-bootstrap-failed` (E). | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §Extension-bootstrap SDK failures | conformance |
| REQ-PIC-86 | — | A `pi.getCommands()` throw at `session_start` is caught, drops the pending-registration list for that pass, and emits one `loom/load/extension-bootstrap-failed` with `details.capability="pi.getCommands"`; the handler MUST NOT set drain state; `/reload` is recovery. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §getcommands-read-failure | conformance |
| REQ-PIC-87 | PIC-21 | The `loom-system-note` renderer is exception-safe: it MUST NOT throw out of the `MessageRenderer` invocation, catching any internal failure and returning a minimal `Component` rendering raw `content` when `display===true` or `undefined` when `display===false`; no diagnostic is emitted for a caught render failure. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §pic-21 | conformance |
| REQ-PIC-88 | — | The factory MUST call `pi.registerMessageRenderer("loom-system-note",renderer)` synchronously in the factory body exactly once; the renderer MUST return a `pi-tui` `Component` (not a string/React node), sourcing styling from the `theme` parameter; `undefined` skips rendering. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §renderer-registration | conformance |
| REQ-PIC-89 | — | The `registerFlag`/factory-time `pi.on` calls return `void` synchronously; the factory MUST NOT `await` them or attach `.then`/`.catch`; failures are synchronous throws only. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §factory-time-synchronous-void-registration | conformance |
| REQ-PIC-90 | — | A loom exposed via another loom's `tools:` materialises a Pi `ToolDefinition` with all five fields; `label` is basename-capitalised, `parameters` is `Type.Unsafe<unknown>(loweredJsonSchema)`, `execute` AJV-validates `params` against the lowered schema before forwarding; the adapter is re-entrant. | `pi-integration-contract/extension-bootstrap-and-per-loom.md` §Per-loom registration | conformance |
| REQ-PIC-91 | — | Subagent-mode callers never call `pi.registerTool`; the `ToolDefinition` is passed through `createAgentSession({customTools,tools})` and dies with `dispose()`, invisible to the user session and other extensions. | `pi-integration-contract/tool-registration-lifetime.md` §tool-registration-lifetime-and-visibility | conformance |
| REQ-PIC-92 | — | Prompt-mode callers maintain an extension-scoped registration cache `Map<schemaSlug,registeredToolName>`; first encounter of a slug calls `pi.registerTool` once with a content-addressed name; subsequent uses of the same lowered shape reuse it. | `pi-integration-contract/tool-registration-lifetime.md` §tool-registration-lifetime-and-visibility | conformance |
| REQ-PIC-93 | PIC-44 | On a cache hit the runtime MUST byte-compare cached vs new canonical-form schema bytes; on mismatch it emits `loom/runtime/registration-cache-collision`, refuses to dedup, and registers under a disambiguated `_<n>` name starting at n=2. | `pi-integration-contract/tool-registration-lifetime.md` §pic-44 | offline-unit |
| REQ-PIC-94 | PIC-17 | Around each query the runtime snapshots `pi.getActiveTools()`, calls `pi.setActiveTools([...loomCallableSetNames, respondToolName?])` (snapshot NOT unioned in), issues the query, then restores the snapshot in a `finally`. | `pi-integration-contract/tool-registration-lifetime.md` §pic-17 | conformance |
| REQ-PIC-95 | PIC-8 | On a restoring `setActiveTools` throw the runtime re-attempts once, then on second failure emits `loom/runtime/active-set-restore-failed` (E), emits a `display:true` note with the verbatim `loom: failed to restore tool active-set after /<name>; ... Run /reload to reset.` template, and propagates the original exception; no third retry. | `pi-integration-contract/tool-registration-lifetime.md` §pic-8 | conformance |
| REQ-PIC-96 | PIC-19 | A throw from the step-1 snapshot or step-2 swap-install is a setup-side failure: the query does not proceed, no restore is owed, and it surfaces on `loom/runtime/internal-error`; PIC-8's retry does not apply. | `pi-integration-contract/tool-registration-lifetime.md` §pic-19 | conformance |
| REQ-PIC-97 | — | Fixtures MUST assert the step-2 install vector exactly (empty set `[]` on untyped/empty-callable turn; `[...loomCallableSetNames, respondToolName]` on forced-respond turn) and that no step-1 snapshot member appears in the step-2 install. | `pi-integration-contract/tool-registration-lifetime.md` §Acceptance criteria | conformance |
| REQ-PIC-98 | PIC-50 | A synchronous throw from `pi.sendUserMessage` maps to `Err(QueryError{kind:"transport",message:<coerced string>,http_status:null,provider:<ctx.model api>,retryable:false})`; it MUST NOT be `internal-error`, MUST NOT propagate uncaught, MUST NOT become `Ok("")`; `provider` is `"unknown"` when `ctx.model` is undefined. | `pi-integration-contract/conversation-drive.md` §pic-50 | conformance |
| REQ-PIC-99 | — | The runtime MUST NOT `await pi.sendUserMessage(...)` and MUST NOT attach `.catch`; it uses `deliverAs:"steer"` when not idle; `deliverAs:"followUp"` is not emitted in loom 1.0. | `pi-integration-contract/conversation-drive.md` §Conversation drive — prompt mode | conformance |
| REQ-PIC-100 | PIC-51 | After `waitForIdle()` resolves the runtime probes the driven turn's trailing `assistant` message; `stopReason:"error"` maps to a transport `Err` with `message=errorMessage` (or `"provider transport failure"` when absent), never `internal-error`, never `Ok(string)`. | `pi-integration-contract/conversation-drive.md` §pic-51 | conformance |
| REQ-PIC-101 | PIC-51b | A non-`"error"` non-normal terminator is classified via the stop-reason arm: `"length"`→`context_overflow`, any other→`transport`; a resolution with no trailing `assistant` message maps to `transport` (`"provider transport failure"`), never `Ok("")`; the HTTP-status arm is not consulted. | `pi-integration-contract/conversation-drive.md` §pic-51b | conformance |
| REQ-PIC-102 | PIC-53 | The untyped-query `Ok(string)` is the concatenated (`\n`-joined) `assistant` text of the final turn, extracted only after the cancellation, `stopReason:"error"`, and non-error-terminator short-circuits pass and only for a normal `end_turn`/`stop`/`tool_use` boundary; a pure tool-use turn yields `""`. | `pi-integration-contract/conversation-drive.md` §pic-53 | conformance |
| REQ-PIC-103 | — | `loomAbort.abort()` invokes the unwrapped Pi-supplied `abort()` (one-shot-guarded against re-entry); the post-`waitForIdle` probe runs unconditionally but if `loomAbort.signal.aborted` the runtime synthesises `Err(QueryError{kind:"cancelled"})`; the runtime MUST NOT use global `pi.on("agent_end")` for completion. | `pi-integration-contract/conversation-drive.md` §prompt-mode-error-detection (Hang handling) | conformance |
| REQ-PIC-104 | PIC-18 | Prompt-mode turn-lifecycle events (`tool_call`/`tool_result`/`message_update`/`turn_end`/`agent_end`) are observed via `pi.on(event,handler)` (process-global, no `off`); these events MUST NOT be used to resolve query completion — their sole role is cancellation forwarding. | `pi-integration-contract/conversation-drive.md` §pic-18 | conformance |
| REQ-PIC-105 | PIC-2 | Prompt-mode bodies execute strictly sequentially within a single user session (at most one open snapshot/restore window at a time), derived from Pi per-session serialisation + load-time rejection of prompt-mode `.loom` callees + suspend-on-invoke + closed subagent→prompt fan-out. | `pi-integration-contract/conversation-drive.md` §pic-2 | conformance |
| REQ-PIC-106 | — | A typed query is a two-phase tool-loop: an unconstrained free phase then one forced respond turn dispatched off-session via pi-ai `complete()` with `options.toolChoice={type:"tool",name}`; the respond turn is the exempt-routed terminator (not debited from `max_rounds`) and attaches no transcript turn. | `pi-integration-contract/conversation-drive.md` §Conversation drive — prompt mode (typed queries) | conformance |
| REQ-PIC-107 | — | loom 1.0 supports typed queries only for `anthropic-messages`/`openai-completions`/`mistral`/`amazon-bedrock`; a loom whose resolved `model:` routes elsewhere emits `loom/load/typed-query-unsupported-provider` (W) at load, still loads, and any runtime typed query returns a transport `Err` (`"<provider> does not support forced tool-use; typed queries unavailable"`, `retryable:false`). | `pi-integration-contract/conversation-drive.md` §Provider compatibility for typed queries | conformance |
| REQ-PIC-108 | — | When a supported-set provider's model ignores forced-tool selection on the respond turn, the runtime classifies it as a schema-validation failure through respond-repair (synthesising one `ValidationIssue`), never as `tool_loop_exhausted`; provider-vs-model non-compliance is not distinguished in loom 1.0. | `pi-integration-contract/conversation-drive.md` §pic-typed-query-noncompliance | live |
| REQ-PIC-109 | — | The runtime maps recognised overflow-signature provider responses to `ContextOverflowError` and every other classifier-reaching response to `TransportError` regardless of HTTP status, except HTTP-200 responses the stop-reason rule routes to overflow. | `pi-integration-contract/provider-error-mapping.md` §provider-error-mapping | offline-unit |
| REQ-PIC-110 | — | `TransportError.retryable` is `true` for network-level failures (no HTTP response), HTTP 5xx, and HTTP 429; `false` for every other captured status (non-429 4xx, HTTP-200 body-envelope error, non-200 2xx, 3xx); the unsupported-provider typed-query case pins `false` independent of HTTP class. | `pi-integration-contract/provider-error-mapping.md` §transport-error-retryable | offline-unit |
| REQ-PIC-111 | — | Overflow signatures per provider are the fixed regexes (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`) matched against the pi-ai-formatted `AssistantMessage.errorMessage`; token counts are extracted only for the first two, else null. | `pi-integration-contract/provider-error-mapping.md` §Overflow signatures | offline-unit |
| REQ-PIC-112 | — | Overflow token-count extraction scans numeric runs (`[0-9]+(?:[,_][0-9]+)*`, separators stripped); exactly two runs → larger=`tokens_used`, smaller=`tokens_limit`; any other count → both null. | `pi-integration-contract/provider-error-mapping.md` §overflow-token-extraction | offline-unit |
| REQ-PIC-113 | — | Stop-reason classification of an HTTP-200 response: `"length"`→`ContextOverflowError` (both counts null); any other non-turn-boundary reason→`TransportError{retryable:false}`. | `pi-integration-contract/provider-error-mapping.md` §stop-reason-classification | offline-unit |
| REQ-PIC-114 | — | The provider seed-field mapping is the static table keyed on resolved `Model<Api>.api` (`openai-completions`→`seed`, `mistral`→`random_seed`, `anthropic-messages`/`amazon-bedrock`→omitted); a build-time `Api`-coverage assertion requires every pi-ai `Api` value to be a row key. | `pi-integration-contract/provider-error-mapping.md` §provider-seed-field-mapping | conformance |
| REQ-PIC-115 | — | Subagent mode spawns a fresh in-process `AgentSession` via `createAgentSession({customTools,tools,model,sessionManager:SessionManager.inMemory(cwd),resourceLoader})`; `tools` is always an explicit allowlist (even `[]`) suppressing Pi built-ins; the loom-constructed `ResourceLoader`'s `getSystemPrompt()` is the only `system:` channel. | `pi-integration-contract/subagent.md` §Conversation drive — subagent mode | conformance |
| REQ-PIC-116 | PIC-23 | The loom SHOULD NOT use `DefaultResourceLoader.systemPromptOverride` (it runs the discovery side-effects then ignores them); the custom-adapter form is preferred. | `pi-integration-contract/subagent.md` §pic-23 | manual/inspection |
| REQ-PIC-117 | PIC-40 | When the resolved model is `undefined` the runtime MUST NOT call `createAgentSession`; it fails the invocation with `loom/runtime/subagent-model-unresolved` (system note on slash/prompt surface, `Err(InvokeInfraError{cause:"subagent_model_unresolved"})` to an invoke parent); the check runs per invocation before spawn and is terminal. | `pi-integration-contract/subagent.md` §pic-40 | conformance |
| REQ-PIC-118 | PIC-41 | The spawn call MUST NOT include a `signal` field; if `loomAbort.signal.aborted` is true when `createAgentSession` resolves the runtime calls `session.abort()` synchronously before registering the listener and before the first turn. | `pi-integration-contract/subagent.md` §pic-41 | conformance |
| REQ-PIC-119 | PIC-43 | The subagent `Ok(string)` is extracted from the terminal `agent_end` event's `messages` array (never the `AgentSession.messages` getter); a `willRetry:true` event is ignored; the same cancellation then transport-`"error"` short-circuits apply before assistant-text assembly. | `pi-integration-contract/subagent.md` §pic-43 | conformance |
| REQ-PIC-120 | PIC-42 | The runtime MUST use the session-local `subscribe` API (unsubscribing before resolving each query) rather than global `pi.on("agent_end")`; a fresh subscription is attached per query. | `pi-integration-contract/subagent.md` §pic-42 | conformance |
| REQ-PIC-121 | PIC-9 | The spawned `AgentSession` is disposed in a `finally` that wraps the whole invocation (normal return, `Err`, panic, exception); `dispose()` is at-most-once/idempotent; a `dispose()` throw logs `loom/runtime/subagent-dispose-failure` (advisory) and never alters the result nor promotes `Ok` to `Err`. | `pi-integration-contract/subagent.md` §pic-9 | conformance |
| REQ-PIC-122 | — | `AgentSession.abort()`'s returned Promise is not awaited from the listener; a synchronous `abort()` throw routes to `internal-error` (advisory), and a late rejection of the discarded promise is silently absorbed and MUST NOT promote to `internal-error`. | `pi-integration-contract/subagent.md` §pic-9 | conformance |
| REQ-PIC-123 | — | A thrown/rejected `createAgentSession`, or a returned handle whose `dispose` is not a function, routes to `loom/runtime/internal-error` (system note / `InvokeInfraError{cause:"internal_error"}`); no session was constructed so the `finally` has nothing to dispose. | `pi-integration-contract/subagent.md` §pic-9 | conformance |
| REQ-PIC-124 | — | The subagent state-isolation matrix is authoritative: inherited-from-frontmatter (`system:`, `model`, `customTools`, `params`), forwarded-from-`ctx` (`cwd`, `modelRegistry`, `getContextUsage`, other members), and not-inherited (caller transcript, system prompt, ambient tools, `loomAbort`, caller `params`, `withActiveTools` scope). | `pi-integration-contract/subagent.md` §subagent-state-isolation-matrix | conformance |
| REQ-PIC-125 | — | loom 1.0 imposes no cap on in-flight loom invocations within an extension instance and no fairness/scheduling rule above Pi's event loop. | `pi-integration-contract/subagent.md` §no-invocation-cap | conformance |
| REQ-PIC-126 | PIC-22 | On N≥2 parallel subagent-mode tool calls the runtime MUST, before any invocation returns, both initiate `createAgentSession` for all N and reach each session's `sendUserMessage`; a fake blocking `sendUserMessage` witnesses that all N sessions were created before any blocked call is released. | `pi-integration-contract/subagent.md` §pic-22 | conformance |
| REQ-PIC-127 | — | The binder pass issues one `complete(model,context,options)` per attempt: `context.messages` is the fixed single `user` message `Bind the slash-command arguments now.`, `context.tools` is exactly the `__loom_bind_<slug>` tool (label `"Loom binder envelope"`, fixed description), `options.temperature=0`, `options.signal=loomAbort.signal`, seed keyed on `Api`, `options.onResponse` registered. | `pi-integration-contract/binder-inference.md` §binder-inference-call | conformance |
| REQ-PIC-128 | — | The binder envelope is extracted from the first `ToolCall` whose `name` matches, and success extraction takes precedence over `stopReason`-based failure classification; a missing/mismatched `ToolCall` is the malformed-envelope class, while transport failures classify through the provider-error table. | `pi-integration-contract/binder-inference.md` §binder-inference-call | conformance |
| REQ-PIC-129 | — | System notes emit through a single `pi.sendMessage({customType:"loom-system-note",content,display,details},{triggerTurn:false})` call; the custom-type channel persists in transcript for replay/`/tree`. | `pi-integration-contract/runtime-event-channel.md` §System notes | conformance |
| REQ-PIC-130 | — | The `details` field carries exactly one of four disjoint-by-key shapes (`diagnostics`, `event`, `structural`, `recovery`), each with a normative `display`/`content` pairing; renderers MUST NOT assume more than one is present; future shapes are additive/disjoint. | `pi-integration-contract/runtime-event-channel.md` §system-note-details-shapes | conformance |
| REQ-PIC-131 | — | The always-log set emits exactly once per occurrence regardless of author disposition; group A emits `details:{event:RuntimeEvent}` (transport/model_tool/code_tool/tool_loop_exhausted/invoke_infra/binder failures), group B emits `details:{diagnostics}` (runtime panics minus the five console.error-routed teardown codes). | `pi-integration-contract/runtime-event-channel.md` §Runtime event channel | conformance |
| REQ-PIC-132 | — | `validation`, `context_overflow`, `cancelled`, and `invoke_callee` are NOT in the always-log set and emit nothing on the channel even at a discard site. | `pi-integration-contract/runtime-event-channel.md` §Runtime event channel (Discard-site disposition) | conformance |
| REQ-PIC-133 | — | The `RuntimeEvent` payload shape is normative and additive-only, carrying `kind`/`code?`/`loom`/`invocation_id`/`query_site?`/`discard_site?`/`message`/`attempts?`/`tokens_used?`/`masked?`/`occurred_at`; panics construct no `RuntimeEvent`. | `pi-integration-contract/runtime-event-channel.md` §Runtime event channel (RuntimeEvent) | conformance |
| REQ-PIC-134 | — | A loom terminating with `Ok(v)` emits no `loom-system-note` keyed on the `Ok(v)` outcome (success-side null-policy). | `pi-integration-contract/runtime-event-channel.md` §success-side-null-policy | conformance |
| REQ-PIC-135 | — | An occurrence produces exactly one always-log emission at its origin; a top-level prompt-mode cascade re-emits the same `RuntimeEvent` verbatim once at the boundary (sharing `occurred_at`); a panic emits exactly one note with `details:{diagnostics:[Diagnostic]}` and `aborted:` framing in `content`. | `pi-integration-contract/runtime-event-channel.md` §Deduplication and lifetime rules | conformance |
| REQ-PIC-136 | PIC-1 | The optional `masked` field carries the closed identifier set `{"ceiling#1".."ceiling#4"}`, omitted when empty (never `[]`); it is a pure read (no second emission), copied verbatim on cascade-twin re-emission, and not part of the dedup key; in loom 1.0 the only non-empty reachable mask is `["ceiling#2"]` on the `validation` typed-query-response event. | `pi-integration-contract/runtime-event-channel.md` §pic-1 | conformance |
| REQ-PIC-137 | — | The `pi.sendMessage` note is best-effort with a fallback chain: `ctx.ui.notify(content,"error")` (skipped on `display:false` or `content:""`), then `loom/runtime/system-note-delivery-failed`, then `console.error`; the fallback never retries the original call nor aborts the handler. | `pi-integration-contract/runtime-event-channel.md` §Runtime event channel (fallback) | conformance |
| REQ-PIC-138 | PIC-54 | The terminal `console.error` of the `sendSystemNote` fallback chain MUST be wrapped in try/catch and its throw silently swallowed, regardless of the reach-path. | `pi-integration-contract/runtime-event-channel.md` §pic-54 | conformance |
| REQ-PIC-139 | PIC-56 | The `loom-system-note` renderer's `Component.render(width)` MUST return no line whose visible width exceeds `width` (wrap or truncate, preserving styling and blank lines); a non-positive width falls back to raw lines. | `pi-integration-contract/runtime-event-channel.md` §pic-56 | conformance |
| REQ-PIC-140 | — | `estimateTokens(message:AgentMessage):number` is imported from `@earendil-works/pi-coding-agent` and called as-imported behind the production `PiTokenEstimator` adapter; loom does not re-specify the algorithm. | `pi-integration-contract/host-interfaces-core.md` §estimatetokens-named-export | conformance |
| REQ-PIC-141 | — | `buildSessionContext(entries,leafId?)` is a free named export (not a `ctx.sessionManager` method); the runtime calls it on the `ReadonlySessionManager` `Pick<>` surface in both modes and consumes `.messages` (`AgentMessage[]`); it MUST NOT substitute `ctx.getContextUsage()` for per-turn accounting. | `pi-integration-contract/host-interfaces-core.md` §buildSessionContext | conformance |
| REQ-PIC-142 | — | The runtime touches only the enumerated `ExtensionContext` members; deliberately-omitted members remain MUST-NOT-call (arm (a) constraint per GOV-18); each signature is a re-validated snapshot at the pin. | `pi-integration-contract/host-interfaces-core.md` §extensioncontext-interface | manual/inspection |
| REQ-PIC-143 | — | The binder-model matcher compares a configured string against `Model<Api>.id` (model identity) and `Model<Api>.provider` (short provider id), never against `Model<Api>.api`. | `pi-integration-contract/host-interfaces-core.md` §binder-model-matcher-inputs | offline-unit |
| REQ-PIC-144 | — | The synthesised tool-execution `ctx` overrides exactly three members in both modes — `signal`→`loomAbort.signal`, `sessionManager`→(user session / spawned in-memory), `abort`→wrapped to call `loomAbort.abort()` — and forwards every other member unchanged. | `pi-integration-contract/host-interfaces-core.md` §extensioncontext-interface (Per-mode override semantics) | conformance |
| REQ-PIC-145 | — | Code-side `<name>(args)` invokes the Pi tool's `execute(toolCallId,params,signal,onUpdate,ctx)` with a `loom-direct:`-prefixed UUID (`IdSource`), lowered JSON `params`, `loomAbort.signal`, no-op `onUpdate`, and the synthesised `ctx`. | `pi-integration-contract/host-interfaces-core.md` §tool-execution-from-loom-code | conformance |
| REQ-PIC-146 | — | `AgentToolResult` has no `isError` field; the runtime filters `content` to `type==="text"` entries and joins `.text` with `\n`; a non-conforming envelope routes to `loom/runtime/internal-error` with `details.kind="tool-return-shape"`; an `execute()` throw maps to `Err(CodeToolError{cause:"execution"})` with the coerced message UTF-8-truncated to ≤4096 bytes on a code-point boundary. | `pi-integration-contract/host-interfaces-core.md` §tool-execution-from-loom-code | conformance |
| REQ-PIC-147 | — | The complete `execute()` outcome routing is fixed: clean resolve→`Ok(text)` (possibly `Ok("")`); throw→`CodeToolError{cause:"execution"}`; aborted→`CodeToolError{cause:"cancelled"}`; non-conforming envelope→`internal-error`; late settlement after cancellation is discarded (no rebind, no second `Err`/`RuntimeEvent`) with a swallowing handler kept attached. | `pi-integration-contract/host-interfaces-core.md` §tool-execution-from-loom-code (Outcome routing summary) | conformance |
| REQ-PIC-148 | — | Subagent mode touches only `AgentSession.{abort,dispose,sendUserMessage,subscribe}`; `abort()` is factory-probed, the other three re-validated by editorial review; the `AgentSession.messages` getter is not loom-load-bearing. | `pi-integration-contract/host-interfaces-core.md` §agentsession-interface | conformance |
| REQ-PIC-149 | — | The loom's authoritative signal is `loomAbort.signal` from a per-invocation `AbortController`, never `ctx.signal` directly (which is `undefined` in idle contexts); the runtime forwards `ctx.signal`/tool `signal`/parent-invoke signals into `loomAbort.abort()` via one-shot listeners. | `pi-integration-contract/host-interfaces-core.md` §cancellation-source | conformance |
| REQ-PIC-150 | PIC-10 | The interpreter `await`s a `Checkpoint.before(kind,site)` immediately before each cancellation checkpoint; production resolution is next-microtask for all kinds except `loop-iter` (one macrotask turn via `Clock.setTimeout(fn,0)`); the seam MUST NOT add `before(...)` calls at non-enumerated sites. | `pi-integration-contract/host-interfaces-services.md` §pic-10 | conformance |
| REQ-PIC-151 | PIC-11 | The injected `SchemaValidator` reports every error in one pass, performs no conversion/defaulting, resolves `$ref` only within the lowered document, silently accepts unknown `format`, is deterministic per (schema,value), and byte-verifies canonical bytes on a cache hit (else `loom/runtime/validator-cache-collision`); one instance per runtime, never a module global. | `pi-integration-contract/host-interfaces-services.md` §pic-11 | conformance |
| REQ-PIC-152 | PIC-12 | The injected `Clock` provides `now()` (monotonic), `wallNow()` (epoch), `setTimeout`/`clearTimeout`; the runtime MUST NOT call `Date.now`/`performance.now`/`Date.prototype.getTime`/global `setTimeout`/`clearTimeout` outside the `WallClock` adapter, enforced by a build-time check rejecting direct lexical references. | `pi-integration-contract/host-interfaces-services.md` §pic-12 | conformance |
| REQ-PIC-153 | PIC-13 | The injected `FileSystem` provides `readText`/`readBytes`/`writeText`/`exists`/`homedir`/`cwd`/`readdir`/`lstat`/`realpath` with pinned Node-style `.code` rejections; the runtime MUST NOT read `process.env.HOME`/`USERPROFILE` or call `process.cwd()`; `readdir` returns raw non-normalised UTF-8 names; `realpath` yields byte-identical case-canonicalised output for one target. | `pi-integration-contract/host-interfaces-services.md` §pic-13 | conformance |
| REQ-PIC-154 | PIC-14 | The `FileWatcher` seam (separate from `FileSystem`) delivers only `add`/`change`/`unlink` events plus an enumerated terminal-signal channel; the 250 ms debounce, build-aside-then-publish, and structural-vs-content split are runtime layers, not seam properties; one instance per runtime. | `pi-integration-contract/host-interfaces-services.md` §pic-14 | conformance |
| REQ-PIC-155 | PIC-16 | The injected `TokenEstimator.estimate(message)` (production `PiTokenEstimator` delegating to `estimateTokens`) is relied on as a deterministic pure function of its argument within a fixed pin; one instance per runtime. | `pi-integration-contract/host-interfaces-services.md` §pic-16 | conformance |
| REQ-PIC-156 | PIC-20 | The injected `IdSource` mints `newInvocationId()`/`newToolCallId()` (canonical lowercase 8-4-4-4-12 UUIDs); the runtime MUST NOT call `crypto.randomUUID()` outside the production adapter; one instance per runtime. | `pi-integration-contract/host-interfaces-services.md` §pic-20 | conformance |
| REQ-PIC-157 | PIC-15 | The runtime depends on exactly seven named SDK capabilities (items 1–7); the seven-capability cardinality is enforced by `CAPABILITY_OBLIGATIONS.length === 7`. | `pi-integration-contract/inventory-audit-intro.md` §pic-15 / §sdk-capability-inventory | conformance |
| REQ-PIC-158 | — | `pi.registerFlag`/`pi.getFlag` are not among the seven capabilities and do not extend the count; they resolve through `SDK_SURFACE_INVENTORY` as audit category-(1) entries. | `pi-integration-contract/inventory-audit-intro.md` §non-capability-pi-members | conformance |
| REQ-PIC-159 | — | Capability items 1–7 (slash-command registration; prompt-mode drive; subagent isolated session; tool registration+gating; cancellation propagation; custom-message channel+renderer; binder LLM model) each pin the specific SDK members the runtime requires. | `pi-integration-contract/capability-inventory-items.md` §sdk-cap-* | conformance |
| REQ-PIC-160 | — | The build-time SDK surface-inventory literal-read assertion imports `@earendil-works/pi-coding-agent` at the candidate version and asserts each of the seven capabilities is present on the imported namespace, plus `CAPABILITY_OBLIGATIONS.length===7` and the `SessionShutdownEvent['reason']` literal-array consistency check. | `pi-integration-contract/version-bump-step2.md` §bump-step-2-positive | conformance |
| REQ-PIC-161 | — | The build-time bidirectional `SessionShutdownEvent['reason']` type-equality assertion is reached by `npm run typecheck`, is included by the loom `tsconfig`, and MUST surface the literal brand string `loom/typecheck/session-shutdown-reason-snapshot` verbatim in a failing `tsc` diagnostic. | `pi-integration-contract/version-bump-triggers.md` (step 5) | conformance |
| REQ-PIC-162 | — | A Pi minor bump MUST follow the seven-step version-bump procedure; a bump commit that moves `peerDependencies` without matching constant-block/spec/inventory updates fails the build-time tests, and a bump is not landed until green end-to-end runtime evidence exercises all six named surfaces. | `pi-integration-contract/version-bump-intro.md` §pi-version-bump-procedure; `version-bump-triggers.md` (outputs) | manual/inspection |
| REQ-PIC-163 | — | On each Pi minor bump the contributor MUST audit the candidate minor against presuppositions (a)–(e) via editorial review and record a per-item pass/fail/N/A outcome in the bump commit message; items (f)–(as) are SHOULD-level but per-item recording is mandatory. | `pi-integration-contract/version-bump-step2.md` §Editorial-review arm | manual/inspection |
| REQ-PIC-164 | — | The build-time gate verifies `package.json#engines.node` equals `@earendil-works/pi-coding-agent`'s `engines.node` floor (`>=22.19.0`) by literal-read test. | `overview-and-orientation.md` §Prerequisites (Host runtime item 1) | conformance |

## AREA: SESS (session-model)

| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-SESS-1 | SM-1 | A Pi extension instance is bound to exactly one active user session at a time; concurrent user sessions in the same host process are out of scope and presupposed absent. | `session-model-and-appendix.md` §sm-1-single-active-session-binding | manual/inspection |
| REQ-SESS-2 | SM-2 | Pi fires `session_shutdown` with `event.reason ∈ {"quit","reload","new","resume","fork"}`; an out-of-set or throwing reason routes through the full teardown and emits one `loom/host/session-shutdown-reason-unknown` (W). | `session-model-and-appendix.md` §sm-2-closed-shutdown-reason-set | conformance |
| REQ-SESS-3 | SM-3a | The handler runs the full fixed teardown sequence (including sub-step-4 watcher closure) for every reason in the closed set. | `session-model-and-appendix.md` §sm-3a-teardown-sequence | conformance |
| REQ-SESS-4 | SM-3b | Session-swap semantics (abort-and-await, per-invocation operator visibility, partial-append fate, invoke-parent observation) apply on session-only reasons, which now run full teardown and arm the fail-fast tripwire (governed-by-rebind). | `session-model-and-appendix.md` §sm-3b-session-swap-semantics | conformance |
| REQ-SESS-5 | SM-4 | On session-only reasons `{"new","resume","fork"}` the runtime runs the same full teardown as `{"quit","reload"}` and arms the fail-fast tripwire; no degraded state, no degraded tag, no `/reload`-recovers path. | `session-model-and-appendix.md` §sm-4-session-only-degraded-state | conformance |
| REQ-SESS-6 | SM-5 | Session-only reasons run the same full teardown; no drain-state tag transition beyond `"shutting-down"` and no degraded slash note are written on any reason. | `session-model-and-appendix.md` §sm-5-tag-transition-predicate | conformance |
| REQ-SESS-7 | SM-6 | The diagnostic-channel signal of a lifecycle violation is the `loom/host/session-swap-instance-survived` (E) row emitted via `console.error` before fail-fast termination; `session-shutdown-runtime-degraded` is retired. | `session-model-and-appendix.md` §sm-6-diagnostic-emission-predicate | conformance |
| REQ-SESS-8 | SM-7a | Cancellation is always independent across modes: every invocation carries its own `loomAbort` and is a distinct `ActiveInvocationRegistry` entry. | `session-model-and-appendix.md` §sm-7a-cancellation-independence | conformance |
| REQ-SESS-9 | SM-7b | Transcript and tool-table isolation are subagent-mode-only (private `AgentSession`); prompt-mode drives the user session's transcript by definition. | `session-model-and-appendix.md` §sm-7b-subagent-mode-isolation | conformance |
| REQ-SESS-10 | SM-7c | Prompt-mode bodies execute strictly sequentially within a single user session (at most one open snapshot/restore window at a time). | `session-model-and-appendix.md` §sm-7c-prompt-mode-sequential-execution | conformance |
| REQ-SESS-11 | SM-7d | loom 1.0 imposes no admission cap and no scheduler above Pi's event loop for in-flight invocations. | `session-model-and-appendix.md` §sm-7d-no-cap-no-scheduler | conformance |
| REQ-SESS-12 | SM-7e | Cancellation propagates downward only (parent→children); a sibling's cancellation does not affect its siblings. | `session-model-and-appendix.md` §sm-7e-downward-only-cancellation | conformance |
| REQ-SESS-13 | SM-8 | Each per-invocation budget (binder retry, `tool_loop.max_rounds`, `invoke`-chain depth) is scoped to a single invocation and is not shared, pooled, or replenished across siblings. | `session-model-and-appendix.md` §sm-8-per-invocation-budget-non-sharing | conformance |

## AREA: GOV (governance)

| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-GOV-1 | GOV-3 | REQ-ID prefixes are byte-exact uppercase ASCII tokens of length 2–4 (`[A-Z]{2,4}`), matched case-sensitively over exclusion-stripped source; every `\b[A-Z]{2,4}-[1-9][0-9]*\b` token in the corpus must have a prefix in the live+retired union. | `governance/req-id-prefix-table-active-a.md` §gov-3 | offline-unit |
| REQ-GOV-2 | GOV-5 | Each row's `Prefix` value is a complete word-boundary-distinct token; two prefixes sharing a substring (e.g. `BNDS`/`BNDR`) are distinct tokens, neither a sub-prefix of the other. | `governance/req-id-prefix-table-active-a.md` §gov-5 | offline-unit |
| REQ-GOV-3 | GOV-6 | At every commit, the set of prefixes in REQ-IDs across `spec_topics/**/*.md` is a subset of the union of (live prefix table, retired prefixes sub-table). | `governance/req-id-prefix-table-active-b.md` §gov-6 | offline-unit |
| REQ-GOV-4 | GOV-24 | The prefix→page binding is append-only and immutable; live `Prefix` values are a key (no duplicates, no overlap with retired), and a many-to-one page→prefix relation is permitted with per-row invariants applied independently. | `governance/req-id-prefix-table-active-b.md` §gov-24 | offline-unit |
| REQ-GOV-5 | — | The Retired prefixes / Retired REQ-IDs / Retired anchor aliases sub-tables are append-only; a retired prefix/ID/alias slug MUST NOT be reused; `Retired in` cells carry only a 7-char SHA or a release tag. | `governance/req-id-prefix-table-retired.md` §Retired prefixes; `governance/anchor-scheme-and-retired.md` §retired-req-ids | offline-unit |
| REQ-GOV-6 | GOV-16 | Inline labels match the two-armed alternation partitioned by the per-page `Tail form` column; every `\b[A-Z][A-Z0-9]{1,5}-(?:[1-9][0-9]*\|[a-z])\b` token must be a well-formed REQ-ID or a well-formed inline label; cross-class malformed tails (`HC3-7`, `NOCEIL-a`, `BNDR-a`) violate the closure invariant. | `governance/stable-inline-labels.md` §gov-16 | offline-unit |
| REQ-GOV-7 | GOV-1 | Each REQ-ID's canonical anchor is the bold-with-period inline marker; the `<a id="prefix-n"></a>` dual-form is required in four HTML-anchor contexts, placed same-line before the marker; per-page progressive normalisation applies when a commit touches an existing anchor token. | `governance/req-id-prefix-table-active-a.md` §gov-1 | manual/inspection |
| REQ-GOV-8 | GOV-9 | Every cross-page reference to another page's normative rule resolves as a `#prefix-n` URL fragment (or a section link for narrative pages); every REQ-ID anchor on a non-narrative page must resolve as `#prefix-n`. | `governance/req-id-prefix-table-active-b.md` §gov-9 | offline-unit |
| REQ-GOV-9 | GOV-19 | Loom releases use two lexically-distinguishable literal forms: design-scope `loom <major>.<minor>` / `loom <major>.x` (matches `loom \d+\.(?:\d+\|x)`) and frozen-baseline `loom <major>.<minor>.<patch>` (matches `loom \d+\.\d+\.\d+`); `loom 1.0 ⊇ loom 1.0.0`. | `governance/release-version-naming.md` §gov-19 | offline-unit |
| REQ-GOV-10 | GOV-29 | Every canonical-arm slug (`loom-1-0`/`loom-1-0-0` version segment) MUST resolve uniquely across the corpus; colliding sites append a site-distinguishing aggregator tail while the `v1` alias arms stay byte-identical. | `governance/release-version-naming.md` §gov-29 | offline-unit |
| REQ-GOV-11 | GOV-23 | A published `sm-N-…` orientation anchor MUST NOT be reused for a different obligation; a split appends new sub-letters and a retired `sm-N` anchor is not recycled. | `governance/anchor-scheme-and-retired.md` §gov-23 | manual/inspection |
| REQ-GOV-12 | GOV-17 | The spec corpus MAY cross-reference its dependencies and MUST NOT cross-reference its dependents (no circular Markdown-link cross-references); forbidden targets include `docs/plan.md`, `docs/plan_topics/*.md`, `README.md`, `CHANGELOG.md`. | `governance/corpus-direction-and-scope.md` §gov-17 | offline-unit |
| REQ-GOV-13 | GOV-31 | Each integer-count aggregator MUST preserve a parseable integer literal whose value equals the count of items in its source set at every commit (SDK-capabilities aggregator excepted, enforced by `CAPABILITY_OBLIGATIONS.length===7`). | `governance/req-id-prefix-table-active-b.md` §gov-31 | offline-unit |
| REQ-GOV-14 | GOV-15 | "Loads cleanly under loom 1.0.0" means loading emits no `E`-severity diagnostic (`W` does not disqualify); a file loading cleanly is expected to produce identical return values, ordered diagnostic-code sequences, and byte-equivalent (after variable-placeholder normalisation) `loom-system-note` content across every 1.x release, subject to the ceiling-set and diagnostic-registry carve-outs. | `governance/source-language-stability.md` §gov-15 / §gov-15-loads-cleanly | manual/inspection |

## DEFERRED / out-of-scope (Cluster 6)

Deferred, not-yet-implemented, out-of-scope, non-goal, future, or purely-informational corpus-convention material.

**loom 1.0 non-goals (ship without a forward-compatibility seam)** — `future-considerations/model-changes-and-non-goals.md` §loom-1-0-non-goals:
- No per-loom sandbox or capability model (loom code runs at full host-process privilege; a future per-extension privilege facet is editorial-review-detected only).
- No formal source-language migration mechanism for major-version transitions (loom 1.0 → loom 2.0 requires a separate mechanism).
- No non-Node JavaScript host support (Bun/Deno/browser-embed require a probe re-design).
- No concurrent user sessions in the same host process (presupposition; no Step-0 probe, no new disposition for a second session that reaches the extension).
- No reliance on a Pi extension-host stdio-capture facet (teardown `console.error` is best-effort; §pi-stdio-capture-facet).
- No parallel-`invoke` surface (every `invoke(...)` to a prompt-mode callee suspends the parent).
- No parallel fan-out to prompt-mode `.loom` callees (rejected at load time; Pi-host tools and subagent-mode `.loom` callees may still fan out).
- No admission cap on in-flight loom invocations and no cross-sibling aggregation/throttling/storm-detection.

**Model-level changes (no loom 1.0 seam expected)** — `future-considerations/model-changes-and-non-goals.md` §Model-level changes: first-class loom values (`Loom<T>`); loom-level concurrency primitives (`parallel {}`); streaming partial tool results; structured tool output schemas; richer untyped-query return shape; per-parameter `mut`; value-carrying `break expr`; `match` guards and rest patterns; non-decimal/underscore number literals; integer-division operator; additional string/array stdlib methods.

**Surface extensions (loom 1.0 leaves a seam)** — `future-considerations/surface-extensions.md` §Surface extensions: per-call timeouts; pre-flight token-count check; typed-query support for providers without named-tool forcing (widens the four-provider set); per-query overrides for `model`/`tools`/`system`; user-defined error types beyond `QueryError`; `BinderError` as a `QueryError` variant; binder refinement loop (multi-turn `needs_info`); named-argument invocation syntax; richer `system:` expression sublanguage; package-style/project-rooted import paths; mid-loom user-session replacement (`withSession` re-acquisition); symlink-resolution hardening (`openat2` atomic resolve-and-open).

**Surface extensions without a dedicated seam / gated on open design** — `future-considerations/surface-extensions.md` §surface-extensions-no-dedicated-seam / §surface-extensions-open-design: per-loom `bind_temperature`; operator-level `looms.toolLoopMaxRounds` override; user-overridable binder system prompt; Pi-owned subagents exposed as enumerable slash commands (widens `SlashCommandSource` to four arms — `v1-seam-pi-owned-subagents-collision-source-set`); `argumentHint` field on `RegisteredCommand`; automatic context escalation (re-entrancy seam gated on a decision).

**Tooling deferrals (no loom 1.0 impact)** — `future-considerations/surface-extensions.md` §tooling-deferrals: LSP support; `loom test` command; richer runtime-event telemetry (aggregation, latency histograms, cost reporting, consumer read API); automated cadence for re-importing Pi patches (no Renovate/cron; patch-skew window bounded only by contributor cadence).

**Post-1.0 hardening (not a loom-1.0 merge gate)** — the automated build-time **inventory-closure audit** (`inventory-audit-intro.md` §sdk-cap-inventory-closure-audit; `audit-target-categories.md`, `audit-recognised-shapes.md`, `audit-resolution.md`, `audit-failures.md`, `audit-wire-and-canary.md`): its surface-detection categories, recognised import/access shapes, `SDK_SURFACE_INVENTORY` join keys, `// allow-pi-surface:` exemption grammar, malformed/stale-marker discriminators, `audit/<class>/<family>/<symptom>` failure-surface contract, tab-delimited stdout wire format, infrastructure-failure handling, and non-empty-scan canary are all deferred. The **surface-set closure** obligation it mechanizes remains a loom-1.0 MUST enforced by the positive-direction step-2(a) literal-read assertions and the editorial-review checklist (captured as REQ-PIC-160/162/163). The deferred **GOV-15 conformance fixture suite** (mechanical diff of the three observables against a frozen 1.0.0 baseline) is likewise post-1.0.

**Purely-informational corpus-convention governance (arm (b) meta; editorial-convention only)**:
- GOV-7 (mutation procedures Add/Rename/Delete/Merge/promotion/demotion), GOV-8 (REQ-ID lifecycle Split/Merge/Deletion/Pure-rewording — bookkeeping discipline, not mechanically decidable), GOV-22 (progressive coinage of un-anchored obligations) — `governance/req-id-prefix-table-active-a.md`, `req-id-prefix-table-active-b.md`.
- GOV-18 (binding scope: obligations bind only the implementation target (arm a) and the spec corpus (arm b); MUST NOT bind third parties) — `governance/corpus-direction-and-scope.md` §gov-18.
- GOV-20 / GOV-25 / GOV-26 / GOV-27 / GOV-28 (legacy `V1`/`V1.0`/`V1.x`/`V2` token aliases, dual-anchor convention, canonical-arm citation, alias permanence, intensional definition, alias retirement discharge) — `governance/release-version-naming.md`.
- GOV-30 (spec.md aggregator paragraphs informative; lock-step convention — reviewer-enforced editorial convention, no mechanical witness), GOV-31 rename arm and orientation-prose-literal carve-out — `governance/req-id-prefix-table-active-b.md`.
- Terminology REQ-IDs `FRNT-2`/`FRNT-3`/`SUBS-2` (non-executable naming discipline) and the narrative-row rationale — `governance/req-id-prefix-table-active-b.md` §terminology-req-ids.
- Retired REQ-IDs registry rows (GOV-2/GOV-4/GOV-10/GOV-11/GOV-12/GOV-13/GOV-14/GOV-21) — `governance/anchor-scheme-and-retired.md` §retired-req-ids.
- Stable-inline-labels registry mechanics (canonical form, per-page prefix table, third extractor, lifecycle, retirement section, cross-link form) beyond the closure invariant captured as REQ-GOV-6 — `governance/stable-inline-labels.md` §gov-16.

**Host-behaviour presuppositions (editorial-review-detected on Pi minor bump; not build-time-gated)** — `host-prerequisites.md` and the version-bump checklist items (a)–(as): extension-instance survival across session-only shutdown (RESOLVED governed-by-rebind), multi-`session_shutdown` delivery, `/reload`+`ctx.reload()` recovery, teardown-vs-session-swap reason partition, per-session slash-dispatch serialisation, `pi.getCommands()` first-`session_start` completeness, `AgentMessage[]` chronological ordering and leading-`user` guarantee, `TokenEstimator` per-message determinism, triggering-slash-turn commit posture, model-registry population timing, settings write-back key preservation, interactive-session operator-always-present, `pi.registerFlag`/`getFlag` overlap behaviour, `pi.registerTool` post-startup acceptance and registration persistence, `ExtensionRuntime.invalidate` teardown ordering, `convertToLlm` context entry, `waitForIdle()` no-rejection, driven-turn session-commit ordering, user-visible streaming ordering, `complete()` forced-tool / retry-timing / signal-honouring, provider-owned overflow wording, `dispose()`-mid-`abort()` safety, `SessionManager.inMemory` transcript-discard behaviour, factory-time `cwd==project root`, subagent-spawn satellite types. Detection is contributor editorial review under the Pi version bump procedure.
