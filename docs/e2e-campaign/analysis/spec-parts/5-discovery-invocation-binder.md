# Cluster 5 — Discovery, slash-invocation, invocation, binder

## AREA: DISC (discovery)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-DISC-1 | — | Loom files are discovered from exactly five sources: global `~/.pi/agent/looms/*.loom`, project `.pi/looms/*.loom`, packages (`pi.looms` entry or `looms/` fallback), settings `loomPaths`, and CLI `--loom <paths>`. | docs/spec_topics/discovery/discovery-sources.md:3 | conformance |
| REQ-DISC-2 | — | Discovery is non-recursive and matches only `*.loom`; the glob is byte-exact lowercase so `Plan.LOOM` never matches on any platform. | docs/spec_topics/discovery/discovery-sources.md:9 | offline-unit |
| REQ-DISC-3 | — | `.warp` library files are never discovered as slash commands; they are reachable only via `import` with paths relative to the importing file. | docs/spec_topics/discovery/discovery-sources.md:9 | offline-unit |
| REQ-DISC-4 | — | `.loom`/`.warp` extensions, the `pi.looms` manifest key, the `loomPaths` settings array, and the `--loom` flag are coined by the extension (not Pi); `--loom` is registered via `pi.registerFlag('loom', …)` and read via `pi.getFlag('loom')`. | docs/spec_topics/discovery.md:3; docs/spec_topics/discovery/discovery-sources.md:19 | conformance |
| REQ-DISC-5 | — | The `--loom` flag joins multiple paths with `path.delimiter` (`:` POSIX, `;` Windows); each component is a file or directory resolved by the `loomPaths` rules. | docs/spec_topics/discovery/discovery-sources.md:7 | offline-unit |
| REQ-DISC-6 | — | Source priority high-to-low is CLI > settings (project over global) > project > packages > global; a same-name resolution from multiple sources keeps the higher-priority source and emits `loom/load/cross-source-shadow` (warning) naming both paths. | docs/spec_topics/discovery/discovery-sources.md:39 | conformance |
| REQ-DISC-7 | DISC-1 | Any path beginning with `~/` has its leading `~` expanded via the injected `FileSystem.homedir()` seam; the `~user` form is not honoured and `process.env.HOME`/`USERPROFILE` and platform branches are forbidden. | docs/spec_topics/discovery/discovery-sources.md:37 | offline-unit |
| REQ-DISC-8 | — | The active discovery-root set for a session is the union of global root, project root, each scanned package's contributing directory, each `loomPaths` entry (directory contributes itself, file contributes its parent), and each `--loom` component; roots are computed once per pass and cached, recomputed on hot-reload. | docs/spec_topics/discovery/discovery-sources.md:23 | conformance |
| REQ-DISC-9 | DISC-2 | Each source has defined missing/unreadable/wrong-type behaviour per the failure-modes table: conventional roots tolerate absence silently; `pi.looms`/settings/`--loom` explicit references surface a missing path as error (CLI errors on unreadable/wrong-type too). | docs/spec_topics/discovery/discovery-sources.md:47 | offline-unit |
| REQ-DISC-10 | DISC-2 | A discoverable `.loom` file that is itself unreadable is reported `loom/load/unreadable` (warning) regardless of source, is not registered, and the scan continues. | docs/spec_topics/discovery/discovery-sources.md:60 | offline-unit |
| REQ-DISC-11 | DISC-2 | Diagnostics use codes `loom/load/missing-source`, `loom/load/unreadable-source`, `loom/load/wrong-type-source`; each carries a source descriptor (e.g. `"settings entry index 2"`, `"--loom flag #1"`). | docs/spec_topics/discovery/discovery-sources.md:62 | offline-unit |
| REQ-DISC-12 | DISC-2 | Errors are fatal for the offending entry only; other entries and the other four sources still process to completion. | docs/spec_topics/discovery/discovery-sources.md:65 | offline-unit |
| REQ-DISC-13 | DISC-2 | A clean leaf-`ENOENT` (all ancestors `lstat` ok as directories) classifies as missing; any ancestor `EACCES`/`EPERM`/`ENOTDIR`/`ENOENT` classifies as unreadable → `loom/load/unreadable-source` (warning); uniform on POSIX/Windows with no platform branch. | docs/spec_topics/discovery/discovery-sources.md:66 | offline-unit |
| REQ-DISC-14 | DISC-2 | A directory entry (`--loom` or settings) is a valid path regardless of contents: an empty directory (or one with only non-`.loom` files) enumerates zero looms and emits no diagnostic; the wrong-type rule fires only for a non-`.loom`-file, non-directory target. | docs/spec_topics/discovery/discovery-sources.md:68 | offline-unit |
| REQ-DISC-15 | DISC-3 | Two `*.loom` files within one source differing only in case emit `loom/load/case-collision` (warning, per source); the lexicographically-first path under case-sensitive byte comparison wins; the rule is intra-source only. | docs/spec_topics/discovery/discovery-sources.md:70 | offline-unit |
| REQ-DISC-16 | — | A file whose stem matches `^[a-z0-9][a-z0-9_-]*$` but whose extension is a non-canonical case-variant of `.loom`/`.warp` emits `loom/load/non-canonical-extension` (warning, per source, deduped via `realpath`); the file does not register; non-slash-name stems stay silent. | docs/spec_topics/discovery/discovery-sources.md:72 | offline-unit |
| REQ-DISC-17 | — | The slash name is the filename stem verbatim, accepted iff it matches `^[a-z0-9][a-z0-9_-]*$`; non-matching stems are `loom/load/invalid-slash-name` (error), do not register or participate in collision detection, and the validator runs before parse (exactly one diagnostic). | docs/spec_topics/discovery/discovery-sources.md:74 | offline-unit |
| REQ-DISC-18 | DISC-4 | Two or more candidates at the same priority deriving the same slash name emit `loom/load/cross-format-collision` (error) naming every colliding path; loom-vs-loom drops every colliding loom; loom-vs-Pi-owned (`.md` prompt/skill or other extension's command) drops only the loom(s) and the Pi-owned entry stays registered. | docs/spec_topics/discovery/discovery-sources.md:76 | conformance |
| REQ-DISC-19 | DISC-4 | Collision detection runs on the final derived name (after `pi.looms` mapping); settings/`--loom`/package entries resolving to the same absolute path post-tilde-expansion are deduplicated silently before detection. | docs/spec_topics/discovery/discovery-sources.md:84 | offline-unit |
| REQ-DISC-20 | DISC-4 | On `session_start`, a candidate loom colliding with an already-registered Pi prompt/skill/extension command refuses to register (loom loses asymmetrically); a colliding `.md` appearing after registration re-evaluates and drops the loom's registry entry on the next cycle. | docs/spec_topics/discovery/discovery-sources.md:88 | conformance |
| REQ-DISC-21 | — | Package roots are scanned in priority order (project before global): `.pi/npm/`, `.pi/git/<host>/<path>/`, `node_modules/`, `~/.pi/agent/npm/`, `~/.pi/agent/git/<host>/<path>/`; symlink `node_modules/<pkg>` entries (`isSymbolicLink()` true) are filtered out silently. | docs/spec_topics/discovery/package-and-settings.md:7 | offline-unit |
| REQ-DISC-22 | — | Within each root, immediate child directories not beginning with `@` are candidate packages; `@`-prefixed directories are scope directories whose children are candidates; a candidate contributes looms only when its `package.json` parses; the `pi-package` keyword is not required. | docs/spec_topics/discovery/package-and-settings.md:15 | offline-unit |
| REQ-DISC-23 | DISC-5 | `pi.looms` must be a `string[]` of package-root-relative paths; globs are matched with `minimatch` (`nocase` off); `!`/`+`/`-` resolve in fixed order (includes → `!` drop → `+` re-admit → `-` remove, `-` final); a `.loom` match registers, a directory match scans non-recursively, other file types filter out silently per match. | docs/spec_topics/discovery/package-and-settings.md:19 | offline-unit |
| REQ-DISC-24 | DISC-5 | A `pi.looms` value that is not a `string[]` → `loom/load/manifest-invalid` (error, no looms from that package); an entry resolving outside the package root → `loom/load/manifest-escapes-package` (warning, per entry); a glob matching zero files is silent. | docs/spec_topics/discovery/package-and-settings.md:21 | offline-unit |
| REQ-DISC-25 | — | When both `pi.looms` and a `looms/` directory are present the manifest wins and the fallback directory is not merged; with no `pi.looms`, the conventional `looms/` directory is scanned non-recursively for `*.loom`. | docs/spec_topics/discovery/package-and-settings.md:23 | offline-unit |
| REQ-DISC-26 | — | A package present in both a project and a global root is deduplicated by package identity (npm name, git URL without ref, or resolved absolute path); the project copy wins and the global contributes nothing (package-level dedup, not `cross-source-shadow`). | docs/spec_topics/discovery/package-and-settings.md:25 | offline-unit |
| REQ-DISC-27 | DISC-6 | The package walk stops opening `package.json` files once `looms.scanPackagesMaxFiles` (default 2000) or `looms.scanPackagesTimeoutMs` ms (default 2000) is hit → a single `loom/load/discovery-slow` (warning) naming the root and cap; on simultaneous trip the file-count predicate wins (`cap = scanPackagesMaxFiles`). | docs/spec_topics/discovery/package-and-settings.md:30 | offline-unit |
| REQ-DISC-28 | DISC-6 | Each candidate `package.json` read is bounded by a deadline `max(200, floor(scanPackagesTimeoutMs/10))` ms scheduled via injected `Clock.setTimeout`; on per-read timeout the read is abandoned/silenced, the package is unreadable for this scan only, and `loom/load/package-read-timeout` (warning, `details.kind = "package-read-timeout"`) is emitted. | docs/spec_topics/discovery/package-and-settings.md:30 | offline-unit |
| REQ-DISC-29 | DISC-6 | `looms.scanPackages: false` disables the walk wholesale: no `node_modules/`, `.pi/npm/`, `.pi/git/`, `~/.pi/agent/npm/`, or `~/.pi/agent/git/` root is scanned; only Global, Project, Settings, and CLI sources contribute. | docs/spec_topics/discovery/package-and-settings.md:30 | offline-unit |
| REQ-DISC-30 | — | Settings are read from `.pi/settings.json` then `~/.pi/agent/settings.json` (project over global), both optional, directly through the injected `FileSystem` seam (Pi is not consulted). | docs/spec_topics/discovery/package-and-settings.md:40 | offline-unit |
| REQ-DISC-31 | — | Settings failure modes each treat the file as `{}` with one diagnostic: missing/unreadable → `loom/load/settings-unreadable`; invalid UTF-8 JSON → `loom/load/settings-invalid-json`; valid JSON whose root is not an object → `loom/load/settings-value-out-of-range` (once, `(root)`); none fatal. | docs/spec_topics/discovery/package-and-settings.md:49 | offline-unit |
| REQ-DISC-32 | DISC-7 | Project settings override global with deep merge for nested objects, replace for arrays and scalars (arrays not concatenated or deduplicated). | docs/spec_topics/discovery/package-and-settings.md:55 | offline-unit |
| REQ-DISC-33 | — | loom 1.0 reads five keys: top-level `loomPaths` (`string[]`) plus `looms.binderModel`, `looms.scanPackages`, `looms.scanPackagesMaxFiles`, `looms.scanPackagesTimeoutMs`; unknown `looms.*` keys are ignored without diagnostic. | docs/spec_topics/discovery/package-and-settings.md:61 | offline-unit |
| REQ-DISC-34 | — | `looms.binderModel` is a free-form string fallback for the binder when `bind_model:` is omitted; required when any non-bypass loom is in scope (else `loom/load/binder-model-unresolved`); the strict-capability check runs at loom-load (`loom/load/binder-model-not-strict-capable` on failure). | docs/spec_topics/discovery/package-and-settings.md:64 | conformance |
| REQ-DISC-35 | — | A malformed top-level `loomPaths` (non-array) or `looms` (non-object), including `null`, is treated as absent (built-in defaults apply) and logs exactly one `loom/load/settings-value-out-of-range` (error, non-fatal) per malformed top-level key per file; a malformed `looms` does not cascade per nested key. | docs/spec_topics/discovery/package-and-settings.md:71 | offline-unit |
| REQ-DISC-36 | — | A recognised `looms.*` scalar failing its type/range is treated as absent and logs one `loom/load/settings-value-out-of-range` (error, non-fatal) per key per file; acceptance: `binderModel` non-empty string, `scanPackages` boolean, `scanPackages{MaxFiles,TimeoutMs}` integer ≥ 1; `null` is always out of range; integer-ness judged on parsed value (`2000.0` ok, `25.5` not). | docs/spec_topics/discovery/package-and-settings.md:73 | offline-unit |
| REQ-DISC-37 | — | `loomPaths` is `string[]`; a non-string entry → `loom/load/settings-invalid-entry` (error, per entry); paths in `~/.pi/agent/settings.json` resolve relative to `~/.pi/agent/`, in `.pi/settings.json` relative to `.pi/`, `~` expands per DISC-1, absolute as-is; globs/`!`/`+`/`-` follow DISC-5. | docs/spec_topics/discovery/package-and-settings.md:82 | offline-unit |
| REQ-DISC-38 | — | A `loomPaths` directory entry expands to its non-recursive `*.loom` children (`.warp` ignored); a file entry must end in `.loom` (else `loom/load/invalid-extension`, error); non-`.loom` glob matches are filtered per match with the same code; entries resolving to the same absolute path are deduplicated silently; project array fully replaces global. | docs/spec_topics/discovery/package-and-settings.md:82 | offline-unit |
| REQ-DISC-39 | — | Both settings files are read once and cached; a per-path file-watcher invalidates on change with a `250 ms` debounce (drop-and-reschedule) against the `Clock` seam; a settings-triggered registry rebuild is serialized across debounce windows (PIC-49) with no lost edit. | docs/spec_topics/discovery/package-and-settings.md:96 | conformance |
| REQ-DISC-40 | — | Watcher/hot-reload rebuild failures surface as ERR-7 on the `loom-system-note` channel at watcher-event time: `loom/runtime/registry-swap-failed` for a swap throwing before publish, and re-emitted `loom/load/*` / `loom/parse/*` codes for re-parse/re-merge diagnostics. | docs/spec_topics/discovery/package-and-settings.md:98 | conformance |

## AREA: SLSH (slash-invocation)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-SLSH-1 | — | A loom is invoked as a slash command by its filename stem (`/code-review …`); the binder extracts typed `params:` from free-form slash text via an LLM call using the binder model resolved from `bind_model:` or `looms.binderModel` at load time. | docs/spec_topics/slash-invocation.md:1 | live |
| REQ-SLSH-2 | — | On successful binding the runtime appends a one-line echo system note before the loom starts; the echo is on by default, suppressed by `bind_echo: false`, and auto-suppressed for the single-string-param bypass. | docs/spec_topics/slash-invocation.md:9 | offline-unit |
| REQ-SLSH-3 | — | `argument-hint` is passed to the binder as grounding (rendered under `Argument hint:`) but is not displayed in Pi's slash autocomplete; key=value / named-argument syntax is not part of the loom 1.0 surface. | docs/spec_topics/slash-invocation.md:11 | live |
| REQ-SLSH-4 | SLSH-1 | For a no-params loom (`params:` absent or `{}`) the binder is bypassed; the runtime trims leading/trailing slash-argument whitespace and, if the remainder is non-empty, emits one `loom-system-note` `loom /<name>: ignoring extra arguments — this loom takes no parameters` then runs; whitespace-only remainders emit no note; the note fires only on the slash path. | docs/spec_topics/slash-invocation.md:15 | offline-unit |
| REQ-SLSH-5 | — | In prompt mode the loom drives the current conversation; the loom's final `Ok` value is not surfaced to the user and no `Ok(v)`-keyed system note is emitted. | docs/spec_topics/slash-invocation.md:19 | conformance |
| REQ-SLSH-6 | — | In subagent mode a fresh isolated conversation is spawned (system prompt from `system:` if present); only the return value reaches the caller and the intermediate transcript stays private. | docs/spec_topics/slash-invocation.md:20 | conformance |
| REQ-SLSH-7 | SLSH-2 | In prompt mode assistant tokens for untyped and typed queries stream into the transcript in real time (independent of loom resumption after `ctx.waitForIdle()`); the typed forced-respond turn is dispatched off-session via `complete()` and renders no transcript card; in subagent mode nothing surfaces to any ancestor transcript. | docs/spec_topics/slash-invocation.md:22 | conformance |
| REQ-SLSH-8 | SLSH-2 | On mid-stream cancellation or an `Err` after partial assistant text, the streamed prefix remains visible (not rolled back) and the system note is appended after the prefix, not interleaved. | docs/spec_topics/slash-invocation.md:25 | conformance |
| REQ-SLSH-9 | SLSH-3 | A loom with a slash caller and no invoke parent that returns `Err(QueryError)` to that boundary gets a one-line system note formatted from the error (prompt or subagent mode alike, in the user's session), summarising category and detail without dumping full JSON; a subagent loom reached via `invoke(...)` is not a slash-dispatch boundary and cascades to its parent instead. | docs/spec_topics/slash-invocation.md:31 | conformance |
| REQ-SLSH-10 | SLSH-4 | Renderers MUST emit the surrounding per-kind template text verbatim, interpolating only `<…>` placeholders, with the code-span backticks stripped from template text (backticks inside interpolated content pass through). | docs/spec_topics/slash-invocation.md:33 | offline-unit |
| REQ-SLSH-11 | SNK-a | `validation` (`cause: "schema_validation"`) renders `loom /<name> returned Err: model failed schema after <n> respond-repair attempts`. | docs/spec_topics/slash-invocation.md:39 | offline-unit |
| REQ-SLSH-12 | SNK-b | `validation` (`cause: "empty_template"`) renders `loom /<name> returned Err: rendered query template was empty — no provider turn was issued`. | docs/spec_topics/slash-invocation.md:40 | offline-unit |
| REQ-SLSH-13 | SNK-c | `transport` renders `loom /<name> returned Err: transport — <message>`. | docs/spec_topics/slash-invocation.md:41 | offline-unit |
| REQ-SLSH-14 | SNK-d | `model_tool` renders `loom /<name> returned Err: tool <tool_name> failed — <message>`. | docs/spec_topics/slash-invocation.md:42 | offline-unit |
| REQ-SLSH-15 | SNK-e | `context_overflow` renders `loom /<name> returned Err: context overflow`. | docs/spec_topics/slash-invocation.md:43 | offline-unit |
| REQ-SLSH-16 | SNK-f | `cancelled` renders `loom /<name> cancelled`. | docs/spec_topics/slash-invocation.md:44 | offline-unit |
| REQ-SLSH-17 | SNK-g | `code_tool` renders `loom /<name> returned Err: tool <tool_name> call failed (<cause>) — <message>`. | docs/spec_topics/slash-invocation.md:45 | offline-unit |
| REQ-SLSH-18 | SNK-h | `tool_loop_exhausted` renders `loom /<name> returned Err: tool-call loop exhausted after <rounds> rounds (last tool: <last_tool_name>)`, with `<last_tool_name>` the literal `respond` when `last_tool_name` is `null`. | docs/spec_topics/slash-invocation.md:46 | offline-unit |
| REQ-SLSH-19 | SNK-i | `invoke_infra` renders `loom /<name> returned Err: invoke of <callee_path> failed (<cause>)`. | docs/spec_topics/slash-invocation.md:47 | offline-unit |
| REQ-SLSH-20 | SNK-k | Any unlisted `kind` (catch-all) renders `loom /<name> returned Err: <kind> — <message>`, making the renderer total over the QueryError union. | docs/spec_topics/slash-invocation.md:48 | offline-unit |
| REQ-SLSH-21 | SLSH-4 | `invoke_callee` has no dedicated row: the descriptive text is computed from the innermost non-`invoke_callee` leaf `kind` (placeholders drawn from the leaf), and the table is exhaustive over the nine-variant loom 1.0.0 QueryError union plus the catch-all. | docs/spec_topics/slash-invocation.md:50 | offline-unit |
| REQ-SLSH-22 | SLSH-5 | When the leaf failure cascaded from an `invoke`d child (`invoke_callee`, possibly nested), renderers append, per hop in leaf-first order, ` from <callee_path> invoked at <parent_path>:<line>` (post-`realpath` absolute paths; `<line>` the 1-indexed source line of the `invoke(`/callee-name call-site token), each hop single-space separated; applies to every row including the catch-all. | docs/spec_topics/slash-invocation.md:54 | offline-unit |
| REQ-SLSH-23 | SLSH-5 | A `.loom` callable invoked by the model during a `@`-query loop feeds its failure back as a tool-error result, not an `invoke_callee` cascade, so no chain suffix is emitted for that surface. | docs/spec_topics/slash-invocation.md:54 | offline-unit |
| REQ-SLSH-24 | — | Every top-level-`Err` row emits as a `loom-system-note` carrying `details: { event: RuntimeEvent }` (same payload as the originating failure site); the session is not aborted and the user can type a follow-up. | docs/spec_topics/slash-invocation.md:52 | conformance |

## AREA: INV (invocation/invoke)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-INV-1 | — | `invoke("./path.loom", args…)` is the only way for a `.loom` to spawn/attach to another `.loom` by inline path literal; `import` is reserved for `.warp` library code. | docs/spec_topics/invocation.md:1 | offline-unit |
| REQ-INV-2 | — | The `invoke` path is a string literal resolved at parse time relative to the calling loom's directory, must end in `.loom` byte-exact lowercase (else `loom/parse/invoke-non-loom-extension`, same code for `tools:` `.loom` entries), uses forward-slash separators only (backslash is a parse error), and dynamic runtime-computed paths are unsupported. | docs/spec_topics/invocation.md:10 | offline-unit |
| REQ-INV-3 | — | The resolved callee path (after `realpath`) must lie within the union of active discovery roots under segment-boundary containment (equal to a root, or root + single separator) compared byte-for-byte on `realpath` output; an escape is `loom/load/invoke-path-escape`, the parent does not register the call site, and an escaping `tools:` `.loom` entry fails to register the callable. | docs/spec_topics/invocation.md:12 | offline-unit |
| REQ-INV-4 | INV-1 | The realpath+containment check re-runs at runtime open using the currently-active roots; a swapped symlink landing outside every root surfaces on both channels — `loom/load/invoke-path-escape` on the drain and `Err(InvokeInfraError { cause: "load_failure", callee_path })` to the parent — with identical semantics to the load-time check. | docs/spec_topics/invocation.md:14 | conformance |
| REQ-INV-5 | — | `invoke<Schema>(...)` AJV-validates the child's return value against `Schema`; untyped `invoke(...)` returns `Result<null, QueryError>` and discards the child's return value. | docs/spec_topics/invocation.md:28 | offline-unit |
| REQ-INV-6 | — | When both the annotated `Schema` and the callee are statically resolvable, the parser checks `T_calleeReturn ⊑ Schema` (compatibility, not equality); a mismatch is `loom/parse/invoke-return-type-mismatch`; when either side is not statically resolvable the runtime AJV check is the safety net. | docs/spec_topics/invocation.md:30 | offline-unit |
| REQ-INV-7 | — | Against an empty-tail callee (inferred return `null`): `invoke<null>(...)` is accepted; a non-`null`-compatible `invoke<Schema>(...)` is `loom/parse/invoke-return-type-mismatch` when statically resolvable, else `Err(InvokeInfraError { cause: "return_validation" })` at runtime. | docs/spec_topics/invocation.md:32 | offline-unit |
| REQ-INV-8 | — | Static resolution uses a single per-load-pass parse cache shared by argument-type checking, return-type inference, and cycle detection, walked transitively; each file is parsed once and its diagnostics aggregate into the entry loom's drain sorted by `(file, line, col)`. | docs/spec_topics/invocation.md:18 | offline-unit |
| REQ-INV-9 | — | A callee that is unreadable/unparseable/fails structural checks is not statically resolvable → `loom/load/callee-has-errors` at the referencing site (listing underlying sites via `related`): severity error for a `tools:` `.loom` entry (parent does not register), warning for a literal `invoke(...)` (parent registers, static checks skipped, runtime AJV is the net). | docs/spec_topics/invocation.md:22 | offline-unit |
| REQ-INV-10 | — | `invoke` arguments bind positionally to the callee's `params:` in declaration order, each type-checked against the param schema (`loom/parse/invoke-arg-type-mismatch` when statically resolvable, else runtime AJV); the slash-boundary LLM binder does not run for `invoke(...)`. | docs/spec_topics/invocation.md:38 | offline-unit |
| REQ-INV-11 | — | Arity is checked before per-argument type: fewer than the non-defaulted param count → `loom/parse/invoke-arity-too-few` when statically resolvable (else `Err(InvokeInfraError { cause: "validation" })`); more than the total param count → `loom/parse/invoke-arity-too-many` always at parse time; same rules apply to `.loom` callable calls via `tools:`. | docs/spec_topics/invocation.md:48 | offline-unit |
| REQ-INV-12 | — | The callee's mode (not the caller's) controls conversation attachment per the cross-mode matrix: prompt→prompt attaches to the caller's current conversation; any subagent callee (or subagent caller with a prompt child) spawns/uses an isolated conversation and only the return value crosses back. | docs/spec_topics/invocation.md:55 | conformance |
| REQ-INV-13 | — | The child uses its own frontmatter `model`, `tools`, and `system`; the caller's settings are not inherited. | docs/spec_topics/invocation.md:64 | conformance |
| REQ-INV-14 | — | For prompt→prompt, the runtime snapshots the user session's ambient tools and calls `pi.setActiveTools(childCallableSet)` on entry, restoring in a `finally`; `invoke` to a prompt-mode callee suspends the parent's body until the child returns, so nested prompt→prompt invokes execute strictly sequentially with LIFO-safe snapshot/restore. | docs/spec_topics/invocation.md:66 | conformance |
| REQ-INV-15 | — | A callee's final value flows to an `invoke` parent only on success (through the `Ok` payload in both prompt and subagent mode); on failure or cancellation no final value flows and the caller observes only `InvokeCalleeError` (callee returned `Err`) or `InvokeInfraError`. | docs/spec_topics/invocation.md:36 | conformance |
| REQ-INV-16 | — | `invoke` returns `Result<T, QueryError>`; invoke-specific failures surface as `InvokeInfraError` (`kind: "invoke_infra"`, `cause` enum covering load/parse/arg-validation/return-validation/panic/pre-spawn model-resolution/internal_error) around the callee body, and `InvokeCalleeError` wrapping the callee's own `Err` in `inner: QueryError`. | docs/spec_topics/invocation.md:70 | offline-unit |
| REQ-INV-17 | — | Invocation cycles are detected at parse time by walking the per-load-pass static-resolution graph → `loom/load/invocation-cycle` ("invocation cycle: A → B → A"); an unresolvable callee terminates its walk arm as a leaf; recursion through subagent/prompt looms is allowed but must terminate via control flow. | docs/spec_topics/invocation.md:79 | offline-unit |
| REQ-INV-18 | INV-4 | The interpreter caps `invoke`-chain nesting depth at 32 (counting direct `invoke(...)`, `.loom` callable calls via `tools:`, and cross-file `.warp` `fn` calls; per-chain, not per-process); the top-level loom is depth 0 and the cap breaches when about to push frame 33 → runtime panic `loom/runtime/invoke-depth-exceeded` ("invoke chain depth exceeded: 33 > 32"). | docs/spec_topics/invocation.md:83 | offline-unit |
| REQ-INV-19 | INV-4 | A `loom/runtime/invoke-depth-exceeded` at top level surfaces as a Pi system note; inside an invoke chain it surfaces to the parent as `Err(InvokeInfraError { cause: "panic" })`; the counter crosses subagent-mode boundaries unchanged (subagent invokes do not reset it). | docs/spec_topics/invocation.md:87 | conformance |
| REQ-INV-20 | INV-2 | The invocation AST node carries a `style: "positional" \| "named"` discriminator; loom 1.0's only invocation surface is positional, so only the `"positional"` arm has defined behaviour (named-argument surface deferred). | docs/spec_topics/invocation.md:40 | offline-unit |
| REQ-INV-21 | INV-3 | The runtime-internal invoke options record is an open struct tolerating unknown fields; loom 1.0 exposes no author-facing per-call timeout (the deferred per-call-timeout extension lands additively). | docs/spec_topics/invocation.md:44 | offline-unit |

## AREA: BINDER (binder)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-BINDER-1 | — | The binder runs once per slash invocation before any of the loom's own queries; it does not apply to `invoke(...)` calls or registered-tool callers (both pass already-typed values). | docs/spec_topics/binder.md:1 | live |
| REQ-BINDER-2 | — | The binder is runtime infrastructure: it never adds turns to the user's session (prompt mode) or the spawned conversation (subagent mode), and loom code never sees the intermediate envelope. | docs/spec_topics/binder.md:1 | conformance |
| REQ-BINDER-3 | — | The binder model is resolved at loom-load time from `bind_model:` then `looms.binderModel` with no further fallback (the loom's own `model:` is not consulted); when neither resolves and the loom is not bypass-eligible it fails to load with `loom/load/binder-model-unresolved` and does not register. | docs/spec_topics/binder/binder-model-and-context.md:3 | conformance |
| REQ-BINDER-4 | — | Binder-model resolution runs loom's exact-match over `ctx.modelRegistry.getAvailable()`: a bare `modelId` matches `Model<Api>.id`, a `provider/modelId` matches short-provider `Model<Api>.provider` + `Model<Api>.id`; a bare `modelId` ambiguous across providers resolves to no model (not pick-first) → `loom/load/binder-model-unresolved`. | docs/spec_topics/binder/binder-model-and-context.md:10 | conformance |
| REQ-BINDER-5 | — | The resolved model is duck-typed probed for `strictCapable`: `true` admits (no diagnostic), `false` emits `loom/load/binder-model-not-strict-capable` (E, refuses registration), `undefined` emits `loom/load/binder-model-strict-capability-unknown` (W, registers); the probe is short-circuited when resolution yields no model; bypass-eligible looms skip both checks. | docs/spec_topics/binder/binder-model-and-context.md:10 | conformance |
| REQ-BINDER-6 | BNDR-11 | A `looms.binderModel`-only settings edit MUST NOT re-run binder-model resolution or the `strictCapable` probe for any already-loaded loom (each uses its previously-resolved handle until its next load); no loss-side note is emitted. | docs/spec_topics/binder/binder-model-and-context.md:14 | conformance |
| REQ-BINDER-7 | BNDR-11 | When a `looms.binderModel` change lets previously binder-model-unresolved looms re-resolve, a single consolidated `loom-system-note` (`display: true`, `details.recovery.looms`) is emitted formatted `loom watcher: looms.binderModel changed; <N> previously-failed loom(s) now resolve a binder model; run /reload to retry: <names>` with `<names>` `/`-prefixed, comma-space-joined, code-point ascending; not emitted when the set is empty; window-coalesced over the 250 ms debounce. | docs/spec_topics/binder/binder-model-and-context.md:14 | conformance |
| REQ-BINDER-8 | — | `bind_context:` is `none` (default) or `session`; `none` shows the binder only slash text + frontmatter; `session` (prompt-mode only) adds a bounded recent session slice; `bind_context: session` on a subagent-mode loom is `loom/parse/bind-context-session-on-subagent` (warning). | docs/spec_topics/binder/binder-model-and-context.md:18 | offline-unit |
| REQ-BINDER-9 | BNDR-10 | At slash-invocation time the runtime MUST treat `bind_context: session` on a `mode: subagent` loom as `none` for binder-input construction: the session-context truncation walk is skipped and no *Recent session context* block is emitted. | docs/spec_topics/binder/binder-model-and-context.md:29 | conformance |
| REQ-BINDER-10 | BNDR-12 | The binder-invocation path MUST be re-entrant per loom turn: the binder input record and resolved binder-model handle are constructed afresh on every binder call with no cached state that could serve a stale `bind_context` snapshot. | docs/spec_topics/binder/binder-model-and-context.md:31 | conformance |
| REQ-BINDER-11 | — | The session-context walk (bind_context: session) includes turns newest-to-oldest, excluding a whole turn (and everything older) the moment the running token sum would exceed 8000 or the running turn count would exceed 20 (both boundaries inclusive; partial messages never split); token counts come from the injected `TokenEstimator` seam. | docs/spec_topics/binder/binder-model-and-context.md:36 | offline-unit |
| REQ-BINDER-12 | — | The compact transcript is emitted chronologically (oldest-to-newest), turn blocks separated by exactly one blank line, each message's first line prefixed by a closed-set role tag (`[user]`/`[assistant]`/`[tool]`/`[custom:<type>]`) + `: `, with per-variant bodies (assistant omits `ThinkingContent`, renders `[tool-call <name>(<args-json>)]` sibling lines; toolResult under `[tool]`; custom rendered even when `display: false`); no one-line sanitisation is applied. | docs/spec_topics/binder/binder-model-and-context.md:45 | offline-unit |
| REQ-BINDER-13 | BNDR-7 | Reference transcript renderings BNDR-7a–BNDR-7j MUST be reproduced exactly (single-message user turn, tool-call/result interleave, loom-system-note custom turn, empty-content user with trailing space, multi-key tool-call args, tool-call-without-text assistant, mixed toolResult blocks, custom array body, void-truncation absence, multi-key non-text block). | docs/spec_topics/binder/binder-model-and-context.md:62 | offline-unit |
| REQ-BINDER-14 | BNDR-8 | Within an assistant message the merged `[assistant]: <text>` line is emitted first followed by every `[tool-call …]` line in `content` array order; all transcript JSON uses the canonical no-whitespace serialisation (object keys ascending Unicode code-point at every nesting level, array order preserved), serialising `ToolCall.arguments`. | docs/spec_topics/binder/binder-model-and-context.md:135 | offline-unit |
| REQ-BINDER-15 | BNDR-9 | A `CustomMessage.customType` is transcript-safe iff non-empty and free of `\n`, `\r`, `]`, and `: `; the binder MUST reject an unsafe included customType before rendering, aborting binding for that invocation and emitting `loom/runtime/custom-type-unsafe` (no proceed-and-drop). | docs/spec_topics/binder/binder-model-and-context.md:137 | offline-unit |
| REQ-BINDER-16 | — | Binder calls use `temperature: 0`; the binder's `context.messages` is the fixed single `user` message `Bind the slash-command arguments now.`; the per-call seed is the 32-bit FNV-1a hash (offset `0x811c9dc5`, prime `0x01000193`) of the UTF-8 bare command name masked to 32-bit unsigned (reference: `code-review`→`0x7ba86b63`, `hello`→`0x4f9f2cab`, `a`→`0xe40c292c`). | docs/spec_topics/binder/determinism-cancellation-failure.md:5 | offline-unit |
| REQ-BINDER-17 | — | The binder participates in cancellation: the runtime checks `loomAbort.signal` immediately before issuing the binder call and forwards it as `options.signal` on the initial attempt and every budgeted retry; a cancelled binder produces the cancelled-binder note and the loom does not run; bypass-eligible looms are exempt. | docs/spec_topics/binder/determinism-cancellation-failure.md:21 | conformance |
| REQ-BINDER-18 | — | No-params bypass: when `params:` is absent or `{}` the binder does not run; `bind_echo: true` on a no-params loom is `loom/load/bind-echo-without-params` (warning, no echo), and `bind_context`/`bind_model` are silently ignored. | docs/spec_topics/binder/binder-bypass-and-envelope.md:10 | offline-unit |
| REQ-BINDER-19 | — | Single-string bypass: when `params:` declares exactly one field of type `string` with no default, the runtime sets the param to the whole slash string (leading/trailing slash-argument whitespace trimmed) and skips the binder (AJV still runs); the no-params check runs before the single-string check. | docs/spec_topics/binder/binder-bypass-and-envelope.md:11 | offline-unit |
| REQ-BINDER-20 | — | Slash-argument whitespace for trimming is exactly the ASCII whitespace set from System-note-rendering rule 1 (not the regex `\s` class), so U+00A0 is not trimmed; only leading/trailing trimming applies (no internal collapse). | docs/spec_topics/binder/binder-bypass-and-envelope.md:5 | offline-unit |
| REQ-BINDER-21 | — | The binder returns one of three envelopes: `{ kind: "ok", args }` (runtime fills defaulted fields, AJV-validates, starts the loom), `{ kind: "needs_info", message }` (shown as system note, loom does not run), `{ kind: "ambiguous", message, candidates }` (shown as system note, loom does not run); the envelope is runtime-internal and never Loom-visible. | docs/spec_topics/binder/binder-bypass-and-envelope.md:15 | live |
| REQ-BINDER-22 | BNDR-1 | The binder envelope schema MUST keep the three-arm `ok \| needs_info \| ambiguous` discriminator; collapsing to two arms is a breaking change. | docs/spec_topics/binder/binder-bypass-and-envelope.md:27 | offline-unit |
| REQ-BINDER-23 | BNDR-2 | `ambiguous.candidates` (`array<string> \| null`) MUST remain in the envelope schema (AJV accepts `null`) but MUST NOT be surfaced on the user-facing system note in loom 1.0. | docs/spec_topics/binder/binder-bypass-and-envelope.md:29 | offline-unit |
| REQ-BINDER-24 | BNDR-3 | The failure-mode templates MUST keep distinct `argument binding needs more info` and `ambiguous arguments` row prefixes; collapsing them is breaking. | docs/spec_topics/binder/binder-bypass-and-envelope.md:31 | offline-unit |
| REQ-BINDER-25 | — | The envelope schema is emitted once per loom at load time; `message`/`candidates[i]` carry `maxLength: 500`; the `args` arm embeds a copy of the lowered params schema with defaulted fields removed from `required` (types unchanged), retaining `additionalProperties: false` and the transitive `$defs` closure. | docs/spec_topics/binder/binder-bypass-and-envelope.md:33 | offline-unit |
| REQ-BINDER-26 | — | The binder system prompt MUST satisfy the eight structure obligations: Loom identity line, conditional Description/Argument-hint lines, Parameters block (per-field lines two-space indented, `<wire-name> (<type>) <requirement>[ — <description>]` fixed token order), User-arguments line, conditional Session-context block, three envelope-kind tokens, and the no-invent-defaults line (`defaulted` + a directive substring). | docs/spec_topics/binder/binder-bypass-and-envelope.md:110 | offline-unit |
| REQ-BINDER-27 | — | Per-field type display uses the declared Loom surface-syntax type (not JSON Schema lowering) per the Type-display reference table; the `default=<literal>` literal is rendered in the Loom literal sublanguage; the Parameter-line reference renderings (and the description-omitted form ending after `required`) MUST be reproduced byte-exact. | docs/spec_topics/binder/binder-bypass-and-envelope.md:129 | offline-unit |
| REQ-BINDER-28 | — | The Session-context block, when emitted, is framed exactly as an opening line beginning `Recent session context` and ending `:`, then the compact-transcript body, then a terminating blank line (block ends `\n\n`), emitted regardless of position; when the walk yields zero turns or `bind_context: none` the entire block is omitted (no empty-body header). | docs/spec_topics/binder/binder-bypass-and-envelope.md:110 | offline-unit |
| REQ-BINDER-29 | — | Runtime default-fill is fill-if-absent keyed on the field's wire name in the binder `args`: present → binder value preserved (untagged); absent → declared default applied (tagged `(default)` in the echo); the post-default-merge AJV validation runs the depth-walk first then AJV against the lowered params schema. | docs/spec_topics/binder/defaulting-system-note-echo.md:3 | offline-unit |
| REQ-BINDER-30 | — | All binder system notes share one line discipline: rule 1 replaces each `\r`/`\n`/`\r\n` in model-supplied substrings with a space and collapses/trims ASCII whitespace (U+00A0 and U+2000–U+200A preserved); rule 2 caps the fully-rendered note at 120 Unicode code points (scalar-aligned truncation, trailing `…` counted). | docs/spec_topics/binder/defaulting-system-note-echo.md:13 | offline-unit |
| REQ-BINDER-31 | — | Note grammar: failure-arm notes follow `loom /<name>: <fixed-phrase> — <sanitised-suffix>` and the echo follows `Running /<name>: <formatted-args>`; a `message` empty after stripping (or a `candidates` array all-empty after stripping) is treated as a malformed envelope, not an empty note; `ambiguous.candidates` is not rendered in loom 1.0. | docs/spec_topics/binder/defaulting-system-note-echo.md:13 | offline-unit |
| REQ-BINDER-32 | — | The echo (`bind_echo:` default `true`) renders top-level params in declaration order, comma-separated, with the value-formatting rules: strings unquoted iff every code point matches `[A-Za-z0-9_.-]` (else quoted, escaping `"` and `\`), booleans/null as lowercase tokens, arrays `[a, b, c]` or `[a, b, c, …+N more]` for ≥4, objects `{first-field-value, …}`, enum variants as underlying wire string; defaulted-only fields tagged `(default)`; total line under the 120-code-point cap. | docs/spec_topics/binder/defaulting-system-note-echo.md:32 | offline-unit |
| REQ-BINDER-33 | BNDR-4 | `integer` values render as canonical decimal: optional leading `-`, base-10 magnitude, no leading zeros (except `0`), no separators/decimal point/exponent; `-0` renders as `0`. | docs/spec_topics/binder/defaulting-system-note-echo.md:36 | offline-unit |
| REQ-BINDER-34 | BNDR-5 | `number` values render as the shortest round-tripping decimal with no scientific notation at any magnitude (fixed-point), a non-integral value carries ≥1 fractional digit, an integral value carries no trailing `.0`, and `-0` renders as `0`. | docs/spec_topics/binder/defaulting-system-note-echo.md:37 | offline-unit |
| REQ-BINDER-35 | BNDR-6 | Echo value reference renderings BNDR-6a–BNDR-6x MUST be reproduced exactly (empty/plain/quoted/escaped/non-ASCII strings, object/inline/variant first-field forms, array forms, integer/number/`-0`/`1e21`/`1e-8`, booleans, null, enum wire strings). | docs/spec_topics/binder/defaulting-system-note-echo.md:47 | offline-unit |
| REQ-BINDER-36 | — | `bind_echo: false` suppresses the echo; the single-string bypass auto-suppresses echo regardless of the setting; an explicit `bind_echo: true` on a single-string-bypass loom is `loom/parse/bind-echo-on-bypass` (warning). | docs/spec_topics/binder/defaulting-system-note-echo.md:76 | offline-unit |
| REQ-BINDER-37 | — | The failure-class taxonomy partitions each attempt's outcome via the provider-error classifier plus post-classifier envelope/AJV checks into transport (retry-eligible), malformed-envelope (retry-eligible; includes plain-text-instead-of-tool-call), AJV-on-`args` (not retried; depth-walk fast-fail at the params boundary routes here), and context-overflow (treated as transport for retry). | docs/spec_topics/binder/determinism-cancellation-failure.md:29 | conformance |
| REQ-BINDER-38 | — | The seven failure-mode note templates are rendered verbatim (only `<…>` interpolated): needs_info, ambiguous, transport-failure (`argument binder unavailable (<provider>: <message>)`), malformed-envelope (`argument binding failed — could not parse arguments`), AJV-on-`args` (`argument binding produced invalid args — <ajv-summary>`), cancelled (`argument binding cancelled`), and custom-type-unsafe (`custom-message type is not transcript-safe: '<value>'`). | docs/spec_topics/binder/determinism-cancellation-failure.md:40 | offline-unit |
| REQ-BINDER-39 | — | `<ajv-summary>` is the in-order `<path> <message>` concatenation of `ValidationIssue` entries joined by `; ` in canonical `validation_errors` order; the depth-walk fast-fail sub-case renders the single canonical issue `<JSON-Pointer> JSON document depth exceeds 5` (no separator). | docs/spec_topics/binder/determinism-cancellation-failure.md:40 | offline-unit |
| REQ-BINDER-40 | — | Each retry-eligible class has one retry budget per slash invocation (≤1 transport retry, ≤1 malformed retry, interleave-agnostic), so the runtime issues at most 3 binder LLM calls per invocation; a cross-class retry consumes the observed class's budget; the surfaced note matches the most-recent failure; AJV-on-`args` is never retried; retries re-issue immediately (no backoff) against the same envelope schema. | docs/spec_topics/binder/determinism-cancellation-failure.md:58 | conformance |
| REQ-BINDER-41 | — | An abort observed during any budgeted retry suppresses it and surfaces the cancelled-binder note immediately; an abort after `ok` but before AJV lets validation complete (AJV is uncancellable) and surfaces at the next loom-body checkpoint (no retroactive `Ok`→`Err`). | docs/spec_topics/binder/determinism-cancellation-failure.md:60 | conformance |

## DEFERRED / out-of-scope (Cluster 5)
- Named-argument / `key=value` invocation surface is deferred (INV-2 seam); loom 1.0's only invocation surface is positional. — docs/spec_topics/invocation.md:40
- Per-call `invoke` timeout is deferred (INV-3 seam); loom 1.0 has no configurable per-invoke timeout — cancellation is the only externally driven termination. — docs/spec_topics/invocation.md:44; docs/spec_topics/invocation.md:77
- Symlink-resolution hardening for invoke-path containment (single atomic resolve-and-open primitive, e.g. `openat2` RESOLVE_NO_SYMLINKS/RESOLVE_BENEATH) is deferred; loom 1.0 accepts the residual load↔open race. — docs/spec_topics/invocation.md:12
- Binder refinement loop (only `needs_info` reopening for a clarifying turn while `ambiguous` terminates) is deferred; loom 1.0 preserves the three-arm split via BNDR-1/2/3 carriers only. — docs/spec_topics/binder/binder-bypass-and-envelope.md:25
- Automatic context escalation (a `needs_info` binder call auto-retrying with `bind_context: session`) is deferred; BNDR-12 commits loom 1.0 only to the binder-path re-entrancy property. — docs/spec_topics/binder/binder-model-and-context.md:33
- Rendering of `ambiguous.candidates` on the user-facing note (and the array-truncation rules) is deferred along with the binder refinement loop; loom 1.0 keeps the field in schema but does not surface it. — docs/spec_topics/binder/defaulting-system-note-echo.md:13
- `strictCapable` is absent under the loom 1.0 Pi-SDK pin, so production behaviour is the universal-`strict-capability-unknown` (W) branch; the `false` diagnostic path is unreachable under the pin. — docs/spec_topics/binder/binder-model-and-context.md:10
- Object-form `loomPaths` entries are not accepted in loom 1.0 (string entries only). — docs/spec_topics/discovery/package-and-settings.md:84
- The per-read-deadline value is derived, not configurable in loom 1.0. — docs/spec_topics/discovery/package-and-settings.md:30
