# Cluster 3 — Query, tool-calls, schemas

Scope: `docs/spec_topics/query.md` + `docs/spec_topics/query/*` (query forms, escapes/stringification, tool-loop, failure & repair), `docs/spec_topics/tool-calls.md`, `docs/spec_topics/schemas.md`, `docs/spec_topics/schema-subset.md`, `docs/reference/schema-subset.md`.

Testability classes: **offline-unit** = pure parse/type-check, schema lowering, canonical hash, or depth walk with no model and no query-composition harness; **conformance** = production query-lowering / prompt-composition / validation-routing path exercised without a model round-trip; **live** = requires a real (or mock) provider turn; **manual/inspection** = editorial / terminology / non-testable posture.

## AREA: QRY (query)

| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-QRY-1 | QRY-1 | Untyped `@`…`` returns `Result<string, QueryError>`; `Ok` holds the assistant's final text. | query/query-forms.md:5 | live |
| REQ-QRY-2 | QRY-2 | Typed `@`…`` infers its response schema from the surrounding type sink, hands it to the provider as a strict tool-input/structured-output contract, AJV-validates the response, and returns `Result<Schema, QueryError>`. | query/query-forms.md:15 | live |
| REQ-QRY-3 | QRY-3 | An explicit `@<Schema>` ascription always supplies the response schema and overrides every inference context regardless of query position. | query/query-forms.md:27 | offline-unit |
| REQ-QRY-4 | QRY-3 | Absent an explicit ascription, the response schema flows only from a type sink: binding annotation, enclosing function's declared return type (a `.loom` cannot be its own sink), or enclosing call-site parameter type. | query/query-forms.md:29-33 | offline-unit |
| REQ-QRY-5 | QRY-3 | The inference walk crosses transparent constructs (`(...)`, `let x:T=` RHS, typed call/tool/`invoke` args, declared-return tail, `return` operand, ternary/array-literal when the enclosing form has a sink, postfix `?`) and continues outward. | query/query-forms.md §Schema inference algorithm (query-forms.md:37-45) | offline-unit |
| REQ-QRY-6 | QRY-3 | The inference walk stops (opaque) at binary/unary operators, member access `a.b`, indexed access `a[i]`, `match` scrutinee, and `if`/`while` conditions; inside these only an explicit `@<Schema>` supplies a schema. | query/query-forms.md:46 | offline-unit |
| REQ-QRY-7 | QRY-3 | When multiple sinks enclose a query the nearest (innermost) enclosing sink wins; a walk that reaches a stop with no sink yields an untyped `Result<string, QueryError>`. | query/query-forms.md:41,49 | offline-unit |
| REQ-QRY-8 | QRY-4 | `@<Schema>`…`` overrides inference and is required where no usable type context exists (e.g. `match` scrutinee), which the grammar enforces. | query/query-forms.md:57 | offline-unit |
| REQ-QRY-9 | QRY-4 | When a binding annotation and an explicit `@<Schema>` are both present and `ascription ⋢ annotation`, the parser emits `loom/parse/explicit-schema-mismatch` (one-directional; safe widening is silent; skipped when either side is past the static view). | query/query-forms.md:59-73 | offline-unit |
| REQ-QRY-10 | QRY-5 | Backtick templates span multiple lines (no heredoc form); the rendered text is normalised by newline-trim then dedent, uniformly for every template. | query/query-forms.md:77-93 | offline-unit |
| REQ-QRY-11 | QRY-6 | A template whose static body (post notional newline-trim/dedent, pre-escape-rewrite) is empty/whitespace-only emits `loom/parse/empty-template` (warning) using the pinned ASCII whitespace set; `\n` literal escape suppresses it. | query/query-forms.md:100 | offline-unit |
| REQ-QRY-12 | QRY-6 | A fully-rendered template of length 0 or only ASCII-whitespace (U+0020/U+0009/U+000A etc., not the regex `\s` class) short-circuits to `Err(QueryError{kind:"validation",cause:"empty_template",attempts:0,...})` without a provider round-trip. | query/query-forms.md:101 | conformance |
| REQ-QRY-13 | QRY-6 | An empty-template short-circuit on the original turn MUST NOT trigger respond-repair: zero follow-ups issued, no follow-up turn appended, returned `attempts` is 0 regardless of `respond_repair` config; a follow-up short-circuit consumes no `attempts` slot. | query/query-forms.md:101 | conformance |
| REQ-QRY-14 | QRY-7 | Newline-trim runs first (strips a newline immediately after the opening or immediately before the closing backtick), dedent second; this section is normative authority over `textwrap.dedent`. | query/query-forms.md:108,144 | offline-unit |
| REQ-QRY-15 | QRY-7 | Dedent rules 1-5: whitespace-only lines ignored for the common prefix and normalised to empty; common prefix is the longest common literal prefix (mixed tab/space → nothing stripped); LF-only line splitting with CR/CRLF pre-normalised; common-prefix/whitespace alphabet is only U+0020 and U+0009. | query/query-forms.md:110-118 | offline-unit |
| REQ-QRY-16 | QRY-7 | The 10 normative input→output dedent/newline-trim vectors (table) render exactly as specified. | query/query-forms.md:120-131 | offline-unit |
| REQ-QRY-17 | QRY-17 | Recognised escapes inside a template are `` \` ``, `\$`, `\\`, `\n`, `\t`, `\r`; any other backslash sequence is `loom/parse/illegal-template-escape`; EOF in an unterminated body is `loom/parse/unterminated-template`; `{`/`}` are ordinary text, only `${` interpolates. | query/query-escapes-stringification.md:5-12 | offline-unit |
| REQ-QRY-18 | QRY-18 | `${expr}` renders by the expression's Loom static type per the stringification table (string/integer/number/boolean/null/enum-wire-value/array/object-JSON-compact-with-wire-names), never JavaScript `String(...)`. | query/query-escapes-stringification.md:16-34 | conformance |
| REQ-QRY-19 | QRY-18 | Interpolating a `Result`-typed expression is `loom/parse/interpolated-result` (static where resolvable; runtime panic with the same code where the type is unresolvable). | query/query-escapes-stringification.md §Stringification (query-escapes-stringification.md:35) | offline-unit |
| REQ-QRY-20 | QRY-18 | Stringification runs after evaluation but before newline-trim/dedent; outbound wire-name translation applies recursively with no second map. | query/query-escapes-stringification.md:32,33 | conformance |
| REQ-QRY-21 | QRY-19 | A bare `@`…`` expression-statement is `loom/parse/discarded-query-result`; author must use `?`, `let _ =`, or bind the value; only the bare expression-statement position triggers it. | query/query-escapes-stringification.md:40-50 | offline-unit |
| REQ-QRY-22 | QRY-20 | `let _ = @`…`` (and `void`-tail form) discards at the user surface but preserves an `Err` as one operator-facing `RuntimeEvent`/`loom-system-note` with `display:false` and a `discard_site`, firing exactly once per discarded `Err`; `Ok` discards produce no event. | query/query-escapes-stringification.md:54 | live |
| REQ-QRY-23 | QRY-21 | A panic during `${expr}` interpolation propagates before the `let _ =` binding completes and is not contained by the discard form. | query/query-escapes-stringification.md:58 | conformance |
| REQ-QRY-24 | QRY-8 | A query never throws; both forms return a `Result` carrying `QueryError`, a `kind`-tagged `anyOf` discriminated union; the six query-time variants are `ValidationError`, `TransportError`, `ModelToolError`, `ContextOverflowError`, `CancelledError`, `ToolLoopExhaustedError`. | query/query-failure-and-repair.md:9-11 | live |
| REQ-QRY-25 | QRY-9 | Forced-respond-turn non-compliance (plain text, or invoking a non-respond tool) synthesises one `ValidationIssue` and routes through respond-repair; terminal exhaustion (or `methodology:none`/`attempts:0`) surfaces `Err(kind:"validation",cause:"schema_validation")`. | query/query-failure-and-repair.md:19 | live |
| REQ-QRY-26 | QRY-9 | Forced-respond non-compliance does NOT surface as `tool_loop_exhausted` (the forced respond turn is the CIO-4 `max_rounds`-final exempt terminator, uncounted). | query/query-failure-and-repair.md:21 | live |
| REQ-QRY-27 | QRY-10 | Recognised provider context-overflow payloads map to `ContextOverflowError`; all other provider/network failures map to `TransportError`; classification runs at end-of-stream (mid-stream + clean output-boundary both → `context_overflow`); HTTP-200 error envelopes recognised by body. | query/query-failure-and-repair.md:25 | live |
| REQ-QRY-28 | QRY-10 | `ContextOverflowError.tokens_used`/`tokens_limit` are populated when the provider supplies them and `null` otherwise; both fields MUST remain nullable (`number \| null`) — tightening to non-null is non-conformant. | query/query-failure-and-repair.md:25,27 | live |
| REQ-QRY-29 | QRY-11 | On a typed query's final-response AJV failure the runtime respond-repairs by appending a NEW user turn (not re-issuing the original), preserving conversation history including the malformed response and prior tool calls. | query/query-failure-and-repair.md:31 | live |
| REQ-QRY-30 | QRY-11 | Respond-repair is bounded by `respond_repair.attempts` (default 3); each follow-up that produces a re-validated assistant response counts one slot regardless of its tool-call rounds; terminal exhaustion returns `Err(kind:"validation",cause:"schema_validation")`. | query/query-failure-and-repair.md:33 | live |
| REQ-QRY-31 | QRY-12 | Each non-`none` respond-repair methodology emits its follow-up as a user-role turn; renderers MUST emit template text verbatim (whitespace included), interpolating only `<…>` placeholders. | query/query-failure-and-repair.md:37 | conformance |
| REQ-QRY-32 | QRY-12 | Placeholders: `<ajv-summary>` = in-order `<path> <message>` joined by `; ` in canonical `validation_errors` order (most-recent attempt only); `<schema-json>` = `JSON.stringify(loweredSchema,null,2)`; `<slug>` = the lowered schema's schema slug matching `__loom_respond_<slug>`. | query/query-failure-and-repair.md:39-43 | conformance |
| REQ-QRY-33 | QRY-12 | The `validator_error` and `schema_repeat` follow-up templates are byte-exact as fenced; each rendered body ends with `<schema-json>` followed by U+000A; `schema_repeat` = `validator_error` minus its errors clause and both carry the leading non-compliance sentence. | query/query-failure-and-repair.md:47-63 | conformance |
| REQ-QRY-34 | QRY-8/QRY-11 | A non-validation failure during a respond-repair follow-up (transport/cancelled/model_tool/tool_loop_exhausted/context_overflow/invoke_infra/invoke_callee) propagates as its own variant, terminates repair immediately, and consumes no `attempts` slot. | query/query-failure-and-repair.md §Non-validation failures (query-failure-and-repair.md:65-72) | live |
| REQ-QRY-35 | QRY-10/QRY-11 | `context_overflow` short-circuits respond-repair permanently for the lifetime of that typed query (no further follow-ups). | query/query-failure-and-repair.md:69 | live |
| REQ-QRY-36 | QRY-22 | The runtime execution path for every typed query MUST resolve the declared annotation, lower it per SUBS-1, convey the lowered shape (not the type name) to the model on the forced-respond turn, validate the final response against the lowered schema, route non-conformance through respond-repair, and never bind an unvalidated response. | query/query-failure-and-repair.md:78 | live |
| REQ-QRY-37 | QRY-13 | On model tool-use the runtime executes the requested tool against the loom's callable set, feeds results back, and loops until a final non-tool-call response or the round cap; a response schema is enforced against the final response only, never intermediate tool payloads. | query/query-tool-loop.md:5 | live |
| REQ-QRY-38 | QRY-13 | An in-loop tool failure (`execute()` throws or `{content,isError:true}`) is lowered to that `tool_use` block's `isError:true` tool-result and fed back (round counts, loop continues); it does NOT raise `ModelToolError`; a cancelled model-driven `execute()` surfaces `CancelledError` and is never fed back. | query/query-tool-loop.md:7 | live |
| REQ-QRY-39 | QRY-14 | A typed query runs a two-phase loop: free phase (any frontmatter tool or plain text, may surface `ModelToolError`) then, on a plain-text turn, one forced-respond follow-up whose tool choice is forced to `__loom_respond_<slug>`, whose `execute` AJV-validates the payload against the lowered schema and resolves the query. | query/query-tool-loop.md:11-13 | live |
| REQ-QRY-40 | QRY-14 | At `tool_loop.max_rounds:0` the forced respond turn is the first and only turn (no free-phase call); the opening user body carries the prompt text then the forced-respond instruction separated by exactly one U+000A, with trailing U+000A bytes right-trimmed from the prompt first. | query/query-tool-loop.md:14 | live |
| REQ-QRY-41 | QRY-14 | The forced respond turn is CIO-4's exempt-routed terminator (not counted against `max_rounds`); each respond-repair follow-up restarts the whole two-phase loop with a fresh `tool_loop` budget; non-`end_turn`/`stop`/`tool_use` stop reasons classify to a `QueryError` variant. | query/query-tool-loop.md:16-17 | live |
| REQ-QRY-42 | QRY-15 | The initial forced-respond template body is byte-identical to `schema_repeat` minus its non-compliance sentence, emitted verbatim, interpolating only `<slug>`/`<schema-json>`, with a single U+000A between the instruction sentence and `<schema-json>`. | query/query-tool-loop.md:24-30 | conformance |
| REQ-QRY-43 | QRY-16 | Every query and every follow-up runs its tool-call loop under `tool_loop.max_rounds`; one round = one assistant message's parallel tool-use batch (executed in parallel where supported, all siblings awaited and lowered independently) → next turn; three parallel calls consume one slot. | query/query-tool-loop.md:35 | live |
| REQ-QRY-44 | QRY-16 | Only free-phase rounds count; on cap-reach an untyped query returns `Err(kind:"tool_loop_exhausted")` while a typed query dispatches the forced respond turn instead (so `tool_loop_exhausted` is unreachable on the typed path); the budget is per-query-per-invocation, never shared/pooled/replenished. | query/query-tool-loop.md:35 | live |
| REQ-QRY-45 | QRY-16 | Depth-6-co-fire worked example: `max_rounds:2` + `respond_repair.attempts:0` + depth-6 respond payload returns `Err(cause:"schema_validation", validation_errors:[{schema_keyword:"maxDepth", message:"JSON document depth exceeds 5"}])` and a `RuntimeEvent` with `masked:["ceiling#2"]` (the only non-empty `masked` reachable in V1); `max_rounds:0` omits `masked`. | query/query-tool-loop.md:37-90 | live |
| REQ-QRY-46 | — | The `Ok` payload of an untyped query is a plain `string` of the assistant's final text, assembled symmetrically across prompt mode and subagent mode. | query/query-tool-loop.md:92-94 | live |

## AREA: TOOL (tool-calls)

| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-TOOL-1 | — | Loom code calls a Pi tool or a `.loom` callable via bare-identifier `<name>(args)` where `<name>` is an entry in the resolved callable set (post-`as`-rename, post hyphen→underscore basename rewrite); the callable set is shared with what the model sees during a `@`…`` query. | tool-calls.md:1 | conformance |
| REQ-TOOL-2 | — | A tool call adds no conversation turn, consumes no model tokens, and does not appear in the transcript (code → side-effect / child conversation, not code → model). | tool-calls.md:12 | conformance |
| REQ-TOOL-3 | — | A Pi-tool argument is a single bare object literal parsed by the Loom literal sublanguage (primitive/null/array/bare-key-object literals, `Enum.Variant`, `Cat{...}` only); operators, identifier refs, nested calls, and `${...}` are `loom/parse/tool-arg-not-literal`; a multi-argument form is `loom/parse/tool-arg-arity`. | tool-calls.md:14 | offline-unit |
| REQ-TOOL-4 | — | A `.loom` callable takes its callee `params:` as already-typed values positionally in declaration order (bare object literals do not apply); a type mismatch is `loom/parse/tool-arg-type-mismatch` when the callee is statically resolvable, else runtime `Err(InvokeInfraError{cause:"validation"})`. | tool-calls.md:14 | conformance |
| REQ-TOOL-5 | — | For a Pi tool ("statically resolvable" N/A) an argument not matching the input schema is never a parse error — always the runtime AJV check → `Err(CodeToolError{cause:"validation"})`; `tool-arg-not-literal`/`tool-arg-arity` remain parse-time for Pi tools. | tool-calls.md:14 | live |
| REQ-TOOL-6 | — | Argument arity uses `invoke`'s rules: `loom/parse/invoke-arity-too-few` / `loom/parse/invoke-arity-too-many` apply to a `.loom` callable call. | tool-calls.md:14 | offline-unit |
| REQ-TOOL-7 | — | A `.loom` path in `tools:` that escapes the active discovery roots is rejected with `loom/load/invoke-path-escape` and the callable is not created. | tool-calls.md:14 | offline-unit |
| REQ-TOOL-8 | — | Return type by callee kind: Pi tool → `Result<string, QueryError>` (single string, no output schema); registered subagent-mode loom → `Result<T, QueryError>` where `T` is the callee's inferred/annotated return type (static when resolvable, else runtime AJV). | tool-calls.md:16-23 | conformance |
| REQ-TOOL-9 | — | Failure surfacing splits by callee kind: a Pi-tool call → `CodeToolError`; a `.loom`-callable call → `InvokeCalleeError`/`InvokeInfraError`, with `CodeToolError` arising only in the `"unknown_tool"` safety-net case. | tool-calls.md:25 | live |
| REQ-TOOL-10 | — | `CodeToolError.cause` is closed at exactly `validation`, `execution`, `cancelled`, `unknown_tool`; `unknown_tool` cannot occur within a single invocation (strong refs held by the load-time `tools:` table). | tool-calls.md:25 | conformance |
| REQ-TOOL-11 | — | A non-settling `execute()` Promise blocks at `await` (no loom 1.0 internal timeout) until `loomAbort.signal` fires, surfacing via `cause:"cancelled"`. | tool-calls.md §Outcome enumeration (tool-calls.md:28) | live |
| REQ-TOOL-12 | — | A pre-evaluation dispatch-setup throw in the `.loom`-callable adapter MUST translate to `{isError:true, content:[{type:"text",text:"loom <name> aborted with internal error: <msg>"}]}` (bare callable-set name), emit one `loom/runtime/internal-error` + one `loom-system-note`, and MUST NOT propagate as an unhandled rejection. | tool-calls.md:29 | live |
| REQ-TOOL-13 | — | An `execute()` return violating the `{content,isError}` envelope routes through `loom/runtime/internal-error` with `details.kind="tool-return-shape"`, NOT through `CodeToolError`. | tool-calls.md:30 | live |
| REQ-TOOL-14 | — | After `loomAbort` fires and the checkpoint surfaces `cause:"cancelled"`, any late Promise settlement is discarded (no rebind, no second `Err`, no second `RuntimeEvent`) via a swallowing handler installed before the first microtask boundary; discard is total regardless of the late reject's message. | tool-calls.md:31 | live |
| REQ-TOOL-15 | — | `CodeToolError` is distinct from `ModelToolError`: same `execute()` outcomes are lowered to `Err(CodeToolError{cause:"execution"})` code-side but fed back to the model in the query loop; a code-side call carries no `tool_call_id`/`raw_response`, a model-loop adapter failure carries both. | tool-calls.md:36 | conformance |
| REQ-TOOL-16 | — | For a `.loom` callable: callee-returned failures cascade `InvokeCalleeError`, infra failures `InvokeInfraError`; a non-statically-resolvable input-validation failure is `Err(InvokeInfraError{cause:"validation"})`, distinct from return-side `cause:"return_validation"`. | tool-calls.md:37 | live |
| REQ-TOOL-17 | — | loom 1.0 code-side tool calls are sequential and synchronous-looking: the runtime awaits each call before the next expression, yielding to Pi's event loop (non-blocking); streaming `onUpdate` partials are not surfaced to loom code. | tool-calls.md:38 | live |
| REQ-TOOL-18 | — | In model-driven parallel-tool mode the runtime awaits every sibling in a batch to settle before the next user turn, lowers each outcome independently (a failing sibling → its `isError:true` tool-result fed back alongside successes), and the batch consumes one `tool_loop.max_rounds` slot regardless of the success/failure mix. | tool-calls.md:38 | live |
| REQ-TOOL-19 | — | The `.loom`-callable `defineTool` adapter is re-entrant: concurrent parallel calls into one callee run as independent invocations, each with its own `AgentSession`, captured callable set, derived `AbortSignal`, and private transcript. | tool-calls.md:38 | live |
| REQ-TOOL-20 | — | The schema cache is read-mostly and safe to share across concurrent invocations; compiled validators are immutable post-compile and writes occur only on initial compile or file-watcher invalidation. | tool-calls.md:38 | conformance |
| REQ-TOOL-21 | — | No loom-level concurrency primitive (e.g. `parallel { ... }`) is exposed in loom 1.0. | tool-calls.md:38 | manual/inspection |
| REQ-TOOL-22 | — | The parallel-tool-batch delivery shape (concurrent execution, settle-all-before-next-turn, independent per-sibling lowering) is a loom-side consumption posture entirely Pi-side; detection routes to editorial review, not a build-time SDK signal. | tool-calls.md:40 | manual/inspection |
| REQ-TOOL-23 | — | `invoke("./x.loom",...)` and a `.loom`-callable call are operationally equivalent for subagent-mode callees (fresh isolated conversation, return-value schema validation, same `QueryError` variants, same arity/return-compat/path rules); subagent callees see only their own callable set; the two surfaces share one error model and one schema-lowering pipeline. | tool-calls.md:44 | conformance |

## AREA: SCH (schemas / schema-subset)

| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-SCH-1 | — | `schema X { ... }` declares a named object type; fields are comma-separated (trailing comma optional); every declared field is required (lowered `required` lists every property, `additionalProperties:false` always emitted); optional fields are `T \| null` (no `field?:T`; non-existence and explicit-`null` conflated). | schemas.md:5-8 | offline-unit |
| REQ-SCH-2 | — | An empty object schema body `schema X { }` is `loom/parse/empty-schema-body`. | schemas.md:19 | offline-unit |
| REQ-SCH-3 | — | `field as "WireName": T` attaches an explicit wire name; the loom identifier is used everywhere in code and the wire name appears only in the lowered `properties`/`required` keys and in validated/constructed JSON, translated at the validation boundary. | schemas.md:21-49 | offline-unit |
| REQ-SCH-4 | — | Wire-name rules: single non-empty string literal; two fields cannot share a wire name and a wire name cannot collide with another field's loom name (`loom/parse/wire-name-collision`); a redundant rename equal to the loom name is `loom/parse/redundant-wire-name` (warning). | schemas.md:43-45 | offline-unit |
| REQ-SCH-5 | — | For discriminated unions, discriminator detection runs on the wire name; the explicit `by <field>` form accepts the loom-side name and lowering resolves it to each variant's wire name. | schemas.md:46 | offline-unit |
| REQ-SCH-6 | — | `schema X = ...` is a top-level type alias composing literal unions, primitive unions, object (discriminated) unions, and references to other named types. | schemas.md:50-60 | offline-unit |
| REQ-SCH-7 | — | `enum X { ... }` variant names are PascalCase; the variant name is the model-produced string by default (`Low`→`"Low"`); explicit `Low = "low"` overrides. | schemas.md:62-90 | offline-unit |
| REQ-SCH-8 | — | Enum constraints: top-level only (`loom/parse/inline-enum`); string values only (`loom/parse/non-string-enum-value`); duplicate explicit value across variants `loom/parse/duplicate-enum-value`; two variants sharing an identifier `loom/parse/duplicate-enum-variant-name` (name check runs before value check); empty body `loom/parse/empty-enum-body`. | schemas.md:70-89 | offline-unit |
| REQ-SCH-9 | — | `Enum.Variant` evaluates to the variant's underlying string value but is statically typed `Enum`; an unknown variant reference is `loom/parse/unknown-variant`. | schemas.md:91-93 | offline-unit |
| REQ-SCH-10 | — | An implicit discriminated-union discriminator field must be present in every variant, be a single string-literal type per variant, and be unique across variants. | schemas.md:95-101 | offline-unit |
| REQ-SCH-11 | — | Numeric/boolean literal discriminators are rejected `loom/parse/non-string-discriminator`, for both implicit detection and the explicit `by <field>` form; wire-renamed discriminator fields keep the string-literal constraint on the value. | schemas.md:103-105 | offline-unit |
| REQ-SCH-12 | — | Multiple qualifying discriminator fields → `loom/parse/ambiguous-discriminator`; none qualifying → `loom/parse/missing-discriminator`. | schemas.md:105 | offline-unit |
| REQ-SCH-13 | — | The explicit `schema X by <field> = A \| B` form overrides detection and is admitted only on the `=` union form; `by` on an object body is `loom/parse/by-on-object-schema`. | schemas.md:109-113 | offline-unit |
| REQ-SCH-14 | — | Duplicate discriminator values across variants are `loom/parse/duplicate-discriminator-value`; a nested (non-top-level) discriminator is `loom/parse/nested-discriminator`. | schemas.md:115 | offline-unit |
| REQ-SCH-15 | — | Mixed unions (`string \| Author`, `Author \| null`) are not discriminated; they lower as plain `anyOf` (or the multi-type-array form when all arms are primitives). | schemas.md:117 | offline-unit |
| REQ-SCH-16 | — | Any named-schema reference lowers to `$ref` against the file's `$defs`; self- and mutual recursion are supported transparently (authors write no `$defs`/`$ref`). | schemas.md:119-134 | offline-unit |
| REQ-SCH-17 | — | The depth ceiling applies to runtime JSON data depth, not the schema graph: a recursive schema definition is legal; recursive data is bounded by the runtime cap. | schemas.md:141 | offline-unit |
| REQ-SCH-18 | — | A pure type-alias cycle (`schema X = X`, or transitive through aliases) is `loom/parse/type-alias-cycle` with the cycle path printed; cycles passing through ≥1 object-schema hop remain legal; the alias-cycle detector runs after name resolution, before lowering. | schemas.md:143 | offline-unit |
| REQ-SCH-19 | — | The emitted/enforced subset types are exactly `string`, `number`, `integer`, `boolean`, `object`, `array`, `null`. | schema-subset.md:5 | offline-unit |
| REQ-SCH-20 | — | Composition is `anyOf` only; `oneOf`, `allOf`, `not`, `if`/`then`/`else` are rejected at parse time; the draft is JSON Schema 2020-12, under which all "validates" prose is evaluated. | schema-subset.md:6,14 | offline-unit |
| REQ-SCH-21 | — | The unsupported keyword set (`pattern`, `format`, `minLength`/`maxLength`, numeric bounds, `multipleOf`, item/array/object-count keywords, `patternProperties`, `propertyNames`, `unevaluated*`, `dependent*`, `nullable`, …) is rejected at parse time. | schema-subset.md:16 | offline-unit |
| REQ-SCH-22 | — | Nullability is expressed as a union with `null` lowering to `{"type":["...","null"]}`; the non-standard `nullable:true` modifier is never emitted. | schema-subset.md:11 | offline-unit |
| REQ-SCH-23 | — | Depth counting: scalar and empty `{}`/`[]` = depth 1; non-empty object/array = `1 + max(depth(child))`; `anyOf` arms are not levels (depth measured against the materialised value); cap is `depth ≤ 5` (`42`→1, `{"a":1}`→2, `{"a":[{"b":1}]}`→4, five nested→6 rejected). | schema-subset.md:24-38 | offline-unit |
| REQ-SCH-24 | — | The depth walk runs before AJV at all five enforcement sites: (1) typed-query response, (2) tool-call args model-driven, (3) tool-call args code-driven, (4) `params` at loom invocation, (5) `invoke<T>` return value. | schema-subset.md:39-48 | conformance |
| REQ-SCH-25 | — | A depth violation always carries `schema_keyword:"maxDepth"` (the only Loom-emitted non-AJV keyword), the canonical message `"JSON document depth exceeds 5"`, and `cause:"schema_validation"` (even though the walk short-circuits before AJV). | schema-subset.md:49 | offline-unit |
| REQ-SCH-26 | — | Per-boundary depth-violation routing: #1→`Err(QueryError validation/schema_validation)` (respond-repair applies, walk re-runs per follow-up); #2→tool-error fed back, counts a round, not `ModelToolError`; #3→`Err(CodeToolError validation)`; #4→`invoke` `InvokeInfraError validation` / slash-load ceiling-#3 no-retry; #5→`Err(InvokeInfraError return_validation)`; retry only at #1. | schema-subset.md:51-63 | conformance |
| REQ-SCH-27 | — | The walk runs on the post-decode JSON value (a JSON-parse failure is a parse-validation failure, not a depth failure); `params` restricted to primitives / `array<T>` over primitives are structurally bounded at depth 2 (walk is a no-op but still installed). | schema-subset.md:64-67 | offline-unit |
| REQ-SCH-28 | SUBS-1 | A union all of whose arms are primitive (treating `null` as primitive) MUST lower to `{"type":[...]}`; a union with any non-primitive arm MUST lower to `{"anyOf":[...]}`; vectors `string\|number`→`{"type":["string","number"]}`, `string\|null`→`{"type":["string","null"]}`, `string\|Author`→`{"anyOf":[{"type":"string"},{"$ref":"#/$defs/Author"}]}`. | schema-subset.md:81 | offline-unit |
| REQ-SCH-29 | — | Lowering step 1: every top-level (and transitively `.warp`-imported) named schema becomes one `$defs/<Name>` entry. | schema-subset.md:70 | offline-unit |
| REQ-SCH-30 | — | Lowering step 2: anonymous inline object schemas are hoisted to `$defs` as `__inline_<slug>`; two inline schemas collapse to one entry only when their lowered fragments are byte-identical; a slug match with non-byte-identical fragments is `loom/load/schema-slug-collision` (no silent merge). | schema-subset.md:72 | offline-unit |
| REQ-SCH-31 | — | Lowering step 3 per-form emission: primitive→`{"type":"<p>"}`; named/inline ref→`{"$ref":"#/$defs/<Name>"}`; `array<T>`→`{"type":"array","items":<T>}`; object→`{"type":"object","properties":{...wire...},"required":[...every wire...],"additionalProperties":false}`; literal→`{"const":<v>}`; enum/string-literal-union→`{"type":"string","enum":[...wire values...]}`. | schema-subset.md:74-80 | offline-unit |
| REQ-SCH-32 | — | A discriminated object union lowers to `{"anyOf":[...]}` with NO `discriminator` keyword; each variant carries its own `const`-typed discriminator field and relies on `additionalProperties:false` + per-variant `const`. | schema-subset.md:82 | offline-unit |
| REQ-SCH-33 | — | `Result<T,E>` is not a lowerable type form: `loom/parse/result-in-schema-position` fires before lowering, so no `Result` reaches the lowering step. | schema-subset.md:84 | offline-unit |
| REQ-SCH-34 | — | Array element order: `required`, `enum`, the `{"type":[...]}` primitive-union form, and `anyOf` all emit in loom-source declaration order, with `"null"` last whenever the union admits it. | schema-subset.md:85 | offline-unit |
| REQ-SCH-35 | — | Lowering step 4: the per-query schema document is built lazily with the query's response schema as root and only transitively-reachable `$defs` copied in (unused pruned). | schema-subset.md:86 | conformance |
| REQ-SCH-36 | — | Lowering step 5 sidecar per `$defs` entry: (1) a wire-name translation map used by validation (post-decode) and construction (pre-encode); (2) a named-enum-positions map keyed by JSON Pointer, valued by the declaring `enum`'s loom-side name, included iff the source type was a named `enum` (anonymous string-literal-unions absent). | schema-subset.md:87 | offline-unit |
| REQ-SCH-37 | — | Lowering step 6: discriminator detection runs on the lowered `anyOf` form as a parse-time sanity check; the lowered schema carries no extra discriminator marker; lowering is a pure function of parsed source, performed once per file load. | schema-subset.md:88-90 | offline-unit |
| REQ-SCH-38 | SUBS-2 | Authors/implementers MUST use `schema slug` (or bare `slug`) for the 16-hex hash output; the synonyms `schema hash`, `schema-hash`, `sha12`, `lowered-schema hash`, `lowered-schema content hash` are drift to avoid (source of truth for a future grep gate). | schema-subset.md:96 | manual/inspection |
| REQ-SCH-39 | — | Canonical schema hash: input is the lowered JSON Schema fragment (not the loom AST); canonical form = keys sorted by Unicode code-point, no insignificant whitespace, embedded numerics rendered by the `integer`/`number` algorithm (`1e21`→`1000000000000000000000`, `42.0`→`42`, `-0`→`0`), array elements in lowering order, RFC 8259 minimal string escapes; digest = SHA-256; schema slug = first 16 lowercase hex (64 bits). | schema-subset.md:92-107 | offline-unit |
| REQ-SCH-40 | — | The schema slug appears in exactly four synthesised-name forms (this list is authoritative): `__inline_<slug>`, `__loom_respond_<slug>`, `__loom_callee_<slug>__<post-rename-name>`, `__loom_bind_<slug>`. | schema-subset.md:108 | offline-unit |
| REQ-SCH-41 | — | Slug-collision posture: every slug-keyed cache/dedup site verifies byte-equality of the candidate's canonical-form bytes against the stored bytes before treating fragments as identical, disambiguating on mismatch; the three sites and diagnostics are `__inline_<slug>` `$defs` dedup → `loom/load/schema-slug-collision`, per-query AJV validator cache → `loom/runtime/validator-cache-collision`, prompt-mode `pi.registerTool` cache → `loom/runtime/registration-cache-collision` (counter-suffixed). | schema-subset.md:112-118 | conformance |
| REQ-SCH-42 | — | Canonical-form key sorting is independent of emitted `$defs` property order: the hash sorts keys for reproducibility while the emitted schema retains loom-source field order; changing source field order changes emitted property order but not the canonical hash. | schema-subset.md:110 | offline-unit |

## DEFERRED / out-of-scope (Cluster 3)

- **Per-call timeout / per-query `model`/`tools`/`system` overrides** — the runtime-internal options record is an open struct; per-call timeout and per-query overrides are deferred to a minor revision (`docs/spec_topics/query/query-failure-and-repair.md:3` loom 1.0 seam; `docs/spec_topics/tool-calls.md:42` loom 1.0 seam). Not conformance-testable; the seam itself is a non-normative maintainability convention.
- **Pre-flight token-count estimation** — out of loom 1.0 scope; `ContextOverflowError.tokens_used`/`tokens_limit` stay `null` when the provider omits them, reserved for a deferred local-estimate extension (`docs/spec_topics/query/query-failure-and-repair.md:25,27`).
- **Oversized rendered templates** — no pre-flight bound in loom 1.0; detected reactively via the provider overflow error envelope only (`docs/spec_topics/query/query-forms.md:104`).
- **Internal `execute()` timeout enforcement** — no loom 1.0 attempt; a non-settling Promise blocks until cancellation (`docs/spec_topics/tool-calls.md:28`, NOCEIL-1).
- **Streaming partial results (`onUpdate`)** — not surfaced to loom code in loom 1.0 (`docs/spec_topics/tool-calls.md:38`).
- **loom-level concurrency primitive** (`parallel { ... }`) — not exposed in loom 1.0 (`docs/spec_topics/tool-calls.md:38`).
- **Richer untyped-query return structure** — deliberately kept as plain `string` in loom 1.0; richer structure deferred (`docs/spec_topics/query/query-tool-loop.md:92`).
- **Parallel-tool-batch delivery shape** — a loom-side consumption posture, not a pinned host contract; detection is editorial-review-only, not build-time testable (`docs/spec_topics/tool-calls.md:40`).
- **Native provider structured output** — loom 1.0 uses the synthesised one-shot tool + forced tool choice; native structured output where supported is a future-revision technique (`docs/spec_topics/query/query-tool-loop.md:18`).
- **SUBS-2 synonym grep gate** — the terminology obligation is stated as the source of truth for a *future* grep gate; the gate itself is not yet implemented (`docs/spec_topics/schema-subset.md:96`).
