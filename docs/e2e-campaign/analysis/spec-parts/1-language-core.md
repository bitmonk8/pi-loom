# Cluster 1 — Language core

## AREA: LEX (lexical)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-LEX-1 | — | Source files are UTF-8; a leading UTF-8 BOM (`EF BB BF`) is consumed and ignored. | docs/spec_topics/lexical.md:5 | offline-unit |
| REQ-LEX-2 | — | Any non-UTF-8 BOM or invalid UTF-8 byte sequence (incl. lone surrogates) is `loom/load/invalid-encoding`, reporting file path and zero-based byte offset of the first invalid byte (offset `0` for a non-UTF-8 BOM). | docs/spec_topics/lexical.md:5 | offline-unit |
| REQ-LEX-3 | — | No transcoding is performed; a UTF-16 save fails fast rather than producing mojibake. | docs/spec_topics/lexical.md:5 | offline-unit |
| REQ-LEX-4 | — | Before lexing, line endings are normalised: `\r\n` → `\n` and bare `\r` → `\n`; a CRLF file tokenises byte-identically to the LF equivalent. | docs/spec_topics/lexical.md:6 | offline-unit |
| REQ-LEX-5 | — | The `RestOfLine` terminal is a possibly-empty maximal run containing no `\n`; no escape processing is applied and an empty `RestOfLine` (bare `///`) is admitted. | docs/spec_topics/lexical.md §RestOfLine terminal | offline-unit |
| REQ-LEX-6 | — | Path literals (`import`, `invoke`, `.loom` `tools:` entries) use forward-slash separators only; a backslash inside a path literal is `loom/parse/invalid-path-separator`. | docs/spec_topics/lexical.md:8 | offline-unit |
| REQ-LEX-7 | — | A backslash byte outside any string literal, path literal, or `@`...`` query body is `loom/parse/stray-backslash`; there is no line-continuation marker. | docs/spec_topics/lexical.md §Stray backslash | offline-unit |
| REQ-LEX-8 | — | `.loom` / `.warp` extensions are matched byte-exact in lowercase ASCII at every site with no case-folding; a settings/CLI extension mismatch is `loom/load/invalid-extension`. | docs/spec_topics/lexical.md §Extension matching | offline-unit |
| REQ-LEX-9 | — | Diagnostic line/column numbers are 1-indexed on the post-BOM, post-normalisation stream; a CRLF source line counts as one newline. | docs/spec_topics/lexical.md:11 | offline-unit |
| REQ-LEX-10 | — | Identifiers match `[A-Za-z_][A-Za-z0-9_]*` and are case-sensitive. | docs/spec_topics/lexical.md §Identifiers | offline-unit |
| REQ-LEX-11 | — | PascalCase (uppercase first letter) is required for schema names, enum names, enum variant names, and type-like bindings (and built-ins `Ok`/`Err`/`Result`); violation is `loom/parse/schema-case-mismatch`. | docs/spec_topics/lexical.md:15 | offline-unit |
| REQ-LEX-12 | — | lowercase-first (lowercase letter or `_`) is required for `let`/`let mut` bindings, function parameters, function names, and schema field names; violation is `loom/parse/binding-case-mismatch`. Both snake_case and lowerCamelCase are accepted. | docs/spec_topics/lexical.md:16 | offline-unit |
| REQ-LEX-13 | — | The reserved keyword set (`let mut fn if else for in while break continue return match schema enum import export from as by invoke true false null Ok Err Result string number integer boolean array void`) cannot be used in identifier position: `loom/parse/reserved-keyword-as-identifier`. | docs/spec_topics/lexical.md §Reserved keywords | offline-unit |
| REQ-LEX-14 | — | The discard binding `_` is reserved and cannot be referenced after binding. | docs/spec_topics/lexical.md §Reserved keywords | offline-unit |
| REQ-LEX-15 | — | `array` and `Result` remain reserved as identifiers and double as the closed set of generic-type constructor keywords in type position. | docs/spec_topics/lexical.md §Reserved keywords | offline-unit |
| REQ-LEX-16 | — | Statements are separated by newlines; semicolons are not part of the grammar. | docs/spec_topics/lexical.md §Statement terminators | offline-unit |
| REQ-LEX-17 | — | A statement continues across newlines only when a closed continuation-trigger set holds; blank lines do not break a continuation. | docs/spec_topics/lexical.md §Statement terminators | offline-unit |
| REQ-LEX-18 | — | Single-line `if (x) stmt` does not exist; `if`/`for`/`while`/`fn` bodies are always braced blocks — violation `loom/parse/single-line-if`. | docs/spec_topics/lexical.md §Statement terminators | offline-unit |
| REQ-LEX-19 | — | Only line comments exist: `//` regular, `///` doc; a `///` lowers to a JSON Schema `description:`. | docs/spec_topics/lexical.md §Comments | offline-unit |
| REQ-LEX-20 | — | Block comments `/* ... */` are `loom/parse/block-comment`. | docs/spec_topics/lexical.md §Comments | offline-unit |
| REQ-LEX-21 | — | Single- (`'...'`) and double-quoted (`"..."`) string forms are equivalent; escapes are `\"`, `\'`, `\\`, `\n`, `\t`, `\r`, `\u{XXXX}` (1–6 hex). | docs/spec_topics/lexical.md §String literals | offline-unit |
| REQ-LEX-22 | — | A `\u{...}` whose value exceeds `0x10FFFF` or names a UTF-16 surrogate (`0xD800`–`0xDFFF`) is `loom/parse/invalid-unicode-escape`. | docs/spec_topics/lexical.md §String literals | offline-unit |
| REQ-LEX-23 | — | A backslash followed by any other (non-escape) character is `loom/parse/illegal-escape`. | docs/spec_topics/lexical.md §String literals | offline-unit |
| REQ-LEX-24 | — | EOF inside an unterminated string literal is `loom/parse/unterminated-string`. | docs/spec_topics/lexical.md §String literals | offline-unit |
| REQ-LEX-25 | — | A literal newline inside a regular string is `loom/parse/literal-newline-in-string`. | docs/spec_topics/lexical.md §String literals | offline-unit |
| REQ-LEX-26 | — | Regular strings perform no interpolation; `${` is plain text. | docs/spec_topics/lexical.md §String literals | offline-unit |
| REQ-LEX-27 | — | Number literals are decimal only and carry no sign (negation is unary `-` at parse time); hex/octal/binary and underscore separators surface as `loom/parse/unsupported-feature`. | docs/spec_topics/lexical.md §Number literals | offline-unit |
| REQ-LEX-28 | — | A literal with no fractional or exponent part has type `integer`; otherwise `number`. | docs/spec_topics/lexical.md §Number literals | offline-unit |
| REQ-LEX-29 | — | `integer` widens implicitly to `number` in arithmetic/assignment; the reverse is `loom/parse/integer-narrowing`. | docs/spec_topics/lexical.md §Number literals | offline-unit |
| REQ-LEX-30 | — | An `integer` token with `|value| > 2^53 - 1` is `loom/parse/integer-literal-out-of-range`; magnitude is judged per-token before the unary-`-` fold, so `-12345678901234567890` is rejected. | docs/spec_topics/lexical.md §Number literals | offline-unit |
| REQ-LEX-31 | — | A `number` token whose parsed value is not a finite IEEE-754 double (e.g. `1e400`) is `loom/parse/number-literal-not-finite`. | docs/spec_topics/lexical.md §Number literals | offline-unit |

## AREA: GRAM (grammar/parse)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-GRAM-1 | — | The Loom literal sublanguage is admitted in exactly two positions (RHS of a `params:` default, single positional argument of a Pi-tool call); is-literal failures are `loom/parse/default-not-literal` or `loom/parse/tool-arg-not-literal`, naming the offending sub-expression. | docs/spec_topics/grammar.md §Loom literal sublanguage | offline-unit |
| REQ-GRAM-2 | — | `PrimitiveLit` admits `"-" NUMBER` (unary minus on a numeric literal counts as a literal); `NamedValueLit` is `Ident "." Ident` (Enum.Variant). | docs/spec_topics/grammar.md §Loom literal sublanguage | offline-unit |
| REQ-GRAM-3 | — | `BareObjectLit` is admitted only when an external schema supplies the type; `NamedObjectLit` is used where the type is not supplied externally (incl. discriminated-union variants — `Cat { ... }`, never `Animal { species: "cat", ... }`). | docs/spec_topics/grammar.md §Loom literal sublanguage | offline-unit |
| REQ-GRAM-4 | — | Every declared field of the LHS/variant schema must be present in a literal object — omissions are `loom/parse/missing-object-field`; field order is free; discriminator fields are implicit. | docs/spec_topics/grammar.md §Loom literal sublanguage | offline-unit |
| REQ-GRAM-5 | — | Inside a literal the is-literal check rejects identifier references other than `Enum.Variant`, operators other than the unary-`-` numeric carve-out, function/tool calls, `${...}` and `@`...`` templates, and member access other than `Enum.Variant`. | docs/spec_topics/grammar.md §Loom literal sublanguage | offline-unit |
| REQ-GRAM-6 | — | `LetStmt ::= "let" "mut"? Pattern (":" Type)? "=" Expr`; `let` requires an initialiser and `let x: T` with no initialiser is `loom/parse/let-without-initialiser`. `Pattern` here is `_` or an identifier only. | docs/spec_topics/grammar.md §`let` form | offline-unit |
| REQ-GRAM-7 | — | `ReturnType ::= Type \| "void"`; `void` is admitted only in function-/loom-return position and any other type position is `loom/parse/void-in-non-return-position`. | docs/spec_topics/grammar.md §Type grammar | offline-unit |
| REQ-GRAM-8 | — | `GenericType` is closed in loom 1.0: `array<T>` (arity 1) and `Result<T,E>` (arity 2); a type-argument count other than the declared arity is `loom/parse/generic-arity-mismatch`; nested generics parse. | docs/spec_topics/grammar.md §Type grammar | offline-unit |
| REQ-GRAM-9 | — | A `Result` application in a lowered-schema position (schema field type, `params:` field type, or any type reachable transitively) is `loom/parse/result-in-schema-position`; `Result` remains admitted in all other type positions. | docs/spec_topics/grammar.md §Type grammar | offline-unit |
| REQ-GRAM-10 | — | An inline `ObjectType` `{ field: T, ... }` is legal in any type position; an empty inline object `{}` is `loom/parse/empty-schema-body`; wire-name renames within it are subject to `loom/parse/wire-name-collision` and `loom/parse/redundant-wire-name`. | docs/spec_topics/grammar.md §Type grammar | offline-unit |
| REQ-GRAM-11 | — | The type-union `\|` is right-associative and nullability is written `T \| null`. | docs/spec_topics/grammar.md §Type grammar | offline-unit |
| REQ-GRAM-12 | — | `BlockExpr` requires a trailing tail `Expr`; `FnBody`/`LoomBody`/`StmtBlock` accept zero or more statements with an optional tail. | docs/spec_topics/grammar.md §Block expressions | offline-unit |
| REQ-GRAM-13 | — | A `FnBody`/`LoomBody` parsed without a trailing `Expr` has inferred return type `null` and final value literal `null`. | docs/spec_topics/grammar.md §Block expressions | offline-unit |
| REQ-GRAM-14 | — | A statement-form `if`/`for`/`while` produces no value and is not admissible in expression position (the ternary is the expression form of conditional). | docs/spec_topics/grammar.md §Block expressions | offline-unit |
| REQ-GRAM-15 | — | A tail `Expr` in a `StmtBlock` is evaluated and its value discarded, except a tail `Expr` ending in postfix `?`, which still triggers `?` early-return on failure. | docs/spec_topics/grammar.md §Block expressions | conformance |
| REQ-GRAM-16 | — | `FnDecl` parameter lists are always parenthesised (`fn f()`, never `fn f`), with `,`-separated params and an admitted trailing comma; each `FnParam` is `Ident ":" Type`. | docs/spec_topics/grammar.md §`fn` declarations | offline-unit |
| REQ-GRAM-17 | — | A `fn` parameter carries no default; a `mut` modifier on a `fn` parameter is `loom/parse/mut-on-immutable-context`. | docs/spec_topics/grammar.md §`fn` declarations | offline-unit |
| REQ-GRAM-18 | — | A `match` arm body is a single `Expr` or `BlockExpr`; a bare statement (`if`/`for`/`while`/`let`/assignment/`break`/`continue`/`return`) in arm-body position is `loom/parse/statement-in-arm-body`. | docs/spec_topics/grammar.md §`match` arm body | offline-unit |
| REQ-GRAM-19 | — | The `by <field>` clause is admitted only on the union (`=`) schema form; `schema X by f { ... }` with an object body is `loom/parse/by-on-object-schema`. | docs/spec_topics/grammar.md §`schema X by <field>` | offline-unit |
| REQ-GRAM-20 | — | A `DocComment` (`///` run) is admitted only immediately above `SchemaDecl` (all forms), `EnumDecl`, an object-schema field, an enum variant, or `FnDecl`; anywhere else is `loom/parse/doc-comment-misplaced`. | docs/spec_topics/grammar.md §`///` placement | offline-unit |
| REQ-GRAM-21 | — | The four newline-continuation triggers form a closed set (open bracket, trailing binary/ternary operator, trailing comma, leading binary/ternary operator); blank lines do not break a continuation. | docs/spec_topics/grammar.md §Newline continuation | offline-unit |
| REQ-GRAM-22 | — | The `?` continuation trigger is the ternary head only; the postfix error-propagation `?` is a complete-expression terminator that never triggers continuation. | docs/spec_topics/grammar.md §Newline continuation | offline-unit |
| REQ-GRAM-23 | — | `[]`/`[expr, ...]` literals require a type sink from the exhaustive sink set (binding annotation, call-site parameter type, surrounding constructor field type, or the element type of an enclosing array sink); the `for x in expr` iterand is not a sink, so `for x in []` with no other sink is `loom/parse/array-no-common-type`. | docs/spec_topics/grammar.md §`array<T>` literal type-sink rule | offline-unit |
| REQ-GRAM-24 | — | Nested `fn` declarations are rejected (the surface `FnDecl` is top-level only per the grammar production). | docs/spec_topics/grammar.md §`fn` declarations | offline-unit |

## AREA: EXPR (expressions)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-EXPR-1 | — | The supported expression forms are exactly: literals, identifiers, member access `a.b`, indexed access `a[k]`, calls, unary `!`/`-`, arithmetic `+ - * / %`, comparison `== != < <= > >=`, logical `&& \|\|`, ternary, postfix `?`, parenthesisation, `@`...`` query templates, array literals, schema constructors, `Enum.Variant`, and `Ok`/`Err` constructors. | docs/spec_topics/expressions.md §Supported forms | offline-unit |
| REQ-EXPR-2 | — | Indexed access requires an `array<T>` or object receiver; any other receiver (incl. `string`) is `loom/parse/non-indexable-receiver`. | docs/spec_topics/expressions.md §Supported forms | offline-unit |
| REQ-EXPR-3 | — | An object index expression must be of type `string`; a non-`string` index (e.g. `obj[0]`) is `loom/parse/non-string-object-index` (no literal/dynamic-key distinction). | docs/spec_topics/expressions.md §Supported forms | offline-unit |
| REQ-EXPR-4 | — | `obj[k]`'s static result type is the union of the receiver's declared field types (same element type `values()` produces), applied uniformly regardless of the index. | docs/spec_topics/expressions.md §Supported forms | offline-unit |
| REQ-EXPR-5 | — | At runtime an array index outside `0..arr.length` panics `loom/runtime/index-out-of-bounds`, and an absent object loom-side key panics `loom/runtime/missing-object-key`; the parse-time key-type check runs before the runtime missing-key panic. | docs/spec_topics/expressions.md §Supported forms | conformance |
| REQ-EXPR-6 | — | Unsupported forms (assignment in expression position, field/index mutation, arrow functions/higher-order methods, spread/rest, `new`/`typeof`/`instanceof`/`delete`/`void`/`yield`/`await`, `?.`/`??`, `===`/`!==`, bitwise, `++`/`--`, comma operator, nested template strings, `@`/`match` inside `${...}`) are rejected — `loom/parse/unsupported-feature` unless a more specific code applies. | docs/spec_topics/expressions.md §Not supported | offline-unit |
| REQ-EXPR-7 | — | A bare identifier in call position resolves first-match-wins in order: local `let`/param → top-level `fn` → `.warp` import → callable-set name; no match is `loom/parse/unknown-identifier`. | docs/spec_topics/expressions.md §Identifier resolution | offline-unit |
| REQ-EXPR-8 | — | A `tools:` entry whose post-rename name shadows a top-level `fn` or import in the same file is rejected at load time. | docs/spec_topics/expressions.md §Identifier resolution | offline-unit |
| REQ-EXPR-9 | — | `==` is structural (deep for objects/arrays, value for primitives); there is no `===`. | docs/spec_topics/expressions.md §Equality | conformance |
| REQ-EXPR-10 | — | `NaN == NaN` is `true` and `+0 == -0` is `true`. | docs/spec_topics/expressions.md §Equality | conformance |
| REQ-EXPR-11 | — | `==`/`!=` accept cross-type operands: when neither operand's static type is `⊑` the other, the comparison loads and evaluates to `false` (`==`) / `true` (`!=`), never `loom/parse/non-orderable-operands`. | docs/spec_topics/expressions.md §Equality | conformance |
| REQ-EXPR-12 | — | Only `true`/`false` are accepted in boolean position (`if`, `while`, `&&`, `\|\|`, ternary condition); a non-boolean is `loom/parse/non-boolean-condition`. | docs/spec_topics/expressions.md §Truthiness | offline-unit |
| REQ-EXPR-13 | — | Operands evaluate left-to-right; `&&` evaluates its right operand only when left is `true`, `\|\|` only when left is `false`; a short-circuited right operand runs no queries/tools/invokes and spends no tokens; both operators always produce `boolean`. | docs/spec_topics/expressions.md §Evaluation order and short-circuiting | conformance |
| REQ-EXPR-14 | — | `cond ? a : b` evaluates `cond` first, then only the taken branch. | docs/spec_topics/expressions.md §Evaluation order and short-circuiting | conformance |
| REQ-EXPR-15 | — | `string.length` is the number of UTF-16 code units (no grapheme segmentation). | docs/spec_topics/expressions.md §Built-in methods and properties | conformance |
| REQ-EXPR-16 | — | `string` exposes `toLowerCase()`, `toUpperCase()`, `trim()`, `startsWith(s)`, `endsWith(s)`, `includes(s)`, `split(sep): array<string>` (literal-only; empty separator → per-code-unit), `replace(from, to): string`. | docs/spec_topics/expressions.md §Built-in methods and properties | conformance |
| REQ-EXPR-17 | — | `replace` replaces all occurrences via a single left-to-right non-overlapping scan, literal-only, inserting `$`-sequences literally; empty `from` returns the receiver unchanged. | docs/spec_topics/expressions.md §Built-in methods and properties | conformance |
| REQ-EXPR-18 | — | `replace` reproduces the five normative reference vectors exactly (`"aXbXc".replace("X","[$&]")`→`"a[$&]b[$&]c"`; `"100".replace("0","$$")`→`"1$$$$"`; `"a-b".replace("-","x$1y")`→`"ax$1yb"`; `"abc".replace("","X")`→`"abc"`; `"aaaaa".replace("aa","x")`→`"xxa"`). | docs/spec_topics/expressions.md §Built-in methods and properties | conformance |
| REQ-EXPR-19 | — | `string` is not indexable: `s[i]` is `loom/parse/non-indexable-receiver`. | docs/spec_topics/expressions.md §Built-in methods and properties | offline-unit |
| REQ-EXPR-20 | — | `array<T>` exposes `length`, `join(sep): string` (element type must be `string`, else `loom/parse/non-string-array-join`), `includes(x)` (structural equality), `indexOf(x)` (`-1` if absent), `slice(start, end?)`, `concat(other): array<T ⊔ U>`. | docs/spec_topics/expressions.md §Built-in methods and properties | conformance |
| REQ-EXPR-21 | — | `concat`'s result element type is the LUB of receiver and argument element types under `⊑` (`array<integer>.concat(array<number>)`→`array<number>`; disjoint types union, `array<integer>.concat(array<string>)`→`array<integer \| string>`). | docs/spec_topics/expressions.md §Built-in methods and properties | offline-unit |
| REQ-EXPR-22 | — | `object` exposes `keys(): array<string>` (loom-side names, schema declaration order for named schemas / insertion order otherwise), `values(): array<T>` (same order), `has(k): boolean` (returns `false` for unknown keys, no panic). | docs/spec_topics/expressions.md §Built-in methods and properties | conformance |
| REQ-EXPR-23 | — | Any method not on the built-in list is `loom/parse/unknown-method` (parse error, not a runtime failure). | docs/spec_topics/expressions.md §Built-in methods and properties | offline-unit |
| REQ-EXPR-24 | — | Operator precedence, highest→lowest, is: (1) `.`/`[]`/`()`/postfix `?` left; (2) unary `!`/`-` right; (3) `* / %` left; (4) `+ -` left; (5) `< <= > >=` non-assoc; (6) `== !=` non-assoc; (7) `&&` left; (8) `\|\|` left; (9) `?:` ternary right. | docs/spec_topics/expressions.md §Operator precedence | offline-unit |
| REQ-EXPR-25 | — | Comparison and equality are non-associative; `a < b < c` is `loom/parse/comparison-chaining`. | docs/spec_topics/expressions.md §Operator precedence | offline-unit |
| REQ-EXPR-26 | — | A bare `Schema { ... }` constructor in `if`/`while` condition, `match` scrutinee, or `for` iterand position requires surrounding parentheses; outside scrutinee positions no parens are needed. | docs/spec_topics/expressions.md §Grammar disambiguation | offline-unit |
| REQ-EXPR-27 | — | `match` is exhaustive-destructuring and is itself an expression whose arms evaluate to a value. | docs/spec_topics/expressions.md §`match` expression | offline-unit |
| REQ-EXPR-28 | — | The loom-1.0 pattern set is Wildcard `_`, Identifier (binds), Literal (structural equality), Constructor `Ok(p)`/`Err(p)`, Object/schema (`{ f: p, ... }` with `{ f }` shorthand; unlisted fields ignored), and exact-length Array `[a, b]`; lowercase binds, capitalised refers to constructors/schema names. | docs/spec_topics/expressions.md §Pattern grammar | offline-unit |
| REQ-EXPR-29 | — | Guards (`Ok(x) if ...`) are `loom/parse/match-guard-not-supported`; rest patterns (`[first, ...rest]`, `{ k, ...other }`) are `loom/parse/rest-pattern-not-supported`. | docs/spec_topics/expressions.md §Pattern grammar | offline-unit |
| REQ-EXPR-30 | — | Exhaustiveness is not statically checked; a `match` whose arms fail to cover the scrutinee at runtime raises `MatchError` (`loom/runtime/match-error`). | docs/spec_topics/expressions.md §`match` expression | conformance |
| REQ-EXPR-31 | — | All `match` arms must produce values of a common upper bound under `⊑` (narrowed by any sink on the `match` expression); a mismatch is `loom/parse/match-arm-type-mismatch`. | docs/spec_topics/expressions.md §`match` expression | offline-unit |
| REQ-EXPR-32 | ERR-18 | The operand of postfix `?` must have static type `Result<T, QueryError>` for some `T`; applying `?` to any other type is `loom/parse/question-on-non-result` (static, type-phase — the loom fails to load, no runtime disposition). | docs/spec_topics/expressions.md:201 | offline-unit |
| REQ-EXPR-33 | — | `?` unwraps `Ok` to the inner value and on `Err` early-returns the `Err` from the enclosing function/loom; the enclosing scope's return type must be compatible with `Result<U, QueryError>` else `loom/parse/question-outside-result-fn`; a scope using `?` implicitly returns `Result<T, QueryError>`. | docs/spec_topics/expressions.md §Error propagation | offline-unit |
| REQ-EXPR-34 | — | Schema-typed values are constructed `Schema { field: expr, ... }`; every declared field must be present (`loom/parse/missing-object-field`), extra fields are `loom/parse/extra-object-field`, field order is irrelevant. | docs/spec_topics/expressions.md §Object construction | offline-unit |
| REQ-EXPR-35 | — | A bare object literal (`{ field: expr }` with no schema name) is `loom/parse/bare-object-literal`, with exactly two carve-outs (frontmatter `params:` defaults; single positional Pi-tool call argument), both restricted to the literal sublanguage. | docs/spec_topics/expressions.md §Object construction | offline-unit |
| REQ-EXPR-36 | — | A discriminated-union value is constructed via the variant schema name (`Cat { ... }`), typed as the variant; assignment to a union-typed slot widens it. | docs/spec_topics/expressions.md §Object construction | offline-unit |
| REQ-EXPR-37 | — | `[]` element type is inferred from context; `[a, b, c]` element type is the common type of its elements; an array with no common type and no narrowing context is `loom/parse/array-no-common-type`. | docs/spec_topics/expressions.md §Array construction | offline-unit |
| REQ-EXPR-38 | — | Array/ternary common-type rule 1: with a sink in scope every element must satisfy `T_element ⊑ T_sinkElement`; a mismatch is `loom/parse/array-element-type-mismatch` naming the offending element. | docs/spec_topics/expressions.md §Array construction | offline-unit |
| REQ-EXPR-39 | — | Array/ternary common-type rule 2: with no sink, the parser computes the LUB under `⊑` — identical types collapse (TYPE-1), `integer` widens to `number` (TYPE-2), otherwise elements union via TYPE-5/TYPE-6 (`["a", null]`→`array<string \| null>`; `[1, "a"]`→`array<number \| string>`). | docs/spec_topics/expressions.md §Array construction | offline-unit |
| REQ-EXPR-40 | — | Array/ternary common-type rule 3: object schemas do not unify implicitly — two different named schemas yield `array<A \| B>` only under a union sink, else `loom/parse/array-no-common-type`. | docs/spec_topics/expressions.md §Array construction | offline-unit |
| REQ-EXPR-41 | — | `+` adds two numeric operands (widening to `number` if either is `number`, TYPE-2) or concatenates two `string` operands; mixed-type operands are `loom/parse/mixed-plus-operands`; `+` on `array<T>` is unsupported (use `concat`). | docs/spec_topics/expressions.md §`+` operator | offline-unit |
| REQ-EXPR-42 | — | `-`, `*`, `%` accept only numeric operands and produce `integer` when both are `integer`, widening to `number` when either is `number`; unary `-` preserves the operand's numeric type. | docs/spec_topics/expressions.md §Other arithmetic | conformance |
| REQ-EXPR-43 | — | `/` always produces `number` (no integer-division operator in loom 1.0). | docs/spec_topics/expressions.md §Other arithmetic | conformance |
| REQ-EXPR-44 | — | Division by zero produces IEEE-754 `Infinity`/`-Infinity`/`NaN` and does not panic; modulo by zero (`n % 0`) produces `NaN` and does not panic (an `integer % 0` result widens to `number`). | docs/spec_topics/expressions.md §Other arithmetic | conformance |
| REQ-EXPR-45 | — | An `integer`-typed result of binary `-`/`*`/`%` or unary `-` exceeding `2^53 - 1` is computed in IEEE-754 double, silently loses precision, does not panic, and retains the static `integer` type. | docs/spec_topics/expressions.md §Other arithmetic | conformance |
| REQ-EXPR-46 | — | `<`/`<=`/`>`/`>=` accept two numeric operands (with `integer ⊑ number` widening) or two `string` operands; any other pairing is `loom/parse/non-orderable-operands`. | docs/spec_topics/expressions.md §Ordering comparisons | offline-unit |
| REQ-EXPR-47 | — | Numeric operands order by signed IEEE-754 value; string operands order lexicographically by UTF-16 code unit (not code point or locale). | docs/spec_topics/expressions.md §Ordering comparisons | conformance |
| REQ-EXPR-48 | — | All four ordering operators on `NaN` evaluate to `false` and never panic (deliberately asymmetric with `NaN == NaN` being `true`). | docs/spec_topics/expressions.md §Ordering comparisons | conformance |

## AREA: CTRL (control-flow)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-CTRL-1 | — | `if`/`else` is statement form only (the ternary `cond ? a : b` is the expression form); bodies are braced blocks. | docs/spec_topics/control-flow.md §`if` / `else` | offline-unit |
| REQ-CTRL-2 | — | `for x in expr` iterates an array, binding the iteration variable as a fresh immutable local per iteration. | docs/spec_topics/control-flow.md §`for` ... `in` | conformance |
| REQ-CTRL-3 | — | The `for` iterand must have type `array<T>`; iterating strings/objects/numbers is `loom/parse/non-array-iterand`. | docs/spec_topics/control-flow.md §`for` ... `in` | offline-unit |
| REQ-CTRL-4 | — | The `for` iterand position is not an element-type sink; `for x in []` with no surrounding sink is `loom/parse/array-no-common-type`. | docs/spec_topics/control-flow.md §`for` ... `in` | offline-unit |
| REQ-CTRL-5 | CTRL-1 | The `for` iterand expression is evaluated exactly once at loop entry before the first iteration; any effect commits exactly once (including when the resulting array is empty), and reassigning a `let mut` inside the body does not change the snapshotted sequence. | docs/spec_topics/control-flow.md:15 | conformance |
| REQ-CTRL-6 | — | `while` repeats while its condition is `true`; the condition must be `boolean` (no coercion) — a non-boolean is `loom/parse/non-boolean-condition`. | docs/spec_topics/control-flow.md §`while` | offline-unit |
| REQ-CTRL-7 | — | `break`/`continue` are bare statements legal only inside `for`/`while` bodies; `break` outside a loop is `loom/parse/break-outside-loop`, `continue` outside a loop is `loom/parse/continue-outside-loop`. | docs/spec_topics/control-flow.md §`break` / `continue` | offline-unit |
| REQ-CTRL-8 | — | `break` exits the innermost enclosing loop; `continue` skips to the next iteration; neither carries a value — `break expr` is `loom/parse/break-with-value`. | docs/spec_topics/control-flow.md §`break` / `continue` | offline-unit |

## AREA: FUNC (functions)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-FUNC-1 | — | A function body's value is the value of its last expression (Rust-style); top-level loom files follow the same rule. | docs/spec_topics/functions.md §Function Definitions | conformance |
| REQ-FUNC-2 | FN-1 | `fn` declarations are top-level only in both `.loom` and `.warp` files; nested definitions are `loom/parse/nested-fn`. | docs/spec_topics/functions.md:20 | offline-unit |
| REQ-FUNC-3 | FN-1 | Functions are not first-class; a function name used as a value (bound to `let`, passed as an argument) is `loom/parse/function-as-value` — names appear only in call position. | docs/spec_topics/functions.md:20 | offline-unit |
| REQ-FUNC-4 | FN-1 | Mutual recursion between two top-level `fn`s is allowed (declarations hoisted within the file); a cross-file `.warp` `fn` call counts as a frame against the invoke-depth bound, an intra-file `fn` call does not. | docs/spec_topics/functions.md:20 | conformance |
| REQ-FUNC-5 | FN-2 | A `fn` accepts a leading `///` doc comment preserved on the AST as human-facing documentation only; it does not lower into provider payloads and the runtime does not consume it. | docs/spec_topics/functions.md:22 | offline-unit |
| REQ-FUNC-6 | FN-3 | A `.loom` file's / annotation-less `fn`'s return type is inferred by reconciling the tail-expression type (or implicit `null`) with every syntactically-present `return expr` operand (regardless of reachability) via the LUB under `⊑`; there is no frontmatter `returns:` field. | docs/spec_topics/functions.md:26 | offline-unit |
| REQ-FUNC-7 | FN-3 | The inferred type is wrapped `Result<T, QueryError>` when the body can short-circuit with `Err` (any `?` present, or any contributing operand is `Result`-typed): `Result` operands contribute their success payload `U`, non-`Result` operand `X` contributes `X`, `T` is the LUB of payloads, error arm is `QueryError`. | docs/spec_topics/functions.md:26 | offline-unit |
| REQ-FUNC-8 | FN-3 | Contributing types that share no common upper bound and that no sink narrows make the body `loom/parse/return-no-common-type` (an unreachable `return expr` still contributes). | docs/spec_topics/functions.md:26 | offline-unit |
| REQ-FUNC-9 | FN-3 | A `fn` with an explicit return annotation type-checks its tail and every `return` operand against that annotation instead of inferring. | docs/spec_topics/functions.md:30 | offline-unit |
| REQ-FUNC-10 | FN-4 | A function/loom body with no tail expression (last form is a statement, or literal-text-only) has inferred return type `null` (literal type) and produces literal `null`; absence of a tail does not imply `void`. | docs/spec_topics/functions.md:36 | offline-unit |
| REQ-FUNC-11 | FN-4 | A body with `?` whose last form is a statement infers `Result<null, QueryError>` (not `Result<void, QueryError>`); the implicit `null` is the success payload. | docs/spec_topics/functions.md:36 | offline-unit |
| REQ-FUNC-12 | FN-4 | A body whose last form is `let x = expr?` returns `Ok(null)` on the success path (the `let` is a statement, so the empty-tail rule applies regardless of `x`). | docs/spec_topics/functions.md §`?` interaction with empty tail | conformance |
| REQ-FUNC-13 | FN-5 | A loom/function's final value is the tail-expression value, the operand of a short-circuiting `return expr`, or literal `null` per the empty-tail rule; an `invoke` parent receives it as the `Ok` payload and a subagent-mode parent across the subagent boundary. | docs/spec_topics/functions.md:44 | conformance |
| REQ-FUNC-14 | FN-5 | On failure (`?` propagation, panic, exhausted ceiling) and on cancellation, no final value flows — the caller observes only the corresponding `Err` envelope. | docs/spec_topics/functions.md:44 | conformance |
| REQ-FUNC-15 | — | A function call participates in the loom's current conversation; it does not open a new one (opening a new isolated conversation requires subagent-mode `invoke`). | docs/spec_topics/functions.md §Function Definitions | conformance |
| REQ-FUNC-16 | — | A `void`-return function discards its last expression's value and is the way to drive turns without producing a value. | docs/spec_topics/functions.md §Function Definitions | conformance |

## AREA: RET (return)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-RET-1 | — | `return expr` exits the enclosing function/top-level loom immediately, producing `expr` as that scope's value; `return` is a statement, not an expression. | docs/spec_topics/return.md §Return Statement | conformance |
| REQ-RET-2 | RET-1 | `return expr` is type-checked against the enclosing scope's declared return type when present; when absent (top-level loom or annotation-less `fn`) its operand participates in inferred return type alongside the tail expression. | docs/spec_topics/return.md §Rules | offline-unit |
| REQ-RET-3 | RET-2 | Bare `return` (no argument) is legal only inside a `void`-annotated function; elsewhere — including at the top level of a `.loom` file — it is `loom/parse/bare-return-in-non-void`. | docs/spec_topics/return.md §Rules | offline-unit |
| REQ-RET-4 | — | From a top-level loom, `return expr` exits the loom with `expr` as its return value, exactly as a tail expression would. | docs/spec_topics/return.md §Rules | conformance |
| REQ-RET-5 | RET-3 | Code after a `return` in the same block is unreachable and produces `loom/parse/unreachable-code` (warning, not error). | docs/spec_topics/return.md §Rules | offline-unit |
| REQ-RET-6 | — | The `?` operator's `Err`-arm desugaring is literally `return Err(e)`. | docs/spec_topics/return.md §Rules | conformance |

## AREA: BIND (bindings)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-BIND-1 | — | `let x = ...` is immutable; rebinding it is `loom/parse/immutable-rebinding`. `let mut x = ...` opts into reassignment. | docs/spec_topics/bindings.md §Bindings and Mutability | offline-unit |
| REQ-BIND-2 | — | `let` requires an initialiser; `let x: T` (annotation, no initialiser) is `loom/parse/let-without-initialiser`. | docs/spec_topics/bindings.md §Bindings and Mutability | offline-unit |
| REQ-BIND-3 | — | Reassignment is a statement; the plain form and compound `+=`, `-=`, `*=`, `/=`, `%=` are legal on `let mut` bindings, and the RHS must be compatible with the binding's declared/inferred type per `⊑`. | docs/spec_topics/bindings.md §Reassignment | offline-unit |
| REQ-BIND-4 | — | Assignment in expression position (e.g. `if (x = 1)`) is `loom/parse/assignment-as-expression`. | docs/spec_topics/bindings.md §Reassignment | offline-unit |
| REQ-BIND-5 | — | Mutability is binding-level only; `obj.field = ...` and `arr[i] = ...` are `loom/parse/assignment-to-member-or-index` — update by rebinding the whole value. | docs/spec_topics/bindings.md §Mutability is binding-level only | offline-unit |
| REQ-BIND-6 | — | Function parameters, `for` iteration variables, `match` pattern bindings, and the discard `let _` are always immutable; `mut` on any of them is `loom/parse/mut-on-immutable-context`, and `let mut _` is `loom/parse/mut-on-discard`. | docs/spec_topics/bindings.md §Immutable contexts | offline-unit |
| REQ-BIND-7 | — | `++` and `--` are `loom/parse/increment-decrement` (use `count += 1` / `count -= 1`). | docs/spec_topics/bindings.md §Increment / decrement | offline-unit |

## AREA: TYPE (type-system)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-TYPE-1 | — | Type expressions are built from primitives (`string`/`number`/`integer`/`boolean`/`null`), named types (schema/enum in scope), generics (`array<T>`, `Result<T,E>`), unions (`T \| U`, lowest-precedence), literal types, and inline anonymous objects; inline arrays are not a separate form (`array<T>` only, no `T[]`/`[T]`). | docs/spec_topics/type-system.md §Type System | offline-unit |
| REQ-TYPE-2 | — | `void` is not a value type; it is admitted only as a function/loom-return annotation, any other type position is `loom/parse/void-in-non-return-position`, and it does not participate in type compatibility. | docs/spec_topics/type-system.md §Type System | offline-unit |
| REQ-TYPE-3 | — | The single relation `T₁ ⊑ T₂` governs every compatibility site (typed `let` RHS, function-argument slot, `invoke<T>` return annotation, `match`/ternary common type, array element vs sink, `+` mixed-numeric, `params:` default). | docs/spec_topics/type-system.md §Type compatibility | offline-unit |
| REQ-TYPE-4 | — | When `T₁ ⊑ T₂` holds every `T₁`-typed value AJV-validates against the lowering of `T₂`; AJV validation is necessary but not sufficient, and the parser recognises the structural cases without falling back to AJV, admitting fewer pairs (parser rejection is authoritative). | docs/spec_topics/type-system.md §Type compatibility | offline-unit |
| REQ-TYPE-5 | TYPE-1 | Reflexivity: `T ⊑ T` for every type (identical primitives, named schemas, inline objects). | docs/spec_topics/type-system.md:35 (§type-1) | offline-unit |
| REQ-TYPE-6 | TYPE-2 | `integer ⊑ number`, one-way only; the reverse is `loom/parse/integer-narrowing`. | docs/spec_topics/type-system.md:36 (§type-2) | offline-unit |
| REQ-TYPE-7 | TYPE-3 | Literal-to-primitive: `L ⊑ T` when the value `L` would be statically typed `T` (`"validation" ⊑ string`, `42 ⊑ integer`, `42 ⊑ number`, `true ⊑ boolean`, `null ⊑ null`). | docs/spec_topics/type-system.md:37 (§type-3) | offline-unit |
| REQ-TYPE-8 | TYPE-4 | Variant-to-union: for `schema U = A \| B \| ...`, every variant satisfies `A ⊑ U` (`Cat ⊑ Animal`). | docs/spec_topics/type-system.md:38 (§type-4) | offline-unit |
| REQ-TYPE-9 | TYPE-5 | Union-widening: `T ⊑ T \| U` for any `U` (combined with TYPE-4, `A ⊑ A \| B` even for anonymous unions). | docs/spec_topics/type-system.md:39 (§type-5) | offline-unit |
| REQ-TYPE-10 | TYPE-6 | Union-distributive: `T₁ \| T₂ ⊑ T₃` iff `T₁ ⊑ T₃` and `T₂ ⊑ T₃` (each arm individually). | docs/spec_topics/type-system.md:40 (§type-6) | offline-unit |
| REQ-TYPE-11 | TYPE-7 | Arrays are covariant: `array<T₁> ⊑ array<T₂>` iff `T₁ ⊑ T₂` (`array<Cat> ⊑ array<Animal>`). | docs/spec_topics/type-system.md:41 (§type-7) | offline-unit |
| REQ-TYPE-12 | TYPE-8 | Inline object types are field-wise: `{ f₁:T₁, ... } ⊑ { f₁:U₁, ... }` iff the declared field sets match exactly and `Tᵢ ⊑ Uᵢ`; excess properties never widen (`additionalProperties: false`), order irrelevant, never crossing the inline/named boundary. | docs/spec_topics/type-system.md:42 (§type-8) | offline-unit |
| REQ-TYPE-13 | — | Function-parameter contravariance, optional-field widening, excess-property tolerance, and `number ⊑ integer` are all rejected in loom 1.0. | docs/spec_topics/type-system.md §Type compatibility | offline-unit |
| REQ-TYPE-14 | TYPE-9 | On a static `⊑` failure with both operands resolvable: a typed `let x: T = expr` reports `loom/parse/let-rhs-type-mismatch`; a plain top-level `fn` argument reports `loom/parse/fn-arg-type-mismatch`; a ternary reports through array/ternary machinery (`loom/parse/array-element-type-mismatch` against a sink, else `loom/parse/array-no-common-type`). | docs/spec_topics/type-system.md:50 (§type-9) | offline-unit |
| REQ-TYPE-15 | TYPE-10 | Object-schema named types are nominal — participate in `⊑` only via TYPE-1/TYPE-4/TYPE-5/TYPE-6; a named-schema value is not `⊑` an inline object of identical shape (and vice versa), and two distinct named schemas with byte-identical lowered fragments are incompatible; mismatches surface at parse time, not at runtime AJV. | docs/spec_topics/type-system.md:52 (§type-10) | offline-unit |
| REQ-TYPE-16 | TYPE-11 | Alias-schema transparency — a `NamedType` declared `schema X = R` is replaced by `R` on whichever side it appears and the check re-evaluated (recursing through nested aliases); identified solely by the `=` form; aliasing an object schema unfolds to that schema under TYPE-10; alias cycles are `loom/parse/type-alias-cycle`. | docs/spec_topics/type-system.md:54 (§type-11) | offline-unit |
| REQ-TYPE-17 | — | When either side of a compatibility check is past the parser's static view (inferred binding depending on a parse-time-invisible Pi-tool schema, or an `invoke` against a `loom/load/callee-has-errors` callee), the parse-time check is skipped and the runtime AJV check is the safety net. | docs/spec_topics/type-system.md §Unresolvable operands | offline-unit |
| REQ-TYPE-18 | — | The same type grammar applies in every annotation position (schema fields, `params:`, `let x: T`, function parameters, `@<T>`...`` query schemas); the return position additionally admits `void`. | docs/spec_topics/type-system.md §Type System | offline-unit |

## AREA: RVM (runtime-value-model)
| Campaign ID | Spec tag | Normative statement (testable) | Citation | Testability |
|---|---|---|---|---|
| REQ-RVM-1 | — | Loom values map to JS: `string`→JS string; `number`/`integer`→JS number (same value at runtime); `boolean`→JS boolean; `null`→JS null; `array<T>`→JS Array (recursive); object schema→JS plain object keyed by loom-side names regardless of wire renames. | docs/spec_topics/runtime-value-model.md §Runtime Value Model | conformance |
| REQ-RVM-2 | — | An enum value carries the variant's wire string plus an interpreter-private declaring-enum tag; the tag must not appear in JSON output (`JSON.stringify` of an enum value yields the bare wire string). | docs/spec_topics/runtime-value-model.md §Runtime Value Model | conformance |
| REQ-RVM-3 | — | `Result<T,E>` is internally tagged (`Ok`/`Err` + payload), observed only via constructors/`match`/`?`, is not a lowerable type form (`loom/parse/result-in-schema-position`), and never crosses the wire. | docs/spec_topics/runtime-value-model.md §Runtime Value Model | conformance |
| REQ-RVM-4 | — | `==` is structural deep equality; `==`/`!=` accept operands of any two static types, and when neither operand's static type is `⊑` the other the comparison loads and evaluates to `false` (`==`)/`true` (`!=`), never panicking or failing to parse. | docs/spec_topics/runtime-value-model.md §Equality | conformance |
| REQ-RVM-5 | — | Primitives compare by value with `NaN == NaN` true and `+0 == -0` true; arrays compare element-wise at the same indices with equal length required; objects compare loom-side key set and per-key value equality (declaration order irrelevant). | docs/spec_topics/runtime-value-model.md §Equality | conformance |
| REQ-RVM-6 | — | Enum variants compare the declaring-enum tag and the wire value (`Severity.High == OtherEnum.High` is `false` even when wire values match); `Result` compares the `Ok`/`Err` discriminator and recurses on the payload. | docs/spec_topics/runtime-value-model.md §Equality | conformance |
| REQ-RVM-7 | — | `42 == 42.0` is `true` because `integer ⊑ number` (TYPE-2) routes the pair to per-shape value comparison. | docs/spec_topics/runtime-value-model.md §Equality | conformance |
| REQ-RVM-8 | — | Wire-name translation happens inbound (model output → loom value: after AJV validation, rebuild with loom-side names via each schema's translation map and reattach declaring-enum tags at sidecar-recorded named-enum positions) and outbound (loom value → JSON: walk and produce wire-named JSON before AJV validation), and nowhere else. | docs/spec_topics/runtime-value-model.md §Wire-name translation | conformance |
| REQ-RVM-9 | — | Anonymous string-literal-union positions receive no enum tag inbound, so cross-form comparisons such as `Severity.Low == "low"` remain `false`. | docs/spec_topics/runtime-value-model.md §Wire-name translation | conformance |
| REQ-RVM-10 | — | Frontmatter `params:` defaults bypass the inbound translation pass — parsed as ordinary Loom values, they arrive already branded and loom-side-named, indistinguishable from body-code values. | docs/spec_topics/runtime-value-model.md §Wire-name translation | conformance |
| REQ-RVM-11 | — | Loom code never sees wire names; tools, the model, and external JSON Schema consumers never see loom-side names. | docs/spec_topics/runtime-value-model.md §Wire-name translation | conformance |
| REQ-RVM-12 | — | The runtime targets Node exclusively (`process.versions.node >= 22.19.0`) and assumes IEEE-754 numbers, native `Map`/`Set`, and native `JSON.stringify` as a non-checked invariant — no feature-detect, no polyfill, no diagnostic on violation. | docs/spec_topics/runtime-value-model.md §JavaScript engine assumptions | manual/inspection |
| REQ-RVM-13 | — | The Loom language has no file-writing, network, or process-spawning primitive; every external effect flows through a query, a tool call, or a child loom invocation, bounded by the loom's `tools:` allowlist. | docs/spec_topics/runtime-value-model.md §Effects | manual/inspection |

## DEFERRED / out-of-scope (Cluster 1)
- Hex / octal / binary number forms and underscore separators are not in loom 1.0 (surface as `loom/parse/unsupported-feature`). — docs/spec_topics/lexical.md §Number literals
- No integer-division operator in loom 1.0 (`/` always produces `number`); deferred. — docs/spec_topics/expressions.md §Other arithmetic
- No user-defined methods and no `this`; additional built-in methods may be added non-breakingly later. — docs/spec_topics/expressions.md §Built-in methods and properties
- loom 1.0 exposes no `charAt`/`codePointAt`; strings are decomposed via `split(...)`. — docs/spec_topics/expressions.md §Built-in methods and properties
- `match` guards (`Ok(x) if ...`) and rest patterns (`[first, ...rest]`, `{ k, ...other }`) are not in loom 1.0 (`loom/parse/match-guard-not-supported`, `loom/parse/rest-pattern-not-supported`). — docs/spec_topics/expressions.md §Pattern grammar
- `match` exhaustiveness is not statically checked in loom 1.0 (would be unsound); non-coverage is a runtime `MatchError`. — docs/spec_topics/expressions.md §`match` expression
- Arrow functions / higher-order methods (`map`/`filter`/`reduce`), spread/rest, optional chaining/nullish coalescing, strict equality, bitwise operators, `++`/`--`, comma operator, nested template strings — all unsupported in loom 1.0. — docs/spec_topics/expressions.md §Not supported
- Closures and first-class function values are not part of loom 1.0 (function names appear only in call position). — docs/spec_topics/functions.md:20 (FN-1)
- Function parameters are immutable — a deliberate loom 1.0 simplification (mutable params deferred). — docs/spec_topics/bindings.md §Immutable contexts
- `break expr` (value-carrying break) is not in loom 1.0 (`loom/parse/break-with-value`). — docs/spec_topics/control-flow.md §`break` / `continue`
- `fn` parameters carry no defaults in loom 1.0 (literal-valued defaults only on `params:` frontmatter fields). — docs/spec_topics/grammar.md §`fn` declarations
- A future widening of the `⊑` relation (contravariance, optional-field widening, excess-property tolerance, `number ⊑ integer`) is explicitly excluded from loom 1.0; non-breaking only if it admits new pairs. — docs/spec_topics/type-system.md §Type compatibility
- Bun, Deno, browser embeds, and other non-Node JavaScript hosts are out of loom 1.0 scope; adding a non-Node host is a probe re-design deferred. — docs/spec_topics/runtime-value-model.md §JavaScript engine assumptions
- If a future host-interop surface exposes a live `Result`/enum value to JS, the runtime value-model section must be revisited before that surface ships. — docs/spec_topics/runtime-value-model.md §Reference encoding (non-normative)
