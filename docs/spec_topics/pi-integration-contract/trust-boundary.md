# Trust boundary

<a id="no-extra-mediation"></a>

<a id="pic-52"></a> **PIC-52. No additional access channels.** The runtime interposes no privilege layer between theta code and the Pi extension host. Theta-side filesystem, network, and Pi-API access are bounded only by Pi's extension-host grants and by the per-theta `tools:` allowlist, whose active-set enforcement is specified by [Tool registration lifetime — Active-set allowlist gating (PIC-17)](./tool-registration-lifetime.md#pic-17). The runtime MUST NOT introduce additional access channels (sandbox, capability filter, mediated proxy) in any theta 1.x release; widening or narrowing this rule is a major-version concern. The `tools:` allowlist constrains the *model's* reachable callable set, not the host process: a theta that declares a high-privilege callable (e.g. `bash`) exposes the full underlying capability of that callable to its model. Host-side denials of filesystem, network, or Pi-API access reach theta code through the tool that issued the request: a thrown or `isError: true` return is mapped to `Err(QueryError { kind: "code_tool", cause: "execution", ... })` per [Tool Calls — Failures](../tool-calls.md) and [Tool execution from theta code](./host-interfaces-core.md#tool-execution-from-theta-code); silent success on denial is forbidden.
