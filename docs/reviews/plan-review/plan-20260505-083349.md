# Consolidated Review — C:/UnitySrc/pi-loom/plan.md

_Generated: 2026-05-05T08:33:49Z_
_Lenses run: 16_
_Findings: 128 (deduplicated from ~184 raw findings across 16 lenses; 1 cross-reference heading omitted from count)_

Spec context: `spec.md` + `spec_topics/*.md`
Plan: `plan.md` + `plan_topics/*.md`

---

## plan_topics/conventions.md

### "documented spec reason" undefined
- **Description:** The rule "No `Promise.all`/`Promise.race` outside slices that have a documented spec reason" has two readings: (a) the spec page itself contains an explicit normative statement, or (b) the leaf's plan entry justifies it in prose. Neither is named as canonical, and no slices are pre-blessed.
- **Suggested fix:** Replace with: "No `Promise.all`/`Promise.race` unless the calling leaf cites a spec REQ-ID that mandates concurrency in its `Spec.` field and lists the citation in `Adds.`"
- **Lenses:** spec-lens-clarity

### "speculative APIs" undefined
- **Description:** "No speculative APIs" in the per-phase TDD ritual step 2 is a weasel qualifier. Any API not exercised by the current leaf's tests could qualify.
- **Suggested fix:** Drop the sentence (the "minimum code" rule subsumes it) or define operationally: "every exported symbol must be referenced by at least one test in this leaf or a downstream leaf already merged."
- **Lenses:** spec-lens-clarity

### Exception-handling convention weaker than CLAUDE.md
- **Description:** `conventions.md` permits `catch (Error)` with rethrow-on-mismatch. `CLAUDE.md` is an absolute prohibition: "Never `catch(...)` or `catch(std::exception&)`. Catch specific types or let crash." The H1 ESLint rule will enforce the weaker standard.
- **Suggested fix:** Tighten the plan's convention to match CLAUDE.md: no broad catches, specific subtypes only. Alternatively document the exception explicitly with a cross-reference to the CLAUDE.md parent rule.
- **Lenses:** spec-lens-doc-alignment-broad

### Ambiguous group-level leaf IDs in Deps fields
- **Description:** Many Deps fields use group-level IDs (`V1`, `V2`, `V3`, etc.) rather than specific leaf IDs (e.g. `V1e`, `V2i`). Conventions.md does not define whether a group ID means "all leaves complete" or "last leaf complete." An implementer cannot determine exact prerequisites.
- **Suggested fix:** Either define in `conventions.md` that `VN` means all leaves in that group are complete, or replace each group-level Dep with the specific leaf IDs required.
- **Lenses:** spec-lens-traceability

### CHANGELOG.md / notes.md creation violates CLAUDE.md
- **Description:** `conventions.md` mandates updating `CHANGELOG.md` and appending to `notes.md` after every leaf. Neither file exists in the repository. Creating them is proactive documentation-file creation, which `CLAUDE.md` explicitly prohibits ("NEVER proactively create documentation/README files").
- **Suggested fix:** (a) Remove the `CHANGELOG.md`/`notes.md` requirements and route notes into the existing `README.md` status table, (b) create both files now in a bootstrap commit explicitly justified as necessary, or (c) obtain a project-level CLAUDE.md override.
- **Lenses:** spec-lens-doc-alignment-broad

### REQ-ID system referenced everywhere but no leaf creates it
- **Description:** Conventions require Tests bullets of the form `PREFIX-N: <description>`. The coverage matrix preamble says rows will be re-pivoted to per-REQ-ID granularity. V18o defines a CI gate diffing REQ-IDs from spec pages against the matrix. However: (a) zero `PREFIX-N` markers exist anywhere in `spec_topics/`; (b) no leaf cites a REQ-ID in its Tests bullets; (c) there is no "Phase 12b" leaf anywhere in `plan.md`; (d) the V18o CI gate therefore produces an empty diff trivially and passes vacuously.
- **Suggested fix:** Add an explicit leaf (e.g. `H5` or `V18p`) that owns walking each spec page from the prefix table and inserting `PREFIX-N` anchors, then re-pivoting `coverage-matrix.md` from section rows to REQ-ID rows, and backfilling Tests-bullet citations in all existing leaves. Block V18o's gate on this leaf.
- **Lenses:** spec-lens-traceability, spec-lens-consistency, spec-lens-implementability, spec-lens-assumptions, spec-lens-codebase-grounding-broad, plan-lens-validation

### "V1 reference implementation" identifier collision
- **Description:** Two leaves (V6i, V12a) use the parenthetical "(V1 reference implementation)" in their Spec fields to refer to the spec's initial language-version mechanism, which collides with the plan phase identifier "V1 — Lexer hardening."
- **Suggested fix:** Replace with "(initial typed-query mechanism — not plan phase V1)" or "(first-iteration mechanism; see `IMPL-N` once REQ-IDs are assigned)."
- **Lenses:** spec-lens-traceability

---

## plan_topics/coverage-matrix.md

### "Phase 12b" stale reference and embedded decision-log note
- **Description:** The preamble sentence "The current rows below are section-level scaffolding that pre-dates the REQ-ID assignment pass; rows below will be re-pivoted to per-REQ-ID granularity as Phase 12b assigns REQ-IDs page-by-page" contains two problems: (1) "Phase 12b" does not exist in the plan; (2) the sentence is a historical decision-log entry, not a requirement. No row in the matrix carries any REQ-ID today.
- **Suggested fix:** Delete the full sentence. The outstanding work belongs in a proper leaf (see conventions.md — "REQ-ID system" finding above).
- **Lenses:** spec-lens-cruft, spec-lens-traceability, spec-lens-assumptions

### V3c missing from `Invocation from Pi` coverage row
- **Description:** The matrix row `[Invocation from Pi]` maps to `V16a–V16p, V18i`. V3c's `Spec.` field explicitly cites `slash-invocation.md` for the no-params overflow rule, and M ships a partial version. The closing-leaf-per-spec-rule contract is not satisfied for this rule.
- **Suggested fix:** Add `V3c` (and `M`) to the `Invocation from Pi` row, or add a dedicated sub-row for "No-params overflow → V3c."
- **Lenses:** spec-lens-consistency

### V18n missing from `Invocation` coverage row
- **Description:** V18n implements `loom/runtime/invoke-depth` per `invocation.md` (the depth bound is an Invocation-spec concept) but appears only in the `Errors and Results — runtime panics` row, not the `Invocation` row. REQ-IDs under `INV` for the depth bound would be unmapped.
- **Suggested fix:** Either extend the `Invocation` row to include `V18n`, or add a dedicated sub-row `[Invocation — Invocation depth bound] | V18n`.
- **Lenses:** spec-lens-consistency

### `SlashCommandSource = "subagent"` does not exist in Pi
- **Description:** The spec (`pi-integration-contract.md`) and plan (V14q) include `"subagent"` in the list of Pi-owned slash-command sources to check for cross-format collisions. The installed Pi SDK declares `type SlashCommandSource = "extension" | "prompt" | "skill"` — no `"subagent"` arm. Code checking against this source can never fire; tests synthesising a Pi-owned `.md` subagent command have no Pi API to back them.
- **Suggested fix:** Drop `"subagent"` from the source-collision list in spec and plan. The relevant collision sources per the installed Pi SDK are `"extension" | "prompt" | "skill"`. Move Pi-owned subagents to `future-considerations.md` as a deferred capability.
- **Lenses:** spec-lens-codebase-grounding-broad

### Closed diagnostic registry — many codes have no asserting plan leaf
- **Description:** V18o's CI gate covers REQ-IDs only. The diagnostics spec marks the code registry as closed and normative, but a grep over `plan_topics/*.md` returns zero mentions of: `loom/parse/invalid-path-separator`, `loom/parse/integer-narrowing`, `loom/parse/illegal-escape`, `loom/parse/duplicate-discriminator-value`, `loom/load/case-collision`, `loom/load/invalid-slash-name`, `loom/load/manifest-invalid`, `loom/load/manifest-escapes-package`, `loom/load/unreadable`, `loom/load/unreadable-source`, `loom/load/wrong-type-source`, `loom/load/settings-unreadable`, `loom/load/settings-invalid-json`, `loom/runtime/system-note-delivery-failed`. An unimplemented or wrongly-coded diagnostic is not caught by the existing gate.
- **Suggested fix:** Add a parallel closing CI check to V18o that diffs the registry table in `diagnostics.md` against the set of code-string literals in test files; any unasserted code fails the gate. Distribute Tests bullets for each code to the leaves that own its diagnostic site.
- **Lenses:** plan-lens-validation, plan-lens-spec-coverage

---

## plan_topics/h1-scaffold.md

### `depcheck` gate not set up or self-tested
- **Description:** `depcheck` appears in the `Ships when` condition ("depcheck clean") but is not listed in `Adds` and has no Tests bullet. A repository that never installs `depcheck` satisfies the gate literally while the binary's absence is not a failure mode anyone observes.
- **Suggested fix:** Add `depcheck` to `Adds` as a dev-dep + npm script; add a Tests bullet asserting `npm run depcheck` exits non-zero on a fixture with an unused dependency; include `npm run depcheck` in the Ships-when command.
- **Lenses:** plan-lens-validation

### GitHub Actions workflow added but never validated
- **Description:** H1 adds a GitHub Actions workflow file but no Tests bullet covers syntactic validity, the set of required jobs, or CI execution.
- **Suggested fix:** Add a Tests bullet that parses the workflow YAML and asserts presence of `typecheck`, `lint`, `test`, and `depcheck` jobs; add `actionlint`/`yamllint` invocation in the Ships-when gate.
- **Lenses:** plan-lens-validation

### `no-static-state.test.ts` allow-list undefined
- **Description:** The grep pattern (`^let `, `^var `, `^const ` at module top-level) has three undefined boundaries: (a) "literals" — does `const arr = []` count? `const re = /x/`?; (b) "frozen objects" — does this require lexical `Object.freeze({...})` or recursive freezing?; (c) `^export const` is not mentioned.
- **Suggested fix:** Enumerate the permitted RHS forms exhaustively and state explicitly whether `export const` and TS `as const` are checked.
- **Lenses:** spec-lens-clarity

### H1 missing mandatory Spec field
- **Description:** H1 has no `Spec.` field at all, which is mandatory per `conventions.md` ("**Spec.** Page(s) under `../spec_topics/` the leaf implements").
- **Suggested fix:** Add `**Spec.** (none — infrastructure leaf; no normative spec page)` to make the absence explicit and searchable.
- **Lenses:** spec-lens-traceability

---

## plan_topics/h2-di-skeleton.md

### `Clock` and `RandomSource` DI seams have no spec basis
- **Description:** H2 introduces, fakes, and asserts negative-path tests for `Clock` and `RandomSource`. Neither appears in any `spec_topics/` page. The spec runtime never reads wall-clock time (diagnostics carry `range`, not timestamps) and never generates random values. H2 also commits to test surface for fakes no later leaf consumes.
- **Suggested fix:** Drop both interfaces and their fakes. If a future leaf surfaces an actual spec-driven need, add the seam in that leaf with a spec citation.
- **Lenses:** plan-lens-spec-fidelity, spec-lens-assumptions

### H2 names ten DI seams but specifies zero method signatures
- **Description:** H2 lists all collaborators but declares no TypeScript interface signatures. Downstream leaves use methods by name (`ConversationDriver.send`, `SchemaValidator.compile`, `FileSystem.readText`) without those signatures being defined anywhere. Separate agents will guess incompatible signatures.
- **Suggested fix:** Declare each interface's TypeScript signature in full (fields and return types) in H2, or cite a spec section that already declares it.
- **Lenses:** spec-lens-implementability

### `AgentSession` seam missing from H2 and H4
- **Description:** V12a spawns in-process `AgentSession` (against `FakeAgentSession` in tests), and V18a–V18n test against `AgentSession.dispose()` semantics. `AgentSession` does not appear in H2's seam list or H4's adapter list. No scheduled leaf produces the `AgentSession` seam or `FakeAgentSession` fake.
- **Suggested fix:** Add `AgentSession` (or its abstraction) to H2's seam list, or schedule an explicit leaf in V12 introducing the seam + fake before V12a. Update dependency edges for V18a–V18e.
- **Lenses:** spec-lens-implementability

### H2 missing mandatory Spec field
- **Description:** H2 has no `Spec.` field. See H1 note.
- **Suggested fix:** Add `**Spec.** (none — infrastructure leaf; no normative spec page)`.
- **Lenses:** spec-lens-traceability

---

## plan_topics/h3-diagnostics.md

### "lint rule forbids `throw new Error`" has no asserting test
- **Description:** The Ships-when criterion ("All later phases emit through `DiagnosticsSink` exclusively (lint rule forbids `throw new Error` for spec-defined diagnostics)") is unobservable: no Tests bullet asserts the lint rule fires on a violating fixture, is enabled in ESLint config, or does not over-trigger on legitimate `throw` sites.
- **Suggested fix:** Add Tests bullets: (a) ESLint over a fixture with `throw new Error("loom/parse/foo")` produces the rule's error code; (b) the sanctioned `DiagnosticsSink.report(...)` call does not trip it; (c) the rule is in `.eslintrc` at `error` severity.
- **Lenses:** plan-lens-validation

### H3 plans a serialiser to a Pi shape the spec says is unused
- **Description:** H3 Adds includes "serialiser to Pi's flat `{ path, error }` shape." The spec (`diagnostics.md`) explicitly states: "Pi's own `LoadExtensionsResult.errors` field is **not** used … A failure there is a bootstrap failure … orthogonal to the diagnostics defined here." Every loom diagnostic routes through `pi.sendMessage({ customType: "loom-system-note", details: { diagnostics: Diagnostic[] } })`.
- **Suggested fix:** Replace "serialiser to Pi's flat `{ path, error }` shape" with "serialiser to the spec's `<file>:<line>:<col>: <code>: <message>` line format and to the structured `details: { diagnostics: Diagnostic[] }` payload."
- **Lenses:** spec-lens-codebase-grounding-broad

### H3 omits the `loom/lex/*` namespace
- **Description:** H3 enumerates typed code-namespace constants `loom/parse/*`, `loom/type/*`, `loom/load/*`, `loom/runtime/*` but omits `loom/lex/*`, which the spec's diagnostics registry tags with a `lex` phase. Downstream V1 lexer leaves cannot route a `lex`-phase code if one is later promoted.
- **Suggested fix:** Either add `loom/lex/*` to H3's namespace constants and define its routing, or explicitly state that all lex-time codes use the `loom/parse/*` prefix (and verify no `loom/lex/*` literal appears in any registry row).
- **Lenses:** spec-lens-assumptions

### "Severity round-trips" underspecified
- **Description:** The Tests bullet "Severity round-trips" does not name the boundary: through the `DiagnosticsAccumulator → Pi {path, error}` serialiser? JSON serialisation? A different channel?
- **Suggested fix:** Specify explicitly: "Diagnostic severity survives the `DiagnosticsAccumulator → pi.sendMessage({ customType: 'loom-system-note', details: { diagnostics } })` round trip for each defined severity level."
- **Lenses:** spec-lens-clarity

---

## plan_topics/h4-extension-shell.md

### H4 missing mandatory Spec field
- **Description:** H4 has no `Spec.` field but implements normative obligations from `pi-integration-contract.md` (tool-registration cache, `withActiveTools` snapshot/restore). Those obligations are invisible to the V18o CI gate.
- **Suggested fix:** Add a `**Spec.**` field listing `pi-integration-contract.md` sections; add corresponding rows to `coverage-matrix.md`.
- **Lenses:** spec-lens-traceability, spec-lens-placement

### H4 Ships-when uses undocumented `pi -e <dir>` invocation
- **Description:** The Ships-when criterion `pi -e C:\UnitySrc\pi-loom` passes a project root directory to `-e`. Pi's documented `--extension` flag takes a TypeScript file path (`pi -e ./my-extension.ts`). Passing a directory is not documented in any Pi code path; contributors will see "extension not found / not a TypeScript file."
- **Suggested fix:** Replace with `pi -e C:\UnitySrc\pi-loom\extensions\index.ts` (file path, matching documented usage), or use `pi install file:C:\UnitySrc\pi-loom` (the package-installation flow for local packages).
- **Lenses:** spec-lens-codebase-grounding-broad

### `docs/manual-smoke.md` does not exist and its creation violates CLAUDE.md
- **Description:** H4 Ships-when gates on "manual smoke recorded in `docs/manual-smoke.md`," but the file does not exist and is not listed in `Adds`. Creating it is proactive documentation-file creation, which CLAUDE.md prohibits. H4 also provides no Tests bullet for its reproduction.
- **Suggested fix:** Either append the smoke record to `README.md` under a `## Manual smoke log` heading without creating a new file, or add `docs/manual-smoke.md` to `Adds` with a justification, or replace the Ships-when with a reproducible criterion (scripted integration test).
- **Lenses:** plan-lens-validation, spec-lens-codebase-grounding-broad, spec-lens-doc-alignment-broad

### H4 "no stale closure after session reload" contradicts Pi's reload lifecycle
- **Description:** H4 Tests asserts "`PiToolHost` … accessor returns the most recent `ctx` even after a session reload (no stale closure)." Pi's documented lifecycle: `ctx.reload()` re-runs the extension factory as a new instance; the old `PiToolHost` is torn down. There is no continuous `PiToolHost` across a Pi reload; the property is unobservable on the same instance. The spec itself warns that a captured `pi`/`ctx` is invalidated on session replacement.
- **Suggested fix:** Restate the test to cover only within a single extension-instance lifetime: "accessor reflects the most recent slash-handler `ctx`; on `session_shutdown` the host invalidates its retained `ctx`." Remove the cross-reload claim.
- **Lenses:** spec-lens-codebase-grounding-broad, spec-lens-assumptions, spec-lens-clarity

### H4 "no-logic shims" claim contradicts registration cache and `withActiveTools`
- **Description:** H4 describes `PiModelClient`, `PiToolHost`, etc. as "no logic" shims, then immediately specifies the tool-registration cache (`Map<schema-hash, registeredToolName>`) and `withActiveTools(set, fn)` snapshot/restore helper — both of which carry load-bearing semantics (canonical schema-hash keying, `try/finally` on success/Err/panic/cancel, extension-scoped lifetime).
- **Suggested fix:** Either move the cache + `withActiveTools` into a dedicated leaf with a `Spec.` field pointing at the Pi Integration Contract "Tool-registration lifetime and visibility" subsection, or expand H4's `Spec.` and `Tests.` to cover those constraints explicitly.
- **Lenses:** spec-lens-assumptions

### H4 "typed accessor" for `ExtensionCommandContext` has no signature
- **Description:** H4 Tests bullet says "`PiToolHost` exposes the retained `ExtensionCommandContext` to V14c via a typed accessor" without declaring the accessor's name, signature, or refresh protocol. V14c then consumes this accessor when synthesising tool-call `ctx` arguments.
- **Suggested fix:** Specify the accessor signature in H4 (or in H2's `ToolHost`/`ExtensionAPI` interface declaration) so V14c has a fixed target.
- **Lenses:** spec-lens-implementability

### Tool-registration dedup assumes no schema-hash collision
- **Description:** The registration cache is content-addressed by hash; hash equality is treated as schema equality. A collision silently aliases two distinct tool schemas to the same `registeredToolName`, presenting the wrong schema to the LLM.
- **Suggested fix:** Specify the hash function in H4 and add a Tests bullet: on hash equality, also assert structural equality of the lowered parameters; on mismatch, refuse the dedup and emit `loom/runtime/registration-cache-collision`.
- **Lenses:** plan-lens-risk

---

## plan_topics/m-mvp.md

### M too large — five distinct concerns in one leaf
- **Description:** M bundles: (1) a new mini lexer + parser, (2) runtime execution, (3) slash command registration, (4) discovery (two roots), and (5) no-params overflow system-note behaviour. These are independently implementable and testable. Any single failure blocks the entire leaf.
- **Suggested fix:** Split into `Ma` (lexer + parser for minimal loom) and `Mb` (runtime + slash command registration + discovery + overflow note).
- **Lenses:** plan-lens-step-atomicity

### V5e: `agent_end` global listener instead of `ctx.waitForIdle()`
- **Description:** Both M and V5e describe prompt-mode query completion via an `agent_end` event listener. The spec (`pi-integration-contract.md`) mandates `await ctx.waitForIdle()` as the authoritative completion signal and explicitly prohibits `pi.on("agent_end", …)`: "that event fires for every `AgentSession` in the process … so a global handler cross-fires across concurrent looms or sibling subagents and resolves the wait on the wrong turn." V5e's Tests also assert "agent_end listener cleaned up after each query."
- **Suggested fix:** Rewrite M and V5e so the prompt-mode driver awaits `ctx.waitForIdle()`. Add an architectural test asserting no code path subscribes to `pi.on("agent_end", …)`. If `agent_end` is used only for cancellation forwarding, state that scope explicitly.
- **Lenses:** plan-lens-spec-coverage, spec-lens-assumptions, spec-lens-doc-alignment-broad, spec-lens-clarity

### V5e: `ctx.sendUserMessage()` — method does not exist on `ExtensionCommandContext`
- **Description:** V5e Adds says the driver "issues `ctx.sendUserMessage(text)`." The spec is explicit: "`sendUserMessage` is **not** a method on `ExtensionCommandContext`, so the per-handler `ctx` is consulted only for `ctx.isIdle()`." The method lives on the factory-captured `pi: ExtensionAPI`.
- **Suggested fix:** Correct V5e Adds to "`pi.sendUserMessage(text)` (using the factory-captured `pi` ExtensionAPI reference)." Add a test asserting that `ctx.isIdle()` is the only `ctx` method the driver calls (not `sendUserMessage`).
- **Lenses:** spec-lens-doc-alignment-broad

### M requires `loom-system-note` channel that V18h introduces
- **Description:** M tests assert spec-verbatim system-note text for the no-params overflow note and for "AbortError surfaces as a system note." The `loom-system-note` custom type + renderer + delivery-failure fallback all land in V18h. The format of an "AbortError" note is not normatively defined until V18i. Neither V18h nor V18i is in M's Deps. The spec also mandates the renderer be registered synchronously inside the factory before the first scan (which M triggers).
- **Suggested fix:** Either (1) move the `loom-system-note` channel + renderer into H4 (making it available before M), or (2) explicitly declare in M that the renderer is in scope there and reference V18h as "rule completed by." State the M-time rendering contract explicitly (raw text fallback vs. registered renderer).
- **Lenses:** spec-lens-implementability, spec-lens-assumptions, spec-lens-consistency

### M's "AbortError" system-note path not defined in spec
- **Description:** M Tests say "AbortError surfaces as a system note." The spec's cancellation surface uses `Err(QueryError { kind: "cancelled", … })` — no `AbortError` type name appears anywhere. The cancellation formatter V18m defines the exact system-note text. M-implementers will invent wording that V18m may later have to change.
- **Suggested fix:** Rewrite the M test bullet to cite the spec's `kind: "cancelled"` contract and the V18m note format. Clarify whether M asserts just the presence of any `customType: "loom-system-note"` and V18m tightens later.
- **Lenses:** spec-lens-codebase-grounding-broad, spec-lens-clarity, spec-lens-consistency

### M assumes registration/collision plumbing not yet scheduled
- **Description:** The spec (`pi-integration-contract.md` steps 1–3) mandates that command registration happens in a `session_start` handler (because `pi.getCommands()` is `notInitialized` during the factory call), and that the handler must check for cross-format collisions with Pi-owned commands. M's automated tests include the cross-source-shadow rule (a V14p concern) but not the cross-format-collision check (V14q), and the two-stage factory/session_start split is never stated.
- **Suggested fix:** State in M which subset of the registration pipeline it owns and which rules it defers. At minimum declare ownership of: (a) renderer registration, (b) `session_start` subscription, (c) cross-source priority check, (d) cross-format collision check — with forward pointers.
- **Lenses:** spec-lens-implementability, spec-lens-assumptions

### `loomAbort` controller construction not assigned to any leaf
- **Description:** The spec (`cancellation.md`, `pi-integration-contract.md`) mandates a fresh `AbortController` (`loomAbort`) per invocation, `loomAbort.signal` always defined (never `undefined`), and `ctx.signal === undefined` tolerance at slash-command entry. M's test "AbortError surfaces as a system note" requires this plumbing. M's `Adds` does not mention it; H4's adapters do not; the construction first appears implicitly in V14c. No preceding leaf introduces the controller.
- **Suggested fix:** Move `loomAbort` construction + Pi-event forwarders into H4 (it is foundational shell infrastructure), or state in M that this plumbing is in scope and cite `cancellation.md` section.
- **Lenses:** spec-lens-implementability, spec-lens-assumptions, plan-lens-spec-coverage

### M Ships-when is manual-only for an entire integration slice
- **Description:** M's only integration verification is "Manual: `hello.loom` placed in `.pi/looms/`, slash `/hello` produces an assistant turn." The 10 fakes-driven Tests bullets do not exercise the actual `pi-coding-agent` contract. A breakage in the seam between `PiExtensionAPI` shims and the real Pi runtime slips past every leaf gate from M through V18.
- **Suggested fix:** Add an end-to-end integration test loading the built extension into a Pi test harness and asserting the assistant turn is produced. If no harness exists, scope a leaf to build one before M.
- **Lenses:** plan-lens-validation

### M's `~/.pi/agent/looms/` path expansion unspecified for Windows
- **Description:** M tests on `~/.pi/agent/looms/hello.loom` but `~` expansion is only normatively defined in `discovery.md` for settings paths. Whether the same expansion rule applies to the global discovery root, and which OS API provides it, is unstated. Two implementations may diverge from V14n's own expansion logic.
- **Suggested fix:** State in `discovery.md` (or `pi-integration-contract.md`) that all loom-extension `~` expansion uses `os.homedir()`, and cross-reference from M.
- **Lenses:** spec-lens-implementability

---

## plan_topics/v1-lexer.md

### UTF-8 encoding, BOM consumption, and `loom/load/invalid-encoding` — no plan leaf
- **Description:** The spec (`lexical.md`) mandates: source files are UTF-8; a leading UTF-8 BOM is consumed and ignored; any other BOM or invalid UTF-8 byte sequence is `loom/load/invalid-encoding` reporting file path and byte offset. No V1 leaf, H-phase leaf, or V14/V18 leaf parses or rejects file encoding. The lexer plan starts at numerics (V1a) and assumes a decoded source.
- **Suggested fix:** Add a leaf before V1a (e.g. `V1-pre`): adds UTF-8-only source decoder, BOM consumption, invalid-UTF-8 → `loom/load/invalid-encoding` with file path and first-invalid-byte offset. Tests: plain ASCII, UTF-8 with BOM, UTF-8 sans BOM, UTF-16 LE/BE, lone surrogate, mid-stream continuation byte.
- **Lenses:** plan-lens-spec-coverage

### Newline normalisation (`\r\n`, bare `\r` → `\n`) — no plan leaf
- **Description:** The spec (`lexical.md`) mandates CRLF/bare-CR normalisation before lexing, and states this is observable behaviour: a `.loom` with CRLF tokenises byte-identically to the LF version. No V1 leaf claims this; V5c tests dedent with LF inputs only.
- **Suggested fix:** Add normalisation to the encoding-pass leaf or a sibling; extend V5c Tests to assert CRLF/LF byte-identical tokenisation for dedent vectors, `///` doc-comment joining, and `loom/parse/literal-newline-in-string`.
- **Lenses:** plan-lens-spec-coverage

### Path literals forward-slash rule and `loom/parse/invalid-path-separator` — no leaf
- **Description:** The spec mandates forward-slash separators in all path literals (`import "..."`, `invoke(...)`, `tools:` entries); backslash is `loom/parse/invalid-path-separator`. No leaf cites this code. V14a/V14b/V15a/V17c parse path-bearing positions but assume the path is a raw string.
- **Suggested fix:** Add a Tests bullet to V1b covering backslash inside path literals at every author-visible position emitting `loom/parse/invalid-path-separator`; ordinary non-path string literals still admit backslashes via the standard escape table. Cross-link from V14a, V15a, V17c.
- **Lenses:** plan-lens-spec-coverage, plan-lens-validation

### "Spec's exact-wording errors" without inline quote
- **Description:** V1d Tests say "mismatch produces the spec's exact-wording errors" without quoting or citing any specific line range. An implementer must follow the spec link, find the paragraph, and guess which sentence is canonical.
- **Suggested fix:** Quote the exact message verbatim in the leaf, or cite the spec line range so the test assertion has a single source.
- **Lenses:** spec-lens-clarity

---

## plan_topics/v2-expressions.md

### `loom/parse/integer-narrowing` — no plan leaf
- **Description:** The spec mandates `integer` widens implicitly to `number`; the reverse is `loom/parse/integer-narrowing`. V1a covers numeric tokenisation; V2c covers arithmetic but does not cite this code. No leaf asserts a `number` value used where `integer` is required is rejected with this exact code.
- **Suggested fix:** Add a Tests bullet to V2c: "`number → integer` assignment, parameter passing, and field-typing each emit `loom/parse/integer-narrowing`; `integer → number` widens silently."
- **Lenses:** plan-lens-spec-coverage, plan-lens-validation

### V2d cites wrong panic-routing leaf
- **Description:** V2d Tests say "OOB returns runtime panic (V18o-routed)." V18o is the per-call-timeout marker/coverage gate. Panic routing is V18m (slash surface) and V18n (invoke surface); the OOB panic source is V18k. Implementers following the cite go to the wrong page.
- **Suggested fix:** Replace "V18o-routed" with "V18k (panic source) / V18m (slash surface routing)."
- **Lenses:** spec-lens-clarity

### V2c division-by-zero description incomplete
- **Description:** V2c Tests say "division-by-zero produces `Infinity` per JS." JS produces `Infinity`, `-Infinity`, or `NaN` depending on the operands. The single-result phrasing is incorrect.
- **Suggested fix:** "Division by zero follows JS IEEE-754: `x/0` → `Infinity` for positive `x`, `-Infinity` for negative `x`, `NaN` for `x === 0`."
- **Lenses:** spec-lens-clarity

### V2c "ternary type-checks both arms" — missing assertion
- **Description:** "Ternary type-checks both arms" describes a checker action, not a test condition. Two readings: (a) both arms are visited; (b) arms must produce a common type.
- **Suggested fix:** Replace with: "ternary with arms of incompatible types emits the common-type diagnostic naming both arms; ternary with two equal-type arms produces that type as the result type."
- **Lenses:** spec-lens-clarity

---

## plan_topics/v3-frontmatter.md

### `loom/load/missing-mode` and `loom/load/unknown-mode-value` — no asserting leaf
- **Description:** Two normative registry codes — `loom/load/missing-mode` (frontmatter omits `mode:`) and `loom/load/unknown-mode-value` (e.g. `mode: agent`) — have no Tests bullet in V3a or any other leaf. Implementations that omit these checks or emit wrong codes pass V3a.
- **Suggested fix:** Extend V3a Tests: (a) frontmatter without `mode:` → `loom/load/missing-mode`, loom does not register; (b) `mode: agent` → `loom/load/unknown-mode-value`, loom does not register. Messages must match spec verbatim.
- **Lenses:** plan-lens-spec-coverage, plan-lens-validation

### "Loom-specific fields" vs "unknown fields" boundary undefined
- **Description:** V3a Adds distinguishes "unknown fields" (→ `loom/load/unknown-frontmatter-field` warning) from "loom-specific fields other than `mode`/`description`/`argument-hint`/`params`" (recognised but ignored with a deferred warning). The recognised-but-deferred set is not enumerated, no diagnostic code is named for the deferred warning, and the message template is not fixed.
- **Suggested fix:** Enumerate the recognised-but-deferred set explicitly (`model`, `tools`, `system`, `bind_model`, `bind_context`, `bind_echo`, `coercion`); name the diagnostic code; fix the message template.
- **Lenses:** spec-lens-clarity

### M's collision warning lacks code/severity
- **Description:** M Tests say "Two files producing the same slash name across the two roots: only the project one registers; warning names both paths." No diagnostic code, severity, or message template is named. V14p later defines `loom/load/cross-source-shadow`.
- **Suggested fix:** Either cite the V14p code (noted as provisional) or specify a placeholder code that V14p will subsume.
- **Lenses:** spec-lens-clarity

---

## plan_topics/v4-schemas.md / v10-enums.md / v11-discriminated-unions.md

### Empty schema and enum body diagnostics — no test leaf
- **Description:** The spec mandates `loom/parse/empty-schema-body` for `schema X { }` and `loom/parse/empty-enum-body` for `enum X { }`. V4b tests trailing comma, missing field, `additionalProperties:false` — no test for empty body. V10a tests case rule and string-only values — no test for empty enum.
- **Suggested fix:** Add Tests bullets to V4b (empty schema body) and V10a (empty enum body) asserting the exact diagnostic codes.
- **Lenses:** plan-lens-spec-coverage

### Type-alias cycle detection (`loom/parse/type-alias-cycle`) — no plan leaf
- **Description:** The spec mandates `loom/parse/type-alias-cycle` for pure alias cycles, with cycle path printed. V4c tests primitive unions but not cycle detection. V11g covers self-recursive object schemas (legal). No leaf covers pure-alias cycles.
- **Suggested fix:** Add `V4c.1`: walks the alias graph, emits `loom/parse/type-alias-cycle` with printed path. Tests: `schema X = X`; two-step cycle; three-step cycle; cycle through one object schema → legal (no diagnostic).
- **Lenses:** plan-lens-spec-coverage

### `loom/parse/non-string-discriminator` — no test leaf
- **Description:** The spec mandates rejection of numeric and boolean literal discriminators with `loom/parse/non-string-discriminator`. V11a–V11d cover detection, ambiguous, missing, explicit `by` — no leaf cites this code.
- **Suggested fix:** Add a Tests bullet to V11a: numeric-tag and boolean-tag union variants emit `loom/parse/non-string-discriminator`; same applies under explicit `by f` (V11d).
- **Lenses:** plan-lens-spec-coverage

### Schema-subset depth enforcement missing at three of four required sites
- **Description:** V11i covers depth cap (depth-5 accepted; depth-6 rejected) but only tests the typed-query response site. The spec mandates the depth check runs at four sites: typed-query response, model-driven tool-call arg validation, code-driven tool-call arg validation, and `params` validation at loom invocation. The canonical error shape (`schema_keyword: "maxDepth"`, JSON Pointer, verbatim message) and the pre-AJV ordering are also unasserted.
- **Suggested fix:** Extend V11i (or add V11i.1): test depth-6 rejection at each of the four sites; assert `ValidationIssue.schema_keyword === "maxDepth"`, path is JSON Pointer to first too-deep node, message matches spec verbatim; depth walk runs before AJV.
- **Lenses:** plan-lens-spec-coverage

### Canonical schema hash algorithm unasserted
- **Description:** V4f claims "stable structural hash; structurally-identical inline schemas dedup" and tests key-order-independence, but does not assert the algorithm: SHA-256, first 16 hex chars, RFC-8259-minimal-escape canonicalisation with Unicode-code-point-sorted object keys, no insignificant whitespace, JSON-numeric form. The recipe is described as "part of the on-disk and on-wire contract — changing it is a breaking change."
- **Suggested fix:** Add a Tests bullet to V4f pinning the slug algorithm against a fixture snapshot. Cross-reference from V6i for `__loom_respond_<slug>`.
- **Lenses:** plan-lens-spec-coverage

### V4a "validation produces expected error shapes" is not specific
- **Description:** V4a Tests: "validation produces expected error shapes" — no enumeration of which AJV keywords map to which output shape, no fixture reference, no JSON-Pointer path format. V6j later defines the `ValidationIssue` shape. V4a's wording can be satisfied by any non-empty error object.
- **Suggested fix:** Replace with explicit assertions per AJV keyword (`type`, `required`, `enum`, `const`, `additionalProperties`) or defer explicitly to V6j with a note.
- **Lenses:** plan-lens-validation

### V4i and V11g/V11h/V11i contain duplicated requirements
- **Description:** V4i specifies self-referential schema (`schema Tree`), mutual recursion (`Person ↔ Animal`), and the depth cap in both `Adds` and `Tests`. All three concerns are re-stated at equal or greater specificity in V11g, V11h, and V11i respectively. The V11g dep note acknowledges V4i is the "AJV side" but V4i overshoots that intent.
- **Suggested fix:** Narrow V4i to AJV infrastructure only (prove AJV compiles a synthetic hand-written recursive `$defs`/`$ref` schema). Remove the surface-syntax examples from V4i; those belong exclusively in V11g–V11i.
- **Lenses:** spec-lens-cruft

### V11g and V6d Deps fields contain rationale-only asides (cruft)
- **Description:** V11g Deps: `*(V4i is the AJV side; this is the surface.)*` — pure rationale, not a constraint. V6d Deps: `*(Order: this leaf depends on V9a–V9e; reorder as needed.)*` — process note, not a requirement.
- **Suggested fix:** Delete both parenthetical asides. The dep IDs alone convey the ordering constraint.
- **Lenses:** spec-lens-cruft

---

## plan_topics/v5-untyped-queries.md

### V5b "compact `JSON.stringify`" vague
- **Description:** V5b Tests say "compact `JSON.stringify` with wire-name translation." "Compact" could mean key-sorted, omitting nulls, or just no `space` argument. An implementer cannot tell whether `JSON.stringify(x)` alone satisfies or a custom serialiser is required.
- **Suggested fix:** "Stringification is `JSON.stringify(value)` with no `space` argument and no replacer, after wire-name translation; key order follows the schema's declaration order."
- **Lenses:** spec-lens-clarity

### V5c trailing-whitespace rule states only the negative
- **Description:** V5c Tests say "a trailing `\n` followed by whitespace before the closing backtick is *not* newline-trimmed" — states what does not happen, leaves what does happen unstated. Does the trailing whitespace survive into the pre-dedent string? Does dedent operate on it?
- **Suggested fix:** State the positive: "the `\n` and trailing whitespace are both preserved in the pre-dedent string; dedent then applies to the resulting lines."
- **Lenses:** spec-lens-clarity

### Discarded-query operator-facing observability and `let _ =` interaction — no plan leaf
- **Description:** The spec (`query.md`) mandates that a discarded `Err` from `let _ = @\`...\`` still produces a `display: false` operator-facing event exactly once per occurrence regardless of iterations/coercion follow-ups. `Ok` discards produce no event. Panics during `${expr}` interpolation propagate through the discard form (not silently absorbed). No leaf covers the runtime side of discarded queries.
- **Suggested fix:** Add Tests bullets to V5f (or new V5f.1): a `let _ = @\`...\`` returning `Err` of any always-log kind emits exactly one `display: false` event; `Ok` discard emits zero; N coercion follow-ups still emit one terminal event; a panic in `${expr}` propagates through the discard form (does not get absorbed). Cross-link from V18m.
- **Lenses:** plan-lens-spec-coverage

### Provider error mapping for `ContextOverflowError` — no test leaf
- **Description:** V5g introduces the `context_overflow` variant but no Tests bullet exercises any provider's overflow signature. The spec defines per-provider signatures (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`), HTTP-200-with-error-envelope recognition, `null`-token-count case, and mid-stream end-of-stream classification.
- **Suggested fix:** Add `V5g.1`: for each of the four V1 supported providers, a synthesised provider error matching the spec's signature → `ContextOverflowError` with provider-supplied token counts when present, `null/null` otherwise; HTTP 200 with error-envelope recognised; mid-stream truncation → `context_overflow` at end-of-stream.
- **Lenses:** plan-lens-spec-coverage

### V5e Ships-when: "a real Pi session" is unverifiable from the leaf gate
- **Description:** V5e Ships-when says "A real Pi session can run a multi-query loom." Tests are all against fakes. The "real Pi session" claim is unobservable within the leaf's gate; the only end-to-end validation is M's manual smoke, which doesn't exercise multi-query behaviour.
- **Suggested fix:** Either downgrade Ships-when to "Fake-driven multi-query loom round-trips" (matching what tests assert) or add an integration test running a 3-query loom against a Pi test harness.
- **Lenses:** plan-lens-validation

### V5e "Single turn round-trips" meaningless
- **Description:** "Round-trips" for a one-direction send-and-await does not convey the test condition.
- **Suggested fix:** "A single `send` produces one assistant turn whose text equals the value the `FakeModelClient` returned for that turn (no truncation, no transformation)."
- **Lenses:** spec-lens-clarity

---

## plan_topics/v6-typed-queries.md

### V6c "Spec's worked example" — which one?
- **Description:** V6c Tests cite "Spec's worked example" without specifying which example in `query.md`. Two implementers will pick different examples.
- **Suggested fix:** Cite by anchor or paragraph: `query.md#typed-form, "Inference rule 1" example`.
- **Lenses:** spec-lens-clarity

### V6k tool-loop budget accounting ambiguous
- **Description:** The parenthetical "and the forced respond turn (one slot)" reads as the respond turn consuming a budget slot, but the assertion "a loom that loops to exactly 25 succeeds, 26 fails with `tool_loop_exhausted`" reads as 25 free-phase rounds. Implementers will disagree on whether `max_iterations: 25` permits 25 or 24 free-phase rounds.
- **Suggested fix:** State as a formula: "Total slots = (free-phase rounds) + (1 if a forced respond turn is issued, else 0). Exhaustion fires when total slots would exceed `max_iterations`. With `max_iterations: 25`: at most 24 free-phase rounds + 1 forced respond turn."
- **Lenses:** spec-lens-clarity

### V6 leaf file order: V6k appears before V6j
- **Description:** V6j (Deps: V6i only) has fewer deps than V6k (Deps: V6i, V13f), yet V6k is printed first in the file, contrary to the "ordered by dependencies" guideline.
- **Suggested fix:** Reorder so V6j precedes V6k, or add a one-line note explaining the editorial grouping.
- **Lenses:** spec-lens-consistency

### V6i too large — bundles six distinct concerns
- **Description:** V6i bundles: (1) schema lowering → synthesised respond tool, (2) two-phase loop state machine, (3) AJV validation + `Err({kind:"validation"})`, (4) load-time provider compatibility check, (5) tool-registration snapshot/restore, (6) subagent vs. prompt mode wiring difference. Its test list has 10+ complex bullets; the provider compatibility check is a pure load-time concern orthogonal to the runtime loop.
- **Suggested fix:** Split: `V6i` (two-phase loop core), `V6i-b` (provider compatibility check), `V6i-c` (tool-registration snapshot/restore for typed queries).
- **Lenses:** plan-lens-step-atomicity

### `resources_discover` subscription and `{}` return — no plan leaf
- **Description:** The spec mandates the extension subscribes to `resources_discover` as a re-discovery trigger and returns `{}`. Neither V18f nor H4 claims this subscription, the empty-`{}` return, the use of `event.cwd` over factory-captured cwd, or the `reason: "startup"` vs `reason: "reload"` distinction.
- **Suggested fix:** Add a Tests bullet to H4 or V14k: factory subscribes to `resources_discover`; handler returns `{}`; uses `event.cwd`; `reason: "reload"` triggers a re-walk; `reason: "startup"` triggers the initial scan.
- **Lenses:** plan-lens-spec-coverage

---

## plan_topics/v7-match.md

### V7a "common-type values" undefined locally
- **Description:** V7a Adds says "arms must produce common-type values" without citing which common-type relation is used. The spec defines this under array-construction / operator rules. "Produce common-type values" reads at the value level, not the type level.
- **Suggested fix:** "All arms must satisfy the common-type relation defined in `expressions.md` #object-construction. The match expression's static type is the resulting common type."
- **Lenses:** spec-lens-clarity

---

## plan_topics/v9-functions.md

### V9d "conflicting declaration" undefined
- **Description:** V9d says "conflicting declaration is parse error" for a function that already infers a `Result<_, QueryError>` return type. "Conflicting" is unspecified: does any explicit return type that is not `Result<T, QueryError>` conflict? What about `Result<T, E>` with `E ≠ QueryError`?
- **Suggested fix:** "Conflicting = the explicit return type is not of the form `Result<T, QueryError>` for any `T`. Explicit `Result<T, E>` with `E ≠ QueryError`, plain `T`, or `void` all emit `loom/parse/question-mark-requires-result` with the offending declared type quoted."
- **Lenses:** spec-lens-clarity

---

## plan_topics/v12-subagent.md

### V12c "the deferred future-consideration" — no antecedent
- **Description:** V12c Tests say "rejection message references the deferred future-consideration" with no antecedent for which deferred item is meant or where the message text lives.
- **Suggested fix:** Cite the spec section by anchor and quote the message text the diagnostic must produce.
- **Lenses:** spec-lens-clarity

### `AgentSession.dispose()` failure path unbounded
- **Description:** V12a covers `dispose()` failure via `loom/runtime/subagent-dispose-failure` but does not bound the failure count or cap new spawns. A persistent dispose failure leaks one session per invocation, accumulating orphaned `AgentSession`s over a long-lived extension session.
- **Suggested fix:** Add a Tests bullet asserting dispose failure count is observable via a probe; add a cap-and-refuse policy leaf (or extend V12a) that emits a system note and refuses new subagent spawns once the count crosses a threshold.
- **Lenses:** plan-lens-risk

### V12a missing from V14e Deps
- **Description:** V14e tests subagent-mode behaviour ("subagent-mode invocation triggers zero `pi.registerTool` calls") but its Deps list only includes `V14a, V5e`. There is no subagent-mode invocation path to exercise before V12a is complete.
- **Suggested fix:** Add `V12a` to V14e's Deps.
- **Lenses:** plan-lens-ordering

---

## plan_topics/v13-wire-names.md

### V13 title inconsistency and "retry" terminological conflict
- **Description:** `plan.md` index: "V13 — Wire names, descriptions, retry/coercion." The leaf file H1: "V13 — Wire names, descriptions, coercion." The spec (`frontmatter.md`) explicitly reserves "retry" for a different mechanism and forbids conflating it with coercion.
- **Suggested fix:** Update `plan.md` to match the leaf file's title: "V13 — Wire names, descriptions, coercion."
- **Lenses:** spec-lens-consistency

### Static-resolution cache named three different ways
- **Description:** V15a uses "per-load-pass static-resolution cache," V15n uses "per-load-pass static-resolution graph," V18j uses "per-load-pass parse cache" (the spec's actual term from `invocation.md`). Three names for the same entity across the same plan.
- **Suggested fix:** Standardise on the spec's term "per-load-pass parse cache" throughout all plan leaves.
- **Lenses:** spec-lens-naming, spec-lens-consistency

### Inbound enum-brand re-attachment not covered by any leaf
- **Description:** The spec (`runtime-value-model.md`) mandates: at every position annotated as a named enum, reattach the declaring-enum tag so the value compares equal to a locally constructed variant. Anonymous string-literal-union positions receive no tag. The walk recurses through arrays, nested objects, and `Result` payloads. V13b covers inbound translation generally but no leaf covers the brand re-attachment rule or the `Severity.Low == "low"` → `false` cross-form comparison.
- **Suggested fix:** Add Tests bullets to V13b for each of the four boundaries (typed-query response, model-driven typed tool-call return, `invoke<T>` return, binder `args`): model-emitted `"low"` at a named-enum position arrives branded and compares equal to `Severity.Low`; at an anonymous position it arrives unbranded and `Severity.Low == "low"` is `false`; recursion through arrays, nested objects, `Result` payloads.
- **Lenses:** plan-lens-spec-coverage

---

## plan_topics/v14-tool-calls.md

### V14c tests registered-loom callees before V15e creates them (ordering gap)
- **Description:** V14c Adds says "argument type-checking … use the callee's parsed form from the per-load-pass static-resolution cache (populated for `tools:` entries by V15e)" and its Tests include a loom-callee test. V14c's Deps are `V14a, V13c, V16a` — none introduce the loom-callee surface. The static-resolution cache V14c consults is populated by V15e, which is not yet complete.
- **Suggested fix:** Either (a) split V14c so the registered-loom-callee branch and its test move to V15e, or (b) declare V15e as a forward Dep of V14c with an acknowledgement that it cannot ship green until V15e lands.
- **Lenses:** plan-lens-ordering

### V14c: `toolCallId` suffix scheme unspecified
- **Description:** V14c Adds says "Pi tool's `execute()` invoked directly with `toolCallId` prefixed `loom-direct:`" but the suffix after the colon is unstated. Tests cannot assert on `toolCallId` without knowing whether it is a UUID, counter, or call-site source position.
- **Suggested fix:** Specify the suffix, e.g. "`loom-direct:<crypto.randomUUID()>`" or "`loom-direct:<monotonic-uint per loom invocation>`." Add a test bullet asserting the format.
- **Lenses:** spec-lens-clarity

### V14c bare-object-literal "second carve-out" has no "first"
- **Description:** V14c says "the second documented carve-out to the bare-object-literal prohibition" without anywhere naming the "first." V16a does not call itself "the first carve-out." The numbering is unanchored.
- **Suggested fix:** Drop the ordinal ("a documented carve-out, the same sublanguage as `params:` defaults") or have V16a explicitly tag itself as the first carve-out and cross-link to V14c.
- **Lenses:** spec-lens-consistency

### V14c too large — three distinct concerns
- **Description:** V14c bundles: (1) post-rename resolution + static type-checking against loom-callee cache, (2) bare-object literal sublanguage enforcement for Pi-tool call arguments (with all rejection diagnostics), (3) synthesised `ctx` argument construction with five overrides. Its test list has 10+ bullets.
- **Suggested fix:** Split into `V14c-a` (dispatch + static type-checking + ctx construction) and `V14c-b` (bare-object literal enforcement).
- **Lenses:** plan-lens-step-atomicity

### V14d too hollow — merge into V14c
- **Description:** V14d adds no new implementation; its entire content is a behavioural assertion that V14c already implements ("Code-side tool call bypasses model entirely; transcript unchanged"). Its single test is verification of V14c behaviour, not a new feature.
- **Suggested fix:** Absorb V14d's test into V14c as an explicit test bullet. Delete V14d as a stand-alone leaf.
- **Lenses:** plan-lens-step-atomicity

### V14e missing V12a from Deps (duplicate of V12 ordering finding)
- _See V12 section above._

### Tool execution `content` filtering and 4096-byte truncation — no test leaf
- **Description:** The spec (`pi-integration-contract.md`) mandates: filter `content` to `type === 'text'` entries, join with `\n`, silently discard non-text blocks; `isError:true` with no surviving text → fixed fallback string; long error message truncated at 4096 bytes on a code-point boundary; `execute()` throw → message from `.message` truncated under same rule. V14g covers `CodeToolError` execution cause but not the text-filter rule, truncation, or the empty-text fallback string.
- **Suggested fix:** Extend V14g Tests (or add V14g.1): mixed content array (text + image) → only text reaches loom; multiple text blocks joined with `\n`; empty content → `Ok("")`; `isError:true` with no text → spec's fixed fallback string; long error message truncated at 4096 bytes on code-point boundary; `execute()` throw → `.message` truncated under same rule.
- **Lenses:** plan-lens-spec-coverage

### V14m: scoped packages (`@scope/pkg`) not covered; `node_modules/` walk unbounded
- **Description:** V14m walks `node_modules/*/package.json` but does not match scoped packages at `node_modules/@scope/pkg/package.json`. Also: no timeout, no max-package cap, no progress diagnostic — a monorepo with thousands of `node_modules` entries can visibly hang Pi at startup.
- **Suggested fix:** Walk both `node_modules/*/package.json` and `node_modules/@*/*/package.json`. Add a Tests bullet asserting an upper bound (emit `loom/load/discovery-slow` if walk exceeds N ms or visits more than M `package.json` files). Document a `looms.scanPackages: false` opt-out.
- **Lenses:** spec-lens-clarity, plan-lens-risk

### V14m "precedence per spec" non-specific
- **Description:** V14m Tests say "precedence per spec" without naming the ordering.
- **Suggested fix:** Either inline the five-level ordered list or cite the exact anchor (`discovery.md#source-priority`).
- **Lenses:** plan-lens-validation

### V14n / V14o missing V14q from Deps despite citing its collision rule in Tests
- **Description:** V14n Tests say "two settings entries that resolve to same slash name → `loom/load/cross-format-collision` and neither registers (per V14q)"; V14o Tests have the same reference. Neither V14n nor V14o lists V14q in Deps, though V14m (which makes the same reference) correctly does.
- **Suggested fix:** Add `V14q` to V14n's and V14o's Deps, or move the cross-format-collision tests into V14q itself.
- **Lenses:** spec-lens-consistency, plan-lens-ordering

### V14n malformed settings JSON degrades silently; no fallback to last-known-good
- **Description:** V14n: "malformed JSON file treated as `{}` with a single load-time diagnostic." A user saving a half-typed `settings.json` sees every settings-sourced loom disappear from the slash menu with only one diagnostic. No fallback to last-known-good; the diagnostic may scroll past in a busy drain. Also: the missing-file diagnostic code is unnamed in V14n ("single load-time diagnostic" with no code or severity).
- **Suggested fix:** When a settings file fails to parse, retain the previous load's parsed value and emit a `loom-system-note` ("settings.json malformed — using last known good"). Add a Tests bullet: "second settings load fails to parse → previously registered looms remain registered." Name the missing-file diagnostic code and severity in V14n.
- **Lenses:** plan-lens-risk, spec-lens-clarity

### V14o missing V14n from Deps
- **Description:** V14o explicitly reuses V14n's path-resolution rule ("same rules as settings `looms` entries (V14n)") and its tests rely on that resolver, but V14o's Deps lists only `V14k`.
- **Suggested fix:** Add `V14n` to V14o's Deps.
- **Lenses:** plan-lens-ordering

### V14p "Five-level priority from spec" — no anchor
- **Description:** V14p Adds says "Five-level priority from spec" without naming the ordering or citing the anchor. An implementer reading only V14p cannot determine which source wins.
- **Suggested fix:** Either inline the ordered list or cite `discovery.md#source-priority`.
- **Lenses:** spec-lens-clarity

### Cross-priority shadowing with no opt-out or rollback procedure
- **Description:** V14p emits `loom/load/cross-source-shadow` but provides no `looms.shadow: refuse` policy knob, no per-source `as` rename suggested by the warning text, and no documented recovery beyond "rename one of the files."
- **Suggested fix:** Annotate V14p with "Rollback: rename the higher-priority file," ensure the warning text guides users to it, and consider a `looms.allowShadowing: false` flag promoting the warning to a load error.
- **Lenses:** plan-lens-risk

### `tools:` resolution snapshot invariants — no test leaf
- **Description:** The spec (`frontmatter.md`) mandates a frozen per-loom table where each entry holds a strong reference to the resolved callable; subsequent calls dispatch through the held reference and the runtime never re-queries Pi's tool registry by name during execution. Four enumerated consequences (in-flight unregister survives, etc.) are untested. V14a–V14c cover parsing and dispatch; V14i covers `unknown_tool` but not the captured-reference invariants.
- **Suggested fix:** Add `V14i.1`: a Pi tool unregistered after loom load completes its in-flight call against the captured `execute`; a `.loom` callee changed on disk after invocation completes against captured parse; an architectural probe asserts no name-based re-query during execution.
- **Lenses:** plan-lens-spec-coverage

---

## plan_topics/v15-invoke.md

### `loom/load/invoke-path-escape` — security boundary with single check site and no telemetry
- **Description:** V15a / V15e check the path against active discovery roots once at parse/load. If a future refactor or symlink race between `realpath` and open bypasses the check, looms can read arbitrary files. No re-check at runtime open-time, no telemetry for escape attempts.
- **Suggested fix:** Either re-check the resolved path immediately before the file is read at runtime, or document that the load-time check is the sole boundary and add a Tests bullet for "symlink swapped between realpath and read" (TOCTOU). Add a diagnostic-emit count probe so escape attempts are observable.
- **Lenses:** plan-lens-risk

### V15c "compatibility relation" undefined
- **Description:** V15c says "structural compatibility (using the same compatibility relation as `let x: T = expr`)" but the plan never names or defines that relation anywhere.
- **Suggested fix:** Either introduce the relation by name in V2a/V4, or inline the rule: "T₁ is compatible with T₂ iff every value of T₁ would AJV-validate against the lowering of T₂."
- **Lenses:** spec-lens-clarity

### `InvokeInfraError.reason: "cancelled"` absent from spec schema
- **Description:** V15l's Adds declares `InvokeInfraError.reason` enum including `"cancelled"`. The spec's formal schema (`errors-and-results.md`) lists `reason: "load_failure" | "parse_failure" | "validation" | "panic"` — no `"cancelled"`. Cancellation during `invoke` surfaces as `CancelledError` (`kind: "cancelled"`), confirmed by V18d. The addition is both spec-inconsistent and internally inconsistent with V18d.
- **Suggested fix:** Remove `"cancelled"` from V15l's `InvokeInfraError.reason` enum.
- **Lenses:** spec-lens-naming

### V15n Deps missing V17j; meta-level dep note in Tests is cruft
- **Description:** V15n Tests say "cycle through warp `fn` invokes too (deps on V17j)" — the parenthetical is a planning-process annotation inside a test description (cruft), and V17j is absent from V15n's Deps field despite being a real prerequisite.
- **Suggested fix:** Delete the parenthetical `(deps on V17j)`. Add `V17j` to V15n's Deps field.
- **Lenses:** spec-lens-traceability, spec-lens-cruft

### V15n invocation-cycle message format not pinned to spec template
- **Description:** V15n Tests pin the path notation (`A → B → A`) but do not pin the leading text `"invocation cycle: "`. The spec mandates exactly this template. V17k (import cycle) does pin the leading text verbatim. The two parallel diagnostics have asymmetric guarantees.
- **Suggested fix:** Update V15n Tests to assert the spec's literal `"invocation cycle: <path>"` template, mirroring V17k.
- **Lenses:** spec-lens-consistency

---

## plan_topics/v16-binder.md

### V16e ordering: forward Dep on V16o with misleading file order
- **Description:** V16e Deps correctly lists V16o (which appears later in the same file). V16e is the only leaf in V16 whose Dep points to a later leaf in the same file. An implementer following the topic order top-to-bottom reaches V16e before V16o.
- **Suggested fix:** Move V16o ahead of V16e in the file (V16o's only Dep is V16c, so it can sit immediately after V16d), or insert a callout at V16e ("requires V16o; reorder when scheduling").
- **Lenses:** plan-lens-ordering

### V16e bad `looms.binderModel` setting silently unregisters all affected looms
- **Description:** A typo in `looms.binderModel` drops every non-bypass-eligible loom from registration with only per-loom diagnostics and no aggregate alert. "Next load only" means the user must manually `/reload` after fixing settings. Blast radius: entire loom catalogue silently disappears from the slash menu.
- **Suggested fix:** On settings change, emit one consolidated `loom-system-note` summarising "X looms previously failed to load — run /reload to retry." Add a Tests bullet for settings-fix-then-reload recovering all previously-failed looms.
- **Lenses:** plan-lens-risk

### V16e strict-capability SDK surface not pinned; "best-effort" diagnostic code unnamed
- **Description:** V16e says "If Pi's registry does not surface a strict-capable flag, the load-time check degrades to best-effort (advisory diagnostic noted; no load failure)." Neither the SDK method for querying the flag nor the advisory diagnostic code is named.
- **Suggested fix:** Pin the SDK call by name (or state definitively whether Pi v0.72.1 exposes it). Name the diagnostic code (e.g. `loom/load/binder-model-strict-capability-unknown`) and severity from the H3-defined set.
- **Lenses:** spec-lens-clarity, spec-lens-implementability, plan-lens-risk

### V16g "whichever smaller" ambiguous
- **Description:** V16g says "accumulate until 20 turns or 8000 tokens (whichever smaller)" — "whichever smaller" is non-standard for accumulation bounds and literally compares turns to tokens (nonsensical). Reading (A) "stop as soon as either bound is reached" is presumably intended.
- **Suggested fix:** "accumulate turns newest-to-oldest and stop as soon as either cap (20 turns or 8000 tokens) would be exceeded; the turn that would exceed either cap is excluded entirely."
- **Lenses:** spec-lens-clarity

### V16h binder seed value not specified
- **Description:** V16h specifies `temperature: 0` and "fixed seed (where provider supports)" but names no seed value or derivation rule (per-loom hash? constant? per-call?). The test is written against whichever value the implementer picks, so it passes by tautology.
- **Suggested fix:** State the seed value or derivation rule in V16h or `binder.md`. If implementation-defined, test only "presence of a non-null seed field" rather than equality.
- **Lenses:** spec-lens-implementability, plan-lens-validation

### V16h "seed included for providers that support it" — supported-provider list not pinned
- **Description:** V16h does not identify which providers support the seed parameter or how "supports seed" is detected. A Pi adapter that silently strips `seed` for an unsupported provider would let the test pass against a fake while shipping nondeterminism.
- **Suggested fix:** Pin the supported-provider list (e.g. `openai-completions` supports seed; `anthropic-messages` does not) and add per-provider Tests bullets for seed presence/absence.
- **Lenses:** plan-lens-validation

### V16i "spec's exact examples" but spec disclaims them
- **Description:** V16i Tests say "each formatting rule against spec's exact examples." The spec's `binder.md` explicitly says: "The example below is illustrative — the format rules that follow are normative; no single example string can be (the formatter is data-driven)." There is one illustrative example; no canonical fixture set exists.
- **Suggested fix:** Enumerate per-rule property assertions (quoting only whitespace shape, truncation marker, ordering) rather than "spec's exact examples," or add a normative fixture table to `binder.md`.
- **Lenses:** spec-lens-implementability

---

## plan_topics/v17-warp.md

### V17a too hollow — merge into V17b
- **Description:** V17a adds only AST builder dispatch on file extension with one test verifying token equivalence between `.warp` and `.loom`. No behavioural difference is observable until V17b enforces the body restriction. The Ships-when "`.warp` files parse" is vacuously satisfied by the existing `.loom` parser.
- **Suggested fix:** Merge V17a into V17b. Combined leaf: shared lexer, AST builder dispatches on extension, body restriction enforced. Ships when: `.warp` files parse and reject forbidden top-level forms.
- **Lenses:** plan-lens-step-atomicity

### `enum` permitted in `.warp` files by plan but absent from spec's `.warp` allowlist
- **Description:** V17b says "Top-level: only `import`, `export`, `schema`, `enum`, `fn` allowed." The spec (root `spec.md` and `spec_topics/imports.md`) lists the `.warp` allowlist as `import`, `export`, `schema`, `fn` — no `enum`. The discrepancy is unresolved.
- **Suggested fix:** Decide whether `.warp` may declare enums. Either add `enum` to the spec's `.warp` allowlist (in both locations) or remove `enum` from V17b's permitted forms and document where shared enums must live.
- **Lenses:** spec-lens-consistency

---

## plan_topics/v18-cancellation.md

### V18o wrong diagnostic code for `timeout:` field rejection
- **Description:** V18o criterion 1 asserts `timeout:` → `loom/load/unknown-frontmatter-field` (a load-time warning). The spec (`diagnostics.md` registry and `cancellation.md`) registers `loom/parse/timeout-field-rejected` (parse-phase error) for this site. The two codes differ in namespace, phase, and severity. A test pinning the wrong code will pass against a spec-violating implementation.
- **Suggested fix:** Edit V18o to assert `loom/parse/timeout-field-rejected` (severity error) at every `timeout:` site (frontmatter, per-query, per-tool-call, per-invoke). Update the Tests bullet to cite the registry row by code.
- **Lenses:** plan-lens-spec-fidelity

### V18o bundles per-call timeout marker with coverage-matrix CI gate
- **Description:** V18o combines two unrelated acceptance criteria — (1) timeout-field rejection (a trivial parse rule) and (2) the coverage-matrix CI gate (a project-quality assertion). They can pass or fail independently; Ships-when combines both, preventing either from shipping independently.
- **Suggested fix:** Split into two leaves: `V18o` (per-call timeout marker), with the timeout-rejection test optionally rolled into V3a or V13f; and `V18p` (coverage-matrix CI gate). Update all cross-references.
- **Lenses:** plan-lens-step-atomicity, spec-lens-traceability, spec-lens-placement

### V18o CI command assumes sorted input and literal `PREFIX-` prefix
- **Description:** V18o shows a `comm -23` command without `sort -u` piping (produces noise). The literal `PREFIX-` in the example suggests placeholder rather than the actual per-page prefixes from the appendix table.
- **Suggested fix:** Mark as pseudocode explicitly. Replace with: "Implementation must enumerate every REQ-ID prefix from the appendix table, grep both directories with that union, sort/uniq, and assert empty `comm -23`. The exact script form is non-normative."
- **Lenses:** spec-lens-clarity

### V18f watcher swap has no rollback or kill switch
- **Description:** The atomic registry swap is described but the failure mode of a partial-update swap (some files succeed, some fail; AJV cache eviction succeeds but registry write throws) is unaddressed. A bad swap leaves the registry in a corrupted state with no recovery short of restarting Pi. Blast radius: every active user of the extension.
- **Suggested fix:** Add a sub-leaf (V18f.1): build the new registry off to the side, validate it, publish-or-discard atomically; on discard emit `loom/runtime/registry-swap-failed` and keep the prior snapshot live. Add `looms.watcher: false` settings opt-out. Add "watcher swap fails mid-edit" as a tested failure mode.
- **Lenses:** plan-lens-risk

### V18f structural-change note text unspecified
- **Description:** V18f Adds says "emit a one-line `loom-system-note` informational message prompting the user to run `/reload`" with no exact text, no diagnostic code, no `details` payload schema. M and V18i fix exact text for other system notes; this one does not.
- **Suggested fix:** Fix the text verbatim (e.g. "`<N> loom file(s) added or removed; run /reload to refresh the slash command list`"). Specify whether multiple structural events within the debounce window coalesce into one note or fire separately.
- **Lenses:** spec-lens-clarity

### Settings-file watching silently assumed but excluded from V18f scope
- **Description:** `discovery.md` mandates a per-file watcher on the two settings paths (`~/.pi/agent/settings.json`, `.pi/settings.json`) with debounce. V18f names "discovery roots" only. V16e's "next loom load" wording implies transparent re-resolution on settings change, but V18f's "discovered roots" reads as settings paths being out of scope. No leaf assigns settings-path watching to a concrete step.
- **Suggested fix:** Extend V18f (or split a new leaf) to enumerate settings-file paths under the watcher contract. Tighten V16e's "next loom load" wording to specify whether it requires `/reload` (structural) or fires on settings file change.
- **Lenses:** spec-lens-assumptions

### V18g not independently verifiable — merge into V18f
- **Description:** V18g states its eviction "collapses into the V18f `LoomRegistry` swap … is part of the swap, not a separate watcher-driven path." V18g introduces no new code; its tests verify properties that should be part of V18f's exit gate. Shipping V18f without V18g's eviction half is a known-incomplete state that V18g's own description acknowledges.
- **Suggested fix:** Absorb V18g's eviction work and tests into V18f. If registration-cache lifetime clarification is needed, keep V18g as a purely editorial note leaf (no code, no separate Ships-when gate).
- **Lenses:** plan-lens-step-atomicity, plan-lens-ordering

### Tool-registration cache unbounded growth
- **Description:** V18g notes the per-extension tool-registration cache (`Map<schema-hash, registeredToolName>`) "lives for the extension instance lifetime." Over a long-lived session with many edits, the cache grows unboundedly (no LRU, no bound, no telemetry). Only `/reload` clears it; users receive no signal.
- **Suggested fix:** Add a bounded-growth invariant (emit `loom/runtime/registration-cache-large` once cache exceeds N entries; document that `/reload` clears it). Add a Tests bullet asserting size monotonicity is observable via a probe.
- **Lenses:** plan-lens-risk

### Runtime event channel / always-log set wholly absent from the plan
- **Description:** The spec (`pi-integration-contract.md`, `query.md`, `slash-invocation.md`, `diagnostics.md`) defines a non-trivial subset of `QueryError` failures (always-log set: `transport`, `code_tool`, `model_tool`, `tool_loop_exhausted`, `invoke_failure`, every binder-failure row, every panic) that MUST emit a `RuntimeEvent` through `loom-system-note` exactly once per occurrence regardless of whether the author matched, propagated, or `let _ =`-discarded the `Err`. A grep across `plan_topics/*.md` shows zero mentions of `RuntimeEvent`, "always-log," or "runtime event channel."
- **Suggested fix:** Add a leaf under V18 covering: (a) each always-log member emits exactly one `display: false` event when the `Err` is handled, discarded, or `?`-propagated; (b) the four excluded kinds produce zero events; (c) `?`-propagation through frames produces a single event at origin; (d) coercion follow-ups emit no per-attempt events; (e) payload shape matches spec; (f) dedup key works across propagation chains.
- **Lenses:** plan-lens-spec-coverage, plan-lens-validation

### `loom-system-note` delivery fallback chain unasserted
- **Description:** The spec mandates a three-tier fallback when `pi.sendMessage` fails: `ctx.ui.notify` → `loom/runtime/system-note-delivery-failed` → `console.error`. The renderer must be registered synchronously before the first scan. No leaf covers the fallback chain, the `system-note-delivery-failed` code, the re-entry guard, or the renderer-registration ordering rule.
- **Suggested fix:** Extend V18h (or add a sibling): (a) inject a `FakeExtensionAPI` whose `sendMessage` throws; assert `ctx.ui.notify` invoked; (b) both `sendMessage` and `notify` throw → `loom/runtime/system-note-delivery-failed` emitted; (c) all three fail → `console.error`; (d) instrument factory to assert `pi.registerMessageRenderer("loom-system-note", …)` precedes the first `resources_discover` scan.
- **Lenses:** plan-lens-spec-coverage, plan-lens-validation, spec-lens-assumptions

### V18i per-kind formatter: catch-all row, `last_tool_name=null`, chain recursion unasserted
- **Description:** V18i covers per-kind formatting and two-level chain attribution but does not cover: (a) the catch-all row firing for an unknown/future `kind`; (b) `tool_loop_exhausted` with `last_tool_name=null` rendering as the literal string `"respond"`; (c) chain-suffix recursion into deeper `InvokeCalleeError.inner` so a three-level cascade reports the leaf `kind`.
- **Suggested fix:** Extend V18i Tests: synthetic unknown `kind` produces catch-all line verbatim; `tool_loop_exhausted` with `last_tool_name=null` renders `"respond"`; three-level cascade attributes to the leaf `kind` and prints the full chain.
- **Lenses:** plan-lens-spec-coverage

### V18m / V18o: panic routing has no debug/verbose surface
- **Description:** Every interpreter-level bug gets converted into a one-line user-facing note with no stack, no panic origin, no crash log. Real defects are nearly impossible to triage from a user's bug report. There is no distinction between "spec-defined panic source" and "unexpected interpreter exception."
- **Suggested fix:** Split routing into "spec-listed panic source → system note as written" vs "unexpected exception → also written to `~/.pi/agent/looms/crash-<ts>.log` with full stack + distinct diagnostic." Add Tests bullets for both branches in V18m and V18n.
- **Lenses:** plan-lens-risk

### Binder cancellation checkpoint — no plan leaf
- **Description:** The spec (`cancellation.md`, `binder.md`) mandates checking `ctx.signal` immediately before the binder LLM call and forwarding the signal to the binder model's provider invocation. Both the initial attempt and the transport-failure retry honour the signal. A cancelled binder produces a specific system note. No V18 leaf covers this checkpoint, the during-call abort path, or the cancelled-binder note.
- **Suggested fix:** Add `V18b.1`: pre-binder abort → binder call never issued; mid-flight abort surfaces; retry suppressed if aborted; cancelled-binder system-note text matches spec; bypass-eligible looms do not emit the note.
- **Lenses:** plan-lens-spec-coverage

### `pi.registerCommand` argument-completions slot not wired; dynamic de-registration on collision not covered
- **Description:** The spec mandates `pi.registerCommand(name, { description, getArgumentCompletions, handler })`. H4 and M register with `description` and `handler` only; no leaf claims `getArgumentCompletions` is supplied. The spec also requires dynamic de-registration of a previously-registered loom whose slash name newly collides after a settings reload or another extension's activation — not covered by V14q or V18f.
- **Suggested fix:** Add a Tests bullet to H4 (or M): the `pi.registerCommand` call carries a `getArgumentCompletions` callback (V1 may return an empty list). Add a Tests bullet to V14q: a previously-registered loom whose name newly collides on a `session_start` re-evaluation is de-registered with `loom/load/cross-format-collision`.
- **Lenses:** plan-lens-spec-coverage

### V18f `/reload` re-run-of-factory not asserted
- **Description:** V18f Tests assert that adding a new `.loom` file emits the structural-change note and that "running `/reload` then registers it," but no bullet asserts that `/reload` actually re-runs the extension factory, or that the new `PiExtensionAPI` instance has an empty registration cache.
- **Suggested fix:** Add a Tests bullet: "After `/reload`, the factory is re-invoked exactly once; the new `PiExtensionAPI` instance has an empty `Map<schema-hash, registeredToolName>` cache; the previous instance is unreachable."
- **Lenses:** plan-lens-validation

---

## plan_topics/v18-cancellation.md (continued) — Cross-cutting coverage gaps

### `ToolDefinition` `label` derivation and `Type.Unsafe<unknown>` wrap — no test leaf
- **Description:** The spec mandates the `label` field on each loom's `ToolDefinition` is derived from the basename (`summarise.loom` → `"Summarise"`, `code-review.loom` → `"Code-Review"`); the synthesised respond tool's label is `"Loom typed-query response"`. The `parameters` field must be `Type.Unsafe<unknown>(loweredJsonSchema)`. In-`execute` AJV re-validation of `params` is also required (Pi's structural typing is not load-bearing). No leaf asserts any of these.
- **Suggested fix:** Add a Tests bullet to V14e (cross-link from V6i): label derivation per basename rule; respond-tool label is `"Loom typed-query response"`; `parameters` is `Type.Unsafe<unknown>(loweredJsonSchema)`; `execute` AJV-validates `params` before forwarding.
- **Lenses:** plan-lens-spec-coverage

### `TypeBox` package identifier mismatch — spec says `@sinclair/typebox`, Pi uses `typebox`
- **Description:** `spec_topics/pi-integration-contract.md` says `Type.Unsafe<unknown>` is "imported from the `@sinclair/typebox` peer dependency declared in `package.json`." The installed Pi SDK uses `typebox` (single word, `v1.1.x`); `package.json` declares `"typebox": "^1.1.24"`. `@sinclair/typebox` is a different, unrelated npm package whose version series never reached `1.x`. Implementing per the spec text will import the wrong package, producing incompatible lowered `parameters` artefacts and breaking the registration-cache dedup hash assumptions.
- **Suggested fix:** Change every spec and plan reference of `@sinclair/typebox` to `typebox`. The `package.json` peerDependencies block is already correct.
- **Lenses:** spec-lens-codebase-grounding-broad, spec-lens-doc-alignment-broad

### Plan tests cite "spec's exact wording" / "verbatim" without verifying spec owns each message string
- **Description:** Pervasive across V1d, V2c, V11b/c, V13e, V16i, V16l–p, V3c, M: tests assert "verbatim from spec" or "spec's exact-wording errors." The spec's `diagnostics.md` registry lists `code`, `Trigger`, `Spec rule`, and `Hint`, but not rendered `message` strings for most rows. Tests requiring verbatim message equality will fail unless the spec is amended to include canonical message strings.
- **Suggested fix:** Audit the spec for canonical message strings; either move them into a single normative table all leaves can cite, or weaken the affected tests to "diagnostic includes the named tokens" rather than "verbatim." Otherwise these tests will block on spec amendments not currently scheduled.
- **Lenses:** spec-lens-assumptions

### `bind_echo: true` severity inconsistency across spec pages
- **Description:** `spec_topics/binder.md` calls `bind_echo: true` on a no-params loom a "parse warning." `spec_topics/frontmatter.md`, plan V3c, and plan V16k all call it a "load warning," and the diagnostic code (`loom/load/bind-echo-without-params`) is namespaced `load`. One spec page disagrees with the rest.
- **Suggested fix:** Fix `binder.md` to say "load warning" consistent with the code's `loom/load/` namespace, the other spec page, and both plan leaves. (Alternatively rename the code to `loom/parse/bind-echo-without-params` everywhere, but this is a larger spec change.)
- **Lenses:** spec-lens-consistency

### `ModelToolError` forward-referenced in plan but no leaf implements it
- **Description:** V5g Deps parenthetical says "`model_tool` V14." No V14 leaf adds `ModelToolError`, its detection during the query tool-loop, or its test vectors. The coverage matrix row for QueryError failure modes has no closing leaf for `model_tool`. The spec (`errors-and-results.md`, `query.md`) declares `ModelToolError` as a first-class variant. The plan also never uses the type name, always the wire kind string.
- **Suggested fix:** Add a leaf (e.g. between the CodeToolError leaves and V14j, or extend V6i) that adds `ModelToolError` to the runtime union, populates `tool_call_id`/`raw_response`, and tests propagation through both the original turn and a coercion follow-up. Update V5g's forward reference, the coverage matrix, and standardise on the PascalCase type name in plan prose.
- **Lenses:** spec-lens-consistency, spec-lens-naming

### QueryError variant names inconsistently use wire kind strings vs. type names
- **Description:** The plan uses PascalCase type names for `CodeToolError`, `InvokeInfraError`, `InvokeCalleeError`, `MatchError`, `ValidationIssue` but wire kind strings (`transport`, `validation`, `cancelled`, `context_overflow`, `tool_loop_exhausted`) for five other spec-defined types (`TransportError`, `ValidationError`, `CancelledError`, `ContextOverflowError`, `ToolLoopExhaustedError`). The spec explicitly distinguishes the two forms and assigns PascalCase type names to all nine variants.
- **Suggested fix:** Use PascalCase type names in Adds prose and leaf titles for all nine QueryError variants; reserve wire kind strings for code examples and JSON shapes.
- **Lenses:** spec-lens-naming

---

## plan_topics/h1-scaffold.md (engineering hygiene — scope note)

### H1 scaffolds engineering hygiene without spec basis
- **Description:** H1 adds Prettier, GitHub Actions workflow, `depcheck`, a per-directory presence test, and `no-static-state.test.ts` — none traceable to a spec rule. They trace to `conventions.md` project-level conventions. The `depcheck` Ships-when gate makes them blocking.
- **Suggested fix:** Keep, but acknowledge as plan-convention rather than spec-traced (tag items "convention" so V18o's coverage gate does not look for non-existent REQ-IDs). Consider dropping `depcheck` from the Ships-when gate so plan progress is not blocked on tooling the spec does not require.
- **Lenses:** plan-lens-spec-fidelity

---

## Summary table

| Category | Count |
|---|---|
| Coverage gaps (spec obligations with no plan step) | 28 |
| Clarity / ambiguity in plan text | 25 |
| Consistency / contradictions across leaves | 15 |
| Validation / "done" condition unobservable | 14 |
| Risk / missing mitigation | 12 |
| Traceability / structural issues | 10 |
| Ordering / dependency declaration gaps | 7 |
| Atomicity (leaf too large or too hollow) | 8 |
| Codebase grounding (wrong API / package) | 5 |
| Scope creep / fidelity | 4 |
| **Total (approximate)** | **~128** |
