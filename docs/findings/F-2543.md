---
id: F-2543
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/governance/req-id-prefix-table-active-a.md, anchor: "GOV-1 *Dual-form layout* / GOV-3 anchor" }
  - { path: docs/spec_topics/hard-ceilings/ceilings-3-and-4.md, anchor: "page title / `## Interaction between ceilings` / `## Ceiling-set invariants`" }
  - { path: docs/spec_topics/governance/stable-inline-labels.md, anchor: "GOV-16 *stable inline label*" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [naming]
title: "\"Ceilings 3 and 4\" page undersells four-ceiling ownership"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`hard-ceilings/ceilings-3-and-4.md` is titled and filenamed for two ceilings (#3 and #4), yet the page owns the full cross-ceiling material: CIO-1 … CIO-6 (covering all four ceilings, including CIO-2 for ceiling #1 and CIO-4 for ceiling #2) and the four-ceiling *Ceiling-set invariants*. A reader seeking the four-ceiling interaction order or the set-level invariants would not expect to find them under a page named for only two ceilings.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:**
- Stem collision (vector 1): `dual-form layout` predates the collision (introduced `ce1af28`, 2026-05-06, "resolve GOV-1 leaves anchor placement ambiguous"); the colliding `dual-anchor convention` / `dual-anchored` terminology entered later with the V1→loom 1.0 rename cluster (`e23f5ba` → `a4feafb` → `0c0434b`, 2026-05-27, GOV-25–GOV-29 authoring). The collision arose when the later family reused the `dual-` stem already held by GOV-1.
- "Ceilings 3 and 4" misnomer (vector 2): `f5e89f4` (2026-06-04, "enforce 100KB hard / 30KB recommended size cap on spec set"), which split the hard-ceilings page into the subdirectory+index layout and assigned the title/filename `ceilings-3-and-4.md` to the page that already carried the four-ceiling CIO interaction order and set-level invariants.
- GOV-3 missing short name (vector 3): present since GOV-3's inception relative to the parenthetical-short-name convention; peer REQ-IDs acquired short names progressively (e.g. the prefix-table rewrite `27ea061`, 2026-05-06) without GOV-3 ever being retrofitted.
- "stable inline label" abbreviation (vector 4): `c4ad105` (2026-05-08, "resolve HC3-a–HC3-e and NOCEIL-N labels escape GOV-3/GOV-8"), the commit that authored GOV-16 and introduced both the formal term and the undeclared bare form.

**History:** The four vectors entered through four unrelated commits across 2026-05 to 2026-06; there is no single introducing commit. The cluster is an accretion of independent naming choices, not a regression from one change.

## Solution approach

Rewrite the `# Ceilings 3 and 4` heading on `hard-ceilings/ceilings-3-and-4.md` so the title reflects that the page also owns the cross-ceiling interaction order and the set-level invariants. Keep the filename and the stable anchors (`#cio-1` … `#cio-6`, `#ceiling-interaction-order`, `#ceiling-set-invariants`, `#hc3`, `#ceiling-4-table`) so inbound cross-references continue to resolve.

## Solution constraints

- None.

## Relationships

- F-2542 "`dual-form` / `dual-anchor` terminology collision" — co-resolve
- F-2544 "GOV-3 lacks a parenthetical short name" — co-resolve
- F-2545 ""stable inline label" / "inline label" undeclared abbreviation" — co-resolve
