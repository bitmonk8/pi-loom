---
id: F-1730
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/errors-and-results/queryerror-variants.md, anchor: "Invoke variants* (`InvokeInfraError` schema, `#invoke-variants`)" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [naming]
title: "`InvokeInfraError` input- vs return-validation `cause` tokens are named asymmetrically"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

`InvokeInfraError.cause` carries two AJV-validation arms that name the same kind of failure with different conventions. The input-side arm — args/params failing the callee's input schema — is `"validation"` (unqualified). The return-side arm — a typed `invoke<T>`'s return value failing AJV — is `"return_validation"` (qualified with the side). Within a single enum partition the two related sub-causes therefore disagree on whether the side is part of the token: one is bare, one is prefixed.

The unqualified `"validation"` token is not arbitrary: it deliberately matches `CodeToolError.cause: "validation"` so that an author handling input-validation failures across both callee kinds (`Pi tool` and `.loom` callable) matches one token on both arms. `tool-calls.md` relies on this explicitly: an author handling every input-validation failure in one place must `match` both `CodeToolError { cause: "validation", ... }` and `InvokeInfraError { cause: "validation", ... }`. So the asymmetry is the price of a cross-variant alignment the spec intends.

The residual cost is intra-variant: a reader scanning the `InvokeInfraError` enum sees `"validation"` next to `"return_validation"` and has no in-place signal that the bare token is specifically the *input* side rather than a catch-all. The inline schema comment notes "(input side, like `CodeToolError.cause "validation"`)", but the asymmetry of qualification is never stated as intentional, so it reads as an oversight. The wire strings are fully pinned, so no implementation diverges; the harm is interpretive friction when authoring exhaustive `match` arms.

## Issue introduction

**Verdict:** later-commit-introduced (omission)
**Introducing commits:** `f7a505e` (2026-06-06, *"pi-loom spec: resolve 'cause validation opposite directions on two error types'"*).
**History:** The *Empty-tail callee compatibility* paragraph entered `spec_topics/invocation.md` in commit `52219c4` (2026-05-06, *"Address all 41 spec-review findings"*), originally with `reason: "validation"`. Commit `9bd29ab` (*"cause vs reason sub-discriminator fields inconsistent across error variants"*) renamed `reason` → `cause` in the same paragraph without touching the value. Commit `f7a505e` then introduced the dedicated `"return_validation"` arm to the `InvokeInfraError.cause` enum in `queryerror-variants.md`, repartitioned `"validation"` to mean input/params-side only, and updated the two sibling tables in `schema-subset.md` row #5 and `hard-ceilings/ceilings-3-and-4.md` row 5 to read `cause: "return_validation"` — but did **not** edit the *Empty-tail callee compatibility* paragraph in `invocation.md`, which describes the same return-side AJV path and continued to read `cause: "validation"`. The defect is the omission in `f7a505e`: the partition-introducing commit touched every other call site on the return-side arm and missed exactly this one.

## Solution Space

**Shape:** multiple

### Option A — Keep aligned tokens, make the asymmetry explicitly intentional

- **Approach:** Leave `cause: "validation"` (input) and `cause: "return_validation"` (return) unchanged. Promote the rationale from the inline schema comment into one explicit sentence adjacent to the `InvokeInfraError` schema: the unqualified `"validation"` arm is the input-side cause, named bare so it matches `CodeToolError.cause "validation"` and lets authors handle input-validation across both callee kinds with one token; only the return-side arm carries a side qualifier.
- **Spec edits:** `queryerror-variants.md` *Invoke variants* — add the rationale sentence; no token changes; no edits elsewhere.
- **Pros:** Non-breaking; preserves the deliberate cross-variant alignment between `CodeToolError` and `InvokeInfraError` input arms; single-file, single-sentence surface.
- **Cons:** The visual asymmetry remains; resolution depends on the reader noticing the note.
- **Risks:** None material.

### Option B — Symmetric rename of the input arm

- **Approach:** Rename the input arm to `"input_validation"`, keeping `"return_validation"`, so both arms are side-qualified. For the alignment property to survive, `CodeToolError.cause: "validation"` must be renamed to `"input_validation"` in lockstep.
- **Spec edits:** `queryerror-variants.md` (`InvokeInfraError` and `CodeToolError` enums plus the discriminator-alignment prose), `tool-calls.md` *Failures*, `invocation.md` *Failures*, `ceilings-3-and-4.md` ceiling-#4 table, `schema-subset.md` depth-enforcement table.
- **Pros:** Fully symmetric token naming within `InvokeInfraError`.
- **Cons:** Wide ripple across five-plus files; if `CodeToolError` is not renamed in lockstep it re-introduces two different tokens for input validation across the two variants — the exact cross-variant clash the schema deliberately avoids.
- **Risks:** Reintroducing a cross-variant naming inconsistency while fixing an intra-variant one.

### Recommendation

Option A. Preserve the cross-variant `"validation"` alignment between the `CodeToolError` and `InvokeInfraError` input arms — it is load-bearing for authors writing one `match` arm across both callee kinds — and add a one-sentence note stating that the asymmetry is intentional: the bare `"validation"` token is the input side, qualified only on the return side. The implementer must word the note so it explicitly covers both arms (input is bare to align with `CodeToolError`; return is qualified to disambiguate within the enum), not merely repeat the existing inline comment.

## Consequence

**Severity:** advisory

The wire discriminator strings are fixed by the schema declaration, so no implementer diverges on behaviour. The asymmetric qualification imposes a small interpretive burden: an author writing exhaustive `match` arms must learn that the bare `"validation"` token is the input side while only the return side is qualified, and the spec never states that the asymmetry is intentional.

## Relationships

- F-2205 "The same underlying concept - AJV schema validation rejecting a data payload - is expressed..." — decision-dependency (covers `ValidationError`/`CodeToolError`/`InvokeInfraError` `cause` tokens for AJV failure, including `InvokeInfraError.cause: "validation"`; a corpus-wide decision on whether to document or unify validation-cause tokens constrains this finding's input arm)
