---
id: F-2684
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/version-bump-steps-3-4.md, anchor: "step 3" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-triggers.md, anchor: "step 5 inbound-reference sweep; step 6 provider seed-field re-validation" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2b.md, anchor: "branch tie-breaks / reviewer-discretion clauses" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "editorial-review checklist items (i, u, aa, ab, …) and the bump-commit review-gate precondition home" }
lens: [spec-lens-assumptions]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [assumptions]
title: "State fixture-corpus existence as a precondition and name the initial-population owner"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
decision_axes: 3
---

## Problem

The provider-error re-run (item (i)), forced-tool re-run (item (u)), retry/cancellation re-run (items (aa)/(ab)) in `version-bump-step2.md`, and the provider seed-field re-validation (step 6 in `version-bump-triggers.md`) all run loom-side fixtures against "a maintained corpus" of captured bodies in the `npm test` suite. The corpus's maintenance owner is named, but its initial-population owner and its existence are assumed. A re-run against an empty or partial corpus passes vacuously, so a real provider-side regression can land undetected.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** 6798428 — resolve "Step 3 engines.node test describes mutually exclusive right-hand operands" (2026-06-05, Thomas Andersen); c0259ca — resolve "Version-bump procedure: four obligations have no verifiable acceptance criterion" (2026-06-07, Thomas Andersen); e785f77 — resolve "Version-bump procedure six clarity/scope gaps (T061)" (2026-06-07, Thomas Andersen); 2ecf650 — resolve "Re-validating provider-owned behaviour has no enumerated data source or owner" (2026-06-05, Thomas Andersen)
**History:** Each of the four preconditions is an omission baked into its section when that section was authored, not a regression a later commit introduced into previously-correct text. The three-way `engines.node` equality was reworked at `6798428` without ever adding an absent-field arm; the `src/`/`test/`/`docs/` grep superset and its self-justification were authored under the acceptance-criteria pass at `c0259ca` without stating a top-level-root invariant; the step-2(b) reviewer-discretion tie-breaks (`e785f77`) presupposed a bump-commit review gate that no companion commit ever established; and the "maintained corpus" re-validation language (`2ecf650`) named a maintenance owner but never an initial-population owner or existence precondition. Subsequent targeted edits to each file touched adjacent concerns but never closed these gaps.

## Solution approach

For each of the re-run obligations — item (i), item (u), items (aa)/(ab) in `version-bump-step2.md`, and step 6 in `version-bump-triggers.md` — state corpus existence as a precondition of the obligation and name the initial-population owner that first captures each corpus. Add a non-empty / minimum-coverage guard so a re-validation gate cannot report green against an empty or partial corpus. The Provider error mapping *Re-validation gate* is the home for the provider-error guard.

## Solution constraints

- None.

## Relationships

- F-2263 "Define absent-`engines.node` behaviour at the step-3 floor gate" — co-resolve
- F-2682 "State the grep-root invariant for the step-5 inbound-reference sweep" — co-resolve
- F-2683 "Establish (or reassign) the bump-commit review-gate precondition" — co-resolve
