---
id: F-0758
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/errors-and-results/queryerror-variants.md, anchor: "### Notes" }
lens: [spec-lens-cruft]
tier: nit
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [cruft]
title: "ERR-14 relocation HTML changelog comment in `queryerror-variants.md`"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`docs/spec_topics/errors-and-results/queryerror-variants.md` carries a single HTML comment in the `### Notes` section (line 209):

```html
<!-- ERR-14 (`ValidationIssue` ordering) relocated to ### Query-time variants, co-located with the ValidationError schema, for discoverability. Anchors carried verbatim. -->
```

The comment records a one-off editorial move (ERR-14 was lifted out of `### Notes` and re-anchored alongside the `ValidationError` schema in `### Query-time variants`). It has no normative content, points the reader nowhere, and — being an HTML comment — is invisible in rendered output, so it cannot even function as a forward-link breadcrumb. Once the relocation commit lands the rationale is captured by version control; carrying it inline as a permanent left-behind serves only future archaeologists.

## Issue introduction

**Verdict:** single-commit introduction
**Introducing commit:** `c3ea1d1` — *pi-loom spec: resolve "ERR-14 ValidationIssue ordering buried in Notes"* (2026-06-04)
**History:** `git log -S "ERR-14 (\`ValidationIssue\` ordering) relocated" -- docs/spec_topics/errors-and-results/queryerror-variants.md` returns exactly one commit. The diff shows ERR-14 being moved upward to sit beside the `ValidationError` schema while the originating `Notes` slot was backfilled with the HTML comment in the same hunk — a deliberate breadcrumb left by the relocation patch rather than a stray remnant from an earlier edit.

## Solution Space

**Shape:** single

### Recommendation

Delete line 209 of `docs/spec_topics/errors-and-results/queryerror-variants.md`:

```html
<!-- ERR-14 (`ValidationIssue` ordering) relocated to ### Query-time variants, co-located with the ValidationError schema, for discoverability. Anchors carried verbatim. -->
```

Remove only the comment line; leave the surrounding paragraphs in `### Notes` untouched. The two `<a id="…">` anchors (`#validation-issue-ordering`, `#err-14`) already travel with ERR-14 in `### Query-time variants`, so no link in the corpus needs to chase the comment for resolution.

## Consequence

**Severity:** cosmetic

Pure editorial residue. No reader-observable behaviour, no implementer divergence, no test impact. Left in place it accretes alongside future relocations and dilutes the rule that source-tree commentary on past edits belongs in commit messages, not the spec.

## Relationships

- "queryerror-variants.md — title/link label `Queryerror` vs type name `QueryError`" — same-cluster (same file, independent edit)
- "schema-subset.md — HTML comment embedded mid-sentence in normative prose" — same-cluster (same defect class — orphaned HTML comment as cruft — different file)
