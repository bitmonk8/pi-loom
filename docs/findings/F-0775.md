---
id: F-0775
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/runtime-event-channel.md, anchor: "best-effort fallback paragraph" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "editorial-review checklist items (f)–(ak)" }
lens: [spec-lens-assumptions]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [assumptions]
title: "`runtime-event-channel.md` — `pi.sendMessage` synchronous-only failure surface lacks a re-validation hook"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The best-effort fallback paragraph in `runtime-event-channel.md` asserts `pi.sendMessage` returns `void` (synchronous), forbids the runtime from `await`-ing it or attaching a `.catch` handler, and routes its fallback chain (`ctx.ui.notify` → `loom/runtime/system-note-delivery-failed` → `console.error`) over synchronous throws only. None of the SHOULD-level editorial-review items `(f)`–`(ak)` in `version-bump-step2.md` re-validates that return type or the absence of a deferred-delivery failure channel against each candidate Pi minor, even though every other Pi-behavioural premise this page builds on has a named re-validation item. A future Pi minor that retyped `sendMessage` to `Promise<void>` — or kept `void` but began surfacing deferred-delivery failures via an asynchronous rejection or a side channel — would bypass the synchronous-only fallback entirely and silently degrade the always-log exactly-once guarantee for both group-A `RuntimeEvent`s and group-B panic diagnostics, with no in-band operator signal and no conformance-test surface.

## Issue introduction

**Verdict:** present-since-inception (with later structural reinforcement)

**Introducing commits:**

- `11b9199` (2026-05-04, "pi-loom spec: resolve \"pi.sendMessage failure leaves system notes silently dropped\"") — introduced the best-effort fallback chain itself; the original text framed the fallback as triggering on "any thrown or rejected value", so synchronous-only-ness was not yet asserted but was also not pinned.
- `29c29b2` (2026-05-06, "pi-loom spec: resolve \"pi.sendMessage is synchronous (void); spec treats it as returning a Promise\"") — added the synchronous-only assertion verbatim ("`pi.sendMessage` returns `void` (synchronous); the runtime MUST NOT `await` it and MUST NOT attach a `.catch` handler. The best-effort fallback below covers synchronous throws only.") and rewrote the fallback to drop the "rejected" arm. This is the commit that fixed `sendMessage`'s failure surface as synchronous-only without routing the behavioural premise to a re-validation gate.
- `257c545` (2026-06-05, "pi-loom spec: resolve \"Host presuppositions lack version-bump-procedure re-audit hooks\"") — built the editorial-review checklist machinery that hosts items `(f)`–`(ak)`; multiple sibling presuppositions on the same page acquired re-validation hooks here, but the `sendMessage` synchronous-failure-surface assumption was not added to the checklist in this pass and has remained unhooked since.

**History:** The defect is *present-since-inception* in the structural sense — the spec has always taken Pi's synchronous-throw failure surface as a flat fact — and `29c29b2` is the commit that crystallised it as a verbatim assertion. The structural omission (no re-validation hook) became reviewable only after `257c545` established the checklist pattern that should have caught it; subsequent re-audit passes added items `(f)`–`(ak)` to that checklist but did not include this presupposition, so the gap is a consistent omission across the re-audit campaign rather than a single-commit regression. The fix therefore targets the omission, not any specific commit.

## Solution approach

Add a new SHOULD-level editorial-review item to the checklist in `docs/spec_topics/pi-integration-contract/version-bump-step2.md` that re-validates `pi.sendMessage`'s failure-surface contract against each candidate Pi minor — confirming both that the declared return type stays `void` and that no deferred-delivery / asynchronous-rejection failure channel (a `Promise<void>` retype or a separate failure-callback / buffered-queue surface) has appeared — carrying the same SHOULD-level escalation clause its siblings from `(f)` onward carry. Add a forward-link from the best-effort fallback paragraph in `runtime-event-channel.md` to the new item by anchor, parallel to how the `convertToLlm` paragraph already forward-links item `(r)` at `#bump-checklist-convert-to-llm-context-entry`.

## Solution constraints

- None.

## Relationships

- "runtime-event-channel.md — custom-message context-entry future `SHOULD` opt-out for non-existent field" — same-cluster (both audit the `sendMessage` / `CustomMessage` surface against future Pi minors; the cited finding asks to *remove* a forward-looking SHOULD, this one asks to *add* a re-validation hook — they touch the same paragraph cluster but resolve independently)
- "runtime-event-channel.md — best-effort fallback re-entry guard temporal scope ambiguous" — same-cluster (both edits land on the same best-effort fallback paragraph; resolve independently)
- "version-bump-step2.md — preamble SHOULD range \"(f) through (aj)\" excludes item (ak)" — decision-dependency (adding a new item `(al)` interacts with the preamble's range literal; both edits MUST extend the range in lockstep, and `V18c`'s Ships-when sentence likewise)
- "version-bump-step2.md — items (f)–(ak) repeated escalation boilerplate (~31×) / no triage" — same-cluster (a new item inherits the same boilerplate the cited finding flags; if that finding's resolution refactors the boilerplate, this item rides the refactor)
