---
id: F-1963
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec.md, anchor: "**schema slug** entry — rename-history sentence" }
lens: [spec-lens-cruft]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [cruft]
title: "After listing synonyms to avoid, the entry adds: \"The rename is purely terminology — the..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

After listing synonyms to avoid, the entry adds: *"The rename is purely terminology — the on-the-wire tool names (`__loom_respond_<actual-16-hex-chars>` and `__loom_callee_<actual-16-hex-chars>__<name>`) and the `<slug>` placeholder token in diagnostic registry rows and template bodies are unaffected."* This is rationale for a past rename decision, not a normative constraint on an implementer.
