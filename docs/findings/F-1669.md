---
id: F-1669
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/overview.md, anchor: "H1 \"Overview and Conceptual Model\"" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [naming]
title: "Near-identical spec-topic filenames: `overview.md` vs `overview-and-orientation.md`"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

Two distinct, substantive spec-topic files sit side by side in `docs/spec_topics/` with names that differ only by a trailing `-and-orientation`:

- `docs/spec_topics/overview.md` — titled **"Overview and Conceptual Model"**. It is the conceptual-model document (Code and Model, Scope of a Loom File). It is the file registered in the REQ-ID prefix table (`overview.md | (no IDs — narrative)`) and the file the Reading-order list in `language-and-architecture.md` links to ("Overview and Conceptual Model").
- `docs/spec_topics/overview-and-orientation.md` — titled **"Overview and orientation"**. It is the larger, implementer-facing orientation document (Prerequisites, Scope, Non-functional requirements, Hard-ceilings overview). It is **absent** from the REQ-ID prefix table and is not in the Reading-order list, yet it is the most heavily cross-referenced of the two (≈24 inbound references across the corpus and plan, versus ≈6 for `overview.md`).

Both files open with an identical `## Overview` heading and near-identical first sentences, so the only signal distinguishing them is the filename suffix. A maintainer following a cross-reference, or grepping for "the overview page", cannot tell from the filename alone which document carries which role, and risks editing the wrong file (e.g. appending orientation content to the conceptual-model page). The confusion is compounded by sibling findings in which GOV-30/GOV-31 prose labels `overview-and-orientation.md` targets as "`spec.md`".

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `fecb504` (2026-05-04, original spec-topic split) and `f5e89f4` (2026-06-04, 100KB/30KB size-cap split), compounded by `81bf52a` (2026-06-11, `spec/` → `spec_topics/` collapse)
**History:** `fecb504` created `spec_topics/overview.md` with the title `# Overview and Conceptual Model` as part of the original monolithic-`spec.md` split, at which point that page was the only "Overview"-titled page in `spec_topics/`. `f5e89f4` then enforced the 100KB hard / 30KB recommended size cap by splitting the root `spec.md` into three section pages, one of which — `spec/overview-and-orientation.md`, titled `# Overview and orientation` — duplicated the leading "Overview and …" stem; at that point the collision existed across two directories (`spec/` vs `spec_topics/`). `81bf52a` then collapsed `spec/` into `spec_topics/` to restore the single-delegation-target architecture, content-preservingly, which co-located the two "Overview and …" pages in the same directory. No single commit authored the collision; it is the cumulative effect of the size-cap split inheriting an "Overview and orientation" label without checking it against the pre-existing `overview.md` title, followed by the directory collapse that moved them next to each other.

## Solution Space

**Shape:** multiple

### Option A — Rename `overview.md` to `conceptual-model.md`

- **Approach:** Rename `docs/spec_topics/overview.md` → `docs/spec_topics/conceptual-model.md` and retitle its H1 to "Conceptual Model"; update the REQ-ID prefix-table row and the Reading-order entry; sweep the ≈6 inbound references.
- **Spec edits:** File rename + H1 retitle; `req-id-prefix-table-active-b.md` row `overview.md` → `conceptual-model.md`; `language-and-architecture.md` Reading-order link text/target; repoint the `M-minimal-slash-command` / `M-T-minimal-slash-command` plan Spec-field citations.
- **Pros:** Lowest blast radius (≈6 inbound references). Makes the conceptual-model role legible from the filename. Leaves the heavily-cited `overview-and-orientation.md` and its GOV-30/GOV-31 lock-step links untouched.
- **Cons:** Leaves the long `overview-and-orientation.md` name in place; the `spec.md`-vs-orientation labelling concern in the sibling GOV-31/GOV-23 findings still needs its own fix.
- **Risks:** A stray inbound link to `overview.md` left un-swept would 404; the corpus grep is small, so the risk is low.

### Option B — Rename `overview-and-orientation.md` to `spec-orientation.md`

- **Approach:** Rename `docs/spec_topics/overview-and-orientation.md` → `docs/spec_topics/spec-orientation.md` and retitle its H1 to "Spec Orientation"; register it in the prefix table and Reading-order; sweep the ≈24 inbound references (including GOV-30/GOV-31 source-set links, the occurrences index, `H1a`, and `plan.md`).
- **Spec edits:** File rename + H1 retitle; add a prefix-table row and Reading-order entry; repoint ≈24 inbound references across spec topics and the plan; repoint `H1a`'s "Node version floor" citation.
- **Pros:** Marks the primary implementer entry point explicitly as the spec orientation document; closes the prefix-table/Reading-order registration gap as part of the same edit.
- **Cons:** Largest blast radius (≈24 inbound references, several inside dense GOV-30/GOV-31 prose), maximising the chance of a missed repoint.
- **Risks:** Renaming the most-cited orientation page touches GOV-30 lock-step prose and the occurrences index; a missed link breaks navigation on a core surface.

### Recommendation

Take **Option A** — rename `overview.md` → `conceptual-model.md`. It resolves the name collision with the smallest cross-reference sweep (≈6 vs ≈24 inbound references) and leaves the heavily-cited orientation page and its GOV lock-step links intact. Watch for inbound links that say "overview" generically; grep the corpus and the plan for `overview.md` (anchored, to avoid matching `overview-and-orientation.md`) before considering the sweep complete. The `overview-and-orientation.md`-is-labelled-`spec.md` concern is a separate fix tracked by the related GOV-31 finding.

## Consequence

**Severity:** advisory

The two files are functionally distinct but their names do not signal which is which, so a maintainer can navigate to or edit the wrong document, and cross-references are easy to misdirect. No runtime behaviour diverges and implementers can still produce a working system; the cost is navigational and editorial-correctness risk concentrated on the heavily-cited orientation page.

## Relationships

- F-2216 "GOV-31 labels its integer-count source sets as " `spec.md` - ..." though the content is on orientation pages" — decision-dependency (whether `overview-and-orientation.md` is renamed under Option B determines whether GOV-31's source-set labels/targets must be repointed to a new filename)
- F-2064 "GOV-23 governs a " `spec.md` Session Model section" that is not on `spec.md`" — same-cluster (shares the unresolved "what does `spec.md` denote post-split" convention question; resolves independently against `session-model-and-appendix.md`)
- F-2222 "`schema-subset.md` points to `spec.md` (overview) for the " full per-boundary routing-class table," but that table is not there" — same-cluster (also turns on the ambiguous "`spec.md` (overview)" reference; resolves independently)
