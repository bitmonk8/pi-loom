---
id: F-2784
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9i-subagent-isolation.md, anchor: "Adds / Tests (PIC-22 parallel-spawn clause)" }
  - { path: docs/plan_topics/V9i-T-subagent-isolation.md, anchor: "Tests (PIC-22)" }
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "PIC-22 mapping row" }
  - { path: docs/plan.md, anchor: "V9 leaf listing" }
lens: [plan-lens-step-atomicity]
tier: medium
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: reduced
category: [step-atomicity]
title: "V9i bundles the N≥2 parallel-spawn path (PIC-22) — extract into its own leaf"
relations: []
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

`V9i` bundles `PIC-22` — the N≥2 parallel-spawn concurrency path that dispatches `createAgentSession` for all N callables before any returns via `Promise.all` — into the single-session lifecycle. `PIC-22` carries its own blocking-fake `AgentSession` test harness and ships/tests independently of the single-session spawn/dispose path. Folding it into `V9i` pushes the leaf past the atomicity threshold, inflating the diff and degrading reviewability and the per-finding fix loop's convergence.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `7a3b907` (2026-06-10, "resolve \"subagent parallel-initiation MUST has no closing leaf\""); `e690bb0` (2026-06-23, "fix F-1226 … mint PIC-23 + coverage-matrix/V9i cascade"); `3253d16` (2026-06-25, "fix F-1346 — coin PIC-40..43 on subagent.md + map to V9i coverage")
**History:** At creation (`c6a664e`, 2026-06-10) `V9i` carried a coherent single-session lifecycle (`PIC-9`, the isolation contract, model-guard / dispose-failure diagnostics, ≈3 obligations). The breadth accreted across later fixes: `7a3b907` (same day) added the `PIC-22` parallel-spawn path; `e690bb0` added `PIC-23`; `3253d16` coined `PIC-40`–`PIC-43` and mapped them onto the `V9i` coverage row. An earlier split (`6d7661d`) removed the swallowing-handler concern to `V9o` but did not re-evaluate the remaining surface, so the cumulative additions pushed the leaf past the atomicity threshold.

## Solution approach

Extract the `PIC-22` parallel-spawn path out of `V9i` and `V9i-T` into a new paired implementation/tests leaf whose `Deps` include `V9i`: move the `PIC-22` Tests bullet out of `V9i.Tests` and `V9i-T.Tests`, and the parallel-spawn clause out of `V9i.Adds`, and strike `PIC-22` from the `V9i.Adds` `PIC` anchor enumeration. Retarget the `PIC-22` mapping in `coverage-matrix.md` from the `V9i` row to the new leaf's row. Link the new implementation leaf into the V9 leaf listing in `docs/plan.md`.

## Solution constraints

- `subagent.md`'s `PIC-22` anchor and the spec are read-only for this fix (fix surface is plan-only).
- New leaf IDs follow the `V<N><letter>` scheme and the paired implementation/`-T`-tests ritual in `docs/plan_topics/conventions.md`; do not reuse a retired ID.

## Relationships

- "V9i bundles completion-detection + result-extraction (PIC-42/PIC-43) — extract into its own leaf" — same-cluster (sibling split-child; resolves independently)
