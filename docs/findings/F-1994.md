---
id: F-1994
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/extension-bootstrap-and-per-loom.md, anchor: "extension-bootstrap-and-per-loom.md — Extension-bootstrap SDK failures" }
lens: [spec-lens-error-model]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [error-model]
title: "`details.error: <error.message>` notation contradicts its own reconciling sentence"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

Error-payload content for the five `loom/load/extension-bootstrap-failed` bullets. - **What is missing:** Internally consistent statement of the `details.error` field content. Each of the five bullets writes `details: { …, error: <error.message> }`, but the closing paragraph states "Every `details.error` field in the rules above carries the caught throw's underlying-error string rather than a raw `.message` access." The `<error.message>` placeholder in the bullets reads as the very `.message` access the reconciling sentence forbids.
