---
id: F-1712
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/glossary.md, anchor: "top-level entries list" }
  - { path: docs/spec_topics/overview.md, anchor: "Query-and-Await section" }
  - { path: docs/spec_topics/query.md, anchor: "page title and intro sentence" }
  - { path: docs/spec_topics/query/query-forms.md, anchor: "Schema inference rules / algorithm prose" }
  - { path: docs/spec_topics/query/query-escapes-stringification.md, anchor: "\"Inside a query template:\"" }
  - { path: docs/spec_topics/query/query-tool-loop.md, anchor: "opening free-phase user turn description" }
  - { path: docs/spec_topics/lexical.md, anchor: "string-literals, comments, stray-backslash bullets" }
  - { path: docs/spec_topics/influences.md, anchor: "TypeScript-surface and Original-to-loom bullets" }
  - { path: docs/spec_topics/grammar.md, anchor: "Template interpolation entry" }
  - { path: docs/spec_topics/implementation-notes.md, anchor: "Chevrotain grammar lineage + per-query bullets" }
  - { path: docs/spec_topics/descriptions.md, anchor: "multi-line dedent reference" }
  - { path: docs/spec_topics/frontmatter/frontmatter-fields-a.md, anchor: "`params:` defaults sub-language exclusions" }
  - { path: docs/spec_topics/frontmatter/frontmatter-fields-b-and-templates.md, anchor: "`system:` stringification + parser-entry-point + seam note + interpolation paragraph" }
  - { path: docs/spec_topics/future-considerations/surface-extensions.md, anchor: "`system:` seam anchor note" }
  - { path: docs/spec_topics/diagnostics/code-registry-parse.md, anchor: "`literal-newline-in-string`, `illegal-template-escape`, `unterminated-template`, `empty-template` rows" }
  - { path: docs/spec_topics/diagnostics/code-registry-load.md, anchor: "`typed-query-unsupported-provider` row" }
  - { path: docs/spec_topics/diagnostics/placeholder-rendering-a.md, anchor: "`Result<T, E>` interpolation rule" }
  - { path: docs/spec_topics/pi-integration-contract/conversation-drive.md, anchor: "typed-query provider-compatibility paragraph" }
  - { path: docs/spec_topics/slash-invocation.md, anchor: "SNK-b row" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: reduced
category: [naming]
title: "Query primitive — no canonical term coined, three synonyms in free variation"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The `@`...`` query primitive is referred to by three phrases in free variation — **query template** (bolded as the coined term in `overview.md`'s Query-and-Await section), **query expression** (the opener of `query.md` and the schema-inference walk in `query/query-forms.md`), and bare **query** (the `query.md` page title) — with no governing rule selecting a canonical form. The glossary coins canonical terms with MUST-use entries for `callable set`, `schema slug`, and `.loom callable`, but carries no entry for this primitive even though its `type sink` entry already leans on the undefined term "query expression". A reader cannot tell whether "query expression" is a deliberate sub-distinction or unmanaged drift, and the glossary's coin-once / list-synonyms convention that grounds the existing grep gates does not extend to the most cross-cut primitive in the language.

## Issue introduction

**Verdict:** present-since-inception (with synonym-three added at split)
**Introducing commits:**
- `53f5831` (2026-05-03) — *spec: rewrite query primitive, system prompts, and schema syntax*. Coined the `@`...`` primitive and introduced both **`query template`** (bold, in the Conceptual Model section) and the bare lowercase **`query expression`** (in "The response schema flows into the query expression from any of the following type contexts") in the same commit. The two synonyms were therefore in free variation from the moment the primitive was named.
- `b4fbfac` (2026-05-04) — *pi-loom spec: resolve "no central glossary"*. Created `spec_topics/glossary.md` with eight initial entries (including `type sink`, which already leans on "query expression"); the primitive itself was not given an entry, so the canonical-term convention the glossary later enshrined (`callable set`, `schema slug`, `.loom callable`) skipped over it.
- `fecb504` (2026-05-04) — *Split spec.md and plan.md into per-topic / per-phase files*. Split the query material into `spec_topics/query.md` (page title "Query") and added "the `@`...`` query template is the only construct…" as its opener. The split is what turned the bare page-title **Query** into a third reference form: the page title now reads as a candidate canonical term distinct from both `query template` and `query expression`.

**History:** The defect was present at the construct's coining (`53f5831`) — `query template` and `query expression` shipped together in the same commit, neither flagged as canonical and neither cross-referenced. The glossary-introduction commit (`b4fbfac`) had the opportunity to lock in a canonical term while writing the convention for analogous coinages but did not. The topic split (`fecb504`) introduced the third synonym (the page title `Query`). No later commit has consolidated the three; the original `spec-review` cycle's resolution `bb841d4` ("Glossary type sink defined query-only") refined the surrounding `type sink` entry but did not anchor the primitive it depends on.

## Solution approach

Coin `query template` as the canonical term for the `@`...`` primitive and govern it through the glossary as `callable set`, `schema slug`, and `.loom callable` are governed: add a glossary entry alphabetised at `q`, modelled on the existing `schema slug` entry, stating the MUST-use rule, listing `query expression` and bare `query` as spec-prose synonyms to avoid, and citing `query.md` as the canonical page. Update `overview.md`'s bolded coining in the Query-and-Await section to point at the new glossary entry. Sweep the noun-phrase uses of the primitive across the cited sites to the canonical term.

## Solution constraints

- The rename governs only the noun-phrase form of the primitive. Wire identifiers (`loom/parse/typed-query-*` and `typed-query-unsupported-provider` diagnostic codes, `__loom_respond_<slug>`, `tool_loop`, `respond_repair`, `QueryError`, `ContextOverflowError`), the `typed-query` / `untyped-query` adjective compounds, and verb uses ("to query the model") MUST remain unchanged.

## Relationships

- F-1707 "QRY-19 (discarded query result parse rule) on the wrong page" — same-cluster (the relocated QRY-19 will be reworded under the canonical term; resolves independently)
- F-1640 "Naming-convention MUSTs in glossary (`callable set`, `schema slug`, `.loom callable`)" — decision-dependency (proposes moving naming-convention MUSTs out of the glossary into `governance.md`; if that lands first, the new `query template` entry follows the relocated convention rather than the current glossary one)
