---
id: F-1967
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec.md, anchor: "upstream sibling pin described as tilde, but `@earendil-works/pi-coding-agent` uses caret (semantica" }
lens: [spec-lens-codebase-grounding-broad]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [codebase-grounding-broad]
title: "upstream sibling pin described as tilde, but `@earendil-works/pi-coding-agent` uses caret (semantically equivalent)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

- **Spec reference:** `docs/spec_topics/pi-integration-contract/host-prerequisites.md`, *Pi SDK pin* item 1: "The lock-step minor-version line is inherited from `@earendil-works/pi-coding-agent`'s own `dependencies` block, which pins `@earendil-works/pi-agent-core`, `@earendil-works/pi-ai`, and `@earendil-works/pi-tui` at the same **`~X.Y.Z` minor-version line**." - **Codebase reality:** `@earendil-works/pi-coding-agent/package.json` `dependencies` pins those three siblings with the **caret** operator: `"^0.75.5"` (not tilde).
