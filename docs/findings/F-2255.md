---
id: F-2255
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "editorial-review checklist (items `(a)`–`(aq)`) and the \"2. Re-run the SDK surface-inventory tests\" sub-steps `(a)`/`(b)`" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2b.md, anchor: "`step 2(a)`/`step 2(b)` references" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-triggers.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-intro.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/pi-integration-contract/inventory-audit-intro.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/pi-integration-contract/audit-target-categories.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/pi-integration-contract/audit-wire-and-canary.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/pi-integration-contract/registration-steps.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/pi-integration-contract/subagent.md, anchor: "`step 2(a)` references" }
  - { path: docs/spec_topics/overview-and-orientation.md, anchor: "`step 2(a)` reference" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [naming]
title: "`(a)`/`(b)` label collision between the step-2 editorial-review checklist and the SDK surface-inventory test sub-steps"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

Within the single "step 2" context of `version-bump-step2.md`, parenthesised Latin letters label two distinct, independently-coined things:

1. The **editorial-review checklist** items, lettered `(a)` through `(aq)`. Item `(a)` is the *Extension-instance survival across session-only `session_shutdown`* open question (anchor `bump-checklist-instance-survival`); item `(b)` is *Multi-`session_shutdown` delivery permitted to a single instance*; and so on.
2. The **SDK surface-inventory test** sub-steps under the numbered "2. Re-run the SDK surface-inventory tests" item, which PIC pins as `(a)` *Positive direction — literal-read of the imported namespace* (anchor `bump-step-2-positive`) and `(b)` *Negative direction* (anchor `bump-step-2-negative`).

The phrase `step 2(a)` is the spec's shorthand for the seven-capability literal-read assertion (concept 2). It occurs 112 times across ten spec files, almost always inside the very checklist items `(f)`–`(aq)` whose own labels belong to scheme 1 (e.g. each item closes with "escalates to a build-time pin … on the same footing as the seven-capability literal-read assertion under step 2(a) above"). A reader sitting inside the checklist therefore encounters bare `(a)`/`(b)` (a checklist item) and `step 2(a)`/`step 2(b)` (an SDK-test sub-step) in the same paragraph, two unrelated referents drawn from the same parenthesised-letter alphabet under the same step number. The checklist itself carries no step number of its own, so the "step 2" prefix does not disambiguate scheme 2 from scheme 1 — both are step-2 content.

The defect is a naming collision in a shared label namespace, not an undefined reference: nearly every `step 2(a)` site spells out "the seven-capability literal-read assertion", and the checklist items are cited as "item (a)" rather than "step 2(a)", so context resolves the intended referent in practice. The cost is interpretive burden and a standing risk of mis-citation as the checklist grows.

## Issue introduction
**Verdict:** multi-commit-interaction
**Introducing commits:** `e5cd514` (2026-05-09, "pi-loom spec: resolve \"T35 SDK capability inventory closed-set negative-direction audit\""), `57d8235` (2026-05-11, "pi-loom spec: resolve \"T23 per-session slash-handler serialisation citation\"")
**History:** `git log -S` traces the two label schemes to two commits two days apart. `e5cd514` first introduced the SDK surface-inventory test's `(a)` *Positive direction* / `(b)` *Negative direction* lettering and the `step 2(a)`/`step 2(b)` shorthand (`git log -S 'Positive direction' --reverse` and `-S 'bump-step-2-positive'` both bottom out there). `57d8235` then introduced the *Editorial-review checklist for unpinned host presuppositions* with lettered items `(a)`–`(e)` in the same step-2 context (`git log -S 'Editorial-review checklist' --reverse`; `git show 57d8235` confirms the lettered checklist preamble was added there). Neither commit alone is defective: each label scheme is locally coherent. The collision is the interaction — both reuse parenthesised Latin letters inside step 2 without a disambiguating prefix. Later commits (down to `27a023e`, 2026-06-27) grew the checklist to `(aq)` and multiplied the `step 2(a)` references to 112 across ten files, aggravating the shared-namespace overlap but not introducing it.

## Solution Space

**Shape:** multiple

### Option A — Descriptive direction names, no letter

**Approach:** Drop the `(a)`/`(b)` letters from the SDK-test sub-steps and refer to them by direction only, leaning on the existing word-based anchors `bump-step-2-positive` / `bump-step-2-negative`. Every `step 2(a)` becomes "step 2 positive direction" (or "the SDK surface-inventory literal-read assertion") and `step 2(b)` becomes "step 2 negative direction".

**Spec edits:** Rename the two sub-step headings to drop the letter; rewrite all 112 `step 2(a)` references and the `step 2(b)` references (including `step 2(b) branch (N)` forms) to the descriptive phrasing across the ten files listed above.

**Pros:** Maximally self-describing at every call site; no parenthesised label remains to collide with the checklist alphabet.

**Cons:** Verbose — `step 2(b) branch (2)` becomes "step 2 negative-direction branch (2)" at every one of its many sites; large prose-churn across ten files.

**Risks:** Mechanical rewrite of 112+ sites risks missing an occurrence, leaving mixed phrasings mid-corpus.

### Option B — Roman-numeral sub-labels for the SDK-test directions

**Approach:** Relabel the two SDK-test sub-steps from `(a)`/`(b)` to `(i)`/`(ii)`, reserving Latin letters exclusively for the editorial-review checklist. `step 2(a)` → `step 2(i)`, `step 2(b)` → `step 2(ii)`.

**Spec edits:** Change the two sub-step headings (keeping the `bump-step-2-positive` / `bump-step-2-negative` anchors as-is); rewrite the `step 2(a)`/`step 2(b)` references across the ten files to `step 2(i)`/`step 2(ii)`. The `step 2(b) branch (N)` forms become `step 2(ii) branch (N)` — the branch numbering stays Arabic `(1)`–`(5)`, so directions (Roman), checklist items (Latin letters), and branches (Arabic) occupy three visually distinct alphabets.

**Pros:** Minimal token change at each site; the three coexisting label kinds become unambiguous by alphabet alone; word-based anchors are untouched.

**Cons:** Still requires touching all 112+ reference sites, though each edit is a one-token swap.

**Risks:** Same missed-occurrence risk as Option A, but smaller diff per site lowers it.

### Recommendation

Take **Option B**. The SDK-test directions are the smaller, more contained set (two labels with already-descriptive anchors), so relabelling them rather than the 43-item checklist is the lower-churn side, and Roman numerals separate the three label kinds — directions `(i)`/`(ii)`, checklist items `(a)`–`(aq)`, step-2(ii) branches `(1)`–`(5)` — by alphabet without verbose phrasing. Edge case for the fixer: `step 2(b)` carries a `branch (N)` sub-structure cited extensively (`step 2(b) branch (2)`, `branch (5)`); rewrite the prefix to `step 2(ii)` while leaving the Arabic branch numbers intact, and sweep every file in the Spec Documents list (not just `version-bump-step2.md`) so no `step 2(a)`/`step 2(b)` spelling survives.

## Consequence

**Severity:** advisory

A version-bump contributor reading the editorial-review checklist meets two unrelated referents for `(a)` and `(b)` in the same step-2 namespace, raising the risk of citing or resolving the wrong one. Because nearly every `step 2(a)` site also names "the seven-capability literal-read assertion" and the checklist items are cited as "item (a)", a careful implementer can still build the correct gates; the harm is interpretive burden and citation fragility rather than divergent behaviour.

## Relationships

- F-2252 "The entire checklist (items a-aq) is never assigned a step number in the overall 1-7 procedure." — decision-dependency (assigning the checklist its own step number would move its `(a)`–`(aq)` letters out of step 2's namespace, partly resolving this collision; the chosen numbering constrains how the labels are disambiguated)
- F-2256 "Editorial-review checklist has no resolvable step number / position" — decision-dependency (same positioning/numbering decision; resolving it interacts with how the `(a)`/`(b)` overlap is removed)
