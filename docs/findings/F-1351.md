---
id: F-1351
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/diagnostic-emission-isolation.md, anchor: "teardown console.error per-emission try/catch; serialiser-throw fallbacks; self-wrap; handler-isolation swallow; invocation-site count" }
lens: [spec-lens-traceability]
tier: high
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [traceability]
title: "Coin `PIC-N` anchors on `diagnostic-emission-isolation.md` (un-anchored normative MUSTs)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

The teardown-time `console.error` isolation MUSTs on `diagnostic-emission-isolation.md` (enumerated as `cka-29`) — the per-emission `try`/`catch` wrap of the serialisation-and-emission sequence, the bare-`code` / two-token / three-token serialiser-throw fallback forms, the construction-site self-wrap, the handler-isolation swallow obligation, and the invocation-site count semantics — carry no `PIC-N` REQ-ID anchor. GOV-9's `#prefix-n` cross-link contract is therefore unsatisfiable for these obligations. This page is one instance of the systemic PIC residue and resolves independently.

## Issue introduction

**Verdict:** present-since-inception-with-progressive-extension
**Introducing commits:**
- `c1e5d64` (2026-05-05) — registered the per-page REQ-ID prefix table and deferred per-rule coinage on pages not in the five-page Phase 12a pass (foundational PIC deferral).
- `6d8bea5` / `d4ac81b` (2026-06-04) — drained partway by coining `PIC-3`…`PIC-15`; deliberately incomplete.
- `f5e89f4` (2026-06-04) — split the 576 KB monolith into 30 child files, preserving anchors but coining none.
- `4f588e0` (2026-06-03) — added GOV-22, codifying per-commit progressive drainage.

**History:** The gap is steady-state under GOV-22. The monolith split fanned the residue across 30 files at once; combined with H5e's closed-set load at H6a, the residue cannot remain steady-state through to release, so the defect requires a one-shot drain rather than passive wait for incidental edits.

## Solution approach

Add the next free `PIC-N` REQ-ID anchor at each defining obligation site enumerated above on `diagnostic-emission-isolation.md`, in GOV-1 dual-form, per GOV-22's progressive-coinage obligation. Query the current PIC high-water mark on `governance.md` at edit time to allocate the numeric tails under the registered `PIC` prefix. Narrow the `cka-29` coverage-matrix enumeration to a closed-under-`PIC-N` reference once the sites are anchored.

## Solution constraints

- None.

## Relationships

- F-1344, F-1345, F-1346, F-1347, F-1348, F-1349, F-1350 (sibling per-page PIC-N coinages) — same-cluster
- F-1333 "Compound MUST at diagnostic-shape.md:20" — same-cluster (DIAG-prefix analogue on a different diagnostics page; same GOV-22 discipline)
