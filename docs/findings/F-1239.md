---
id: F-1239
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9b-registration-reload-wiring.md, anchor: "Adds" }
lens: [spec-lens-clarity]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [clarity]
title: "V9b Adds parenthetical lists six items for \"registration steps 1–5\""
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`V9b`'s **Adds** opens with "Registration steps 1–5" followed by a parenthetical that enumerates **six** semicolon-separated items:

1. `registerFlag('loom')` → walk
2. pending-registration list
3. `session_start` collision + `registerCommand`
4. `session_shutdown` subscribe
5. watcher + `LoomRegistry` build-aside-publish swap
6. the step-5 structural-change `loom-system-note`

The count of items (6) does not match the stated range (5 steps). The authoritative spec (`registration-steps.md`) defines exactly five numbered steps, with the structural-change `loom-system-note` belonging to step 5 (it is specified inside that step's *Structural changes* paragraph, not as a sixth step). Items 5 and 6 of the parenthetical are therefore both step 5, but the prose gives no explicit cue: the reader must infer the merge from the trailing item's "step-5" qualifier. An implementer scanning the parenthetical cannot tell at a glance whether the list has a sixth step the prefix forgot, or whether two items collapse into step 5.

## Issue introduction

**Verdict:** single-introducing-commit
**Introducing commit:** `b722ce7` (2026-06-17) — "pi-loom plan: resolve \"registration-steps.md has normative MUSTs but no coverage-matrix row\""
**History:** The plan corpus is git-tracked. `git log -S 'step-5 structural-change' -- docs/plan_topics/V9b-registration-reload-wiring.md` and the corresponding `git show b722ce7` identify `b722ce7` as the commit that appended the sixth item ("the step-5 structural-change `loom-system-note`") to the parenthetical. The immediately prior revision (`44a96f5`, 2026-06-16) carried a clean five-item list — `registerFlag → walk; pending list; session_start collision + registerCommand; session_shutdown subscribe; watcher + swap` — matching the "steps 1–5" prefix exactly. `b722ce7` added the structural-change note as a sixth parenthetical item without reconciling the "1–5" count; the count mismatch is a side effect of that edit.

## Solution Space

**Shape:** single

### Recommendation

In `V9b`'s **Adds**, rewrite the opening parenthetical so each listed item is explicitly bound to its step number and the structural-change note is folded into step 5 rather than dangling as a sixth item. Concretely, replace the current parenthetical body

`(`registerFlag('loom')` → walk; pending-registration list; `session_start` collision + `registerCommand`; `session_shutdown` subscribe; watcher + `LoomRegistry` build-aside-publish swap; the step-5 structural-change `loom-system-note`)`

with a step-labelled enumeration whose items number exactly five, e.g.:

`(step 1 `registerFlag('loom')` → walk; step 2 pending-registration list; step 3 `session_start` collision + `registerCommand`; step 4 `session_shutdown` subscribe; step 5 watcher + `LoomRegistry` build-aside-publish swap, including the structural-change `loom-system-note`)`

The fix is confined to the Adds prose of `V9b`; no Tests, Deps, Ships-when, or spec edits are required, and the step numbering must match `registration-steps.md`'s five numbered steps.

## Consequence

**Severity:** advisory

A reader of `V9b`'s Adds alone is briefly unable to map the six parenthetical items onto the five named steps. Because the cited spec unambiguously fixes the step count at five and places the structural-change note inside step 5, an implementer who consults `registration-steps.md` still produces a correct leaf; the defect is a self-contained enumeration ambiguity, not a divergence in what gets built.

