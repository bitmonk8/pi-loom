---
id: F-2227
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/session-only-degraded-state.md, anchor: "§\"Predicate split\" → \"Seam minimality (normative)\" paragraph and its two follow-on bullets" }
lens: [spec-lens-prescription]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [prescription]
title: "Seam-minimality rule constrains internal variable structure instead of the observable predicate outcomes"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The `**Seam minimality (normative).**` paragraph in `session-only-degraded-state.md` §"Predicate split" forbids the runtime from introducing any handler-scoped state seam beyond `pinnedConstantReadOk` and `capturedEventReason` — banning a `branchTakenWasClosedSet` flag, per-sub-step success roll-ups, a separate `!pinnedConstantReadOk` disjunct, and `"<unreadable>"` sentinel-string equality checks. These constrain the implementation's internal variable inventory, not any externally observable behaviour: an implementation that added a redundant internal flag or recomputed the same predicate via a different local decomposition, while producing identical `runtime-degraded` emission counts and tag transitions, would be non-conformant under the literal rule yet observably indistinguishable from the prescribed form. The genuinely behavioural invariants the rule protects — emission gated on whether the handler-entry snapshot read succeeded, and discrimination against the handler-captured reason rather than a fresh `event.reason` read — are already observable and already asserted by acceptance criteria (d) and (e).

## Issue introduction
**Verdict:** single-commit-introduction
**Introducing commits:** `52b9101` (2026-05-11, "pi-loom spec: resolve \"T24 — Fork-reason watcher closure leaves the extension in an unspecified, silently degraded state\"")
**History:** The spec corpus is git-tracked. `git log -S 'MUST NOT introduce any handler-scoped state seam'` and `git log -S 'branchTakenWasClosedSet'` (run both over the file path and `--all` across the spec tree) resolve to a single non-review introducing commit, `52b9101`, which authored the entire session-only degraded-state branch into the then-monolithic `docs/spec_topics/pi-integration-contract.md`. The seam-count prohibition entered the corpus together with the branch it governs — it was not added by a later edit. The doc-split commit `f5e89f4` (2026-06-04, "docs: enforce 100KB hard / 30KB recommended size cap on spec set") relocated the text verbatim into `session-only-degraded-state.md` via `split_spec.py` but did not alter the rule; `git blame` attributes the current lines to that mechanical split, with the substantive text tracing back through it to `52b9101`.

## Solution approach

Rewrite the `**Seam minimality (normative).**` paragraph in §"Predicate split" to demote the seam-count prohibition and instead state the two observable invariants it stands in for: that the `loom/host/session-shutdown-runtime-degraded` emission is gated on whether the handler-entry snapshot read succeeded (the condition named `pinnedConstantReadOk`), and that the session-only triplet membership test is evaluated against the handler-entry captured reason rather than a fresh `event.reason` access at the discriminator site. Drop the MUST-level ban on additional internal seams (`branchTakenWasClosedSet`, per-sub-step roll-ups, a `!pinnedConstantReadOk` disjunct, sentinel-string equality checks). Any retained explanatory sentence about the two seams must carry no normative modal.

## Solution constraints

- Preserve the asymmetry pinned by acceptance criteria (d), (e), and (f): the broad tag-transition outcome and the narrow emission outcome are distinct and the restatement MUST NOT collapse the two predicates into one.
- Out of scope: the `pinnedConstantReadOk` / `capturedEventReason` seam definitions on `unknown-reason-rule.md` (owned by F-1610).

## Relationships

- F-1610 "handler-scoped variables pinned as cross-clause seams" — decision-dependency (governs the same `pinnedConstantReadOk` / `capturedEventReason` seams on `unknown-reason-rule.md`; the decision to demote variable-level prescription to observable behaviour must be reconciled across both sites, since this rule's prohibition references those seams by name)
- F-2142 "Internal `LoomRegistry` method surface made normative despite being declared non-observable" — same-cluster (identical over-prescription-of-internal-structure pattern on the adjacent drain-state surface; resolves independently)
- F-1685 "PIC-30 is stated entirely in implementation terms - it prohibits specific internal fields and..." — same-cluster (same "prohibit internal fields/gates rather than the observable outcome" pattern on `drain-state-contract.md`; resolves independently)
