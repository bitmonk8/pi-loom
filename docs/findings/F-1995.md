---
id: F-1995
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/frontmatter/frontmatter-fields-a.md, anchor: "The Binder-model root-word convention sub-paragraph reads: \"The binder-model concept uses the..." }
lens: [spec-lens-placement]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [placement]
title: "The Binder-model root-word convention sub-paragraph reads: \"The binder-model concept uses the..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The `Binder-model root-word convention` sub-paragraph reads: "The binder-model concept uses the short root `bind_` in its frontmatter field name (`bind_model`) and the long root `binder` in its settings key (`looms.binderModel`), diagnostic codes (e.g. `loom/load/binder-model-unresolved`), section anchors (e.g. `## Binder model` in [Slash-Command Argument Binding]), and running prose ('binder model')." This is a cross-surface naming convention governing how a single concept is spelled across five distinct spec surfaces (frontmatter field, settings key, diagnostic codes, anchors, prose).
