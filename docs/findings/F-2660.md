---
id: F-2660
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/tool-registration-lifetime.md, anchor: "`## Acceptance criteria — PIC-17 active-set install vector`" }
  - { path: docs/spec_topics/pi-integration-contract/tool-registration-lifetime.md, anchor: "consequences bullet list / `recovery-mutex-runtime-contract` block" }
lens: [spec-lens-testability, spec-lens-error-model, spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [testability, error-model, scope]
title: "PIC-17 finally-restore exception/cancellation acceptance criterion"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

PIC-17 step 4 places the `pi.setActiveTools(snapshot)` restore in a `finally` so that cancellation, panic, and provider exceptions all preserve the active-set invariant. The `## Acceptance criteria — PIC-17 active-set install vector` section asserts only the happy-path install vectors (a) and (b); no criterion verifies the restore actually runs after the protected body throws or is cancelled. A regression that dropped the restore from `finally` would pass every stated criterion.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** f5e89f4 — docs: enforce 100KB hard / 30KB recommended size cap on spec set (2026-06-04, Thomas Andersen); 19b193b — resolve "PIC-17 active-set swap/snapshot throws have no failure-mode rule" (2026-06-05, Thomas Andersen); 823ce54 — resolve "Recovery-mutex acquisition semantics and teardown-budget interaction undefined" (2026-06-06, Thomas Andersen); 8008b91 — fix F-1217: add acceptance-criteria/fixture blocks for four Shard-08 MUSTs (2026-06-25, Thomas Andersen)
**History:** The PIC-17 step-4 `finally` invariant and the PIC-8 restore-failure protocol were carried into this file at its first commit (f5e89f4, the size-cap split); 19b193b added the PIC-19 setup-failure protocol and 823ce54 authored the recovery-mutex contingent contract inline in the consequences bullet — each adding a failure-path MUST without a fixture. The gap crystallised at 8008b91, which added the `Acceptance criteria — PIC-17 active-set install vector` section but scoped its fixtures to the happy-path install vectors (a)/(b) only, never extending coverage to the exception path or the PIC-8/PIC-19 protocols. The defect is the interaction: acceptance-criteria coverage did not keep pace with the failure-path protocols, and the contingency block was never separated from must-build prose.

## Solution approach

Add a criterion (c) to `## Acceptance criteria — PIC-17 active-set install vector` asserting that when the protected query body throws or the turn is cancelled, `pi.setActiveTools(snapshot)` is invoked with the step-1 snapshot and the user session's prior active set is observably restored, with the inner failure or cancellation surfaced rather than masked. Cover both the throw and the cancel sub-cases, which transit the same step-4 `finally`.

## Solution constraints

- Out of scope: PIC-8 / PIC-19 failure-protocol acceptance criteria, owned by F-2661.

## Relationships

- F-2661 "PIC-8 / PIC-19 failure-protocol acceptance criteria" — co-resolve
- F-2662 "Separate the recovery-mutex contingent contract from must-build content" — co-resolve
