---
id: F-2205
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/errors-and-results/queryerror-variants.md, anchor: "Query-time variants (`ValidationError`), Code-side tool-call variant (`CodeToolError`), Invoke variants (`InvokeInfraError`)" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [naming]
title: "AJV schema-validation rejection is named asymmetrically across `QueryError` `cause` arms"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

A single underlying failure — AJV (or the depth walk) rejecting a data payload — is given two different `cause` token names depending on which `QueryError` variant carries it. In `ValidationError` the AJV-rejection arm is `cause: "schema_validation"` (contrasting, within the same `kind: "validation"`, with `cause: "empty_template"`). In `CodeToolError` the equivalent input-rejection arm is `cause: "validation"`, and in `InvokeInfraError` the input-side arm is likewise `cause: "validation"` (its comment even reads "input side, like CodeToolError.cause 'validation'").

The result is that the same observable event — schema validation rejected this payload — is keyed as `"schema_validation"` on one variant and `"validation"` on two others. An author writing handling that pattern-matches on `.cause` across `QueryError` variants must memorise that the validation-failure token is spelled differently for `ValidationError` than for the tool-call and invoke variants. The divergence is deliberate at the `ValidationError` site (the longer token disambiguates the AJV arm from the `empty_template` arm), but nothing in the spec records that rationale at the point of definition, so the asymmetry reads as an inconsistency rather than an intentional choice.

## Issue introduction
**Verdict:** single-commit
**Introducing commits:** 64f30b4 — pi-loom spec: add ValidationError.cause field (schema_validation | empty_template) (2026-05-06, Thomas Andersen)
**History:** The bare `cause: "validation"` token for the tool-call/invoke variants is the older of the two spellings, present since the tool-call error variant was first introduced. `ValidationError` originally had no `cause` field at all — it was a single undifferentiated `kind: "validation"` variant. Commit 64f30b4 added the two-arm `cause: "schema_validation" | "empty_template"` partition and chose `schema_validation` for the AJV-failure arm to contrast with `empty_template` within the same `kind`, even though the sibling variants already named the same AJV-rejection concept `validation` — that commit's own added prose flags the sibling pattern ("consistent with the established `CodeToolError.cause` … patterns"). The asymmetry was introduced at that point and carried verbatim through the f5e89f4 size-cap split that moved the union into `queryerror-variants.md`.

## Solution Space

**Shape:** multiple

### Option A — Record the deliberate asymmetry at the definition site

- **Approach:** Keep all three tokens as-is and add a one-sentence note adjacent to the `ValidationError.cause` definition stating that `"schema_validation"` is the qualified spelling specifically to contrast with the sibling `"empty_template"` arm within `kind: "validation"`, and that it denotes the same AJV-rejection concept that the tool-call and invoke variants key as `"validation"`.
- **Spec edits:** Add the clarifying sentence in the `cause` prose block under *Query-time variants* in `docs/spec_topics/errors-and-results/queryerror-variants.md`. No wire-token changes; no edits outside this file.
- **Pros:** Non-breaking; no callsite churn across `query*.md`; preserves the within-`ValidationError` two-arm legibility the token was chosen for.
- **Cons:** The cross-variant token still differs; authors still memorise two spellings, now with a documented reason rather than none.
- **Risks:** Minimal — purely additive prose.

### Option B — Unify the AJV-rejection token to `"validation"`

- **Approach:** Rename `ValidationError.cause: "schema_validation"` to `"validation"` so all three variants share one token for AJV rejection, and rename the sibling arm `"empty_template"` to a non-colliding name (e.g. `"empty_query"` or `"blank_template"`) so the two arms stay disjoint.
- **Spec edits:** Update the `ValidationError` schema and `cause` prose in `queryerror-variants.md`, plus every `schema_validation` / `empty_template` callsite in `query.md`, `query/query-forms.md`, and `query/query-failure-and-repair.md`, and any glossary mention.
- **Pros:** One token for one concept across all variants; eliminates the cross-variant learning burden.
- **Cons:** Wide-surface rename touching multiple topic files and tests; renames a token that has been stable since 64f30b4.
- **Risks:** Missed callsite leaves a dangling `schema_validation` / `empty_template` reference; conformance fixtures asserting the old strings must be updated in lock-step.

### Recommendation

Take **Option A**. The two-arm partition on `ValidationError` is the reason the longer token exists; documenting that intent resolves the finding without the wide rename and callsite risk of Option B. Watch one edge case: keep the note co-located with the `cause` definition (not buried in *Notes*) so an author reading the schema sees the cross-variant equivalence immediately.

## Consequence

**Severity:** advisory

Implementers transcribe the wire tokens verbatim, so no runtime behaviour diverges. The cost falls on loom authors writing cross-variant `cause` handling, who must learn that the AJV-rejection token is `"schema_validation"` on `ValidationError` but `"validation"` on `CodeToolError` / `InvokeInfraError`; the unrecorded rationale makes the split look accidental and invites a "fix" that breaks one site or the other.

## Relationships

- F-1730 "The two InvokeInfraError.cause values for validation failures are named asymmetrically: the..." — same-cluster (sibling `cause`-token naming issue on the same `QueryError` surface — `validation` vs `return_validation` — but a distinct axis that resolves independently)
