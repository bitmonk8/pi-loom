---
id: F-0833
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "step-2 preamble sentence" }
lens: [spec-lens-clarity (shard-10)]
tier: high
factors:
  blast_radius: narrow
  centrality: core
  core_premise_blocker: no
status: reduced
category: [clarity (shard-10)]
title: "Step-2 preamble SHOULD range \"(f) through (aj)\" excludes item (ak)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The step-2 preamble sentence of `docs/spec_topics/pi-integration-contract/version-bump-step2.md` scopes its SHOULD-level audit and MUST-level recording obligation to items `(f) through (aj)` — twice in that form and once as the dash form `(f)–(aj)`. Item `(ak)` (`bump-checklist-user-visible-streaming-ordering`) sits below `(aj)` in the same enumeration and is itself a SHOULD-level audit with an escalation clause identical to the surrounding items. The preamble's recording rule ("omitting the row is non-conformant") therefore reads `(ak)` out of scope while item `(ak)`'s own SHOULD shape reads it in, admitting two non-equivalent conformant bump commit messages. Downstream plan leaf `V18c` already mandates per-item outcomes for the full `(a)`–`(ak)` range, so the narrower preamble under-specifies the obligation `V18c` reads off it.

## Issue introduction

**Verdict:** single-commit-introduction
**Introducing commit:** `85fc906` — *pi-loom plan: resolve "streamed-token-before-waitForIdle() ordering has no version-bump checklist item"* (2026-06-10)
**History:** `git log -S "(f) through (aj)" -- docs/spec_topics/pi-integration-contract/version-bump-step2.md` and `git log -S "through (ai)" -- docs/spec_topics/pi-integration-contract/version-bump-step2.md` walk the preamble range across its rewrites. Commit `9ced9f1` (2026-06-08, *subagent transcript-discard not routed to bump checklist*) added item `(aj)` and correctly extended the preamble range from `(f) through (ai)` to `(f) through (aj)` in the same edit. Commit `85fc906` then added item `(ak)` two days later (the streaming-ordering presupposition) but did not extend the preamble range in the same edit — `git show 85fc906 -- docs/spec_topics/pi-integration-contract/version-bump-step2.md` shows only the `(ak)` row insertion and a one-line `(aj)` cross-reference update, with the preamble sentence at line 3 untouched. The defect is therefore a co-edit miss in commit `85fc906`: the `(aj)`-adding commit had set the co-edit pattern (extend the preamble range alongside any new tail item), and the `(ak)`-adding commit broke it. The closing-paragraph mandatory-expansion gate already names this category of obligation ("Any future presupposition added to this page ... MUST be added to this checklist in the same edit") but does not call out the preamble-range edit as part of the same-edit obligation, which is the seam the miss slipped through.

## Solution approach

Rewrite the step-2 preamble sentence's three range references — both `(f) through (aj)` occurrences and the `(f)–(aj)` dash form — to extend through `(ak)` (`(f) through (ak)` and `(f)–(ak)` respectively).

## Solution constraints

- Out of scope: the `Baseline acquisition` paragraph's `(aj)` cross-reference list (items `(e)`, `(r)`, `(aj)`) — that list names `dist/*.js` body-read items and `(ak)` is not one, so do not extend it to `(ak)`.

## Relationships

- "version-bump-step2.md — item (f)–(ak) escalation trigger "once Pi exposes a typed contract …" undefined and actor-less" — same-cluster (touches the same `(f)`–`(ak)` tail block but a different concern: the escalation trigger's actor/action, not the preamble range)
- "version-bump-step2.md — items (f)–(ak) repeated escalation boilerplate (~31×) / no triage" — same-cluster (also scoped to the `(f)`–`(ak)` block, addresses repetition rather than range arithmetic)
- "version-bump-step2.md — no merge-gate disposition for a recorded `fail` on items (f)–(aj)" — decision-dependency (uses the same incorrect `(f)–(aj)` range in its own description; once this finding's fix lands, that finding's prose should be re-read against the corrected `(f)–(ak)` range so its proposed disposition sentence covers `(ak)` too)
- "version-bump-step2.md — baseline `dist/*.js` body-read items (r)/(aj) lack a drift-handling fallback" — same-cluster (concerns `(aj)` specifically as a `dist/*.js` body-read item; not affected by the range fix because `(ak)` is not a body-read item)
