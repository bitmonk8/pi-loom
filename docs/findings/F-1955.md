---
id: F-1955
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/conventions.md, anchor: "§Sequential by default — `// allow-sync:` comment set has no formal na" }
lens: [spec-lens-naming]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [naming]
title: "§Sequential by default — `// allow-sync:` comment set has no formal name"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

conventions.md names three allow-list comment sets (lint allow-list, per-site allow-list, ambient-access allow-list) but leaves the `// allow-sync:` set unnamed; H2a Adds coins "exemption allow-list" for it, a plan-owned term absent from the governing rule.

**Suggested fix:** Name the `// allow-sync:` set in conventions.md (e.g. "blocking-runtime allow-list") and use that name in H2a.
