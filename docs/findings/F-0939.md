---
id: F-0939
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9i-T-subagent-isolation.md, anchor: "Tests, Deps" }
lens: [spec-lens-placement]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [placement]
title: "V9i's ERR-8 live-carrier witness (and its `V4c` dep) live only in the implementation leaf, not in the paired `V9i-T` tests task"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

The coverage matrix credits `V9i` as a closing leaf for the live-surface re-assertion of ERR-8 (a mid-stream cancellation inside the real subagent `AgentSession` must not mutate that session's committed turns — the confirmation `V4c` defers from the `H4a` double). `V9i-subagent-isolation.md` carries this as a Tests bullet and lists `V4c` in its `Deps`, but its paired tests task `V9i-T-subagent-isolation.md` carries neither: `V9i-T`'s Tests list has no ERR-8 delegated-live-carrier bullet and its `Deps` omit `V4c`. Under the per-phase TDD ritual the tests task lands first and writes the failing tests for every REQ-ID the feature introduces, so a developer working `V9i-T` from its own page authors no ERR-8 witness; the `V9i` green-test gate then closes the matrix cell without any test ever exercising cancellation against a real subagent `AgentSession`.

## Issue introduction

**Verdict:** single-commit introduction
**Introducing commit:** `7bd65a7` ("pi-loom plan: resolve \"V4c/V4f defer ERR-8 to ERR-13 live-surface confirmation to downstream owners that never re-assert it\"", 2026-06-18)
**History:** `git log -S 'delegated live-carrier witness'` and `git log -S '`V3d`, `V4c`, `H4b`'` both resolve to `7bd65a7` against `V9i-subagent-isolation.md`. That commit added the ERR-8 delegated-live-carrier bullet and the `V4c` dep to the `V9i` implementation leaf, and edited `V4c`, `V4f`, `coverage-matrix.md`, `V13c`, `V14a`, `V15a` in the same change — but `git show --stat 7bd65a7 -- docs/plan_topics/V9i-T-subagent-isolation.md` is empty: the paired tests task was never touched. The asymmetry entered the corpus when the co-witness obligation was pushed onto the impl leaf and the matrix while the tests task was left behind.

## Solution approach

In `docs/plan_topics/V9i-T-subagent-isolation.md`, add a Tests bullet asserting the ERR-8 delegated live-carrier witness, mirroring the obligation already stated on the paired `V9i` leaf, and add `` `V4c` `` to `V9i-T`'s `Deps`. The edit closes the gap by giving the tests task the authoritative red-test source on its own page.

## Solution constraints

- Leave `V9i`'s existing ERR-8 Tests bullet and `V4c` dep in place — close the gap by adding the red-test source on the `V9i-T` side, not by relocating it off the impl leaf.
- Out of scope: `V9i`'s Ships-when surface (owned by F-0930).

## Relationships

- F-0944 "V11h-T omits V2c that V11h declares (incomplete test-side dep)" — same-cluster (identical class: a `-T` leaf missing a `Deps` edge the paired impl leaf declares; resolves independently)
- F-0930 "V9i — Ships when undersells the asserted surface" — same-cluster (same leaf pair; concerns whether `V9i`'s Ships-when surfaces the ERR-8 assertion, resolves independently of the tests-task gap)
- F-0504 "V4b — NOCEIL-3 panic-suppression carve-out defined only in the V4b-T Tests field" — same-cluster (impl/`-T` Tests-parity defect in the opposite direction; resolves independently)
