---
id: F-1931
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9l-session-only-degraded-branch.md, anchor: "`Tests` field" }
  - { path: docs/plan_topics/V9l-T-session-only-degraded-branch.md, anchor: "`Tests` field" }
lens: [spec-lens-traceability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [traceability]
title: "`V9l` last `Tests` bullet bundles two cka-27 obligations but asserts only one"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The fourth (last) `Tests` bullet of `V9l` — mirrored verbatim in its paired
`V9l-T` tests task — cites two distinct, independently-verifiable cka-27 spec
clauses from `session-only-degraded-state.md` (§Seam minimality and §Inline
triplet is normative), but its only assertable predicate ("no handler-scoped
state seam is introduced") covers the first clause alone. The §Inline triplet
is normative clause — requiring the closed set `{"new","resume","fork"}` to be
inlined at the emission site rather than read off the
`SessionShutdownEvent['reason']` snapshot's `literals` field — has no test
bullet asserting it. The cka-27 coverage-matrix row already treats the two as
separate obligations, so an implementation that reads the closed set off the
snapshot's `literals` field violates the inline-triplet requirement yet passes
the V9l suite green.

## Issue introduction

**Verdict:** introduced-by-commit
**Introducing commits:** `55ed974` (2026-06-12, "pi-loom plan: resolve \"degraded-state obligations green over open spec contradiction\"")
**History:** The plan corpus is git-tracked. `git log --follow` on
`docs/plan_topics/V9l-session-only-degraded-branch.md` shows only two commits:
`55ed974` (creation) and `9c42f2d` (2026-06-16, §Blocked-obligations/`Deps`
rework). `git show 55ed974:…V9l…` confirms the collapsed
"§Seam-minimality + *Inline triplet is normative* … no handler-scoped state
seam is introduced" bullet was present in its current form at the leaf's
inception, when the degraded-state branch was lifted out of `V9h` into the new
`V9l`/`V9l-T` pair; `git show 9c42f2d` confirms the later commit did not touch
the `Tests` field. One day earlier, `bad3b99` (2026-06-11) authored the
`cka-27` coverage-matrix row enumerating the *Seam-minimality* prohibition
**and** the *Inline triplet is normative* clause as two distinct obligations.
The bundling defect therefore entered at `55ed974`: the new V9l test bullet
under-asserted relative to the cka-27 row that already distinguished the two
obligations.

## Solution approach

Split the fourth `Tests` bullet into one bullet per obligation, each carrying
its own assertable predicate. Keep a seam-minimality bullet asserting no
handler-scoped state seam beyond `pinnedConstantReadOk` and
`capturedEventReason` is introduced, and add an inline-triplet bullet asserting
the closed set `{"new","resume","fork"}` is sourced inline at the emission site
and that reading it off the `SessionShutdownEvent['reason']` snapshot's
`literals` field fails the assertion. Apply the same split to both
`V9l-session-only-degraded-branch.md` and
`V9l-T-session-only-degraded-branch.md`.

## Solution constraints

- The `Tests` fields of `V9l` and `V9l-T` must remain byte-identical to each
  other, as they are today.
- Keep the existing `(un-anchored; GOV-22 residue)` parentheticals as-is —
  their removal is owned by a separate finding.

## Relationships

None
