---
id: F-1931
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9l-session-only-degraded-branch.md, anchor: "`Tests` field" }
  - { path: docs/plan_topics/V9l-T-session-only-degraded-branch.md, anchor: "`Tests` field" }
lens: [spec-lens-traceability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [traceability]
title: "`V9l` last `Tests` bullet bundles two cka-27 obligations but asserts only one"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The fourth (last) `Tests` bullet of `V9l` — mirrored verbatim in its paired
`V9l-T` tests task — reads:

> `session-only-degraded-state.md` §Seam-minimality + *Inline triplet is normative* (un-anchored; GOV-22 residue): no handler-scoped state seam is introduced.

The heading cites **two** distinct, independently-verifiable cka-27 spec
clauses, but the bullet's only assertable predicate ("no handler-scoped state
seam is introduced") covers the first clause alone:

1. **Seam minimality (normative)** in
   `spec_topics/pi-integration-contract/session-only-degraded-state.md`
   requires the runtime to introduce no handler-scoped state seam beyond
   `pinnedConstantReadOk` and `capturedEventReason` (no `branchTakenWasClosedSet`
   flag, no per-sub-step success roll-up, no separate `!pinnedConstantReadOk`
   disjunct or `"<unreadable>"` sentinel equality check). This is what the
   bullet asserts.
2. **Inline triplet is normative** in the same page requires the closed set
   `{"new","resume","fork"}` to be *inlined at the emission site* as a
   normative runtime literal whose source of truth is that page — **not** read
   off the `SessionShutdownEvent['reason']` snapshot's `literals` field (a
   parallel snapshot read would double-pin the same union in two
   independently-drifting places, and a Pi-bump literal rename would silently
   no-op the diagnostic). **No test bullet asserts this obligation at all.**

The `cka-27` coverage-matrix row already treats the two as separate
obligations: it enumerates "the *Seam-minimality* `MUST NOT introduce any
handler-scoped state seam` prohibition, **and** the *Inline triplet is
normative* clause". The closing leaf collapses them into one bullet that only
exercises the first, so an implementation that reads the closed set off the
snapshot's `literals` field — violating the inline-triplet requirement — passes
the suite green: the failure at the second obligation hides behind a pass at
the first.

## Issue introduction

**Verdict:** introduced-by-commit
**Introducing commits:** `55ed974` (2026-06-12, "pi-loom plan: resolve \"degraded-state obligations green over open spec contradiction\"")
**History:** The plan corpus is git-tracked. `git log --follow` on
`docs/plan_topics/V9l-session-only-degraded-branch.md` shows only two commits:
`55ed974` (creation) and `9c42f2d` (2026-06-16, §Blocked-obligations/`Deps`
rework). `git show 55ed974:…V9l…` confirms the collapsed
"§Seam-minimality + *Inline triplet is normative* … no handler-scoped state
seam is introduced" bullet was present in its current form at the leaf's
inception, when the degraded-state branch was lifted out of `V9h` into the new
`V9l`/`V9l-T` pair; `git show 9c42f2d` confirms the later commit did not touch
the `Tests` field. One day earlier, `bad3b99` (2026-06-11) authored the
`cka-27` coverage-matrix row enumerating the *Seam-minimality* prohibition
**and** the *Inline triplet is normative* clause as two distinct obligations.
The bundling defect therefore entered at `55ed974`: the new V9l test bullet
under-asserted relative to the cka-27 row that already distinguished the two
obligations.

## Solution Space

**Shape:** single

### Recommendation

In both `docs/plan_topics/V9l-session-only-degraded-branch.md` and
`docs/plan_topics/V9l-T-session-only-degraded-branch.md`, replace the single
fourth `Tests` bullet with one bullet per obligation, each carrying its own
assertable predicate so the closing-gate facet check and a reader both see two
independently-verifiable assertions:

- A seam-minimality bullet asserting no handler-scoped state seam beyond
  `pinnedConstantReadOk` and `capturedEventReason` is introduced — e.g.
  `` `session-only-degraded-state.md` §Seam minimality (normative) (un-anchored; GOV-22 residue): no handler-scoped state seam beyond `pinnedConstantReadOk` and `capturedEventReason` is introduced (no `branchTakenWasClosedSet` flag, no per-sub-step success roll-up, no `!pinnedConstantReadOk` disjunct or `"<unreadable>"` sentinel equality check). ``
- An inline-triplet bullet asserting the closed set is sourced from the spec
  page, not the snapshot — e.g.
  `` `session-only-degraded-state.md` §Inline triplet is normative (un-anchored; GOV-22 residue): the closed set `{"new","resume","fork"}` is inlined at the emission site as a normative runtime literal; an implementation that reads the set off the `SessionShutdownEvent['reason']` snapshot's `literals` field fails this assertion. ``

Keep the existing `(un-anchored; GOV-22 residue)` parentheticals as-is (their
removal is a separate finding). The `cka-27` coverage-matrix row already
enumerates both obligations and needs no edit. The two leaves' `Tests` fields
must stay byte-identical to each other, as they are today.

## Consequence

**Severity:** correctness

Two reasonable implementers diverge: one inlines `{"new","resume","fork"}` at
the emission site (conformant), the other reads it off the
`SessionShutdownEvent['reason']` snapshot's `literals` field (non-conformant per
the *Inline triplet is normative* clause) — and both pass the V9l suite green,
because no test asserts the inline-literal source. A later Pi minor renaming a
session-only literal (e.g. `"fork" → "branch"`) would then silently no-op the
`loom/host/session-shutdown-runtime-degraded` diagnostic for the renamed reason
with no failing test to catch it.

## Relationships

None
