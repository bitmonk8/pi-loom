---
id: F-1723
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/hard-ceilings/ceiling-invariants-and-audit.md, anchor: "Audit methodology* → ***loom 1.0.0 rejected-candidate record (closed)." }
lens: [spec-lens-cruft]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [cruft]
title: "`loom 1.0.0 rejected-candidate record` decision log is non-normative cruft in a normative ceiling page"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`ceiling-invariants-and-audit.md` carries a `***loom 1.0.0 rejected-candidate record (closed).***` paragraph under the *Audit methodology* section (`id="audit-methodology"`). It is a closed alternatives-considered / worked-examples log: it enumerates three candidate ceilings rejected during loom 1.0.0 and records where each concern is now handled, but every routing destination it cites is already stated normatively elsewhere — the no-regex parse-time stance, the Schema-Subset exclusion list, and the `NOCEIL-3` / `NOCEIL-4` fall-through arms. Nothing in the paragraph imposes a MUST, defines a surface, or changes runtime behaviour; its own closing clause freezes the list and instructs future editors not to append to it. Unlike the sibling *Routing obligation* and *Release gate* paragraphs, which carry live spec-editor MUSTs, this closed record has no normative home.

## Issue introduction

**Verdict:** single-commit introduction
**Introducing commits:** `2ff03c2` (2026-05-10) — *pi-loom spec: resolve "Source-language stability bullet leaves equivalence undefined at the aggregator and silently contradicts the open-set ceiling clause"*
**History:** `git log -S "candidate ceiling cases considered and rejected"` and `git log -S "V1.0 rejected-candidate"` both name `2ff03c2` as the first commit to introduce the paragraph string. That commit added the entire *Audit methodology* / *Four-axis closure list* / *Routing obligation* / *V1.0 rejected-candidate record* / *Release gate* block to `docs/spec_topics/hard-ceilings.md` in one edit (`git show 2ff03c2 -- docs/spec_topics/hard-ceilings.md` shows the paragraph added as one of the new `***...***` blocks in a `+23 / -2` diff). One later commit moved it without re-authoring its content: `f5e89f4` (2026-06-04, *docs: enforce 100KB hard / 30KB recommended size cap on spec set*) split `hard-ceilings.md` into the index page plus the two children `ceilings-3-and-4.md` and `ceiling-invariants-and-audit.md`, relocated the paragraph wholesale into `hard-ceilings/ceiling-invariants-and-audit.md`, and performed the `V1.0` → `loom 1.0.0` rename pass. No subsequent commit edits the paragraph body. The defect is therefore a property of the introducing block as authored at `2ff03c2`, not of any later evolution: the "closed worked examples" framing and the explicit "do not append" instruction were present in the original prose.

## Solution approach

Delete the `***loom 1.0.0 rejected-candidate record (closed).***` paragraph from `ceiling-invariants-and-audit.md` under `id="audit-methodology"`. The three concerns it documents retain their normative homes in `expressions.md`, `schema-subset.md`, and the `NOCEIL-3` / `NOCEIL-4` arms in the same file, so removal loses no normative content.

## Solution constraints

- Out of scope: the *Routing obligation* and *Release gate* paragraphs in the same *Audit methodology* section — F-2108 territory.

## Relationships

- F-2108 "These two sub-paragraphs specify a detailed spec-editorial governance process: how future spec..." — same-cluster (same *Audit methodology* section; targets the *Routing obligation* + *Release gate* paragraphs, which are procedural MUSTs to be moved, not deleted)
