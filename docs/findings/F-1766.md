---
id: F-1766
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/audit-target-categories.md, anchor: "`strict-capability-absence-pin` bullet" }
lens: [spec-lens-cruft, spec-lens-placement]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [cruft, placement]
title: "Historical relocation note embedded in the strict-capability-absence bullet"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The `strict-capability-absence-pin` bullet in `audit-target-categories.md` closes with a historical relocation annotation ("…has moved to … above, since it extends the step-2(a) SDK surface-inventory literal-read assertion…") rather than a normative statement. The clause records where the absence-under-the-probed-name gate used to live and why it was moved — a decision-log breadcrumb, not a constraint on an implementer. The gate's normative home at `inventory-audit-intro.md#strict-capability-absence-under-probed-name` already cross-links back to this anchor, so the clause also creates a redundant bidirectional "above/below" cross-reference loop that conveys nothing beyond the plain link.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `b0cdf1b` (May 6 2026, "pi-loom spec: resolve \"Re-validating obligation has no defined output and no enforcement gate\"") authored the original "If no such indicator has been added, this step is a no-op" sentence; `6829a79` (June 6 2026, "pi-loom spec: resolve \"Pi-SDK hidden-assumption cluster (strictCapable absence, decode steps, pi.on origin)\"") later introduced the absence-under-the-probed-name arm that the sentence now contradicts.
**History:** `git log -S "this step is a no-op"` shows `b0cdf1b` first lands the step (then numbered 6) with the "field absent → no-op" arm only; at that point the procedure had no symmetric red gate for the field-present case, so the sentence's enumeration was exhaustive. `git log -G "absence-under-the-probed-name"` shows the *absence-under-the-probed-name arm* (the build-time literal-read red triggered when a reachable `Model<Api>` declaration exposes `strictCapable` under the probed name) is introduced in `6829a79`, which adds it adjacent to the `strict-capability-absence-pin` anchor and explicitly names "the rename-detection arm owned by bump-procedure step 7 (which catches the field present under a *different* name)" — but does not edit step 7 to name the new arm or retract the no-op claim. The subsequent moves (`bfe08cf` fixing the strictCapable package attribution, `bf92fe6` relocating the absence-arm MUST from `audit-target-categories.md` to `inventory-audit-intro.md`) preserved the contradictory wording on both sides. The defect is therefore the **interaction**: the no-op sentence was correct at authoring and became incorrect when the symmetric red arm was added.

## Solution approach

Delete the relocation-note clause (the "…has moved to … since it extends…" sentence) from the `strict-capability-absence-pin` bullet in `audit-target-categories.md`, retaining the bullet's remaining sentences (the production-time absence claim, the single-citation-site role, and the closing `path`-exemption note). If a forward pointer is judged useful for discoverability, replace the clause with a plain non-historical link to `inventory-audit-intro.md#strict-capability-absence-under-probed-name`.

## Solution constraints

- Out of scope: the *Strict-capability absence-under-the-probed-name arm* at `inventory-audit-intro.md#strict-capability-absence-under-probed-name` (the gate's normative home) and its back-link to `#strict-capability-absence-pin` — leave both intact.

## Relationships

- F-2206 "HTML relocation comment in `queryerror-variants.md` Notes section" — same-cluster (same defect pattern — a past-relocation breadcrumb left in spec source; resolves independently in a different file)
