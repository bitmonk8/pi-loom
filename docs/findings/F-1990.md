---
id: F-1990
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/discovery/discovery-sources.md, anchor: "discovery/discovery-sources.md (CLI source) and discovery.md (framing paragraph)" }
lens: [spec-lens-prescription]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [prescription]
title: "`--loom` flag help text left elided"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

under-prescribes contract. The flag registration is shown as `pi.registerFlag('loom', { type: 'string', description: '…' })`. The `description` value is an operator-observable surface (it appears in CLI `--help`), but its text is left as an ellipsis and is not pinned here or cross-referenced to a definition. - **Suggested adjustment:** Either pin the `--loom` flag description string (or state it is implementer-chosen and not a conformance point) or add a cross-reference to where the CLI help text is defined (e.g. Registration steps).
