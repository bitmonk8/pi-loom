---
id: F-1989
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics.md, anchor: "diagnostics.md` — introductory paragraph" }
lens: [spec-lens-clarity]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [clarity]
title: "\"The diagnostics defined here\" has two competing antecedents: (a) every diagnostic in the..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

"The diagnostics defined here" has two competing antecedents: (a) every diagnostic in the entire diagnostics cluster (including `loom/load/extension-bootstrap-failed`, which fires at factory time — before the extension is live), or (b) only the diagnostics *other than* `extension-bootstrap-failed` that the preceding sentence contrasted with `LoadExtensionsResult.errors`. Reading (a) produces an outright contradiction with `extension-bootstrap-failed`'s trigger; reading (b) is coherent but requires inferring the antecedent boundary. - **Suggested clarification:** Rewrite to make the scope explicit: e.g.
