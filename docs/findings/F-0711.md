---
id: F-0711
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-interfaces-core.md, anchor: "Tool execution from loom code (line 85: prefix definition)" }
  - { path: docs/spec_topics/tool-calls.md, anchor: "loom 1.0 seam" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [naming]
title: "`loom-direct:` `toolCallId` prefix uses divergent vocabulary from the rest of the code-side tool-call surface"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The code-side tool-call surface uses a single "code" vocabulary almost everywhere: the `QueryError` variant is `CodeToolError`, its wire `kind` is `"code_tool"`, and the lowering site is titled *"Tool execution from loom code"*. The one exception is the synthesised `toolCallId` prefix `loom-direct:` at `host-interfaces-core.md` §*Tool execution from loom code* (anchor `tool-execution-from-loom-code`), mirrored in `tool-calls.md`'s *loom 1.0 seam — per-call timeout* note. "direct" was a coherent distinguisher against the pre-rename `ToolCallError` / `ToolFailureError` pair, but after that pair became `CodeToolError` / `ModelToolError`, nothing in the corpus distinguishes "direct" from "code" semantically. A reader greping the "code" vocabulary does not surface the prefix paragraph and must discover the second spelling independently.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `fae85d3` (2026-05-04, *"Tighten spec for implementation-plan readiness"*) + `6750c81` (2026-05-05, *"spec: consolidate QueryError variants; rename to InvokeInfraError / CodeToolError / ModelToolError"*)
**History:** The `loom-direct:` prefix was introduced in `fae85d3`, the same commit that first articulated code-side tool execution against the then-monolithic `spec.md`. At that time the code-side error variant was named `ToolCallError` with wire `kind: "tool_call"`, and the sibling model-driven variant was `ToolFailureError` with wire `kind: "tool_failure"` — so the prefix word "direct" was a coherent distinguisher against the *call*/*failure* sibling pair, neither of which used the word "code". Commit `6750c81` then swept the error-variant family, renaming `ToolCallError` → `CodeToolError` (wire `"tool_call"` → `"code_tool"`) and `ToolFailureError` → `ModelToolError` (wire `"tool_failure"` → `"model_tool"`); per its own commit message it "Slash-invocation per-kind table updated; human-facing 'failed' wording preserved per decision", but the sweep did not reach the `toolCallId` prefix at the lowering site or its cross-reference in `tool-calls.md`. The two-sited `loom-direct:` token thereafter sat as residue from the pre-rename vocabulary, surviving the file split (`fecb504`) and relocation to `docs/spec_topics/` (`31ff060`) untouched. The defect is the *interaction* between the two commits, not a defect of either in isolation — `fae85d3` was internally consistent against then-current siblings; `6750c81` would have been sweep-complete had it included the prefix in its scope.

## Solution approach

Rename the synthesised `toolCallId` prefix from `loom-direct:` to `loom-code:` at both citation sites: the prefix definition at `host-interfaces-core.md` §*Tool execution from loom code* and the parenthetical reference in `tool-calls.md`'s *loom 1.0 seam* note. The "code" stem aligns the prefix with `CodeToolError`, `kind: "code_tool"`, and the section title, restoring one grep-able discriminator across the code-side tool-call surface.

## Solution constraints

- Out of scope: the synthesised `toolCallId` suffix-format / uniqueness definition, owned by the co-resolve companion finding (see Relationships) — rename the prefix string only.

## Relationships

- "tool-calls.md — synthesised `loom-direct:` `toolCallId` format/uniqueness undefined" — co-resolve (the suffix-format text that finding adds and this finding's prefix rename land at the same two paragraphs; resolve the rename first so the format definition reads against the final prefix string)
