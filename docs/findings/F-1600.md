---
id: F-1600
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2b.md, anchor: "steps 3 and 4 (tail of file)" }
  - { path: docs/spec_topics/pi-integration-contract.md, anchor: "version-bump ToC list (lines 34–37)" }
lens: [spec-lens-placement]
tier: high
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: triaged
category: [placement]
title: "Version-bump steps 3 and 4 are stranded in a file titled and scoped to step 2(b)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

The Pi version-bump procedure is a single numbered checklist (step 1 … step 7) split across four topic pages: `version-bump-intro.md` (intro + step 1), `version-bump-step2.md` (step 2(a)), `version-bump-step2b.md` (step 2(b)), and `version-bump-triggers.md` (steps 5–7). The full text of numbered steps **3** ("Re-confirm the `engines.node` floor") and **4** ("Update the version pin in `peerDependencies` and the equivalent literal here") is appended to the tail of `version-bump-step2b.md`, after the file's five step-2(b) branch-resolution sub-sections.

That file's H1 is `# Version bump step2b`, and its sole entry in the hub ToC (`pi-integration-contract.md`) reads `Version bump step2b` — both signalling step-2(b) content only. Steps 3 and 4 appear in neither the title nor the ToC. A contributor walking the procedure by step number, or scanning the ToC for the home of steps 3–4, finds `step2b` then jumps to `steps 5–7` with no page named for the two intervening steps; the steps are effectively invisible to step-number and ToC navigation even though their text is present in the corpus.

The defect is purely organisational: the content of steps 3 and 4 is correct and complete; only their location (a file whose title and ToC entry promise step-2(b) content) is wrong. The fix relocates them to a correctly-titled home and leaves `version-bump-step2b.md` ending at step 2(b)'s branch (5).

## Issue introduction

**Verdict:** single-commit
**Introducing commit:** `f5e89f4` (2026-06-04) — "docs: enforce 100KB hard / 30KB recommended size cap on spec set"
**History:** The `step 2(a)` / `step 2(b)` textual designations predate the split — `git show f5e89f4^:docs/spec.md` and the pre-split `pi-integration-contract.md` already carry the `step 2(a)` form, so the designation namespace was not introduced by this commit. `f5e89f4` is the commit that, to satisfy the size cap, split the consolidated version-bump prose out of `pi-integration-contract.md` into the child files `version-bump-intro.md`, `version-bump-step2.md`, `version-bump-step2b.md`, and `version-bump-triggers.md` (`git log --diff-filter=A --follow` dates the creation of both `version-bump-step2.md` and `version-bump-step2b.md` to `f5e89f4`, and `git show f5e89f4:…/version-bump-step2b.md` shows the `# Version bump step2b` title already present at creation). The defect is the choice, at that split, of a letter-suffix overflow naming scheme (`step2` + `step2b`) that collides with the older `(a)`/`(b)` designation namespace; it is attributable to that single commit rather than to a multi-commit interaction.

## Solution Space

**Shape:** multiple

### Option A — Fold steps 3–4 into `version-bump-triggers.md` and retitle it "steps 3–7"

- **Approach:** Move the step-3 and step-4 blocks from the tail of `version-bump-step2b.md` to the head of `version-bump-triggers.md`, ahead of step 5; retitle that file's H1 from `Version bump steps 5–7` to `Version bump steps 3–7`; update the ToC entry in `pi-integration-contract.md` (line 37) to match; repoint `V18c`'s `version-bump-step2b.md step 3` citations to the retitled file.
- **Spec edits:** `version-bump-step2b.md` (delete steps 3–4), `version-bump-triggers.md` (prepend steps 3–4, retitle H1), `pi-integration-contract.md` (ToC line 37).
- **Pros:** One fewer file in the cluster; all post-2(b) numbered steps stay contiguous on one page; reuses the existing grouped-step file pattern.
- **Cons:** Grows `version-bump-triggers.md`, working against the size-cap split that created these files; retitles an existing page and its ToC entry.
- **Risks:** `version-bump-triggers.md` could approach the 30KB recommended page cap.

### Option B — Create a new `version-bump-steps3-4.md`

- **Approach:** Create `version-bump-steps3-4.md` titled `Version bump steps 3–4`; move the two step blocks into it; insert a ToC entry in `pi-integration-contract.md` between `Version bump step2b` (line 36) and `Version bump steps 5–7` (line 37); repoint `V18c`'s `version-bump-step2b.md step 3` citations to the new file.
- **Spec edits:** `version-bump-step2b.md` (delete steps 3–4), new `version-bump-steps3-4.md`, `pi-integration-contract.md` (new ToC entry).
- **Pros:** Matches the established per-section file convention (`intro` / `step2` / `step2b` / `steps 5–7`); smallest per-file content churn; respects the size-cap motivation that drove the split.
- **Cons:** Adds another small file to the cluster.
- **Risks:** Minimal.

### Recommendation

Adopt **Option B**: a new `version-bump-steps3-4.md` titled `Version bump steps 3–4`, slotted between `step2b` and `steps 5–7` in the hub ToC. It mirrors the existing grouped-step file convention, requires the least churn, and keeps `version-bump-triggers.md` under the size cap. Under either option, `version-bump-step2b.md` MUST end at step 2(b)'s branch (5), and every file-anchored citation to "step 3 / step 4 in `version-bump-step2b.md`" — notably `V18c-version-bump-checklist.md` lines 5 and 10 — MUST be repointed to the new home. Edge case the fixer must watch: steps 3–4 carry same-page relative prose (e.g. step 4's "update the … Pi-SDK pin literal at the top of this page", and "[Step 0 (a)] above" references) that assumes those steps share a page with step 2(b)'s neighbours; once relocated, rewrite each such relative reference to a resolving cross-link so it does not silently point at the wrong page.

## Consequence

**Severity:** advisory

A contributor performing a version bump by following the procedure's step numbers, or by scanning the ToC, will search for steps 3 and 4 in a file after `step2b` and not find a page named for them; the two required steps (`engines.node` floor re-confirmation and the `peerDependencies` pin move) can be skipped by anyone who stops reading at the end of the apparent step-2(b) page. The steps are fully specified, so a careful reader who reads `step2b` to its end still finds them — the gap is discoverability, not correctness.

## Relationships

- F-2263 "Step 3 assumes the installed Pi `package.json` always declares `engines.node`" — same-cluster (same step-3 text in the same file; that finding fixes the `engines.node` gate's undefined-behaviour, this one fixes step 3's file home — they resolve independently)
