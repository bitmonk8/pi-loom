---
id: F-2085
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/binder/binder-bypass-and-envelope.md, anchor: "System-prompt structure (normative)* item 4 + *Type display* table" }
lens: [spec-lens-implementability]
tier: medium
factors:
  blast_radius: narrow
  centrality: core
  core_premise_blocker: no
status: triaged
category: [implementability]
title: "Binder per-field `<type>` rendering is unpinned for compound param types beyond the *Type display* reference set"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

`System-prompt structure (normative)` item 4 requires each Parameters-block per-field line to render `<wire-name> (<type>) <requirement>`, with `<type>` "rendered per *Type display* below." *Type display* states the general rule — render the field's declared Loom type "written in the surface syntax of [Type System], not the JSON Schema lowering" — and then pins a reference table whose renderings are "normative; conforming implementations MUST reproduce these exactly." The table enumerates only eight forms: `string`, `integer`, `boolean`, an enum name, a named-schema name, `array<integer>`, `string | null`, and `Cat | Dog`.

The Loom type grammar admits considerably more than these. `type-system.md` and `grammar.md#type-grammar` permit inline anonymous object types (`ObjectType ::= "{" Field ("," Field)* ","? "}"`), literal types (`"validation"`, `42`, `true`), nested generics (`array<{ a: string }>`, `Result<T, E>`), and arbitrary unions over all of the above — every one of which is legal in a `params:` annotation position. For these compound forms the spec gives no canonical byte-exact rendering. "Surface syntax" does not pin the whitespace the grammar leaves free: the brace-interior spacing of an inline object (`{a: string}` vs `{ a: string }`), the post-comma spacing in a multi-field object, whether a trailing comma is emitted, and the spacing inside nested generic brackets are all unconstrained. Note that the sibling token-order rule added later ("renderers MUST NOT insert additional whitespace between [the three tokens] beyond the single U+0020") pins whitespace *between* `<wire-name>`, `(<type>)`, and `<requirement>`, but says nothing about whitespace *inside* the `<type>` rendering itself.

This collides with the binder's input-reproducibility contract. The system-prompt bytes are not revisable illustration: `binder-model-and-context.md#why-the-transcript-bytes-are-pinned` establishes that loom controls "byte-identical binder input," and the Parameters block is part of those bytes. Two conforming implementations handed the same loom whose `params:` declare a compound-typed field would emit different `<type>` bytes, producing different binder input, defeating the byte-identical-input guarantee (and any prompt-cache keyed on it) for those looms. The "MUST reproduce these exactly" clause therefore makes a promise the spec does not deliver for any param type outside the eight enumerated rows.

## Issue introduction
**Verdict:** single-commit
**Introducing commits:** 5a4ec51 — pi-loom spec: resolve "Binder system prompt information content lacks verifiable field set" (2026-05-06, Thomas Andersen)
**History:** Commit 5a4ec51 introduced both item 4's "rendered per *Type display* below" rule and the *Type display* table with its "conforming implementations MUST reproduce these exactly" clause into the predecessor `spec_topics/binder.md`. The table enumerated only the same closed reference set then in place (`string`, `int`, `boolean`, enum, named schema, `array<int>`, `string | null`, `Cat | Dog`) with no general canonical-whitespace rule for compound types, so the gap was present at the rule's introduction. The later token-order/indentation commit (4188412, 2026-05-07) pinned whitespace *between* the three tokens but not *within* `<type>`, and the size-cap split (f5e89f4, 2026-06-04) moved the rule verbatim into `binder/binder-bypass-and-envelope.md`; neither closed the gap.

## Solution Space

**Shape:** multiple

### Option A — Pin canonical surface-syntax rendering for compound types

**Approach:** Extend *Type display* from a closed reference table into a recursive canonical-rendering rule covering every type form the grammar admits. Fix the free whitespace: inline object types render as `{ field: T, ... }` with one U+0020 after `{`, before `}`, and after each field-separating comma, no trailing comma; nested generics render with no spaces inside the angle brackets (`array<{ a: string }>`); literal types render their source literal verbatim (`"validation"`, `42`, `true`); unions keep the existing ` | ` separator; recursion applies element/field/arm types. Add a worked reference rendering for at least one inline-object and one nested-generic case.

**Spec edits:** Replace the *Type display* "Reference renderings" framing with a normative recursive rendering definition plus the existing table as illustrative rows; add the brace/comma/angle-bracket whitespace rules and ≥1 inline-object reference row.

**Pros:** Preserves the byte-identical-input contract for every loom regardless of param type; consistent with how the rest of the binder prompt (separators, indentation, em-dash) is pinned byte-exact; no carve-out to reconcile.

**Cons:** Larger normative surface; must enumerate the whitespace rule for each compound form precisely.

**Risks:** A rendering rule that is itself ambiguous would re-introduce the gap; the rule must be stated against the grammar's free positions, not by example alone.

### Option B — Scope byte-exactness to the enumerated set; declare the rest non-normative

**Approach:** State explicitly that `<type>` rendering is normative (MUST-reproduce-exactly) only for the enumerated reference forms and implementation-chosen (only internally deterministic) for all other param types, and reconcile that carve-out against the input-reproducibility contract in *Why the transcript bytes are pinned*.

**Spec edits:** Add a scoping sentence to *Type display* and a reconciling clause to `binder-model-and-context.md#why-the-transcript-bytes-are-pinned` exempting compound-typed `<type>` bytes from the byte-identical-input guarantee.

**Pros:** Minimal edit; avoids enumerating recursive whitespace rules.

**Cons:** Directly punctures the input-reproducibility contract — two conforming implementations may produce different binder input for the same loom, the exact divergence the contract exists to prevent; weakens prompt-cache stability and auditability for the affected looms.

**Risks:** "Internally deterministic but cross-implementation-free" is a contract regression that downstream auditing and cache-keying assumptions may not anticipate.

### Recommendation

Adopt **Option A**. The binder system prompt is pinned byte-exact precisely because its input-reproducibility contract depends on it; carving compound types out (Option B) contradicts that contract for the very looms most likely to use rich `params:`. The implementer must state the whitespace rule against the grammar's free positions (brace interior, post-comma, trailing-comma suppression, angle-bracket interior) rather than by example, and make the rule recurse through unions, `array<T>`, `Result<T, E>`, and nested inline objects so no compound form is left unpinned. Align the resolution shape with the sibling `default=<literal>` rendering finding so both rendering tables are treated consistently.

## Consequence

**Severity:** correctness

A loom whose `params:` use an inline-object, literal, nested-generic, or `Result<T, E>` field type reaches a `<type>` rendering the spec does not pin byte-for-byte. Two reasonable implementers diverge on the interior whitespace, emitting different binder system-prompt bytes for the same loom — a direct violation of the input-reproducibility contract the binder prompt is built to honour, and a possible silent prompt-cache miss.

## Relationships

- F-2089 "`default=<literal>` rendering is undefined for object-typed and nested-composite param defaults" — same-cluster (parallel defect in the same item 4: *Default-literal rendering* leaves compound default values unpinned just as *Type display* leaves compound types unpinned; resolve at separate sites but the pin-vs-carve-out decision should be made consistently)
