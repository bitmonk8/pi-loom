---
id: F-2459
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics/code-registry-runtime.md, anchor: "runtime-namespace introduction paragraph" }
  - { path: docs/spec_topics/diagnostics/code-registry-runtime.md, anchor: "`cancelled-by-session-shutdown` row, mutual-exclusion EXCEPT clause" }
  - { path: docs/spec_topics/diagnostics/code-registry-runtime.md, anchor: "`cancelled-by-session-shutdown` row, \"exclusion mirrors `cancelled`\" sentence" }
  - { path: docs/spec_topics/diagnostics/code-registry-runtime.md, anchor: "`reload-teardown-timeout` row, mirrored \"MAY also subsequently emit\" clause" }
lens: [spec-lens-clarity, spec-lens-implementability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [clarity, implementability]
title: "Replace the RFC-2119 \"MAY both\" modal with factual-occurrence wording"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The residual-gap mutual-exclusion EXCEPT clause on the `cancelled-by-session-shutdown` row uses the permissive RFC-2119 **MAY** for a contingent runtime outcome ("MAY both contribute … AND later emit"), and the `reload-teardown-timeout` row mirrors it. The modal reads as an implementer-granted option rather than a possible factual co-occurrence of the stamp-throw path. This risks an implementer misreading the dual-surface appearance as optional behaviour to design around.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** 7f37dd7 (2026-05-07, "resolve Session-swap behaviour for in-flight loom invocations is under-specified"); 52b9101 (2026-05-11, "resolve T24 — Fork-reason watcher closure leaves the extension in an unspecified, silently degraded state"); 47fe3cd (2026-06-05, "resolve exactly six panic sources references a tag/column that does not exist")
**History:** The corpus is git-tracked. `git log -S`/`-G --follow` across renames (the file was later split out under `docs/spec_topics/diagnostics/` by f5e89f4) shows the three defect facets entered at distinct commits and compounded. 7f37dd7 added the `cancelled-by-session-shutdown` row, including the bare "exclusion mirrors `cancelled`" sentence (facet 2). 52b9101 added the residual-gap "MAY both contribute … AND later emit" EXCEPT clause and its `reload-teardown-timeout` mirror (facet 1). 47fe3cd recast the intro from the loose "(the rows below tagged as panics)" wording into an explicit per-code accounting ("the table below carries one row per code … One further code — …") but never enumerated the already-present `cancelled-by-session-shutdown` row (facet 3); every subsequent "One further code —" addition (validator-cache-collision, watcher-terminated, custom-type-unsafe) perpetuated the gap. The omission is thus an interaction: a row introduced on one date became silently unaccounted-for when a later commit converted the intro into an exhaustive enumeration without revisiting it.

## Solution approach

Rewrite the permissive **MAY** in the `cancelled-by-session-shutdown-mutual-exclusion` anchor's EXCEPT clause, and in the mirrored "MAY also subsequently emit" clause on the `reload-teardown-timeout` row, into non-normative factual-occurrence phrasing so the dual-surface appearance reads as a possible outcome of the stamp-throw path rather than an implementation choice.

## Solution constraints

- Preserve the existing SHOULD guidance directing operator-tooling consumers to treat the dual-surface appearance as a residual-gap signal, not a contract violation.

## Relationships

- F-2458 "Add the `cancelled-by-session-shutdown` row to the runtime-namespace intro accounting" — co-resolve
- F-2460 "Cross-reference the `cancelled` always-log exclusion" — co-resolve
