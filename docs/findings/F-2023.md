---
id: F-2023
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/overview-and-orientation.md, anchor: "The section states: \"The runtime targets the Node JavaScript engine (per the loom 1.0 host..." }
lens: [spec-lens-placement]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [placement]
title: "The section states: \"The runtime targets the Node JavaScript engine (per the loom 1.0 host..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The section states: *"The runtime targets the Node JavaScript engine (per the loom 1.0 host floor in [Pi Integration Contract — Step 0 (a)], which pins `process.versions.node >= 22.19.0`)"* and enumerates non-functional runtime prerequisites: IEEE-754 double semantics, native `Map`/`Set`, native `JSON.stringify`, and the exclusion of Bun, Deno, browser embeds, and other non-Node hosts. The section explicitly labels these a "non-checked invariant." The toc for `docs/spec_topics/overview-and-orientation.md` (line 57) shows a dedicated **Non-functional requirements** subsection already exists in that file.
