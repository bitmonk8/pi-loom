---
id: F-1215
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/audit-wire-and-canary.md, anchor: "final paragraph (the *step-2(b) wiring* paragraph, beginning \"The audit runs as part of the SDK surface-inventory tests step 2 below names…\")" }
lens: [spec-lens-assumptions]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [assumptions]
title: "In-process audit wiring assumes Vitest leaves stdout discriminator-clean"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`audit-wire-and-canary.md` permits two wiring choices for the surface-set-closure audit's fail-closed gate: in-process to the harness (e.g. a Vitest test case whose `expect(...).toBe(0)` asserts on the audit's exit code) or as a spawned child process. The *Wire serialisation* clause separately pins that the audit emits `audit/<class>/<family>/<symptom>` line-delimited records on **stdout** as the sole machine-parseable surface a CI parser may consume, and that "the audit's record emission MUST reach a sink whose contents survive the harness wrapping". That sink-survival rule is written as an obligation on the audit, but the in-process variant pushes the obligation onto the harness: Vitest (and most test runners) capture child `console.log` / `process.stdout.write` calls into per-test buffers, reformat them with test-case headers and progress markers, or attach them to a reporter slot. A green `npm test` running the audit in-process under a default Vitest configuration can easily ship a stdout stream where the `audit/<class>/...` lines are interleaved with `✓ inventory audit (12ms)`-shaped reporter output, prefixed with file-and-line headers, or buffered behind a non-default reporter that elides them entirely — none of which violates the audit's own obligations, all of which break the CI parser.

Two preconditions are required for the in-process variant to satisfy the wire contract end-to-end, and neither is stated: (1) the harness MUST be configured so the audit's raw stdout bytes pass through to the `npm test` process's stdout unmodified — no per-test capture, no reformatting, no buffering behind a reporter that suppresses passing-test output; and (2) no harness-emitted line (reporter banners, test names, progress dots, summary tables) may share the `audit/<class>/<family>/<symptom>` leading-token shape, since the CI parser routes by leading-token prefix match. Without either precondition pinned, two reasonable implementers wire `npm test` differently and produce different stdout streams from the same audit.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `e5cd514` — *pi-loom spec: resolve "T35 SDK capability inventory closed-set negative-direction audit"* (2026-05-09).
**History:** `git log --follow --diff-filter=A` on `audit-wire-and-canary.md` returns `f5e89f4` (the 2026-06-04 spec-size-cap split that lifted the file out of `pi-integration-contract.md`), but `git log -S "interprets empty stdout as 'no failures'"` and `git log -S "audit's record emission MUST reach a sink"` both bottom out at `e5cd514`, the commit that first introduced the audit-wiring paragraph (then anchored to an `H1` leaf). The original `e5cd514` paragraph already stated the in-process / child-process choice, the empty-stdout prohibition, and the sink-survival rule — and already omitted the harness-side stdout-cleanliness precondition the finding names. A later edit (`9e53613`, the corpus-direction sweep that dropped `H1`/`plan_topics` references) and the `f5e89f4` split rewrote the surrounding prose but did not touch the precondition gap. The defect is therefore a single-commit inception in `e5cd514`, untouched by subsequent edits.

## Solution Space

### Recommendation

In `audit-wire-and-canary.md`'s final *step-2(b) wiring* paragraph, immediately after the sentence "An implementation wiring that interprets empty stdout as 'no failures' violates this obligation.", insert one sentence stated as a MUST binding the in-process variant: when the audit runs in-process to the harness, the harness MUST be configured to pass the audit's raw stdout bytes through to the `npm test` process's stdout without per-test capture, reformatting, or buffering behind a reporter that suppresses passing-test output; and no harness-emitted line (reporter banners, test names, progress dots, summary tables) may share the `audit/<class>/<family>/<symptom>` leading-token shape, since the CI parser routes by leading-token prefix match. An implementation that cannot meet both preconditions under its chosen harness MUST use the child-process variant. Leave the *Wire serialisation* clause unchanged — this precondition is wiring-shape-specific, not a record-format change.

Pin two enforcement edge cases:

- `V18b` must review and pin the specific Vitest configuration switches it relies on, because Vitest's default `pool: 'threads'` and per-test stdout capture would otherwise reformat or buffer the audit's records — e.g. `printConsoleTrace: false` and a reporter that does not capture passing-test output, or running the audit case under a `pool: 'forks'` boundary.
- `V18b-T` must enforce the reserved-prefix rule with an explicit fixture asserting that no non-audit line on the live `npm test` stdout matches `^audit/`, so a future reporter swap that prints `audit/`-shaped progress lines reds the test rather than silently breaking CI.

## Consequence

**Severity:** correctness

A green in-process `npm test` whose Vitest configuration captures or reformats stdout — the default in many Vitest setups — ships an `audit/<class>/...` stream a CI parser cannot consume, defeating the per-discriminator routability obligation the same page pins. The audit still fails-closed on exit code, so the spec's primary safety property holds, but the contributor-facing routing table the *Failure-surface contract* and *Non-empty-scan canary* clauses build on is silently disabled — broken CI integration without a build-time signal.

## Relationships
- F-1126 "audit-wire-and-canary.md — wall-clock budget MUST has no bound; counter key names left to implementation (CI not name-portable)" — same-cluster (both touch the wire-and-canary contract's machine-parseability, but resolve independently — name-portability of counter keys is orthogonal to harness stdout survival)
