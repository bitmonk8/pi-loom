---
id: F-1344
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/conversation-drive.md, anchor: "No additional access channels; prompt-mode-error-detection; untyped-query-ok-extraction; prompt-mode pi.on" }
lens: [spec-lens-traceability]
tier: high
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: reduced
category: [traceability]
title: "Coin `PIC-N` anchors on `conversation-drive.md` (un-anchored normative MUSTs)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
decision_axes: 3
---
## Problem

`conversation-drive.md` carries normative-obligation sites with no co-located `PIC-N` REQ-ID anchor, leaving GOV-9's `#prefix-n` cross-link contract unsatisfiable for each. The un-anchored sites are: the `no-extra-mediation` *No additional access channels* `MUST NOT` (no sandbox / capability filter / mediated proxy in any loom 1.x release); the `prompt-mode-error-detection` transport-failure-probe MUSTs; and the `untyped-query-ok-extraction` trailing-turn assistant-text-concatenation MUSTs. Without a normative cite-handle, no test, review, or coverage-matrix row can bind to these obligations by `#prefix-n` fragment.

## Issue introduction

**Verdict:** present-since-inception-with-progressive-extension
**Introducing commits:**
- `c1e5d64` (2026-05-05) — registered the per-page REQ-ID prefix table and deferred per-rule coinage on every page not in the five-page Phase 12a pass; `pi-integration-contract.md` was not among the five, so its `PIC` prefix was registered with no `PIC-N` site coined (foundational deferral).
- `6d8bea5` / `d4ac81b` (2026-06-04) — drained partway by coining `PIC-3`…`PIC-6` and `PIC-7`…`PIC-15`; deliberately incomplete (cross-cutting prose-heavy obligations on conversation-drive et al. left for later).
- `f5e89f4` (2026-06-04) — split the 576 KB monolith into 30 child files, preserving anchors but coining none, so the residue was inherited verbatim.
- `4f588e0` (2026-06-03) — added GOV-22, codifying per-commit progressive drainage; subsequent commits anchored only the obligation each edited.

**History:** The gap is steady-state under GOV-22 (per-page coinage as edits flow through). What is unusual about PIC is that the monolith split fanned the residue across 30 files in one move; combined with H5e's closed-set load at H6a, the open residue cannot remain steady-state through to release, so the defect's permanence requires a one-shot drain rather than passive wait for incidental edits.

## Solution approach

Coin the next free `PIC-N` REQ-ID anchor at each un-anchored defining site under the page's registered `PIC` prefix, in GOV-1 dual-form layout, querying the live PIC high-water mark per GOV-3 at edit time. Preserve the existing page-local HTML sub-anchors (`prompt-mode-error-detection`, `untyped-query-ok-extraction`, `waitforidle-no-rejection-presupposition`) as navigation aids. Close the corresponding `coverage-matrix.md` rows (`cka-18`, `cka-24`, `cka-25`) against the newly coined PIC-N IDs, and repoint inbound cross-references to the new anchors where they target these sites.

## Solution constraints

- Out of scope: `PIC-N` coinage on any other PIC page — the sibling per-page residues are owned by F-1345–F-1351; confine edits to `conversation-drive.md` and its coverage-matrix rows.

## Relationships

- F-1345, F-1346, F-1347, F-1348, F-1349, F-1350, F-1351 (sibling per-page PIC-N coinages) — same-cluster (each resolves independently as its own commit under the same GOV-22 discipline)
- F-1334 "session-only-degraded-state acceptance criteria" — same-cluster
