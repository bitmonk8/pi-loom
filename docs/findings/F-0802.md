---
id: F-0802
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/subagent.md, anchor: "Conversation drive" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "Editorial-review checklist for unpinned host presuppositions" }
  - { path: docs/spec_topics/pi-integration-contract/capability-inventory-items.md, anchor: "item 3 (sdk-cap-subagent-isolated-session)" }
lens: [spec-lens-assumptions]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [assumptions]
title: "`subagent.md` — in-memory `SessionManager` UI-suppression property has no behavioural re-validation hook"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`subagent.md` asserts that "the in-memory `SessionManager` is the mechanism that prevents subagent output from reaching Pi's user-facing UI," and capability inventory item 3 (`sdk-cap-subagent-isolated-session`) promises a private subagent transcript on the same property. Neither claim has a behavioural re-validation hook in the editorial-review checklist. Item (o) re-validates only the factory *signature*; item (aj) re-validates only transcript *retention* (no-persistence). A Pi minor that preserved the signature and continued to discard the transcript while newly streaming the in-memory session's assistant tokens or tool-call cards into the user-facing TUI would silently falsify both claims with no signal at load time, at the SDK surface-inventory assertion, or at any bump-checklist item.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:**

- `e466d1f` (2026-05-04, *pi-loom spec: resolve "Streaming visibility of query responses is unspecified"*) — introduced the original *"User-visible streaming — subagent mode"* paragraph in `spec_topics/pi-integration-contract.md` with the load-bearing claim "The subagent's `AgentSession` uses an in-memory `SessionManager`; nothing it produces flows to Pi's user-facing UI." No re-validation hook was attached because no bump-checklist existed yet at that revision.
- `af6e5f6` (2026-05-05, *pi-loom spec: resolve "Prompt-mode streaming edge cases live on the wrong page"*) — moved the user-visible-streaming prose to `slash-invocation.md` and minted the current `subagent.md`-resident phrasing "The in-memory `SessionManager` is the mechanism that prevents subagent output from reaching Pi's user-facing UI; user-visible behaviour is specified in [Invocation from Pi — User-visible streaming](./slash-invocation.md)." This is the present-day defect-token site. No re-validation hook accompanied the move.

**History:** `git log -G` against the spec corpus shows the UI-suppression claim has been carried in the same form (with no behavioural re-validation hook) since the two May-2026 commits above. Subsequent maintenance built up the adjacent hooks without covering this property: `5d55c12` (2026-06-05) introduced item (o) for satellite-type *signatures*, `252acda` (2026-06-05) annotated capability 3 with "the loom asserts no independent behavioural check that the in-memory session discards rather than persists the transcript", and `9ced9f1` (2026-06-08) added item (aj) for the *transcript-discard* (retention) behaviour and routed capability 3's prose to it. None of those commits introduced an item covering the in-memory ⇒ no-UI-surfacing property, and `subagent.md`'s satellite-type-pins paragraph still cites only item (o). The defect is therefore present-since-inception of the UI-suppression claim, with later commits adding *adjacent-but-not-overlapping* hooks rather than ever closing this specific gap.

## Solution approach

Add an editorial-review checklist item under `Editorial-review checklist for unpinned host presuppositions` in `version-bump-step2.md` that audits the in-memory `SessionManager.inMemory(cwd)` session's UI-non-surfacing behaviour on each Pi minor bump, modelled on the retention audit at item (aj). Route the load-bearing UI-suppression cross-reference in `subagent.md`'s `subagent-spawn-satellite-types` paragraph and in `capability-inventory-items.md` item 3 (`sdk-cap-subagent-isolated-session`) to the new item alongside the existing item (o) / (aj) pointers. Update the checklist preamble range, the `bump-baseline-acquisition` JS-body-read enumeration, and `V18c-version-bump-checklist.md`'s range clauses to include the new item.

## Solution constraints

- The audited property is scoped to TUI event-streaming suppression; it must not extend to operator-observability of the cancellation system-note or `loom-system-note` channel, which are independently observable and out of scope.

## Relationships

- "capability-inventory-items.md — Capability 3 transcript-privacy predicate is an untestable MUST about an external entity" — co-resolve (names the same underlying presupposition from the capability-item side; both findings collapse into a single edit set, since the residual obligation — route a behavioural re-validation obligation distinct from the signature check to the editorial-review checklist at Pi minor bump — is exactly what this finding does for the UI-surfacing arm).
