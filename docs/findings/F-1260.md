---
id: F-1260
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/conversation-drive.md, anchor: no-extra-mediation }
lens: [spec-lens-placement, spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [placement, scope]
title: "no-extra-mediation trust-boundary NFR sits on conversation-drive.md instead of its runtime-wide owner"
relations:
  - { kind: related, id: F-1261 }
  - { kind: related, id: F-1262 }
  - { kind: related, id: F-1263 }
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

The "No additional access channels" paragraph (anchor `#no-extra-mediation`) states the runtime's no-sandbox / no-capability-filter / no-mediated-proxy obligation — a runtime-wide trust-boundary NFR — but it sits as the first paragraph of `conversation-drive.md`, a prompt-mode driver page. `overview-and-orientation.md`'s Non-functional-requirements §"Trust boundary" bullet already forward-links to it as the normative source, and `future-considerations/model-changes-and-non-goals.md` and `runtime-value-model.md` cite the same anchor. The placement mis-scopes a runtime-wide NFR as a prompt-mode concern.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:**
- `52219c4` (2026-05-06) — "Address all 41 spec-review findings" — first introduced the *No additional access channels* paragraph at this location.

**History:** Split from F-1143 (shard-07 placement cluster) under the finding-atomicity invariant; this is the standalone obligation for the `no-extra-mediation` NFR relocation.

## Solution approach

Move the "No additional access channels" paragraph and its `<a id="no-extra-mediation"></a>` anchor off `conversation-drive.md` to a runtime-wide owner (the parent `pi-integration.md` page or a dedicated trust-boundary topic page), migrating the anchor in the same edit so existing citations continue to resolve. Add a forward-link at the old site pointing at the new home.

## Solution constraints

- Out of scope: the degraded-state material owned by F-1166.

## Relationships

- F-1261 "Extension-manifest paragraphs sit on host-prerequisites.md" — same-cluster (shard-07 placement; resolves independently)
- F-1262 "PIC-7 reason-set pin split from its handler" — same-cluster (shard-07 placement; resolves independently)
- F-1263 "Two named host presuppositions inline in registration-steps.md" — same-cluster (shard-07 placement; resolves independently)
