---
id: F-1515
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/session-only-degraded-state.md, anchor: "§\"Emergent fast-path (sub-steps 2–3 only)\"" }
lens: [spec-lens-naming, spec-lens-scope]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [naming, scope]
title: "\"Emergent fast-path\" section heading mischaracterizes universal sub-step 2/3 sequencing as degraded-state behavior"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The final section of `session-only-degraded-state.md` is headed `## Emergent fast-path (sub-steps 2–3 only)`. The heading coins a non-literal label ("emergent" carries no defined meaning; "fast-path" invites a reading that session-only reasons take a shortened sequence — which the body must then spend a sentence denying). The body itself scopes the sub-step 2/3 sequencing rule to all five closed-set reasons and references the always-tear-down branch (`"quit"`, `"reload"`), so the behavior is not degraded-state-specific; the heading frames general-handler sequencing as if it were a degraded-state capability, in a file whose subject is the degraded branch.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `5e8b499` (2026-05-06, "pi-loom spec: resolve \"Hot-reload teardown contract for the loom extension instance is unspecified\""); `9559e19` (2026-05-07, "pi-loom spec: resolve \"session_shutdown unknown reason: no closed-set validation, no fail-safe\""); `f5e89f4` (2026-06-04, "docs: enforce 100KB hard / 30KB recommended size cap on spec set")
**History:** `git log -S` on the defect tokens against `pi-integration-contract.md` and the post-split `session-only-degraded-state.md` traces the misframing to an interaction of three commits, not a single edit. `5e8b499` first seeded the reason-attributed framing in the `new`/`resume`/`fork` edge-case bullet ("a no-teardown reason makes the sequence a fast-path no-op (no active invocations exist at session boundaries because Pi serialises turns)") — already conflating the fast-path with the reason rather than with registry emptiness. `9559e19`, while adding the closed-set validation and Unknown-reason rule, appended the specific clause this finding targets: "This emergent fast-path is available only for the five enumerated reasons; an unknown `event.reason` … MUST run the full five-sub-step sequence even when the registry is empty." `f5e89f4` (the 30/100 KB split) then restructured the bullet into the dedicated *Emergent fast-path (sub-steps 2–3 only)* section, narrowing the fast-path to sub-steps 2–3 and adding "a session-only reason does **not** skip sub-step 4 or any other sub-step" — which sharpens the contradiction by retaining the reason-gated sentence beside an explicit registry-emptiness scoping. The durable fix addresses the interaction (the retained reason-gated sentence versus the narrowed emptiness-driven scoping), not a revert of any one commit.

## Solution approach

Rename the `## Emergent fast-path (sub-steps 2–3 only)` heading to a literal description of the section's content (the empty-registry behavior of sub-steps 2–3), and rewrite the body to drop the "emergent fast-path" coinage in favor of plain wording — the registry is frequently empty on session-only reasons, making sub-steps 2 and 3 instantaneous, and a non-empty registry is handled identically to the always-tear-down branch.

## Solution constraints

- Preserve in meaning the two normative statements the body carries: that a session-only reason does not skip sub-step 4 or any other sub-step, and that an unknown `event.reason` runs the full five-sub-step sequence even on an empty registry.
- Out of scope: relocating the section out of `session-only-degraded-state.md`.

## Relationships

- F-2226 "Gated and non-gated capabilities interleaved without visual separation" — same-cluster (both reorganize this file's section structure — that finding proposes splitting buildable-now from gated sections — but resolve independently)
