---
id: F-1717
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/provider-error-mapping.md, anchor: "**Provider error mapping** opening sentence and **Re-validation gate (loom 1.0.0)** sub-paragraph" }
lens: [spec-lens-clarity, spec-lens-testability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [clarity, testability]
title: "Re-validation gate — MUST obligation vs SHOULD acceptance-mechanism modal mismatch"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The opening sentence of **Provider error mapping** in
`provider-error-mapping.md` sets a MUST-level obligation that the matching
rules "be re-validated on each upgrade", but the only acceptance mechanism
the page states — the *Re-validation gate (loom 1.0.0)* sub-paragraph — is
SHOULD-level and explicitly acknowledged to be un-gated (no CI step re-runs
the provider-error fixtures). The two modals contradict: under MUST a bump
that skipped the fixtures has shipped non-conforming, while under the SHOULD
acceptance mechanism the same bump conforms. A reader cannot derive a
definitive pass/fail signal, and the spec offers no mechanical adjudicator.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:**

- `82be125` (`spec: adopt P1 typed-query two-phase loop (Option A)`, 2026-05-04) — earliest commit reachable from `main` to introduce a "MUST be re-validated on each upgrade" sentence into the spec corpus.
- `aebec4e` (`pi-loom spec: resolve "Pi version-bump gate cannot detect three unstated host/provider presuppositions"`, 2026-06-04) — added the *Re-validation gate (loom 1.0.0)* sub-paragraph in `provider-error-mapping.md`, initially worded as "a contributor performing the bump **is expected to** run them" (non-modal, so no MUST/SHOULD contradiction yet).
- `c326bfa` (`pi-loom spec: resolve "Shard-13 clarity / cruft cluster on provider-error and session-shutdown pages"`, 2026-06-07) — rewrote that sub-paragraph's "is expected to run them" to "**SHOULD** run them … per item (i) of the *Editorial-review checklist*". This is the commit that introduced the literal MUST/SHOULD pairing on the same paragraph and made the modal contradiction explicit.

**History:** The MUST-level obligation on the matching rules predates the
*Re-validation gate* sub-paragraph by a month. When the sub-paragraph was first
inserted (`aebec4e`) it used the non-modal phrase "is expected to", which is
soft enough that it does not visibly contradict the MUST above it. Three days
later (`c326bfa`) the soft phrase was rewritten as SHOULD to align with the
editorial-review checklist's vocabulary, which is internally consistent — but
the rewrite did not revisit the still-MUST obligation in the immediately
preceding sentence on the same page, surfacing the defect. The durable defect
is therefore the interaction between a pre-existing MUST and a later SHOULD on
adjacent sentences, not a single regression.

## Solution approach

Change the headline modal in the **Provider error mapping** opening sentence
of `provider-error-mapping.md` from MUST to SHOULD, bringing the
re-validation obligation into line with its only stated acceptance mechanism
(the SHOULD-level *Editorial-review checklist* the *Re-validation gate
(loom 1.0.0)* sub-paragraph cites). Leave the *Re-validation gate
(loom 1.0.0)* sub-paragraph in place — it carries the acceptance-mechanism
citation the SHOULD now points at.

## Solution constraints

- Out of scope: the **Provider seed-field mapping** "MUST be re-validated on
  each upgrade" obligation on the same page — its MUST is backed by a
  build-time `Api`-coverage mechanical gate and is correctly MUST-level.
- Out of scope: `version-bump-step2.md` item (i) and `version-bump-intro.md`.

## Relationships

None
