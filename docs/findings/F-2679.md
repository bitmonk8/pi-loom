---
id: F-2679
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/version-bump-intro.md, anchor: "Pi version bump procedure / step 1 / Non-goals" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "Editorial-review arm; items (a)–(as)" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2b.md, anchor: "heading" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-steps-3-4.md, anchor: "heading" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-triggers.md, anchor: "heading; step 5 groupings/triggers; `.assert.ts` glob" }
lens: [spec-lens-prescription, spec-lens-naming, spec-lens-placement, spec-lens-scope, spec-lens-cruft]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [prescription, naming, placement, scope, cruft]
title: "Give step 1 a discoverable home (TOC entry or dedicated file)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

Step 1 ("Re-typecheck against the new package") of the Pi version bump procedure exists only as a numbered list item mid-page inside `version-bump-intro.md` under `#pi-version-bump-procedure`, with no dedicated topic file and no page-index entry. Steps 2, 2b, 3–4, and 5–7 each have their own file and heading. Step 1 is therefore not discoverable from the procedure's page index in the way the other steps are.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** no single commit; organizational residue accreted across the page-split and checklist-growth history. Most identifiable single-defect origins: `a7a172c` ("version-bump-triggers.md H1 mis-scopes page contents", 2026-06-05) re-titled the H1 from "Version bump triggers" to "Version bump steps 5–7" while leaving the filename `version-bump-triggers.md` (the filename/heading mismatch); `11ee11c` (F-2252, "position editorial-review checklist as step 2's editorial arm") introduced the third label alongside the pre-existing "step 2 audit"; `1a59ccf` (F-2264) added the MUST item (ar) among the SHOULD items and demoted the inbound-reference audit gate to post-1.0; `74633aa` folded the inventory-closure post-1.0 framing inline across nine pages.
**History:** `git log --follow` on the five `version-bump-*.md` files shows the procedure originated as a single section, was fragmented into `spec_topics/` topic pages by `81bf52a` ("collapse spec/ section pages into spec_topics/") and the `f5e89f4` size-cap enforcement, then further split (`59819f8` F-1600 relocated steps 3–4 to their own file; `23d28a1` F-0958 realigned audit-cluster sibling files to basenames) — producing the inconsistent filename/heading styles and the step-1 placement. The ~35–40 SHOULD-escalation clauses and items (f)–(as) accreted incrementally over a long series of fix commits (e.g. F-0560, F-0718/F-0823, F-0824, F-0833, F-0981, F-1227, F-1618, F-2264). No commit set out to introduce a defect; the residue is the cumulative byproduct of repeated structural edits, which is why the durable fix is per-site cleanup rather than a revert.

## Solution approach

Give step 1 the same per-step discoverability the sibling steps have: either add an anchored subsection (with a page-index entry) for step 1 within `version-bump-intro.md`, or split step 1 into a dedicated `version-bump-step1.md` matching the sibling step files. Choose whichever option fits the topic-page size and layout conventions.

## Solution constraints

- None.

## Relationships

- F-2675 "Separate post-1.0 hardening notes from loom-1.0 obligations and add an impacted-sections summary" — co-resolve
- F-2676 "Restate the `CAPABILITY_OBLIGATIONS.length === 7` count gate behaviourally" — co-resolve
- F-2677 "Collapse the duplicated SHOULD-escalation clauses into one preamble statement" — co-resolve
- F-2678 "Visually mark item (ar) as a MUST distinct from the surrounding SHOULD items" — co-resolve
- F-2266 "Rename `version-bump-triggers.md` to match its heading and normalise version-bump heading style" — co-resolve
- F-2680 "Unify the three labels for the step-2 editorial audit" — co-resolve
- F-2681 "Disambiguate the overlapping `(i)`–`(iv)` grouping vs `(i)`–`(ii)` trigger ordinals in step 5" — co-resolve
