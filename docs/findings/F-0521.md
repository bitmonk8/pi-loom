---
id: F-0521
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9b-registration-reload-wiring.md, anchor: "leaf body (Adds / Tests / Deps / Ships when)" }
  - { path: docs/plan_topics/V9b-T-registration-reload-wiring.md, anchor: "paired tests leaf" }
lens: [plan-lens-step-atomicity]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [step-atomicity]
title: "V9b bundles four unrelated concerns in one leaf"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`V9b` (`docs/plan_topics/V9b-registration-reload-wiring.md`) carries four
behaviourally-distinct concerns: registration steps 1–5, the single
`ReloadFailureInjector` declaration, the two step-5 structural-change
watcher-note scenarios (`cka-23.note-empty`/`cka-23.note-rename`), and the
model-reference-matcher production wiring. The model-matcher wiring is the
clear outlier — a different verb (construct-and-inject rather than
register-and-watch), a different spec page, a different downstream consumer
(`V6a`'s `loom/load/model-unresolved` resolution), and the sole reason `V9b`
carries its `V6a` dependency edge. The leaf's `Ships when.` consequently packs
seven independently-failable behaviours into one acceptance claim, so a
reviewer cannot distinguish a partially-built `V9b` from a complete one.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `c6a664e` (initial plan build — registration pipeline + structural-change note present at inception, alongside the drain-state contract later split to `V9m`); `d64dce5` / `fdce2a2` (introduced and consolidated the `ReloadFailureInjector` watcher-time reload failure-injection seam onto `V9b`); `5953b18` / `ccedadf` (assigned the model-reference-matcher producer role and its tests to `V9b`).
**History:** The plan corpus is git-tracked. `git log --follow` on the leaf (renamed `V9b-registration-drain-state.md` → `V9b-registration-reload-wiring.md` at `44a96f5`) shows the four-concern bundle is an accretion, not a single-commit introduction: the registration pipeline and the structural-change watcher note were present at inception (`c6a664e`); the `ReloadFailureInjector` seam was grafted on while resolving the watcher-time reload-injection findings (`d64dce5`, `fdce2a2`); the matcher wiring was grafted on while resolving the `V6a`-producer and matcher-untested findings (`5953b18`, `ccedadf`). Each prior finding-resolution legitimately added an orthogonal concern to the same leaf; a prior systemic over-bundling pass (`cb8737a`) did not unbundle this one.

## Solution approach

Extract the model-reference-matcher production wiring out of `V9b` and `V9b-T`
into a new paired V9-phase leaf (implementation leaf plus its `-T` tests
partner). Move the matcher `Adds.` paragraph and the
`model-reference-matcher production wiring` `Tests.` bullet off both `V9b` and
`V9b-T`, strike the matcher clause from `V9b`'s `Ships when.`, and re-point the
`V6a` and `V11a` cross-references — together with `V9b`'s `V6a` `Deps.` edge —
onto the new leaf. Leave registration steps 1–5, the `ReloadFailureInjector`
declaration, and the step-5 watcher-note scenarios on `V9b`.

## Solution constraints

- The model-matcher cross-references in `V6a` and `V11a` (and `V9b`'s `V6a` `Deps.` edge) must re-point to the new leaf together with the move, or the matcher producer is left orphaned.
- The new leaf ID and its `-T` partner follow the plan's `V<N><letter>` / `-T` scheme (next free letter in phase V9, no reuse); keep `docs/plan_topics/conventions.md` and `docs/plan_topics/coverage-matrix.md` consistent, leaving the `cka-23` coverage-matrix row single-leaf at `V9b`. The spec is read-only for this fix.

## Relationships

- F-1113 "V9c interleave-note vs Deps contradiction (`V11a`/`V9b` → `V9c`)" — same-cluster (touches V9b but resolves independently)
- F-0495 "Step bundles four independent enforcement mechanisms" — same-cluster (H2a)
- F-1101 "Step bundles three distinct deliverable types" — same-cluster (H4a)
