---
id: F-1255
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V13c-T-query-tool-loop.md, anchor: "Tests, Deps" }
lens: [spec-lens-cruft]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [cruft]
title: "V13c-T omits the ERR-13 delegated-witness bullet and `V4f` Dep its impl partner carries"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`V13c` (impl) carries an `ERR-13` delegated live-carrier witness bullet — "a query-tool-loop callee driven to completion on the live `V13c` surface, then a downstream `?`/panic/cancel fired, leaves the completed callee's side effect in place with no compensating turn injected" — and lists `V4f` in its **Deps**. Its paired tests leaf `V13c-T` carries neither: its Tests list stops after the depth-6 co-fire bullet, and its **Deps** are `V13b, V9c, V16a, V5e, V8c, H4b` (no `V4f`).

`V4f` delegates the live-surface re-assertion of its completed-callee-finality vector to the `V14a`, `V13c`, and `V15a` production surfaces, and `coverage-matrix.md`'s `ERR-13` row lists `V13c` as a co-witness ("delegated completed-callee-finality live-carrier"). Under the per-phase TDD ritual (`conventions.md` §Per-phase TDD ritual — the tests task must "write the failing tests for *every* spec REQ-ID the feature introduces", authored red before the impl leaf), an implementer reads `V13c-T` to author the failing tests first. Because the `ERR-13` bullet is absent from `V13c-T`, the delegated witness is never authored red, and `V13c-T` would be pickable without `V4f` complete even if its suite references the delegated surface.

## Issue introduction

**Verdict:** introduced-by-specific-commit
**Introducing commits:** `7bd65a7` — "pi-loom plan: resolve "V4c/V4f defer ERR-8 to ERR-13 live-surface confirmation to downstream owners that never re-assert it"" (2026-06-18)
**History:** `git log -S` confirms the `ERR-13` delegated-witness bullet and the `V4f` Deps edge first entered `V13c-query-tool-loop.md` in `7bd65a7`; `git log -S "ERR-13"` and `git log -G "V4f"` over `V13c-T-query-tool-loop.md` return no commits — neither has ever appeared in the tests leaf. That commit's `--stat` touched the `V13c`, `V14a`, `V15a`, `V4c`, and `V4f` impl leaves but no `-T` leaf, so the fix that pushed the delegated witness onto the three live-carrier impl leaves left all three paired tests leaves stale at the moment of introduction.

## Solution Space

**Shape:** single

### Recommendation

In `docs/plan_topics/V13c-T-query-tool-loop.md`:

- Add the `ERR-13` delegated live-carrier witness bullet to the **Tests** list, copied from `V13c`'s bullet: `` `ERR-13` (delegated live-carrier witness for `V4f`'s completed-callee-finality deferral): a query-tool-loop callee driven to completion on the live `V13c` surface, then a downstream `?`/panic/cancel fired, leaves the completed callee's side effect in place with no compensating turn injected. ``
- Add `V4f` to the **Deps** field so it reads `` `V13b`, `V9c`, `V16a`, `V5e`, `V8c`, `V4f`, `H4b` ``, matching the impl leaf's edge.

`V4f` is in shard-03; the edit is confined to the `V13c-T` plan leaf and touches no spec file.

## Consequence

**Severity:** correctness

The delegated `ERR-13` live-carrier witness `coverage-matrix.md` maps onto `V13c` is never authored as a red-first test, so `V13c` can ship green without ever verifying that a completed query-tool-loop callee's side effect survives a downstream terminal event. The co-witness mapping passes vacuously, and an implementer following the TDD ritual from `V13c-T` never encounters the requirement.

## Relationships
- F-1258 "V14a-T Tests/Deps stale vs paired impl leaf (missing ERR-13 delegated witness + V4f dep)" — same-cluster (identical defect on the `V14a`/`V14a-T` pair from the same commit `7bd65a7`; resolves independently in its own file)
- F-1255 "Tests/Deps stale vs paired impl leaf (missing ERR-13 delegated witness + V4f dep)" — same-cluster (the `V15a-T-invocation-core.md` instance; same defect and commit, resolves independently in `V15a-T`)
- F-1243 "ERR-8 delegated live-carrier witness has no red-first test and no Ships-when gate" — same-cluster (structurally identical missing-red-first-witness defect for the `ERR-8` delegation on the `V9i`/`V9i-T` pair)
