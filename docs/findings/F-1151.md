---
id: F-1151
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics/diagnostic-shape.md, anchor: "**Serialised content format** paragraph" }
lens: [spec-lens-clarity (shard-04)]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [clarity (shard-04)]
title: "`diagnostic-shape.md` — `Serialised content format` does not pin the relative order of the hint continuation and the related-site lines"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The **Serialised content format** paragraph in `docs/spec_topics/diagnostics/diagnostic-shape.md` builds each per-`Diagnostic` block from three line classes — a mandatory main line, an optional `"\n  hint: <hint>"` continuation, and zero or more two-space-indented related-site lines (one per `Diagnostic.related[]` element). The prose introduces the hint and related-site lines independently but never says which class comes first when both are present, nor whether related-site lines may interleave with the hint. Two conformant implementations — one emitting hint-then-related, one related-then-hint — both satisfy the prose yet produce byte-divergent `content` strings on any diagnostic carrying both a `hint` and a non-empty `related` array. DIAG-4 is silent on inter-line composition of the surrounding `content` string and so does not absorb the gap.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commit:** `fae85d3` (2026-05-04, *Tighten spec for implementation-plan readiness*)
**History:** The original **Serialisation to Pi's flat shape** paragraph entered the then-monolithic `spec.md` in `fae85d3` as a single sentence: *"… optionally followed by `"\n  hint: <hint>"` when a hint is present. Related sites are appended as additional indented lines."* The two appended-line classes (hint continuation, related-site lines) were both authored "appended" without an order rule. Commit `fecb504` split `spec.md` into `spec_topics/` and carried the paragraph verbatim into `spec_topics/diagnostics.md`; `f5e89f4` (2026-06-04) split `diagnostics.md` into the subdirectory layout and carried the paragraph verbatim again into `spec_topics/diagnostics/diagnostic-shape.md`. Commit `8837a93` (2026-06-05, *resolve "Serialised content related-site line format is undefined"*) replaced the terse *"Related sites are appended as additional indented lines."* sentence with the current per-line format spec (`"  <file>:<line>:<col>: <message>"`, two-space indent matching the hint line, `range.start` endpoint, no `<code>` prefix), but kept the "appended" framing for both line classes and did not introduce an intra-block ordering rule. The ordering gap has therefore been present since the paragraph's first authoring; the `8837a93` enrichment closed the per-line *format* gap while leaving the relative-order question untouched.

## Solution approach

Clarify the **Serialised content format** paragraph in `docs/spec_topics/diagnostics/diagnostic-shape.md` to pin the relative order of the three intra-block line classes — main line, `hint` continuation, and related-site lines — when more than one is present, and to state whether related-site lines may interleave with the hint. Make the rendered `content` deterministic for any `Diagnostic` carrying both a `hint` and a non-empty `related` array, and pin related-site lines to `Diagnostic.related` array order.

## Solution constraints

- Out of scope: which string is the canonical `content` value relative to the DIAG-4 *Message* column — owned by F-1149.
- Out of scope: the main-line format and the location-less closed set — owned by F-1152.

## Relationships
- F-1149 "diagnostic-shape.md / code-registry-runtime.md — three competing strings for a runtime-panic note (DIAG-4 Message vs serialised-content vs row prose)" — same-cluster (also touches the **Serialised content format** paragraph's relationship with DIAG-4's *Message* column; resolves independently because it disambiguates which string is the canonical `content` value, not the intra-block layout of that value)
- F-1152 "diagnostic-shape.md — location-less closed set omits load/discovery codes that have no token span" — same-cluster (sibling defect on the same paragraph's main-line format rather than on the appended-continuation ordering; resolves independently)
