---
id: F-2424
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/binder/determinism-cancellation-failure.md, anchor: "Failure-mode templates (normative)" }
  - { path: docs/spec_topics/binder/defaulting-system-note-echo.md, anchor: "System-note rendering rule 1" }
lens: [spec-lens-clarity, spec-lens-implementability, spec-lens-testability]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [clarity, implementability, testability]
title: "Pin the transport-row `<message>` source for the binder path"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
decision_axes: 2
---

## Problem

The transport-failure row of the Failure-mode templates table renders `loom /<name>: argument binder unavailable (<provider>: <message>)`, but only `<provider>` is pinned. `<message>` has no pinned source. The sole `message` derivation in `queryerror-variants.md` is the prompt-mode path, which does not apply to the binder's off-session `complete()` call, so the binder-path source for `<message>` is unstated and conforming implementations diverge on the rendered text.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** 83802db — spec: collapse to single typed argument style; add LLM-driven slash-command binder (2026-05-04, Thomas Andersen); be82734 — pi-loom spec: resolve "Ceiling #3 (binder retry budget): provider-failure classification rule undefined" (2026-05-07, Thomas Andersen); f41af5d — pi-loom spec-plan: fix F-1131 — add the custom-type-unsafe Failure-mode-templates row (2026-06-24, Thomas Andersen)
**History:** The failure-mode-templates table was introduced with the LLM-driven binder at 83802db, where the transport row's `<message>` placeholder was already present and unpinned. be82734 added the rule routing structural `ContextOverflowError` into that same transport row without defining how an error variant lacking a `provider` field populates `<provider>`/`<message>`. f41af5d later added the custom-type-unsafe row carrying `<value>` without pinning its source or escaping. (A subsequent clarity pass, 2fb5783, pinned `<provider>` and `<ajv-summary>` explicitly but left `<message>` and `<value>` unpinned.)

## Solution approach

In the `Failure-mode templates (normative)` prose, pin the transport row's `<message>` to `TransportError.message`, rendered through `System-note rendering` rule 1. State the off-session `message` source for binder transport failures, reconciling against the `message` derivation in `queryerror-variants.md` (either extend it to cover the binder's `complete()`-sourced failures or cross-reference it and confirm which arm applies off-session).

## Solution constraints

- Out of scope: the `ContextOverflowError`→transport-row `<provider>`/`<message>` mapping (owned by F-2425) and the custom-type-unsafe row's `<value>` and its escaping (owned by F-2426).

## Relationships

- F-2425 "Define the `ContextOverflowError`→transport-row `<provider>`/`<message>` mapping" — co-resolve
- F-2426 "Pin `<value>` and its escaping in the custom-type-unsafe row" — co-resolve
