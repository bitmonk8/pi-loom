---
id: F-1200
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/query/query-forms.md, anchor: "§ \"Dedent and newline-trim — normative behaviour\"" }
lens: [spec-lens-clarity]
tier: high
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: triaged
category: [clarity]
title: "`query/query-forms.md` — dedent has two normative authorities (CPython `textwrap.dedent` vs the eight-vector table)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The "Dedent and newline-trim — normative behaviour" section of `docs/spec_topics/query/query-forms.md` opens with a sentence that names *two* normative references in one breath: "The normative reference is the behaviour of CPython 3.x `textwrap.dedent` *as illustrated by the table below*; an implementer who cannot read the CPython source can still pass the conformance tests from the spec alone." This invites a divergence whenever CPython's behaviour and the table's coverage are not identical — i.e. always, because the table has eight rows and `textwrap.dedent` has an effectively unbounded input domain.

The three numbered behaviour bullets and the eight-vector table cover ASCII space and ASCII tab inputs with LF line endings only. CPython's `textwrap.dedent` (an `re.sub` over the `(?m)^[ \t]+` pattern in stripped lines, with `_whitespace_only_re` matching `[ \t]+$`) has externally observable behaviour on inputs the spec never names: CRLF and bare-CR line endings (does the LF or the whole CR/LF participate in "whitespace-only line" detection? what does the common-prefix walk do across mixed line terminators?), non-ASCII leading whitespace such as U+00A0 NO-BREAK SPACE or U+3000 IDEOGRAPHIC SPACE (CPython's whitespace-only-line regex `^[ \t]*$` does *not* match these — they participate in the literal common prefix; the spec's three bullets say "tab-only and space-only … uniformly" but do not address the wider Unicode whitespace question), and exotic inputs such as a sole `\r\n` template body. Two implementers — one who pins behaviour to the table and a Python port of `textwrap.dedent`, one who pins behaviour to the spec's three bullets plus the eight vectors — will produce divergent rendered prompt text on these inputs without either contradicting the spec text. Because the rendered text is what the model sees, this is a wire-observable divergence.

The asymmetry is structural: the table is *illustrative of* CPython behaviour, not a superset or replacement of it. Whenever the two are co-listed as "the normative reference", an implementer hitting an input outside the table is forced to guess which authority wins. The fix is to pick one — and the natural choice given the surrounding ecosystem (loom is implemented in TypeScript, not Python) is to make the spec itself authoritative and the CPython reference a non-normative pointer to a working implementation of the same algorithm.

## Issue introduction

**Verdict:** introduced-by-commit
**Introducing commits:** `66e419d` (2026-05-04 — "pi-loom spec: resolve \"Dedent normalisation has no normative test vectors\"")
**History:** The prior wording (commit `53f5831`, 2026-05-03, "spec: rewrite query primitive, system prompts, and schema syntax") named a single authority — "(Python's `textwrap.dedent` algorithm)" — as an offhand parenthetical and supplied no table. Commit `66e419d` added the table, the three behaviour bullets, and the dual-authority sentence ("The normative reference is the behaviour of CPython 3.x `textwrap.dedent` *as illustrated by the table below*; an implementer who cannot read the CPython source can still pass the conformance tests from the spec alone") in a single hunk against `spec_topics/query.md`. That commit was a direct response to a prior review finding that the dedent rule lacked normative test vectors; in fixing the missing-vectors problem it introduced the dual-authority problem this finding now names. The file split (`f5e89f4`, 2026-06-04) and the addition of vector 8 (`d083d70`, 2026-06-06) carried the dual-authority sentence forward verbatim without revisiting it.

## Solution Space

**Shape:** single

### Recommendation

Make the spec text the sole normative authority for dedent and newline-trim behaviour, and demote the CPython reference to a non-normative pointer.

In `docs/spec_topics/query/query-forms.md` § "Dedent and newline-trim — normative behaviour":

- Rewrite the opening sentence so the spec text — the three behaviour bullets, the eight-vector table, and the obligations added below — is the only normative reference. Add a short non-normative aside noting that CPython 3.x `textwrap.dedent` is one working reference implementation of the same algorithm.
- Extend behaviour bullets 1–3 to close the currently-ambiguous inputs:
  - **Line endings.** State that newline-trim and dedent operate on LF (`\n`)-terminated lines: a literal `\r` byte in the template body is ordinary content that does not participate in line splitting and does not satisfy the "whitespace-only line" predicate. This matches every other line-oriented rule in the spec.
  - **Whitespace alphabet.** Pin "leading whitespace" to ASCII space (U+0020) and ASCII tab (U+0009) only. State that any other code point — including U+00A0 NO-BREAK SPACE, U+3000 IDEOGRAPHIC SPACE, and the rest of the Unicode whitespace block — is treated as ordinary content for *both* the common-prefix walk *and* the whitespace-only-line predicate. Cross-reference `binder/defaulting-system-note-echo.md` § "System-note rendering" rule 1 for this ASCII-whitespace alphabet rather than restating it, keeping one whitespace definition across the spec.
- Append two or three vectors to the table (with matching commentary entries) exercising the LF-only line-ending rule and a non-ASCII-whitespace leading character (e.g. a line indented with U+00A0 or U+3000), so the new obligations have falsifiable fixtures.

Site the rationale for the LF-only line-ending rule beside it, so authors pasting Windows-line-ending templates understand that a `\r` is preserved as content. The `descriptions.md` cross-reference ("same algorithm as query-template dedent") inherits whatever this section says, so authors of `///` doc-comment lines see the same alphabet.

## Consequence

**Severity:** correctness

Two implementers conforming to the literal spec text can produce different rendered prompt strings on inputs outside the eight tabulated vectors (CRLF templates, templates whose lines are indented with U+00A0 or U+3000, templates mixing CRLF and LF). Different rendered prompts produce different model outputs, so a conformance gap here directly weakens cross-implementation reproducibility of typed-query results.

## Relationships
- F-1155 "discovery/package-and-settings.md — DISC-6 `discovery-slow` details shape incomplete; DISC-5 intra-class insertion-order unfalsifiable; DISC-1 `~user` outcome unstated; query-tool-loop right-trim vector absent" — same-cluster (also a missing-normative-vector gap in the query family, but on the typed-query right-trim rule rather than on dedent)
