---
id: F-1350
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/unknown-reason-rule.md, anchor: "membership-check / constant-source / handler-trycatch sub-anchors; closed-set / diagnostic-code / details.failure MUSTs" }
lens: [spec-lens-traceability]
tier: high
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [traceability]
title: "Coin `PIC-N` anchors on `unknown-reason-rule.md` (un-anchored normative MUSTs)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

`unknown-reason-rule.md` carries three sub-anchored runtime obligations (`#unknown-reason-rule-membership-check`, `#unknown-reason-rule-constant-source`, `#unknown-reason-rule-handler-trycatch`) plus the anchor-stable contract-surface MUSTs (the two diagnostic codes, the closed-reason-set literal, and the three `details.failure` discriminator literals), none of which is coined as a `PIC-N` REQ-ID. GOV-9's `#prefix-n` cross-link contract is therefore unsatisfiable for these sites. This is one page of the systemic PIC residue and resolves independently.

## Issue introduction

**Verdict:** present-since-inception-with-progressive-extension
**Introducing commits:**
- `c1e5d64` (2026-05-05) — registered the per-page REQ-ID prefix table and deferred per-rule coinage on pages not in the five-page Phase 12a pass (foundational PIC deferral).
- `6d8bea5` / `d4ac81b` (2026-06-04) — drained partway by coining `PIC-3`…`PIC-15`; deliberately incomplete.
- `f5e89f4` (2026-06-04) — split the 576 KB monolith into 30 child files, preserving anchors but coining none.
- `4f588e0` (2026-06-03) — added GOV-22, codifying per-commit progressive drainage.

**History:** The gap is steady-state under GOV-22. The monolith split fanned the residue across 30 files at once; combined with H5e's closed-set load at H6a, the residue cannot remain steady-state through to release, so the defect requires a one-shot drain rather than passive wait for incidental edits.

## Solution approach

Coin the next free `PIC-N` REQ-ID anchors in GOV-1 dual-form at the defining obligation sites on `unknown-reason-rule.md` — the three sub-anchored obligations and the anchor-stable contract-surface MUSTs — per GOV-22, querying the current PIC high-water mark at edit time. Keep the existing page-local sub-anchors as navigation aids. Update the `cka-28` coverage-matrix row to reference the coined `PIC-N` IDs.

## Solution constraints

- Out of scope: the in-paragraph code/literal restatements the rule's own carve-out excludes from anchor-stable contract surface — coin `PIC-N` only at the defining obligation sites, not at the illustrative restatements.

## Relationships

- F-1344, F-1345, F-1346, F-1347, F-1348, F-1349, F-1351 (sibling per-page PIC-N coinages) — same-cluster
