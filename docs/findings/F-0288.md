---
id: F-0288
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V13a-query-render.md, anchor: "`V13a` leaf body" }
  - { path: docs/plan_topics/V13a-T-query-render.md, anchor: "paired `V13a-T` tests leaf" }
  - { path: docs/plan_topics/H5b-warn-only-canary.md, anchor: "`Deps` `V13a`–`V13d` range" }
  - { path: docs/plan.md, anchor: "`V13 — Query` index list" }
lens: [plan-lens-step-atomicity]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [step-atomicity]
title: "`V13a` bundles five independent query surfaces into one leaf"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

`V13a` ("Query render and escapes") bundles five separable query-surface concerns — template-text rendering, escape handling, interpolation stringification, result-discipline / discard observability, and degenerate-template short-circuit — into a single feature leaf. `conventions.md` §"Vertical slices" defines a leaf as "the smallest feature that can ship and be tested independently," but the result-discipline / discard-observability surface concerns the `Result` lifecycle and the `V9d` runtime-event channel rather than template text, so it is independent of how a template renders. Bundling it with the render pipeline makes `V13a` materially larger than its V13 siblings.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `c6a664e` (2026-06-10) "pi-loom plan: build/update plan for spec.md + review"
**History:** `V13a` was authored at its current five-surface scope in the initial plan-build commit `c6a664e`. `git log --follow` over `docs/plan_topics/V13a-query-render.md` shows the three later commits (`119d8ec`, `94fce1b`, `6698d91`) only refined `Deps` and `Tests` wording (adding `V9d`, fixing the illegal-escape citation, splitting the discard-event phrasing); none changed the leaf's scope. The bundling is original to the leaf, not introduced by a later edit.

## Solution approach

Carve the result-discipline / discard-observability surface out of `V13a` into its own new feature leaf with a paired `-T` tests task, leaving render → escapes → stringify → degenerate short-circuit in `V13a` as one cohesive rendering pipeline. Move the `loom/parse/discarded-query-result` parse-error and runtime discard-observability `Tests` content, its matching `Adds` clause, and the `V9d` `Deps` entry from `V13a` onto the new leaf so the `V9d` dependency and the runtime-event assertions travel together. Add the new impl leaf to `H5b`'s `Deps` and to the `coverage-matrix.md` QRY row in the same change, per the Transitive-completeness plan-maintenance clause.

## Solution constraints

- The spec is read-only for this fix; no spec edits.
- New leaf IDs follow the plan's `V<N><letter>` scheme with a paired `-T` task per `conventions.md` §"Leaf format"; do not reuse a retired ID.

## Relationships

None