---
id: F-1262
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-prerequisites.md, anchor: session-binding-contract }
lens: [spec-lens-placement, spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [placement, scope]
title: "PIC-7 reason-set pin is split from the session-shutdown handler that consumes it"
relations:
  - { kind: related, id: F-1260 }
  - { kind: related, id: F-1261 }
  - { kind: related, id: F-1263 }
  - { kind: related, id: F-1166 }
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

PIC-7 *Session-binding contract* is defined on `host-prerequisites.md` (the single-active-session premise, the `SessionShutdownEvent['reason']` union pin, and the teardown-vs-session-swap partition discriminator at presupposition (d) under `#degraded-state-host-prerequisites`), but the handler that branches on the reason-set membership and that partition lives on `session-shutdown-semantics.md`. The closed reason set and its partition are defined away from the handler that consumes them. The cross-links resolve, so this is a navigability split, not a broken reference.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:**
- `d4ac81b` (2026-06-04) — "resolve 'PIC sections beyond Probe-wide invariants — missing REQ-ID anchors'" — coined the `pic-7` anchor on the Session-binding-contract paragraph, fixing the split between PIC-7 metadata and the handler at that location.

**History:** Split from F-1143 (shard-07 placement cluster) under the finding-atomicity invariant; standalone PIC-7 split-definition obligation.

## Solution approach

Keep the single-active-session premise sentence on `host-prerequisites.md` (a genuine Pi-side prerequisite). Move the `SessionShutdownEvent['reason']` union pin and the teardown-vs-session-swap partition framing onto `session-shutdown-semantics.md` beside the consuming handler, re-home the `pic-7` / `session-binding-contract` anchor onto the handler-side body, and leave a forward-link pointer at the former prerequisites site. Update the `session-model-and-appendix.md` SM-3b citations and confirm the V18c checklist item (f) on `version-bump-step2.md` and `version-bump-intro.md`'s step-5 type-equality citation still resolve to the re-homed anchor.

## Solution constraints

- Out of scope: the session-shutdown degraded-state material on `session-shutdown-semantics.md` that F-1166 reworks — re-home the anchor and pin without editing that material.

## Relationships

- F-1166 "Session-only degraded-state feature — record governed-by-rebind and remove the feature" — must-follow (F-1166 reworks/removes session-shutdown degraded material on `session-shutdown-semantics.md`; land the `pic-7` re-home after that rework so the anchors settle once)
- F-1260 "no-extra-mediation NFR on conversation-drive.md" — same-cluster (shard-07 placement; resolves independently)
- F-1261 "Extension-manifest paragraphs on host-prerequisites.md" — same-cluster (shard-07 placement; resolves independently)
- F-1263 "Two named host presuppositions inline in registration-steps.md" — same-cluster (shard-07 placement; resolves independently)
