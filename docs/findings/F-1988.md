---
id: F-1988
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics/diagnostic-shape.md, anchor: "Normative elaboration of the masked?: array<string> field, including \"implementations MUST NOT..." }
lens: [spec-lens-placement]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [placement]
title: "Normative elaboration of the masked?: array<string> field, including \"implementations MUST NOT..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

Normative elaboration of the `masked?: array<string>` field, including "implementations MUST NOT emit `masked: []`", the per-site reachable mask domain, and the delegation to PIC-1 for the V1 reachable predicate and pure-read MUST. The `masked?` field is declared in the `Diagnostic` shape code block near the top of the same file.
