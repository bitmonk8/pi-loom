---
id: F-2433
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/binder-inference.md, anchor: "`context.messages` bullet (single-non-system-message reliance)" }
  - { path: docs/spec_topics/pi-integration-contract/binder-inference.md, anchor: "`context.tools` bullet (forced binder tool field enumeration)" }
lens: [spec-lens-assumptions, spec-lens-implementability]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [assumptions, implementability]
title: "State the forced binder tool's required `execute` member"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The `context.tools` bullet in `binder-inference.md` enumerates only `parameters`, `name`, `label`, and `description` for the forced binder structured-output tool, but `execute` is the fifth required member of Pi's `ToolDefinition`. The forced binder tool is never invoked — loom extracts the envelope directly from the matching `ToolCall` — so what loom supplies for the required `execute` member, and its behaviour if Pi ever did call it, is unstated. Two reasonable implementers diverge on the member's shape.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** 61592d7 — pi-loom spec: resolve "Binder structured-output tool name and label undefined" (2026-06-05, Thomas Andersen); b884cda — pi-loom spec: resolve "Binder complete() context.messages content undefined" (2026-06-05, Thomas Andersen)
**History:** `binder-inference.md` entered the corpus at `f5e89f4` (the spec-set topic split) with both the `context.messages` and `context.tools` lines under-specified. Commit `61592d7` pinned the binder tool's `name`/`label`/`description` as the "three remaining required `ToolDefinition` fields", crystallising the omission of the fifth required `execute` member. The next-day commit `b884cda` replaced the vague "rendered binder input transcript" with the fixed single-`user`-message text and its "satisfy providers that reject a system-only request" rationale, introducing the unpinned provider presupposition. Neither commit added a presupposition pin or the `execute` member; the two defects were introduced independently during the same binder `complete()` specification buildout.

## Solution approach

Clarify the `context.tools` bullet in `binder-inference.md` to state the `execute` member loom supplies for the forced binder structured-output tool, and pin its contract for the case where Pi invokes it. Specify this consistently with how the synthesised typed-query respond tool's `execute` is specified at its owning site (`query/query-tool-loop.md` QRY-14).

## Solution constraints

- None.

## Relationships

- F-2432 "Pin the single-non-system-message provider reliance as a named presupposition with a re-validation item" — co-resolve
