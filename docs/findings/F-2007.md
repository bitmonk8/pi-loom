---
id: F-2007
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/patch-skew-degradation.md, anchor: "patch-skew-degradation.md` → the (P-a)/(P-b-1)/(P-c-1) \"full five-sub-step teardown still runs\" obligation" }
lens: [spec-lens-prescription]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [prescription]
title: "over-prescribes implementation"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

over-prescribes implementation. The fixture obligation prescribes the test-double technique rather than only the observable property: "the fixture MUST assert that each of the eight call sites … was invoked on this teardown, **via spies on the injected `LoomRegistry`, `chokidar`-watcher, `Clock`, and signal-listener seams**," and "via a per-entry registry-iteration spy" / "via an injected `disposeBarrier` or `Clock.setTimeout` spy." The required behaviour is that each call site is observably invoked;
