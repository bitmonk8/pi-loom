---
id: F-2561
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-interfaces-core.md, anchor: "*`ExtensionContext` (member surface loom touches)* (edited) — (k)" }
  - { path: docs/spec_topics/pi-integration-contract/host-interfaces-services.md, anchor: "PIC-10 rules bullet; PIC-12 heading (edited) — (f), (i)" }
  - { path: docs/spec_topics/hard-ceilings/ceilings-3-and-4.md, anchor: "HC3-d (edited) — (f)" }
  - { path: docs/spec_topics/glossary.md, anchor: "new `presupposition` entry (edited) — (g)" }
  - { path: docs/spec_topics/pi-integration-contract/provider-error-mapping.md, anchor: "*Re-validation gate (loom 1.0.0)*; `#overflow-token-extraction` anchor (edited) — (j), (m)" }
lens: [spec-lens-clarity, spec-lens-naming, spec-lens-cruft, spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [clarity, naming, cruft, scope]
title: "Normalise the PIC-12 seam heading to fake-first ordering"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

PIC-12's seam heading reads real-first — `Clock` / `FakeClock` interface — while its sibling DI-seam headings read fake-first: PIC-13 (`FakeFileSystem` / `FileSystem`) and PIC-14 (`FakeFileWatcher` / `FileWatcher`). The seam catalogue is self-inconsistent in its real-vs-fake heading ordering. PIC-12 is the only divergence site among the three seam headings.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `15c0f17` (k); `eb77784` (j); `aebec4e` (m); plus long-standing residues (f), (g), (i) with no single origin
**History:** The corpus is git-tracked; the six residues entered independently, so the cluster has no single introducing commit.
- (k) — `git show 15c0f17` ("fix F-1284 — add uniform Pi-owned-snapshot markers to the four host-interfaces-core.md member-pinning surfaces", 2026-06-24) added eight lines (`+8`) intending one marker per member surface; the `ExtensionContext` surface received two (one after the `interface` block and one after the trailing `CompactOptions` paragraph), so the duplication is an artefact of that single edit.
- (j) — `git log -S "overflow-token-extraction"` first introduces the anchor at `eb77784` ("resolve tokens_used/tokens_limit extraction underspecified", 2026-06-05); the anchor was coined without matching its heading slug and has not been touched since.
- (m) — `git log -S "post-loom 1.0.0 maintenance follow-up"` first lands the follow-up sentence at `aebec4e` ("Pi version-bump gate cannot detect three unstated host/provider presuppositions", 2026-06-04), inline in the error-mapping body from the outset.
- (f) — `git log -S "binder LLM call"` shows the phrasing present since the earliest corpus history (predating the `docs/` reorg `31ff060`), never reconciled to the later canonical *binder inference call* anchor.
- (g), (i) — present since their sites were authored: "presupposition" has been used as a named category without ever being glossed, and PIC-12's `Clock / FakeClock` heading has read real-first since the seam was written while the PIC-13/PIC-14 siblings were authored fake-first.

## Solution approach

Rename the PIC-12 heading text (under `<a id="pic-12">`) from `Clock` / `FakeClock` interface to `FakeClock` / `Clock` interface so it matches the fake-first ordering of PIC-13 and PIC-14.

## Solution constraints

- Preserve the existing `<a id="clock--fakeclock-interface">` anchor unchanged — its slug encodes the old ordering, but five inbound cross-references resolve to it, so the heading rename must not regenerate or rename it.

## Relationships

- F-2557 "Delete the duplicated *Pi-owned snapshot* paragraph on the `ExtensionContext` surface" — co-resolve
- F-2558 "Relocate the post-1.0 re-validation-gate follow-up out of normative prose" — co-resolve
- F-2559 "Align the two *binder LLM call* sites to the canonical *binder inference call*" — co-resolve
- F-2560 "Add a glossary entry for *presupposition*" — co-resolve
- F-2562 "Fix the `overflow-token-extraction` anchor / heading slug mismatch" — co-resolve
