---
id: F-2781
corpus: plan
fix_surface: [plan]
status: reduced
shape: single
state: reduced
tier: medium
lens: ["plan-lens-validation"]
category: ["validation"]
factors: { blast_radius: moderate, centrality: core, core_premise_blocker: no }
location:
  - { path: "docs/plan_topics/V9j-T-binder-inference-provider-mapping.md", anchor: "Tests" }
  - { path: "docs/plan_topics/V9j-binder-inference-provider-mapping.md", anchor: "Tests, Ships when" }
title: "V9j: per-provider seed-field table, provider→`QueryError` mapping, and stop-reason classification gated by a single generic assertion"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Problem

`V9j`'s **Adds** enumerates a per-provider seed-field table, a provider-error→`QueryError`-variant/`retryable` mapping, and stop-reason classification (the closing leaf for `cka-35`), but the **Tests** in `V9j-T` (mirrored in `V9j`) assert these surfaces only generically. The classifier bullet asserts "each provider response to its `QueryError` variant and `retryable` flag" as a single undifferentiated assertion rather than per-row, so a wrong variant or `retryable` value for any provider not on the single exercised path passes green; the `complete()` bullet asserts "the mapped seed field" (singular) rather than the per-provider table; and stop-reason classification has no Tests bullet at all. `V9j`'s **Ships when** claims a "provider→`QueryError` table" gate the Tests do not deliver. The result: a wrong seed-field row, a wrong variant/`retryable` mapping, or a missing stop-reason for a non-exercised provider ships while `cka-35` is reported closed.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** c6a664e ("pi-loom plan: build/update plan for spec.md + review")
**History:** `git log --follow` over `docs/plan_topics/V9j-binder-inference-provider-mapping.md` returns two commits. The leaf was authored in c6a664e with the generic Tests bullets ("maps each provider response to its `QueryError` variant"; "the mapped seed field") and no stop-reason assertion already in place. The only later commit (5331061) added the `V11c` Deps edge and did not touch the Tests. The weak-gate defect therefore dates to the leaf's creation, not to a later edit.

## Solution approach

In `V9j-T`, rewrite the classifier Tests bullet to assert the provider-error→`QueryError`-variant and `retryable` mapping per row, so each provider row reds independently. Rewrite the `complete()` Tests bullet to assert the emitted seed field per provider against the per-provider seed-field table, citing the [Provider seed-field mapping](../spec_topics/pi-integration-contract/provider-error-mapping.md#provider-seed-field-mapping) surface as source of truth. Add a Tests bullet asserting stop-reason classification per provider. Mirror these bullets into `V9j`'s **Tests** and update `V9j`'s **Ships when** so its claimed gate matches what the Tests now assert (per-provider seed field, per-row variant/`retryable`, stop-reason), keeping the existing deterministic-envelope clause, the context-overflow assertion, and the `loom/load/typed-query-unsupported-provider` (W) assertion.

## Solution constraints

- Out of scope: the `V18c` seed-field drift fixture and `Api`-coverage bump gate — this fix strengthens `V9j`'s own steady-state mapping gate, not `V18c`'s Pi-bump drift trigger.
- The cited spec surfaces under `docs/spec_topics/pi-integration-contract/` are read-only source-of-truth for this fix, not edit targets.

## Relationships

- "V18c: too large — nine automated build-time gates + a manual editorial-review checklist in one Ships-when" — same-cluster (both touch the provider seed-field table; `V18c` gates seed-field drift on a Pi bump while this strengthens `V9j`'s own steady-state mapping gate — they resolve independently)
