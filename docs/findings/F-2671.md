---
id: F-2671
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/session-shutdown-semantics.md, anchor: "sub-step 1, `\"shutting-down\"` drain-state-tag arm (the `branch (k) … below` reference)" }
  - { path: docs/spec_topics/pi-integration-contract/unknown-reason-rule.md, anchor: "Unknown-reason rule paragraph (the `String(event.reason)` coercion pin)" }
lens: [spec-lens-clarity]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [clarity]
title: "`unknown-reason-rule.md` coercion pin lacks an RFC-2119 modal"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The coercion-pin obligation in the Unknown-reason rule paragraph is stated as a bare imperative — "Pin the coercion to `String(event.reason)` explicitly — a template-literal coercion (`` `${event.reason}` ``) re-introduces the symbol-throw bug `String()` is chosen to avoid" — with no RFC-2119 modal. The adjacent, equally load-bearing obligation "The coercion itself MUST be wrapped in `try`/`catch`" carries one. The requirement is implementation-determining yet not normatively pinned, so it is non-normative and unverifiable.

## Issue introduction

**Verdict:** multi-commit-interaction (cross-reference); present-since-inception (coercion modal)

**Introducing commits:**
- `6cd6c56` (2026-05-07, "resolve 'details.event.reason coercion is unspecified for non-string Pi values'") — authored the `String(event.reason)` coercion clause and the template-literal warning; it never carried an RFC-2119 modal.
- `52b9101` (2026-05-11, "resolve 'T24 — Fork-reason watcher closure …'") — introduced the branch-(k) seam-tag-write-throw recovery text and its `… below` cross-reference while the whole Pi-integration contract was a single document, where "below" resolved correctly in reading order.
- `f5e89f4` (2026-06-04, "enforce 100KB hard / 30KB recommended size cap on spec set") — split the contract into per-topic files, separating the reference from its target.
- `c3de47a` (2026-06-27, "fix F-1602 — relocate universal session_shutdown teardown to session-shutdown-semantics.md; sweep spec + plan citations") — relocated the citing prose into `session-shutdown-semantics.md`, leaving branch (k) in `session-only-degraded-state.md` (which the Contents list orders *before* the citing file), so the surviving "below" became both cross-file and directionally wrong.

**History:** The coercion-modal gap has existed since the clause was first authored. The cross-reference was valid when authored in a single-file corpus and was degraded into a dangling reference by the later topic-split and relocation commits that separated reference and target across files without re-resolving the "below" directional word.

## Solution approach

Rewrite the coercion-pin sentence in `unknown-reason-rule.md` (`id="unknown-reason-rule"` paragraph) to carry RFC-2119 modals: the `details.observed` coercion MUST use `String(event.reason)` and MUST NOT use a template-literal coercion (`` `${event.reason}` ``). Keep the existing rationale clause naming the symbol-`toString`-throw bug.

## Solution constraints

- Out of scope: the branch-(k) `… below` cross-reference in `session-shutdown-semantics.md` — owned by F-2670.

## Relationships

- F-2670 "`session-shutdown-semantics.md` "branch (k)" reference points outside the file" — co-resolve
