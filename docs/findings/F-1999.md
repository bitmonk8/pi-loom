---
id: F-1999
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-interfaces-core.md, anchor: "host-interfaces-core.md` — `**Non-normative.**` paragraph under `**\\`estimateTokens\\` (named export)**" }
lens: [spec-lens-cruft]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [cruft]
title: "The paragraph is explicitly marked Non-normative and describes Pi's current implementation..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The paragraph is explicitly marked **Non-normative** and describes Pi's current implementation algorithm: "derives a conservative character-quarter count (`Math.ceil(chars / 4)`-style, counting UTF-16 code units over the message's contributing strings); for orientation, a user message whose only content is `"😀😀😀"` (six UTF-16 code units) estimates to roughly 2 tokens. This description is orientation only, is owned by Pi, and may drift on any Pi revision; the authoritative behaviour is Pi's implementation at the pin." No conformance obligation flows from this content.
