---
id: F-2776
corpus: plan
fix_surface: [plan]
status: triaged
shape: single
state: shaped
tier: medium
lens: ["plan-lens-validation"]
category: ["validation"]
factors: { blast_radius: moderate, centrality: core, core_premise_blocker: no }
location:
  - { path: "docs/plan_topics/V9f-tool-registration-lifetime.md", anchor: "Tests" }
  - { path: "docs/plan_topics/V9f-T-tool-registration-lifetime.md", anchor: "Tests" }
title: "`V9f`/`V9f-T` 'callable set's visibility tracks the invocation' Tests bullet pins no observable"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Finding

`V9f` and its paired `V9f-T` both carry the Tests bullet *"The callable set's visibility tracks the invocation."* Unlike every other Tests bullet on the pair, this one names no concrete observable and pins no REQ-ID. The siblings each cite `PIC-8` (restore-failure protocol), `PIC-19` (snapshot/swap-install failure), `PIC-44` (cache-hit byte-equality), and the `ToolDefinition.label` derivation (`cka-17`) — each with a pinned assertion. This bullet alone says nothing about *what* the active set / callable visibility contains during the invocation window, what is true before and after, or what an isolated concurrent invocation observes. An implementer cannot tell what to assert.

The bullet restates the headline design property of `tool-registration-lifetime.md` §Tool-registration lifetime and visibility ("tool visibility tracks the loom's invocation rather than the process lifetime"). But the concrete observables that realise that property are already owned and gated by other leaves, exactly as the coverage matrix records: the prompt-mode per-query active-set snapshot/set/restore-in-`finally` facet of `PIC-17` is closed by `V9c` (with the prompt→prompt-invoke restore facet on `V15d`), and the subagent-mode isolated `AgentSession` (no shared tool table; released at `dispose()`) facet `PIC-9` is closed by `V9i`.

The bullet is also not referenced by `V9f`'s Ships-when, which names only `PIC-8`, `PIC-19`, and the `ToolDefinition.label` derivation — so it gates nothing. An implementer marking the leaf done is not blocked by it, but is left unsure whether and what to assert for it, with the standing risk of re-implementing a redundant copy of the `PIC-17` active-set check that `V9c` already owns.

## Consequence

**Severity:** advisory

An implementer reading `V9f`/`V9f-T` cannot tell what this bullet requires them to assert, so it is either skipped or re-implemented as a redundant copy of `V9c`'s `PIC-17` active-set check — duplicating coverage already owned elsewhere. The leaf's gated obligations (`PIC-8`, `PIC-19`, `PIC-44`, `cka-17`) are pinned and unaffected, so the leaf stays pickable and shippable; the cost is wasted or ambiguous effort, not a coverage gap.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commit:** `c6a664e` (2026-06-10, "pi-loom plan: build/update plan for spec.md + review")
**History:** The bullet entered both `docs/plan_topics/V9f-tool-registration-lifetime.md` and `docs/plan_topics/V9f-T-tool-registration-lifetime.md` at the leaf-pair's creation commit `c6a664e`, originally worded "The callable set's visibility tracks the invocation; `registration-cache-collision` disambiguates a slug clash." (`git log --diff-filter=A --follow` confirms `c6a664e` created both files; `git log -S "visibility tracks the invocation"` returns only `c6a664e`). A later edit, `47915a2` (2026-06-27, coverage-matrix REQ-ID mapping pass), split the `registration-cache-collision` clause out into the dedicated `PIC-44` byte-equality bullet, leaving the bare unanchored "visibility tracks the invocation" sentence. The defect — an observable-free, REQ-ID-free clause — was present at inception; the later split only made its orphaned state more conspicuous.

## Solution Space

### Recommendation

Delete the Tests bullet "The callable set's visibility tracks the invocation." from both `docs/plan_topics/V9f-tool-registration-lifetime.md` Tests and the identical bullet in `docs/plan_topics/V9f-T-tool-registration-lifetime.md` Tests. Make no other field changes.

The visibility observable is already pinned and gated by the leaves that own it: `V9c` (with `V15d` for the prompt→prompt restore facet) closes the `PIC-17` prompt-mode per-query active-set snapshot/set/restore-in-`finally` facet, and `V9i` closes the isolated-subagent-session facet (`PIC-9`), as the coverage matrix records. The bullet therefore names no observable `V9f` uniquely owns. `V9f`'s gated obligations (`PIC-8`, `PIC-19`, `PIC-44`, `cka-17`) are untouched, and `V9f`'s Ships-when does not reference the bullet, so no exit gate changes. No spec edits are required.

## Relationships

- "V9f: the "no-`unregisterTool` rule" is stated in Adds but unasserted" — decision-dependency (choosing Option B repoints this bullet to the no-`unregisterTool` observable and would resolve that finding; Option A leaves it to be handled there)
- "V9f/V9f-T exercise the active-set gating window (PIC-17, owned by V9c) without depending on V9c" — same-cluster (touches the same `V9f` active-set / `PIC-8` machinery; resolves independently)
