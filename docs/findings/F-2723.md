---
id: F-2723
corpus: plan
fix_surface: [plan]
status: triaged
shape: single
state: shaped
tier: medium
lens: ["spec-lens-implementability"]
category: ["implementability"]
factors: { blast_radius: moderate, centrality: supporting, core_premise_blocker: no }
location:
  - { path: "docs/plan_topics/conventions.md", anchor: "§REQ-ID discipline (the authoritative un-rowed-page recogniser definition and the hub-stub exclusion) and the §Loom 1.0 release-time residue inspection item 5 (which restates the hub-stub carve-out)" }
  - { path: "docs/plan_topics/H5e-un-anchored-must-gate.md", anchor: "Adds / Tests (un-rowed-page bullet) / Ships when (the seeded GOV-24 hub-stub fixture)" }
title: "H5e un-rowed-page recogniser: the GOV-24 hub-stub exclusion has no mechanical discriminator"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Finding

The `H5e` un-anchored-MUST gate's **un-rowed-page recogniser** reddens CI on any `spec_topics/**` page absent from the [REQ-ID prefix table](docs/spec_topics/governance/req-id-prefix-table-active-a.md) altogether, treating it as an un-rowed-page residue defect — *"unless it is a [GOV-24] table-of-contents hub stub — a subtree TOC file that deliberately carries no row and no REQ-ID anchors — which is excluded"* (`conventions.md` §REQ-ID discipline; restated in `H5e` Adds/Tests/Ships-when).

The two properties the rule supplies to identify an excluded hub stub — *"no row"* and *"no REQ-ID anchors"* — are exactly the properties every in-scope un-rowed page also has. An un-rowed page is in scope *because* it has no prefix-table row, and a page with no registered prefix carries no `PREFIX-N` REQ-ID anchors. So the excluded hub stub and the reddening residue page are indistinguishable by the only mechanical properties the rule names. The remaining discriminators — *"table-of-contents"*, *"subtree TOC file"*, *"deliberately"* — are semantic descriptions, not properties a token/path scan can read. The cited GOV-24 (`req-id-prefix-table-active-b.md`) does not close the gap either: it describes a hub stub as a subtree-TOC file written with a trailing-slash `Page` cell whose stub *"carries no REQ-ID anchors and is not itself a table row,"* but supplies no in-file marker, filename rule, or anchor pattern by which the gate can recognise one.

A faithful implementer of `H5e` therefore has to invent a discriminator. Two reasonable implementers will pick different ones, and at the live-corpus gate (activated by `H6a`) the divergence is observable: an over-broad rule false-fails a legitimate hub stub (blocking the release), while an over-narrow rule false-cleans a genuine un-rowed obligation page (silently letting its content escape the gate the recogniser exists to enforce).

## Consequence

**Severity:** correctness

Two good-faith implementations of the un-rowed-page recogniser diverge: one false-fails a legitimate hub stub at the `H6a` release gate (blocking release), the other false-cleans a real un-rowed obligation page (the un-anchored MUST that page may carry escapes the very gate the recogniser exists to enforce). Because the recogniser binds only against the live corpus at the `H6a` flip, the divergence surfaces at the single most expensive point to remediate.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:** a593446 — pi-loom plan: resolve "un-anchored-MUST recogniser un-rowed-page branch" (2026-06-17, Thomas Andersen)
**History:** Commit a593446 added the un-rowed-page recogniser branch together with its GOV-24 hub-stub carve-out, inserting the "in scope … unless it is a GOV-24 table-of-contents hub stub — a subtree TOC file that deliberately carries no row and no REQ-ID anchors — which is excluded" wording into `conventions.md` §REQ-ID discipline, the parallel H5a gate text, and residue-inspection item 5 in the same commit. That commit supplied the exclusion without any mechanical signal distinguishing a hub stub from an in-scope un-rowed page, so the defect has been present since the branch was authored. The `H5e` leaf later carried the wording verbatim when H5a's arms were split into separate leaves (f3bb326 / 5f33832, 2026-06-18).

## Solution Space

**Shape:** single

### Recommendation

Define a canonical in-file sentinel — an HTML-comment marker `<!-- GOV-24 hub stub -->` — that marks a `spec_topics/**` file as a TOC hub stub, and make the un-rowed-page recogniser's exclusion turn on the sentinel's presence: an un-rowed page is excluded from the residue defect iff the file carries the sentinel token, and any other un-rowed page reddens. This is mechanical through the same `MUST`-token text scan the recogniser already performs, is robust to arbitrary hub-stub filenames and directory layouts, and gives the recogniser a single unambiguous test (a page is a hub stub exactly when it carries the token).

Plan edits:

- In `conventions.md` §REQ-ID discipline, replace the semantic "subtree TOC file that deliberately carries no row and no REQ-ID anchors" exclusion clause with the sentinel test, naming the exact marker token `<!-- GOV-24 hub stub -->` that the recogniser greps for.
- Mirror the same token in `H5e` Adds/Tests/Ships-when, and make the seeded GOV-24 hub-stub fixture page carry the sentinel so the green-path test exercises the real discriminator rather than an implicit one.
- Update residue-inspection item 5's hub-stub parenthetical to the same sentinel test.

Spec edits:

- Pin the sentinel in GOV-24 as the canonical hub-stub marker, written as a GOV-24 authoring obligation so the spec owner writes it when creating a hub stub.
- Add the sentinel to each existing live hub-stub page under `docs/spec_topics/**` before the `H6a` live-corpus activation; any live hub stub lacking the sentinel false-fails at the release gate.

## Relationships

- "*REQ-ID discipline* — section heading too narrow for its scope" — same-cluster (same `conventions.md` §REQ-ID discipline section; resolves independently)
- "*REQ-ID discipline* — "un-anchored-obligation class" vs "un-anchored normative MUST"" — same-cluster (same section, naming of the un-anchored-MUST class; resolves independently)
- "Closing-gate coverage assertions bind only at release (H6a); mid-build gaps are warn-only at best" — same-cluster (the H6a-only binding posture that makes this discriminator's divergence surface at release; resolves independently)
