---
id: F-2508
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/errors-and-results/queryerror-variants.md, anchor: "Discriminator type-openness seam (ERR-15)" }
  - { path: docs/spec_topics/errors-and-results/error-model.md, anchor: "Mid-stream cancellation, conversation state (ERR-8 / ERR-9)" }
lens: [spec-lens-testability]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [testability]
title: "Name the loom-side conformance oracle for ERR-8 / ERR-9 non-mutation"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
decision_axes: 3
---

## Problem

ERR-8 and ERR-9 in `error-model.md` forbid the loom runtime from mutating or injecting compensating turns into the Pi-committed conversation after a mid-stream cancellation, but both obligations are stated entirely against Pi-internal conversation state with no loom-side observable named. A conformance test therefore has nothing concrete to assert against and risks being written as a vacuous always-green check.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:** 02d7ca5 — pi-loom spec: resolve "T02 surface-extension seam obligations not consistently marked" (2026-05-08, Thomas Andersen); 2180f59 — pi-loom spec: resolve "Mid-stream cancellation: no observable rule for loom-runtime conversation behaviour" (2026-05-08, Thomas Andersen)
**History:** The two obligations entered the corpus independently, each in its own same-day commit, and each was authored without a loom-side acceptance criterion from the outset. `git log -S` localises the ERR-15 type-openness seam MUST to 02d7ca5, which added the seam blockquote ("`kind` … MUST be typed as `string`") carrying only the runtime-emission conformance assertion. The mid-stream non-mutation MUST NOTs trace to 2180f59, which introduced the "Mid-stream cancellation, conversation state" paragraph (later split into ERR-8…ERR-12 by cd0e82c and relocated into `error-model.md` by the f5e89f4 size-cap split); that paragraph stated the prohibition against Pi-committed conversation state but never named a loom-side observable. Neither defect is an interaction between the two commits — they are two independent single-commit introductions bundled under one review heading.

## Solution approach

Add a conformance-oracle clause to ERR-8 / ERR-9 in `error-model.md` naming a loom-side observable: the loom→Pi conversation-driver surface whose absence of mutating invocations (the truncate / re-write / replace / remove and compensating-append operations the two obligations already enumerate) over the ERR-11 scope window witnesses conformance. Ground the oracle in the offline harness double for prompt and subagent modes and cross-reference the real-host smoke gate (`V9i` / `real-host-smoke-gate.md`) for the live re-assertion.

## Solution constraints

- Out of scope: ERR-15's type-level acceptance criterion (owned by F-2507); this finding addresses only the ERR-8 / ERR-9 non-mutation oracle.

## Relationships

- F-2507 "Add a type-level acceptance criterion to ERR-15" — co-resolve
