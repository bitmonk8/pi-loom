---
id: F-1954
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan.md, anchor: "§Blocked obligations / `V9l` leaf — fallback disposition split across " }
lens: [spec-lens-placement]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [placement]
title: "§Blocked obligations / `V9l` leaf — fallback disposition split across two homes"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The normative "pre-designed fallback" for `V9l` (what to implement if host-prerequisites clause (a) resolves *branch unreachable*: collapse to full teardown + `/reload`, no degraded-tag write, spec-side rework) lives only in `plan.md` §Blocked obligations, unreachable from the V9l leaf an implementer reads after unblocking; meanwhile `V9l` Ships-when carries a conditional pointer back to that section. The content is split between the two locations. Cross-shard candidate **confirmed** (V9l leaf is shard-03; §Blocked obligations is shard-01).

**Suggested fix:** Put the normative "what to implement on unblock" in the V9l leaf's Adds/inline note; keep §Blocked obligations as the blocking-rationale owner with only a forwarding pointer; trim V9l Ships-when to an unconditional criterion.
