---
id: F-2417
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/audit-failures.md, anchor: "*Failure-surface contract* / *Per-family record-shape table*" }
  - { path: docs/spec_topics/pi-integration-contract/audit-resolution.md, anchor: "*Category-(2) inventory join key* (lex-min tie-break sentence)" }
lens: [spec-lens-testability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [testability]
title: "Audit per-family `proposed-resolution` record-shape MUST is mechanically untestable"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The *Per-family record-shape table* in `audit-failures.md` (anchor `audit-per-family-record-shape-table`) pins a violation record as four fixed fields — `path`, `line`, `symbol`, `proposed-resolution` — and states the conformance MUST as: a record "that omits any of the four fields, or that carries a value **outside the per-family range** below for a given family, violates this rule." For `path`, `line`, and `symbol` the per-family cells give a mechanically decidable shape (a bare member identifier, a package-qualified name, a normalised single-line shape literal, or the exact `<n/a>` sentinel). For `proposed-resolution`, the cells give only a semantic-type description — e.g. family (1)'s "inventory-entry-suggestion: the `SDK_SURFACE_INVENTORY` entry the contributor would add … or the canonical `// allow-pi-surface:` marker", family (4)'s "recognised-shape suggestion", family (5)'s "deletion-or-rewrite suggestion … naming whichever of that branch's five sub-cases the marker falls under". None of these is a grammar or predicate, so "outside the per-family range" is not a decidable test: a conformance suite cannot mechanically determine whether a given `proposed-resolution` string is in-range, and two implementers can emit materially different free-text suggestions that both pass (or both fail) against the notional check the MUST promises.

The `proposed-resolution` field carries a second normative content obligation that is equally unverifiable. The *Category-(2) inventory join key* in `audit-resolution.md` closes with: "For legal multi-segment matches … the audit's `proposed-resolution` field reports the entry with the lexicographically-smallest `path` for determinism." To assert that the audit reported *that specific* entry, a test must locate the entry reference inside the `proposed-resolution` string — but the field's content format is never pinned (no leading token, no machine-parseable entry reference). The lex-min selection therefore imposes a content requirement with no test oracle: an implementation could select the correct entry and serialise it in a form no parser can confirm.

The defect is confined to `proposed-resolution`. The `audit/<class>/<family>/<symptom>` discriminator token — the field a CI parser actually routes on — is fully pinned and testable, so the audit's fail-closed routing behaviour is unaffected; only the record-shape MUST's claim that `proposed-resolution` is a checkable obligation, and the lex-min selection's verifiability, are at issue.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `e5cd514` (2026-05-09) — "pi-loom spec: resolve 'T35 SDK capability inventory closed-set negative-direction audit'"
**History:** The corpus is git-tracked. `git log -S` on each defect token — `"outside the per-family range"`, `"Per-family record-shape table"`, and `"lexicographically-smallest"` (and the fuller `"reports the entry with the lexicographically-smallest"`) — bottoms out in every case at `e5cd514` (2026-05-09), the single commit that introduced the entire inventory-closure audit; `git show e5cd514` confirms all four tokens are additions in that commit (the *Per-family record-shape table* / *Failure-surface contract* block and the *Category-(2) inventory join key* paragraph were authored whole there). The untestable `proposed-resolution` "range" MUST and the unverifiable lex-min selection were both present in the construct's first commit. They carried through unchanged; the later `f5e89f4` size-cap split and the `spec_topics/` collapse only relocated the prose across files without touching the wording.

## Solution Space

### Recommendation

Narrow the *Per-family record-shape table*'s conformance MUST in `audit-failures.md` (anchor `audit-per-family-record-shape-table`) so its "carries a value outside the per-family range … violates this rule" predicate scopes only to the three fields with a mechanically decidable shape — `path`, `line`, and `symbol`. Reword that MUST sentence so the "range"/"violates this rule" predicate applies to `path`/`line`/`symbol` and add a clause marking `proposed-resolution` as advisory, contributor-facing remediation guidance whose free-text content is explicitly *not* a conformance-checkable obligation.

In `audit-resolution.md`, demote the *Category-(2) inventory join key*'s lex-min sentence to a non-normative recommendation: change "reports the entry with the lexicographically-smallest `path` for determinism" to "reports a single entry chosen by a deterministic, run-stable tie-break the implementation pins (e.g. the lexicographically-smallest `path`)". Retain only a pinned same-implementation reproducibility invariant — two runs over the same tree emit byte-identical output — as the testable property, matching the permissive formulation the *Wire serialisation* file-iteration ordering rule already uses.

This keeps `proposed-resolution` human-useful as remediation text while removing the untestable conformance claim, and lets the conformance suite (V18b-T) assert deterministically over only the fields with a decidable shape. The audit's machine-routing rides entirely on the `audit/<class>/<family>/<symptom>` discriminator token, which remains fully pinned, so no downstream consumer depends on `proposed-resolution` being parser-checkable.

Edge case the editor must watch: if a future requirement genuinely needs a *cross-implementation* byte-identical or machine-confirmable suggestion, do not demote — instead pin a machine-parseable leading entry-reference token in `proposed-resolution` (for families (1)/(2)/(3), the resolved `SDK_SURFACE_INVENTORY` entry's `path` or canonical `// allow-pi-surface:` marker as a bare leading plain-ASCII token, with families (4)/(5) leading tokens defined or the pin scoped to families (1)–(3)) and state the cross-implementation reproducibility need explicitly as its justification. No clause in the audit cluster states such an intent today.

## Consequence

**Severity:** correctness

The record-shape MUST asserts a conformance check ("violates this rule") over a `proposed-resolution` value that has no decidable in-range predicate, and the lex-min selection has no parseable target, so two reasonable implementers diverge both on what `proposed-resolution` content is conformant and on whether the field is enforced at all — and a conformance test (V18b-T) cannot be written to gate it. The audit still fails closed and routes by the testable discriminator token, so the gate's safety property holds; the divergence is confined to the emitted record's `proposed-resolution` content and to the missing test oracle for it.

## Relationships

- "audit cluster — clarity/naming/cruft residue" — same-cluster (its item (f) `proposed-resolution` intro sentence vs per-family table conflict touches the same field; resolves independently)
- "audit cluster — violation-record `line` field: multi-line value undefined + indexing base unspecified + `path` form unspecified + decode-offset has no field" — same-cluster (pins the testable shape of the other three fields in the same four-field record; resolves independently)
- "audit-target-categories.md / audit-resolution.md — Category-(1)/(3) resolution not scoped by entry kind; rightmost-segment match false-resolves across carriers; mis-pointed join-key cross-reference" — same-cluster (same *Category-(2) inventory join key* paragraph that houses the lex-min rule; resolves independently)
