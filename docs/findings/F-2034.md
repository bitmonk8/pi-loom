---
id: F-2034
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/future-considerations/surface-extensions.md, anchor: "future-considerations/surface-extensions.md` — \"Surface extensions gated on an open design decision\" — *Automatic contex" }
lens: [spec-lens-cruft]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [cruft]
title: "The annotation Decision required before this item can be scoped: whether automatic escalation..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The annotation `*Decision required before this item can be scoped:* whether automatic escalation surfaces a user-visible turn (composing with the Binder refinement loop) or stays operator-only.` is an explicit "(decide later)" marker left in the spec body. The sub-section heading already declares this bucket as "gated on an open design decision," so the annotation restates what the structure already communicates.
