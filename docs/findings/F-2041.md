---
id: F-2041
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/future-considerations/surface-extensions.md, anchor: "Glossary — entry `schema slug" }
lens: [spec-lens-scope]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [scope]
title: "The entry states \"a future grep gate uses this entry as its source of truth.\" This introduces..."
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The entry states "a future grep gate uses this entry as its source of truth." This introduces a future tooling enforcement requirement (an automated grep gate) into normative glossary content without it being separated into a future-considerations section or non-goals list. An implementer reading the entry cannot tell whether building or integrating this grep gate is in scope for loom 1.0 or deferred. The phrase "future" is used without a phase label or pointer to the spec's `future-considerations` section, so it sits indistinguishably alongside current normative content.
