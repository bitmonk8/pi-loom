---
id: F-2737
corpus: plan
fix_surface: [plan]
status: triaged
shape: single
state: shaped
tier: medium
lens: ["plan-lens-risk", "plan-lens-validation"]
category: ["risk", "validation"]
factors: { blast_radius: moderate, centrality: core, core_premise_blocker: no }
location:
  - { path: "docs/plan_topics/V14b-tool-calls-parallel-batch.md", anchor: "Adds / Tests / Ships when" }
title: "V14b real-Pi parallel-batch delivery shape is never measured against a real host"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Finding

`V14b` models the concurrent / settle-all-before-next-turn / independent-per-sibling-lowering shape of a model-driven parallel tool-call batch. Its **Adds** correctly concedes this shape is a real-Pi *consumption posture*, not a loom-side guarantee: `@earendil-works/pi-coding-agent` encodes no typed contract pinning it, loom performs no batching or settle-barrier of its own on this path, and the in-process `H4a` session double "does not enumerate parallel-batch delivery in its fidelity contract." `V14b`'s **Tests** and **Ships when** drive the batch (one succeeding + one failing sibling) only through that double. The double's own modelling of the batch is therefore unconstrained by the fidelity contract, so the host-behaviour premise the lowering rules rest on is asserted by nothing observable.

The only routing for the real-Pi behaviour is version-bump editorial-review item **(an)** (`version-bump-step2.md`; `tool-calls.md#parallel-tool-batch-delivery-presupposition`). Item (an) is a SHOULD-level item in the `(f)`–`(aq)` band: per `V18c`'s Ships-when, a recorded `fail` / `not-performed` on a SHOULD-level item is a conformant non-blocking outcome. Item (an) also only asks the contributor to confirm the *candidate* Pi minor "still" delivers the assumed shape — it detects a future host-side *change*, never establishes that the *current* pinned Pi delivers the assumed shape at all. The real-host smoke gate does not close the gap either: its scored pass criteria (a)–(e) do not include parallel-batch delivery, and criterion (d)'s four fidelity-contract axes (streamed-token order, single-turn append, `pi.on` cancel-forward, cancellation propagation) do not cover it; a turn-ordering divergence would in any case fall under the smoke's "benign live-model variance in turn order/count is not a fail" rule.

The leaf frames its routing as "mirroring the way SLSH-2's real-Pi streaming-ordering behaviour routes to item (ak)." That parity is asymmetric: SLSH-2's streamed-token ordering *is* one of the real-host smoke's four scored fidelity axes (criterion (d) axis (i)), so SLSH-2 carries a real-host measurement in addition to its editorial item. Parallel-batch delivery carries no such axis, so the "mirroring" framing overstates `V14b`'s real-host coverage relative to its cited sibling.

## Consequence

**Severity:** advisory

The leaf is implementable and spec-faithful, so an implementer can still produce a working `V14b`. But the real-Pi parallel-batch delivery shape that `V14b`'s lowering rules entirely presuppose is measured by no gate at any point for loom 1.0 — not by the double (`H4a` does not enumerate it), not by the real-host smoke (not a scored criterion or fidelity axis), and not by item (an) (which only audits a future host change, not the current pin). If real Pi diverges — constructing the next user turn before every sibling settles, or collapsing a failing sibling — `V14b` ships green while prompt-mode parallel tool calls misbehave in live sessions, and the divergence surfaces, if ever, only at a non-blocking version-bump editorial audit.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `eaa5000` — "pi-loom plan: resolve \"V13c bundles four independent spec obligations into one leaf\"" (2026-06-16, created the `V14b` leaf); `2123dc9` — "pi-loom spec-plan: fix F-0560 — surface V14b parallel-tool-mode presupposition via new bump checklist item (an)" (2026-06-21, added the editorial-review disposition)
**History:** The corpus is git-tracked. `V14b-tool-calls-parallel-batch.md` was added in `eaa5000` (2026-06-16) as part of a V14 leaf split; from inception its Tests and Ships-when drove the parallel batch only against the `H4a` in-process double — the no-real-host-measurement gap is original to the leaf. `2123dc9` (2026-06-21, F-0560) then added the consumption-posture paragraph to the `V14b` Adds, the `#parallel-tool-batch-delivery-presupposition` section to `tool-calls.md`, and checklist item (an) to `version-bump-step2.md`. That commit acknowledged the presupposition and routed it to SHOULD-level editorial review; it did not add a measurement step, so the underlying gap (and the asymmetry with SLSH-2's real-host smoke axis) predates the disposition the finding critiques.

## Solution Space

**Shape:** single

### Recommendation

In `V14b-tool-calls-parallel-batch.md`, edit the Adds section to remove the false real-host parity framing and state the measurement status accurately. Replace the clause that says `V14b` "mirrors the way SLSH-2's real-Pi streaming-ordering behaviour routes to item (ak)" with an accurate statement: unlike SLSH-2's streamed-token ordering — which is one of the four scored fidelity axes of the real-host smoke gate (criterion (d) axis (i)) — the parallel-batch delivery shape is covered by no real-host smoke criterion or fidelity axis. For loom 1.0 it is therefore dispositioned but unmeasured against a real host: the only routing for the real-Pi behaviour is version-bump editorial-review item (an), which is SHOULD-level and audits only a future host-side *change* at a version bump rather than confirming that the current pinned Pi delivers the assumed shape.

Keep the spec's SHOULD-level editorial routing unchanged: do not alter `tool-calls.md#parallel-tool-batch-delivery-presupposition`, `version-bump-step2.md` item (an), or the real-host smoke gate. The fix is plan-internal and confined to the `V14b` Adds prose; it corrects the disclosure so the leaf no longer overstates its real-host coverage, while leaving the unmeasured-presupposition posture as the spec deliberately designed it.

## Relationships

- "V12a: SLSH-2 streamed-before-resume ordering verified only against the session double" — same-cluster (same double-vs-real validation gap; SLSH-2 additionally has a real-host smoke fidelity axis, which is the asymmetry this finding turns on)
- "Smoke criteria presuppose specific H7a fixture content never required of H7a" — decision-dependency (Option A depends on `H7a`'s fixture being required to contain a parallel batch)
- "Real-host fidelity / divergence has no CI-enforced gate; no merge-time detection of trigger-(2)" — same-cluster (both concern real-host coverage gaps in the smoke/version-bump path; resolve independently)
