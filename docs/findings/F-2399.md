---
id: F-2399
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/inventory-audit-intro.md, anchor: "PIC-15 and *Non-capability `pi.<member>` surfaces* paragraph" }
  - { path: docs/spec_topics/pi-integration-contract/audit-resolution.md, anchor: "*Citation-token grammar* and *Generated-output-in-src limitation*" }
  - { path: docs/spec_topics/pi-integration-contract/audit-wire-and-canary.md, anchor: "*Wire serialisation*, *Infrastructure-failure handling*, *Green-path emission contract*" }
lens: [spec-lens-cruft, spec-lens-naming]
tier: high
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: reduced
category: [cruft, naming]
title: "`inventory-audit-intro.md`: directional words for the seven-capability enumeration"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

On `inventory-audit-intro.md`, PIC-15 ("enumerated below"), the *Non-capability `pi.<member>` surfaces* paragraph ("enumerated above"), and the "items 1–7 below" / "items 1–7 above" prose all point at the numbered seven-capability enumeration, which lives on the sibling shard `capability-inventory-items.md`, not on this page. The below/above directional words imply same-page placement and contradict each other. A reader following them cannot locate the referenced enumeration.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** e5cd514 — pi-loom spec: resolve "T35 SDK capability inventory closed-set negative-direction audit" (2026-05-09, Thomas Andersen); f5e89f4 — docs: enforce 100KB hard / 30KB recommended size cap on spec set (2026-06-04, Thomas Andersen)
**History:** e5cd514 authored the inventory-closure-audit prose inside the single monolithic `docs/spec_topics/pi-integration-contract.md`, where *Citation-token grammar*, the *Stale-marker discriminator* sub-bullets, the seven-capability enumeration, *Wire serialisation*, and *Failure-surface contract* were all contiguous, so the "below"/"above"/"there" directional words and the "clause of the *Failure-surface contract*" nesting all resolved correctly within one page. f5e89f4 enforced a size cap by splitting that 967-line page into sibling shard files (`audit-resolution.md`, `audit-failures.md`, `audit-wire-and-canary.md`, `inventory-audit-intro.md`, `capability-inventory-items.md`, …) but carried the intra-page directional language over verbatim, turning each reference into a broken cross-file pointer. A later cross-ref sweep (23d28a1, F-0958) realigned link basenames on `inventory-audit-intro.md` but did not rewrite these directional words.

## Solution approach

Rewrite PIC-15 (`id="pic-15"`) and the *Non-capability `pi.<member>` surfaces* paragraph (`id="non-capability-pi-members"`) on `inventory-audit-intro.md` to replace the bare "below" / "above" directional qualifiers and the "items 1–7 below/above" prose with explicit cross-file forward-links to `capability-inventory-items.md`, so each reference to the seven-capability enumeration no longer implies same-page placement.

## Solution constraints

- Out of scope: the *Stale-marker discriminator* soft-surface sub-bullet pointers on `audit-resolution.md` (owned by F-2400) and the *Wire serialisation* parent-clause mis-attribution on `audit-wire-and-canary.md` (owned by F-2401).

## Relationships

- F-2400 "`audit-resolution.md`: "below"/"there" pointers to the *Stale-marker discriminator* soft-surface sub-bullets" — co-resolve
- F-2401 "`audit-wire-and-canary.md`: *Wire serialisation* mis-attributed as a "clause of the *Failure-surface contract*"" — co-resolve
