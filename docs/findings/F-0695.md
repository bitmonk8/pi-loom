---
id: F-0695
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/expressions.md, anchor: "Built-in methods and properties › string table row, the replace(from, to) refere" }
lens: [spec-lens-naming]
tier: high
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [naming]
title: "`string.replace` method name diverges from JS first-only semantics"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

In `docs/spec_topics/expressions.md` the `string` stdlib row for `replace(from, to)` specifies "Replaces all occurrences via a single left-to-right, non-overlapping scan … (matching host `String.prototype.replaceAll`)." The spec's own clarifying parenthetical names the host method whose semantics actually match — `replaceAll`, not `replace`. JavaScript's `String.prototype.replace(str, str)` replaces only the **first** occurrence; this is the dominant prior art any author writing loom expressions will carry into the language.

The result is a silent semantic divergence on a familiar name. A loom author writing `s.replace("X", "Y")` expecting first-only substitution (the JS default for the literal-string overload) instead gets every occurrence replaced. The mismatch produces wrong values rather than a parse-time or type-time diagnostic, and the surrounding prose makes the divergence pointedly visible — the spec already concedes the behaviour is `String.prototype.replaceAll`, then attaches that behaviour to a different name.

The five normative reference vectors (`"aXbXc".replace("X", "[$&]")` → `"a[$&]b[$&]c"`, etc.) bake the all-occurrences semantics into conformance; an implementation that mapped `replace` to JS `String.prototype.replace` first-only behaviour would fail the vectors. The defect is therefore the surface name, not the behaviour: the method's behaviour is well-pinned, but its identifier carries the wrong host-language association.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `53f5831` (2026-05-03, "spec: rewrite query primitive, system prompts, and schema syntax")
**History:** The `string` stdlib surface was first enumerated in `53f5831`, which introduced the row `replace(from, to)` together with the "Replaces all occurrences" semantics in the same edit — the JS-name / non-JS-semantics pairing has been present since the stdlib's first appearance. The file was split out of `spec.md` into `spec_topics/expressions.md` in `fecb504` with no semantic change. Later edits refined the row's wording (`8984e3d` removed historical-rationale prose, `ccab317` added the `$`-sequence and empty-`from` clauses, `d292b46` added the reference-vector table, `3b4cd71` added the explicit "matching host `String.prototype.replaceAll`" parenthetical) but none touched the method name. `3b4cd71` is the commit that makes the divergence textually self-evident, but the defect predates it.

## Solution Space

**Shape:** multiple

### Option A — Rename to `replaceAll(from, to)`

- **Approach.** Treat the divergence as a naming bug and align the loom method name with the host method whose semantics it already matches.
- **Spec edits.**
  - In `docs/spec_topics/expressions.md`, rename the row, the reference-vector table caption, the five vector expressions, and the paragraph that follows the vectors to use `replaceAll`. Drop the now-redundant "(matching host `String.prototype.replaceAll`)" parenthetical or shorten it to a JS-semantics note like the sibling rows.
  - In `docs/spec_topics/hard-ceilings/ceiling-invariants-and-audit.md`, replace "`split` / `replace`" with "`split` / `replaceAll`".
  - Update the V3f / V3f-T leaf prose ("`String.replace` reference vectors", "the five `replace` vectors") and the anchor target if the heading shifts.
- **Pros.** Removes the JS-collision trap entirely; readers' JS instincts now align with loom behaviour; no permanent caveat in the spec text.
- **Cons.** Forecloses adding a first-occurrence `replace(from, to)` later under the obvious name without an aliasing rule.
- **Risks.** Coordinated rename across spec + plan leaves + any in-flight implementation work; the heading-anchor shift may break external links if any exist.

### Option B — Keep the name, add a prominent divergence note

- **Approach.** Leave the surface name `replace`; promote the divergence from a parenthetical to a leading caution on the row so authors cannot miss it.
- **Spec edits.** In the `replace(from, to)` row, lead with a marker such as "**Diverges from JS `String.prototype.replace`:** replaces *all* occurrences (semantics match host `String.prototype.replaceAll`)" before the existing prose. No other files change.
- **Pros.** Minimal spec churn; no plan/leaf changes; reserves the `replaceAll` name should a first-only `replace` ever be added.
- **Cons.** The trap persists for any author who skims the row or relies on autocomplete/JS muscle memory; the spec carries a permanent caveat to compensate for a name choice.
- **Risks.** Notes on table rows are easily missed; the divergence-marker style is not used elsewhere in the stdlib table, so it introduces a one-off convention.

### Recommendation

Take **Option A**. The defect is the surface name; a note only papers over it. The rename is mechanical (one method on one row, five reference-vector expressions, two plan leaves, one cross-reference in `ceiling-invariants-and-audit.md`), the reference vectors already pin the behaviour so no semantic drift is possible, and reserving `replaceAll` for the operation that actually `replaceAll`s aligns loom with the JS prior art every author already carries. Watch two edge cases when applying: the section heading anchor (`#built-in-methods-and-properties`) is stable so internal cross-references survive, but the `V3f` / `V3f-T` link text and `Ships when` strings reference the old name verbatim and must be updated in lockstep with the spec edit.

## Consequence

**Severity:** correctness

Authors writing loom expressions silently produce wrong output when they reach for the JS-familiar single-occurrence `replace` and get all-occurrences instead. The error surfaces only at runtime (or in author-written tests), not at parse or type-check; nothing in the lexer, grammar, or type system constrains the receiver-string contents enough to catch the mistake. The conformance vectors and host cross-reference make the semantics unambiguous for implementers, so the cost falls on every loom author rather than diverging implementations.

## Relationships

- "expressions.md — `array<T>.concat` result type `T ⊔ U` undefined for disjoint named schemas" — same-cluster (sibling row in the same Built-in methods table; resolves independently)
