---
id: F-1109
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V2b-type-compat-engine.md, anchor: "Tests, TYPE-11 bullet" }
  - { path: docs/plan_topics/V2b-T-type-compat-engine.md, anchor: "Tests, TYPE-11 bullet" }
lens: [spec-lens-clarity, spec-lens-placement]
tier: high
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [clarity, placement]
title: "TYPE-11 Tests bullet diverges between paired leaves V2b-T and V2b — each carries only half of the normative rule"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The paired leaves `V2b` (implementation) and `V2b-T` (tests) carry divergent `TYPE-11` Tests bullets — each states only one facet of one normative rule. `V2b-T` carries the object-alias→nominal sub-assertion (`schema Y = SomeObjectSchema` re-enters TYPE-10's nominal case) but omits the `=`-form discriminator; `V2b` carries the `=`-form discriminator (the alias is identified by the `schema X = R` `=` form, not by what `R` resolves to) but omits the object-alias→nominal facet. Both facets are normative in `type-system.md` TYPE-11, which states them in one paragraph. Because the coverage matrix maps `TYPE-11` closure to `V2b`, an implementer working from `V2b` alone never exercises the object-alias→nominal behaviour, and the `V2b-T` author may key transparency on what `R` resolves to rather than on the `=` form — yielding two compliant-but-divergent test suites.

## Issue introduction

**Verdict:** single-commit introduction
**Introducing commits:** `b93334a`
**History:** Both TYPE-11 bullets were first authored in `b93334a` (2026-06-19, "pi-loom spec-plan: fix F-0811 — type-compat alias-schema transparency (TYPE-11), spec rule + plan coverage (cross-boundary)"). `git log -S "re-enters TYPE-10's nominal case"` and `git log -S "not by what"` over `docs/plan_topics/` each return only `b93334a`, and `git show b93334a` confirms the commit added the `TYPE-11` line to `V2b-T` and to `V2b` in the same diff with two non-matching formulations — `V2b-T` got the object-alias→nominal clause, `V2b` got the `=`-form discriminator clause. The divergence was therefore introduced at the moment TYPE-11 entered the plan corpus (the leaves themselves predate it, created in `c6a664e`, but carried no TYPE-11 bullet before `b93334a`). No later commit has touched either bullet.

## Solution approach

Rewrite the `TYPE-11` Tests bullet in both `V2b-type-compat-engine.md` and `V2b-T-type-compat-engine.md` to a single shared formulation, sourcing it from `type-system.md` TYPE-11. The shared bullet must carry both facets currently split across the leaves: the `=`-form discriminator (the alias is identified by the `schema X = R` `=` form, not by what `R` resolves to) and the object-alias→nominal re-entry (`schema Y = SomeObjectSchema` unfolds to that object schema and participates under TYPE-10's nominal rules, so transparency never reopens TYPE-10's nominal case). Use identical text in both leaves.

## Solution constraints

- The spec is read-only for this fix — `type-system.md` TYPE-11 already states both facets and is the source, not an edit target.

## Relationships

None
