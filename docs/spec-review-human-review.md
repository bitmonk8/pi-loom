# pi-loom spec-review audit — human-review queue

_Generated by `/spec-review-audit` on 2026-05-14T00:00:00Z._
_Source reports directory: `C:/UnitySrc/pi-loom/.pi/tmp/spec-review-audit/2026-05-13T22-09-55_df33e0/reports`_

## Summary

| Bucket | Count |
|---|---:|
| NO_ACTION | 40 |
| INLINE_FIX (applied) | 1 |
| HUMAN_REVIEW (queued below) | 1 |
| bad-report (skipped) | 0 |

## Human-review queue (1 findings)

### T15b — Move concurrency semantics into Extension Architecture / Implementation Notes Concurrency-model subsection

**Overall risk:** MEDIUM

## Risk synthesis

The dominant risk is an unresolved structural choice embedded in the Solution approach: the finding instructs the fixer to add the new `Concurrency model` subsection under "either `## Extension Architecture` or `## Implementation Notes`" and to follow whichever home the T15 / T16 placement cluster selects, but the sibling findings (T15a reduces in place, T15c lifts to Non-goals, T16a–d only restructure the Trust-boundary bullet) do not in fact select a home for relocated prompt-vs-subagent-mode content. The fixer is left to make a structural placement decision with no spec-side anchor, and the inner fix-loop may oscillate between candidates across passes. This cannot be neutralised by a wording tweak — picking one home is a design call, not a phrasing fix, and removing the constraint without replacement would silently undo the finding's intent. HUMAN_REVIEW is required to pin the home.

## Human-review note

The reviewer must decide which subsection home the new `Concurrency model` aggregator goes in: `## Extension Architecture` (as a sibling to Pi Extension Integration) or `## Implementation Notes` (as a new bulleted entry). The deferral in the current Solution approach to "whichever home the rest of the T15 / T16 placement cluster selects" is non-actionable because the sibling findings in that cluster do not relocate prompt-vs-subagent-mode mechanics to either home — there is no precedent to follow. A second, lesser question for the reviewer: should the bottom-up co-resolve constraint with T15a be retained, or is a single combined commit preferable given the explicit "transient duplication" warning?

The inline applier cannot resolve this without that decision because picking a home is a structural call (it changes which existing section gains a subsection, alters the document's table-of-contents shape, and may interact with placement-lens expectations downstream). A wording-only edit cannot satisfy the finding's intent — leaving "either X or Y" in the spec would not produce a determinate post-fix text, and arbitrarily picking one home would be a design decision out of the applier's remit.

_Source report:_ `C:/UnitySrc/pi-loom/.pi/tmp/spec-review-audit/2026-05-13T22-09-55_df33e0/reports/t15b-move-concurrency-semantics-into-extension-architecture-implementation-notes.md`

---
